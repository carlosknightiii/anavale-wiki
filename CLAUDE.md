# CLAUDE.md — Anavale Wiki Project
*Auto-loaded by Claude Code at session start. Last updated: Aug 25 2026 (Decision Log split by subsystem — see §13 for the new file map).*
*As of Aug 25 2026, the Decision Log is split across per-subsystem files in `docs/` (Field Guide, Combat, Character Sheet, Spell System, General) instead of living entirely in this file — this file itself now holds only the universal sections (1–12) plus a short index (§13) pointing to each. If you're looking for a specific subsystem's history, go straight to §13, not the old single-log habit.*

**Tool roles:**
- **Claude Code** — all file edits, git operations, debugging
- **Claude Chat** — planning, strategy, review
- **DM Tools → Supabase** — all world/content/session data changes

**What lives where (never duplicate here):**
- World lore, nations, creatures, session notes → search `anavale-dm-docs` in project knowledge
- Character system spec, SCC state, stage structure → search `docs/` in project knowledge
- Code state → read the actual file before every edit

**Feature requests & bug reports:**
The Supabase `requests` table (`title`, `description`, `area`, `status`, `source`, `notes`) is the single, live tracker for every DM feature request and bug report — real `status` values in use: `not-started` / `in-progress` / `blocked` / `needs-decision` / `partial` / `done` / `declined` (the DM decided against building it — distinct from `not-started`, so it stops showing up as open work, and distinct from `done`, so it isn't misrepresented as built). This list must never be copied into CLAUDE.md, a chat, a handoff doc, or any other file as text — Supabase is the only copy. A duplicate anywhere else just goes stale the moment the real one changes.
Every new row must also set `size` — `small` / `medium` / `large`, `unknown` if it genuinely needs investigation before it can be sized at all, or `n/a` for an item that's already finished.

---

## 1. Every Session

**Start:**
1. Run `git log --oneline -5` and report HEAD before doing anything else.
2. Search project knowledge for any file or function before touching it.
3. If what you find in the file conflicts with project knowledge, stop and say so.
4. Never answer from memory or assumption — read the file first.
5. Before starting new work: check `git worktree list` / `git diff --stat main...<other-branch>` for parallel work on the same files, **and** check Section 3 (⚠ Pending / Blocked) for a known blocker already sitting on the same file/area. Flag either before proceeding.
6. Every new task — including deferred features and future SCC UX edits — gets its own fresh worktree, created when that work actually begins. Never reuse a worktree from a task that's already merged, completed, or removed, even if it's named in old chat history or a handoff doc — always create a new one. Name each worktree for what it's actually doing (e.g. `anavale-wiki-combat-encounters`, not `anavale-wiki-2` or a reused old name), so it's unambiguous from the name alone which task it belongs to.
7. Log the ask before acting on it. The FIRST action on any new, distinct DM ask is creating its `requests` table row — before the content edit or the code prompt that fulfills it, not a follow-up done afterward and not skipped because the ask is small or quick enough to "just do." This applies identically to direct content edits (Supabase writes) and code work — the requests table is not just for things that go to Code. If a message is clearly a refinement of something already logged earlier in the same session (e.g. "no, bigger" right after a change), update that existing row's notes instead of creating a duplicate — but it must still be logged, never left untracked. (Closes a real gap: roughly 25 real content requests from a single session were completed correctly but never logged as their own row, because logging had only ever been tied to work that went to Code.)

**End (standing requirement — do not wait to be asked):**
1. Append an entry to the appropriate `docs/decision-log-*.md` file (date, what changed, why) — see §13's index for which file covers which subsystem; genuinely cross-cutting work goes in `decision-log-general.md`.
2. Update "Current Focus" below to reflect what comes next.
3. If any rule turned out to be wrong or incomplete, correct it here and commit.
4. If a new bug pattern was discovered, add it to the relevant `docs/decision-log-*.md` file (see §13).
5. If a branch merged to `main` this session: check Section 3 (⚠ Pending / Blocked) — does anything there just become unblocked? If so, flag it to the DM in this session's summary and move it to Next Priorities.
6. Whenever you finish and push work, do both of these without being asked: (a) if it corresponds to an item in the Supabase `requests` table, update that item's `status` yourself (same anon key/URL already used everywhere else in this codebase) as part of finishing the task — don't leave it for the DM to remember; (b) if it's something the DM would want to check themselves (a UI change, a fix, a new feature), end the report with the exact local link to view it — the real port number of whatever server is actually running, the exact path (e.g. `/sheet/index.html?token=...`), and which folder/branch it's serving from. Your own in-app browser verification is not a substitute for (b) — give the DM a real, clickable link every time.

**Feature branches don't touch CLAUDE.md:**
While working on a feature branch, skip End items 1–4 (Decision Log, Current Focus, self-correction edits, new-bug-pattern logging) entirely — write nothing to this file, not even a small entry. Every branch inserting at the same anchor point (the top of the Decision Log, the top of Current Focus) is what causes merge conflicts when several branches land close together, even when the actual code never overlaps. Instead, state clearly in the final report to the DM everything that would have gone into the Decision Log and Next Priorities — same content, same detail, just not committed to the file yet. Once a batch of parallel branches has finished merging into `main`, one follow-up consolidation pass — on the designated `Anavale-wiki-MAIN-3` worktree — adds that whole batch's entries to CLAUDE.md in a single commit, directly on `main`, where no merge conflict is possible since nothing else is touching the file at that point.

**A removed worktree's session is gone — route follow-ups to `Anavale-wiki-MAIN-3`, not back to it:**
Once a Code session removes its own worktree, that session is done — its folder no longer exists, so it cannot receive any further prompts, no matter how small the follow-up. Any work that comes up after a worktree removal (deleting the now-unused remote branch, any other cleanup) always goes to the designated `Anavale-wiki-MAIN-3` worktree instead, never back to the session that just removed itself.

**Check for an existing service before reaching for a new one:**
Before creating or suggesting any new third-party account, service, or external dependency, check whether an existing one already used by this project (Supabase, GitHub, ElevenLabs) can do the job instead. Never default to a new service without checking first. If it's genuinely unclear whether an existing service covers it, say so and ask, rather than defaulting to something new.

---

## 2. Current Focus

*Session Aug 20 2026 (`anavale-wiki-tabs` branch, merged to `main`) — Field Guide tab's left panel (search box, filter buttons, session-scoped card list — `scc-fg-left`/`sccRenderFieldGuide`/`sccFgDraw`/`sccFgCardDetail` and friends) removed entirely; `scc-fg-doc` now fills the full tab width (including the doc column's own `max-width`, dropped in a same-branch follow-up). Confirmed first, against real code, that the link-insertion feature (`sccFgOpenLinkPicker`/`sccFgLinkPickerSearch`/`sccFgInsertLink`) has zero dependency on the removed panel — it reads global app-wide entity arrays, not the panel's session-filtered subset. Verified live end-to-end: full-width layout confirmed via computed style, a real link inserted/saved/rendered/clicked through to the real lookup drawer, then reverted with the document back to its exact pristine 140-block state (confirmed via SQL). Zero console errors. Full detail in the Decision Log's top entry.*

*Session Aug 19 2026 (`anavale-wiki-tabs` branch, merged to `main`) — Live Session tab's right-column accordion (NPCs/Locations/Quests/Scenarios/Field Guide) converted to horizontal tabs — one section visible at a time via `sccLiveSwitchSection()`, same content per section as before, left column (Party HP/Conditions/Current Beat/Die Roller/Timer/DM Safety Net) untouched. Verified live against real Session 1 data plus a safe synthetic audit-mode test; zero console errors. Matching `requests` row (`a7759f51`) marked `done`. Full detail in the Decision Log's top entry.*

*Session Aug 19 2026 (`anavale-wiki-spells-table` branch) — 2 fixes to the Class tab's Spells Learned table (`sheet/index.html`), both matching real `requests` rows, now `done`. (1) Row height on narrow screens: root cause was that `renderSpellsLearnedTable()` runs at page load while the Class tab is still `[hidden]` (Your Story is the default active tab) — an element under `display:none` always measures `scrollHeight===clientHeight===0`, so the description-overflow check was silently wrong for every row from boot onward, not a rendering-timing/`table-layout:auto` issue as first suspected. Fixed at the real cause (re-run the overflow sweep in `setActiveTab()` when the Class tab actually becomes visible), not with a deferred timer — confirmed `requestAnimationFrame` doesn't even fire reliably in this backgrounded-tab test harness, a red herring that would have shipped broken. Description now clamps to 3 lines with a real "Show more"/"Show less" toggle that only appears when a row's real content genuinely overflows the clamp. (2) Added Search (name+school+description) + a fixed Level filter + a Favorites-only toggle, scoped only to `character_sheet.learned_spells` (still no spell-browsing UI — DM-assignment-only is unchanged) — reuses `.cs-inv-filter-row`/`-field`/`-input`/`-select` verbatim from the Inventory tab's own-inventory search rather than the old Round 1 6-filter catalog browser. New `character_sheet.favorite_spells` column (`text[]`, same shape as `attuned_items`/`learned_spells`) backs a per-spell star toggle. Verified live and end-to-end on both real spellcasters (Hana Sable, Kael Evander): real typed search, real Level/Favorites-only selections, real favorite-toggle-then-reload persistence (confirmed via SQL before/after), and confirmed the search box's typed value survives a Prepared-checkbox re-render underneath it (the reason the filter row lives in the static template, not inside the tbody-only render). Zero console errors at desktop and mobile widths on both characters; all test mutations reverted, confirmed clean via SQL.*

*Session Aug 19 2026 (`anavale-wiki-class-features` branch, merged to `main`) — 3 small class-feature builds, DM QA'd and approved, all verified live on Kael/Hana's real sheets, real values persisted: Ranger's second language (`player_characters.language_extra_2`, Kael → Pre-Partition Script), Expertise (`player_characters.expertise_skills`, Kael → Survival), and Druid's Primal Order (`player_characters.primal_order`, Hana → Warden — shown as the first Key Features row, icon+name+description, not a standalone note). Full detail in the Decision Log's top 2 entries. The 3 matching `requests` rows are marked `done`; a 4th unrelated one surfaced in the same search (`Hana: free Speak with Animals spell`, `needs-decision`) is still open.*

*Session Aug 19 2026 (`anavale-wiki-live-session` branch) — 5 direct Live Session/Manage-tab edits landed: Quick Actions and the Inspiration control both removed from the Live Session strip (Inspiration lives only on the player sheet now, no duplicate), the Live Session panel is a real two-column layout (sticky strip left, accordion right), the Quick Note FAB only shows on the Session Command Center page, and Quests no longer appear in the Manage tab's entity list (never editable from there anyway). All 5 corresponded to real `requests` rows, now `done`. One thing NOT fully confirmed: sticky positioning itself couldn't be visually verified end-to-end by this session's own tooling — a real, tool-level `position:sticky` limitation (reproduced even on a totally isolated test element), not a code issue; a real ancestor-chain CSS bug that WAS blocking it got found and fixed via direct reasoning + computed-style checks, but the DM should confirm the strip visibly stays put while scrolling the accordion in a real browser. Full detail in the Decision Log.*

*Session Aug 19 2026 — merge of `anavale-wiki-scc-ux` into `main`, combining two bodies of work developed independently, in parallel, across this whole session. This entry describes the real combined state post-merge, not a session-by-session narrative — see the Decision Log for the full blow-by-blow of both threads, interleaved chronologically.*

**DM Tools (`dm.html`) — the Session Command Center UX/IA overhaul is complete and merged.** Setup has its own top-level tab with a `session_scenarios` editor and Combat-roster quick-add wiring. NPCs/Locations/Quests/Scenarios/Field Guide are merged into one "Live Session" accordion tab. Combat has real-time AC/stat-blocks on the live initiative tracker, an inline condition picker, initiative reordering with correct turn-tracking, and loot-award-to-one-player with no double-count at End Combat. Notes has Tagged Notes with category filtering, and Copy Post-Session Summary correctly pulls both the 5 recap fields and real Tagged Notes together. History/Audit Mode genuinely locks a DM into read-only review of a past session — all 14 real write paths across Live Session, Combat, and Field Guide are guarded, confirmed to hold together across tab-switch round-trips, and re-verified in a final continuous-session sanity pass that found and fixed 2 more feature-interaction bugs (a Vendor/Shop refresh gap, an audit-mode Notes tab-switch leak) immediately before this merge. Vendor/Shop (buy-only, fixed per-vendor stock), Combat filters (Tier/Category/Region), and Field Guide's multi-session document storage (block-based, inline `[[type:id]]` entity links, a collapsible per-session editor) are all live.

**Character Sheet (`sheet/index.html`) — the Class-tab/spell system now runs on the real 2024 PHB rules, not 2014.** Spell Slots, Spells Prepared, and Spells Learned (DM-assignment-only — no player self-serve, mirrored by DM Tools' own Players-page Spell Assignment panel) are rebuilt against the correct half-caster/prepared-caster progression tables. A full spell-catalog audit corrected level/school/classes/casting-time/range/components/duration across the reachable catalog and closed under a new standing content policy (§11): Anavale is 2024-PHB-only, supplement spells are flagged via `classes=[]`, never deleted. Saving Throws, Spellcasting stats, Initiative/Hit Dice, and Magic Item Attunement all shipped in earlier rounds and are unaffected by this merge.

**Currency is now fully merged and consistent across both files.** The player-facing sheet locks Gold/Silver/Copper to genuine `readonly`; DM Tools' Players page carries the one real editor (`renderPlayerCurrencySection`/`sccPlayerCurrencyBlur`) — both confirmed reading and writing the identical `character_sheet.gold_cp` column. This closes the "the two branches disagree on which sheet has the lockdown" gap flagged repeatedly through the DM-side half of this session.

**Still open, as separate future projects — not merge blockers, nothing here was regressed by merging:**
- **Combat encounter persistence.** No schema exists for pre-staging an encounter or reviewing what happened in one after the fact — confirmed these are the same underlying plan→run→resolve lifecycle, not two separate needs, worth one schema decision. The real minimum ("what happened") is just persisting what `sccEndCombat()`'s own Combat Summary popup already computes instead of discarding it; that same data closes the XP/loot session-tagging gap too (`quest_progress.xp_awarded`/`loot_awarded` and `session_notes.inspiration_awarded`/`session_summary` are all confirmed dead, unreferenced columns).
- **Creature/NPC stat-block unification.** Two independent renderers still exist (Combat's own vs. the lookup drawer's), the same risk shape as the already-completed NPC/Location field-union — flagged, not built.
- **Cross-session Tagged Note search.** Everything is still scoped to one session; a second real session will make this a real gap, not a theoretical one.
- **Quest/entity linking beyond the manual `[[type:id]]` convention** already in place for Field Guide and quest text — no automatic cross-referencing, no durable `quest_progress` history (a beat touched in two sessions loses its first session's record — needs a real append-only log if that history ever needs to be durable).
- **Spell catalog cleanup items** — 4 possibly-non-core spell flags and 5 duplicate spell-name pairs both still need a DM decision; the Tier 1 (level/school/classes) audit of the 41 already-flagged-unavailable spells is deferred, lowest priority.

*Session Aug 16 2026 (same session, real PC AC + a death-save tracker for downed PCs, both ported from the character sheet's own logic) — `dm.html`.*
Two asks: PCs' AC badge was showing nothing (only creatures had `c.ac` populated at staging time), and no death-save tracking existed anywhere in the Combat Tracker.
**AC**: ported `computeAC()` verbatim from `sheet/index.html` as a new `sccComputePcAC(pc, sheet)` — same armor-weight-class dex-cap rules (heavy=none, medium=capped, light/none=full dex), same shield/magic-bonus math — reading `window.ITEMS` (already fetched app-wide in `sccInit()`) and the PC's own `character_sheet.equipped`, so a combatant's AC always matches what their own sheet would compute. Wired into `sccAddToCombatStaging()` where creatures already got `creature.ac` — PCs get `sccComputePcAC(pc, SCC_SHEETS[id])` in the same slot. Verified against hand-computed values for all 3 real characters (Kael: leather armor + DEX 15 → 13; Hana/Visenya: no armor + DEX 10 → 10) — exact match, both from the raw function call and in the actual rendered row.
**Death saves**: new `.scc-init-ds-panel`, rendered below a combatant's row only when `c.isPC && isDowned` (monsters have no death-save mechanic in 5e — creatures correctly get nothing here). Reads/writes the *same* `character_sheet.death_save_successes/failures` columns the player's own sheet uses (not a parallel combat-only value) — a save marked mid-fight is the exact save the sheet shows if the player checks live. Same click-to-set/click-filled-to-undo pip behavior as the sheet's `renderDeathSavesHtml()`. Also added the sheet's existing heal-clears-death-saves rule (crossing back above 0 resets both to 0) to `sccAdjustHP()`/`sccSetHP()`, which didn't have it before — a real gap, since without it a PC who stabilized and got healed would still show old death-save marks the next time they dropped.
**Found via testing, not fixed**: rapid-fire pip clicks (zero delay, direct function calls) can race the same way this session's relationship-badge fix (2 rounds ago) documented — out-of-order PATCH responses can land a value that doesn't match the last click. Confirmed this does NOT happen under realistic click-to-click timing (verified both ways: zero-delay calls landed succ=1 despite 2 successive clicks meaning succ should be 2; the same sequence with real gaps landed the correct succ=2). Not fixed here — death-save clicking during a real tense moment is closer to "one deliberate click" than the relationship badge's "rapid cycle through 5 states" use case that justified debouncing there, but it's on record as a latent risk shared with the existing (also un-debounced) `sccSetHP`/`sccAdjustHP`.
Verified live end-to-end with a real encounter: AC badges for all 3 PCs matched hand-computed values; dropped Kael to 0 HP, confirmed the death-save panel appeared with correct pips as clicked (2 success, 1 failure — with real inter-click delays), confirmed via SQL the values landed in Supabase; healed him to 5 HP, confirmed both local state and Supabase show death saves reset to 0/0 and the panel disappeared. Screenshotted the downed state showing the AC badges, Downed flag, and death-save pips together. Reverted all 3 PCs to their real original HP (Kael 1, Hana 12, Visenya 10) and confirmed death saves 0/0 via a final SQL read. Zero console errors. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty.

*Session Aug 16 2026 (same session, 5-item annotated screenshot on the Combat Encounter Tracker's initiative rows — all 5 built directly, none needed clarification) — `dm.html`, `css/tokens.css`.*
DM's screenshot: (1) thicker/brighter row-type border colors, (2) a small uploaded-character-photo thumbnail next to PC names, (3) move the initiative number to the far right of the row, (4) replace the plain HP steppers with the character sheet's own health-bar visual, (5) replace the plain "AC 11" text with the sheet's own AC badge styling.
**1 — Borders**: `--scc-init-active-bg/border`, `--scc-init-pc-border`, `--scc-init-enemy-border` (tokens.css) moved from translucent rgba (0.12/0.4 opacity) to solid full-opacity `var(--gold)`/`var(--blue)`/`var(--red)`; `is-pc`/`is-enemy` border-left widened 3px→5px, the active-turn box-shadow ring widened 1px→2px.
**2 — Photo thumbnail**: `dm.html` never fetched `character_photo` before — added it to `sccLoadPlayers()`'s existing `Promise.all` (alongside `character_sheet`/`classes`), building `SCC_PLAYER_PHOTOS` keyed by character_id, same public-storage-URL pattern `sheet/index.html` already uses for the player's own portrait upload (confirmed live: despite CLAUDE.md's storage table calling `character-photos` "private," the real code path is `/storage/v1/object/public/character-photos/...` — a stale doc note, not a real blocker). `sccRenderInitiativeList()` renders a 26px circular `<img>` before the PC's name only when a real photo exists — no placeholder for a PC who hasn't uploaded one. All 3 real characters have real uploaded photos, confirmed via SQL and verified live.
**3 — Initiative number moved**: relocated from the row's first element to second-to-last (just before the Remove button, which stays the row's absolute final control per this file's own convention for destructive actions) — satisfies "farthest right" without displacing Remove from its predictable end position.
**4 — Health bar**: new `.scc-init-hp-wrap` is now a pill-shaped bar (`--red-track`/`--red-strong`, same tokens and look as the sheet's own `.cs-mi-hp-compact-bar/-fill`) with a `position:absolute` fill sized to the real HP%. Kept the existing −/input/+ quick-adjust controls layered on top rather than switching to the sheet's click-to-edit-only model — a judgment call, not asked either way, made in favor of combat speed (this file has consistently protected fast mid-fight interactions over visual purity elsewhere). Required an explicit z-index fix: CSS positions the fill above static siblings by default regardless of DOM order, so the −/input/+ elements needed `position:relative;z-index:1` to actually render on top of it.
**5 — AC badge**: new `.scc-init-ac-badge` ports the sheet's blue/rounded-bottom-only, stacked-label-over-number treatment (`.cs-mi-ac-badge`) at a scale that fits the compact combat row (0.5rem/0.8rem vs. the sheet's 24px/32px).
Verified live end-to-end with a real encounter (all 3 PCs + a live Grusk Enforcer, real Supabase HP/photo data): confirmed via `getComputedStyle()` all 4 border treatments render solid full-opacity; all 3 PC rows show real photo thumbnails with correct URLs, the enemy row correctly shows none; the enemy's AC badge renders "AC" + "11" stacked; the HP bar fill width tracks real HP% exactly (Kael at 1/12 → 12px of a 150px bar); confirmed the −/input/+ controls sit at z-index:1 above the fill's z-index:0, not hidden beneath it. Screenshotted. Zero console errors. No Supabase writes this round (combat staging/initiative is pure client-side state, confirmed unchanged from earlier in this file's own history) — nothing needed reverting. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty.

*Session Aug 16 2026 (same session, a 4th independent XP-badge render site found — the Live Session "Current Beat" panel's `.scc-quest-beat-ref-xp` had never been touched by any earlier gold/blue/green pass) — `dm.html`.*
DM's screenshot showed "+50 XP on completion" in green in the Live Session panel's "Current Beat" card. Grepped every `+... XP` render site in the file to be thorough rather than trust the two already-fixed classes covered everything — found `.scc-quest-beat-ref-xp` (line ~13338, a completely separate class from `.scc-beat-xp`/`.scc-live-beat-xp`), still hardcoded to the original green from before any of this session's beat-color rounds touched it. Recolored to gold, matching the other two XP badge surfaces exactly. A 5th grep hit (`.scc-combat-stat-val`/`-label` "XP", a creature's combat XP-value stat) is a different concept entirely (combat reward, not quest-beat completion) and was correctly left untouched.
Verified live via a real dropdown-driven `in_progress` state change (not a direct function call): confirmed via `getComputedStyle()` the badge resolves to `rgb(240,180,41)`/`rgba(240,180,41,0.1)`, matching the other XP badges exactly. Reverted the test write, confirmed via SQL. Zero console errors. Pre-push checks clean.

*Session Aug 16 2026 (same session, DM: "In Progress is the same as Active, change it to green" — Active/In Progress moves off gold to its own color, everywhere) — `dm.html`, `css/tokens.css`.*
Direct correction, applied immediately: Active (Quest Status) and In Progress (beat status) are the same concept and both move to green — `--dm-quest-active-bg/text` and `--scc-beat-prog-bg/text/border` repointed from gold to `var(--green)`/`rgba(34,197,94,...)`, plus `.scc-beat-item--active`'s `!important` overrides and its pulse keyframe (previously gold-toned, now green-toned to match). This resolves the gold/gold Stakes collision flagged the last two rounds without touching Stakes at all — Available=blue, Active/In Progress=green, Blocked=red, Completed=grey is now 4 fully distinct hues with zero overlap anywhere on a quest card.
Verified live via a real dropdown change (`in_progress` on beat 1): item border-left, status dropdown bg/text all `rgb(34,197,94)`; confirmed Quest Status's own `.q-status-active` resolves the same green. Reverted the test write, confirmed via SQL. Zero console errors. Pre-push checks clean.

*Session Aug 16 2026 (same session, DM flagged the beat-status colors as "Wrong" via 3 screenshots — extended the Quest Status blue/gold/red/grey scheme from earlier the same session down to per-beat status too) — `dm.html`, `css/tokens.css`.*
DM sent 3 screenshots of the beat detail list's status states (Done=green, Available=grey, In Progress=blue) captioned "Wrong colors," no further detail. Read them against the Quest Status scheme specified earlier the same session (Available=Blue, Active=Gold, Blocked=Red, Completed=Grey) — beats have the same 3 analogous states minus Blocked (not_started≈Available, in_progress≈Active, completed≈Completed), and none of the 3 currently matched. Applied the same mapping: not_started→blue, in_progress→gold, completed→grey.
This is the beat-progress color's 4th distinct scheme this session (gold→blue→"blue-for-everything-but-completed"→now blue/gold/grey) — implemented as a direct supersession of the "make everything blue except completed" decision from earlier the same session, since that decision was itself explicitly about avoiding a Stakes-badge collision, and this round's instruction reintroduces gold for the in-progress state regardless.
Repointed the shared `--scc-beat-todo-*`/`--scc-beat-prog-*`/`--scc-beat-done-*` tokens (not new hex — reused `--blue`/`--gold` and a grey literal matching Quest Status's own `#a0a0a0` exactly) so every consumer updates from one place: rail segments, beat item-box backgrounds/borders, the `.scc-beat-item--active` highlight (including its `!important` overrides, which had been hardcoded blue — now gold, and its pulse keyframe, left gold-toned when this rule went blue earlier this session, now correctly matches its own base state again instead of silently fighting it), the status dropdown's 3 modifier classes, and the Live Session panel's independent beat-dot renderer (repointed from `--dm-green`/`--dm-blue` literals onto the same shared tokens directly, so this third render surface can't drift out of sync again — it's had to be chased down and fixed twice already this session).
**Flagged, not silently absorbed**: gold for the in-progress beat highlight reintroduces the exact Stakes-badge collision (Mid Stakes = gold) that blue was chosen earlier this session specifically to avoid — now happening at both the quest level (Active status) and the beat level (in-progress highlight) simultaneously. Implemented as instructed since the DM's ask was explicit; not resolved here since fixing it means recoloring Stakes, which wasn't part of either ask.
Verified live end-to-end via real dropdown-driven state changes (not direct function calls): set beat 1 to in_progress and beat 2 to completed through the actual `<select>` elements, confirmed via `getComputedStyle()` that the rail segments, item boxes, dropdowns, and (after switching tabs) the Live Session panel's dots all resolve to the correct gold/grey/blue across all 3 states — then reverted both via the same real dropdowns and confirmed via a fresh SQL read that all 4 beats are back to `not_started`/`null`. Zero console errors. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty.

*Session Aug 16 2026 (same session, the actual final cause of the XP/dropdown vertical-alignment complaint — the previous round's fix reduced it but didn't fully close it, per a follow-up screenshot showing it "still" off — plus a wording change) — `dm.html`.*
DM sent a zoomed screenshot of the real "+50 XP" / dropdown pair on the live localhost instance, still visibly offset, with "Fix alignment" and a second ask: rename the dropdown's not-started option from "To Do" to "Available." Re-measured live via `getBoundingClientRect()` rather than assuming the prior round's fix was sufficient just because it had reduced the gap — confirmed the previous round's `align-items:center` fix genuinely helped (centers were only 3.2px apart, both boxes near-equal height) but two real causes remained, found by reading the full render chain rather than stopping at the first plausible one:
1. **Native `<select>` intrinsic height doesn't reliably match a plain span's, even with identical padding** — a real cross-browser quirk, not something `align-items:center` alone fixes. Gave both `.scc-beat-xp` and `.scc-beat-status-select` an explicit shared `height: 1.35rem` plus `display:inline-flex; align-items:center` directly on the select itself (not just its container) — removes any dependency on native select box-model differences between browsers/engines.
2. **The real remaining cause, found after equalizing heights and still seeing a 3.2px offset**: `.scc-beat-actions` (the div wrapping the status select + Live Session button — confirmed via grep it has exactly one render call site, always nested inside `.scc-beat-meta`) still carried `margin-top: 0.4rem`, a leftover from before the XP badge and the actions row were consolidated onto one line. A flex row's `align-items:center` centers each child's full margin box, so that leftover top margin was pushing the actions group down relative to its sibling XP badge even with equal content heights. Removed the margin entirely — nothing else uses this class, so it's safe to drop rather than override per-context.
3. **"To Do" → "Available"**: the not_started beat-status option's display text, one render site (grep-confirmed), changed directly — no value/enum change, this was purely display wording.
Verified live via `getBoundingClientRect()` on all 4 real beats after both fixes: XP badge and status dropdown centers are now pixel-identical (0px difference) on every beat, not just close. Screenshotted the Session>Quests panel confirming "Available" now shows in all 4 dropdowns, level with their XP badges. Zero console errors. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty.

*Session Aug 16 2026 (same session, real root-cause fix for a vertical-alignment complaint the DM had flagged multiple times before — first time it was actually diagnosed via computed layout rather than guessed at) — `dm.html`.*
DM sent a DevTools screenshot with `.scc-beat-meta` selected, "+50 XP" and "To Do" visibly off-center from each other, captioned "vertical center the damn exp text! I've complained about it multiple times." Read the actual CSS before touching anything, per D2 — root cause: `.scc-beat-meta` (the flex row holding the XP badge and the status dropdown+button group) had no `align-items` set, defaulting to `stretch`. The status-dropdown group (`.scc-beat-actions`) is taller than the XP badge's own natural content height (select padding vs. a plain span), so the stretch forced `.scc-beat-xp` to match that taller height — but the span itself has no internal vertical-centering of its own, so its text sat pinned to the top of the now-taller box instead of centering within it. This is exactly why the complaint kept recurring: nothing about the XP badge's own declared styles was wrong in isolation, the misalignment only appeared once it sat next to its taller sibling in a stretch-default flex row.
Fixed at the actual cause, not by nudging the text with padding: `.scc-beat-meta` gained `align-items: center` (each child now sizes to its own natural height and centers against its siblings, instead of one being stretched to match another), and `.scc-beat-xp` gained `display: inline-flex; align-items: center` as a second, defensive layer so the badge's own text stays centered even if its box height ever changes for another reason (e.g. wrapping). Also removed one exact duplicate `.scc-beat-status-select:hover` rule sitting immediately below (found while reading this block, zero behavior change, matches this file's own no-duplicate-rules convention).
Verified live via `getBoundingClientRect()`, not just a screenshot: the XP badge's vertical center and the actions group's vertical center are now 3.2px apart (630.2 vs 633.4) on real rendered content, down from the badge being stretched to the full 25.6px row height with its 17px of text pinned to the top — and confirmed visually via screenshot that "+50 XP" now sits level with "To Do" across all 4 beats. Zero console errors. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty.

*Session Aug 16 2026 (new session, DM specified the full Quest Status color scheme directly — a real recolor, not a consistency sweep) — `css/tokens.css`. A new gold/gold and red/red collision with Stakes was created as a direct, foreseeable consequence and is flagged below, not fixed unprompted.*
DM's instruction was direct and itemized: Available=Blue, Active=Gold-Yellow, Blocked=Red, Completed=Grey. Read the current tokens first, per C1 — Available (blue) and Completed (grey) already matched exactly; only Active (was green) and Blocked (was a red-orange `#ff8066`, itself reused from Active's pre-recolor hex 2 sessions ago) needed real value changes. All 4 colored quest-status render sites (Quest Dashboard cards, Quest Dashboard detail badge, quest-card top border, Session>Quests "Active Quests This Session" panel) share one set of tokens (`--dm-quest-active/avail/blocked/done-bg/text`) and the shared `.q-status-*` classes — this was a single-file, token-only edit, zero `dm.html` changes needed, every render site updates automatically. Active repointed to `var(--gold)`/`rgba(240,180,41,0.15)`, Blocked repointed to `var(--red)`/`rgba(248,113,113,0.15)` (was a raw literal hex, now a real token reference like the other 3 states).
**Flagging a real, foreseeable collision this creates, not silently absorbed**: Quest Stakes (Low/Mid/High) already uses the identical green/gold/red traffic-light scale, and a Stakes badge renders immediately next to the Status badge on every quest card. Active (now gold) will look identical to Mid Stakes (gold) on any quest that's both; Blocked (now red) already visibly collides with High Stakes (red) on the one real quest that's both today (Anomaly 7-C — confirmed live, screenshotted, both badges render as the same red side by side). No quest in the real data is currently `active` status, so the gold/gold case isn't visible yet, but it will be the moment one is. This is the same one-hue-two-meanings pattern this whole session has been eliminating elsewhere — implemented exactly as instructed since the ask was explicit and itemized, but not resolved unilaterally since fixing it means recoloring Stakes (not asked) or changing how Status is signaled (e.g. dropping the badge in favor of relying on the border-top color, already present) — a DM call, not mine to make silently.
Verified live: created a real quest-status badge test element in the DOM for `active`/`completed` (no `active` quest exists in real data to verify against directly) and read real `available`/`blocked` badges off actual quest cards — all 4 states confirmed via `getComputedStyle()`: Available `rgb(96,165,250)`, Active `rgb(240,180,41)`, Blocked `rgb(248,113,113)`, Completed `rgb(160,160,160)` — exact matches to the DM's spec. Screenshotted the Quest Dashboard showing the new colors and the real Blocked/High-Stakes collision on Anomaly 7-C. Zero console errors.

*Session Aug 15 2026 (same session, a sixth round on the same beat list — investigated a "beat numbers wrong color" report that turned out to already be correct, then two real, direct fixes: XP badges reverted to gold, status dropdown recolored per its current value) — `dm.html`. Full detail in Decision Log.*
DM's screenshot annotated "Change back to yellow-gold" on the beat number labels ("1"/"2"/"3") and repeated "Change to blue" on the XP badges. Investigated the number labels first, per D2 — grepped `.scc-beat-num` and confirmed it had never been touched by any edit this session, still `color: var(--dm-gold)`; live computed-style check on all 4 beat numbers confirmed `rgb(240,180,41)` (`#f0b429`, the app's real gold), matching a fresh screenshot too. Reported this back as already-correct rather than making an unnecessary change — same "verify before assuming a regression" discipline as two earlier rounds this session that also didn't reproduce. DM's next message clarified the real remaining ask directly: XP text should be gold-yellow (reverting last round's move to blue), and the beat-status dropdown's own color should reflect its current value instead of a flat neutral grey regardless of state.
**XP badges**: `.scc-beat-xp` (Quest Dashboard's "Active Quests This Session" beat list) and `.scc-live-beat-xp` (the Live Session panel's independent beat-dot renderer — checked as a sibling per this session's established habit, since the two have drifted before) both reverted from blue back to gold (`var(--dm-gold)`, `rgba(240,180,41,...)` background/border) — undoing exactly the one XP-badge color change from 2 rounds ago, nothing else on either surface touched.
**Status dropdown reflects state, not a flat neutral**: `.scc-beat-status-select` previously rendered identically regardless of whether the beat was To Do/In Progress/Done — the dropdown's *text* said the state but its color didn't. Added 3 modifier classes (`--not_started`/`--in_progress`/`--completed`), reusing the exact tokens the beat rail already uses for the same 3 states (`--scc-beat-todo-*` grey, `--scc-beat-prog-*` blue, `--scc-beat-done-*` green — the grey token was sitting unused since the rail itself moved to all-blue-except-completed 2 rounds ago, now given a real second job here) — render code appends `scc-beat-status-select--' + st` to the `<select>`'s class. Deliberately did NOT make the dropdown follow the rail's own blue-for-not-started choice: the rail's "blue is the whole quest's active theme" reframing was a DM-driven, comprehensive instruction for that specific element; the dropdown is a different control being asked to do a different job here ("reflect current status" only makes sense if its 3 states are visually distinct from each other), so giving it its own honest not_started/in_progress/completed traffic-light coloring using existing tokens is the correct application of the same design language, not a reversal of it.
Verified live end-to-end, not just via computed style on the resting state: cycled a real beat through all 3 values via the actual dropdown's `change` event (not direct function calls) — confirmed not_started renders grey (`rgba(255,255,255,0.04)` bg / muted text), in_progress renders blue (`rgb(96,165,250)`), completed renders green (`rgb(34,197,94)`) — then reverted to not_started the same way and confirmed via a fresh SQL read that all 4 real `quest_progress` rows for the test quest are back to their original `not_started`/`null` state. XP badges confirmed gold (`rgb(240,180,41)`) on both the Quest Dashboard panel and the Live Session panel. Zero console errors. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty.

*Session Aug 15 2026 (same session, the DM re-annotated the same "make blue" request across all 4 rail segments plus all 3 XP badges — reversing the prior round's declined interpretation) — `dm.html`. Full detail in Decision Log.*
The prior round declined coloring "not started" beat segments blue, reasoning it would collide with "in progress." This round's screenshot annotated literally every rail segment (1-4, including the ones that were never in-progress) and every XP badge with "Make blue" — a DM's repeated, comprehensive annotation overrides a prior inferred design read, so implemented directly rather than re-litigating. Reframed the model: blue isn't "in progress" specifically, it's this quest's whole-card active theme (matching its "Available" status), with only Completed staying green as a real, distinct win-state. `.scc-beat-seg--not_started` and `.scc-beat-xp` both moved to blue. Checking for siblings before calling it done surfaced a third independent render of the same beat-progress concept — the Live Session panel's own beat dots/XP text — still gold; fixed there too so a DM switching tabs mid-session doesn't see the collision reappear. Also tightened `.scc-beat-meta`'s top margin (0.4rem → 0.2rem) per "too much empty space on bottom of text." Verified live across both surfaces via computed style and a real dropdown-driven state change.
"Fix alignment" pointed at a real, measured bug: the active beat's `+XP` badge sat 3px right of every other beat's, because `.scc-beat-item--active`'s 4px left border (vs. the base item's 1px) pushed its flex content over — confirmed via `getBoundingClientRect()` before touching anything, not assumed from the screenshot alone. Fixed by reserving the same 4px on every beat item (transparent when inactive), so becoming active no longer shifts the content. The "▶ In Progress" dropdown reported as still gold did not reproduce — checked via computed style and a fresh screenshot on the exact code just shipped, confirmed neutral/grey both ways; most likely a stale browser cache, an on-record recurring issue in this file. Declined, with reasoning given back: making the "not started" rail segments (2/3/4) blue too, per two of the screenshot's arrows — blue is now specifically "in progress," and coloring "not started" the same blue would recreate the exact one-color-two-meanings collision this session has spent three rounds fixing.

*Session Aug 15 2026 (same session, one more annotated screenshot — a real gold/gold collision on the same "Active Quests This Session" card, plus a click-to-cycle button replaced with a dropdown) — `dm.html`, `css/tokens.css`. Full detail in Decision Log.*
The prior round's status-badge parity fix turned out not to be the whole story — the DM's "this should be Blue" annotation pointed at the beat-progress rail's "in progress" highlight, which was still gold, sitting directly under the "Mid Stakes" badge (also gold) on the same card. Recolored `--scc-beat-prog-bg/text/border` to blue, plus a second `!important` gold override (`.scc-beat-item--active`, the expanded beat box's pulsing highlight) that the token change alone wouldn't have reached. Separately, replaced the per-beat status button (click cycled through To Do → In Progress → Done with no visible indication of what clicking would do) with a real 3-option dropdown, mirroring the quest-status dropdown pattern from 2 rounds ago. Verified live: both the compact rail segment and the expanded beat box now render blue, confirmed via computed style; the dropdown correctly reads/writes the same `quest_progress` table the old button did.
`sccRenderQuestPanel()` (Session → Quests, not the Quest Dashboard) has its own independent quest-card renderer, `sccRenderQuestPanel()` — already reused the shared `q-status-*`/`q-stakes-*` CSS classes correctly, so status color was already consistent, but had two of its own bugs the Quest Dashboard fixes never reached: the "Local/Regional/Arc/Campaign" beat-count tier badge hardcoded the *exact* same blue as the Available status badge sitting right next to it (a real collision, not just similar), and the stakes badge still showed the raw lowercase enum (`"mid"`) instead of the `stakesLabel()` fix from the Quest Dashboard round. Tier badge recolored to the same neutral grey used for other non-status informational badges (Suggested Level, First Available); stakes badge switched to `stakesLabel()`. Verified live: "Regional"/"✦ Available"/"Mid Stakes" now three visually distinct badges, confirmed via computed style.

*Session Aug 15 2026 (same session, live DM review of the Quest Dashboard pass above, from an annotated screenshot) — `dm.html`. Full detail in Decision Log.*
Two direct annotations plus one real color collision surfaced by a third: (1) added a status-colored top border to each quest card in the list (`quest-card--status-*`, reusing the exact status colors already established, not new hex) — a second always-visible signal beyond the badge text. (2) Moved the status dropdown from the very bottom of the selected quest's detail panel to the top right, next to the title — matches the same "move it to where a DM looks first" reasoning as the Suggested Level move earlier this round. (3) The DM asked "why is this blue?" about the Suggested Level card badge — real answer: no reason, it was reusing the exact same blue as the Available status badge, which became a genuine collision the moment status also started driving the card's top border. Recolored to a plain neutral grey (matching the sibling "First available" badge's own existing color) since Suggested Level isn't a status and shouldn't borrow one's hue.

*Session Aug 15 2026 (same session, 5-item DM feedback round — Deity reclassified as an entity type not a relationship, AC/Stats fixed in active combat, a global Quick Note button, and a Quest Dashboard UX pass) — `dm.html`, `css/tokens.css`, `session_notes.npc_relationships`. Full detail in Decision Log.*
**Deity is an entity type, not a relationship — fixed at the data-model level, not just relabeled.** Removed `deity` from `SCC_REL_CYCLE` entirely (now `neutral/ally/caution/hostile`, 4 states) and added a separate, purple, non-cyclable "✦ God" badge driven by the real `world_characters.category === 'god'` field, shown alongside the relationship badge wherever it renders. Oro's relationship (previously `'deity'`, now an invalid enum value) migrated to `'ally'` — his god-status is now correctly carried by his real `category` field instead. Also deleted a fully dead legacy CSS block (`.scc-npc-card.rel-*`/`.scc-rel-badge.rel-*`) discovered while touching this — referenced `--scc-rel-*` tokens removed as dead code last round, confirmed via grep to have zero real consumers even before that.
**Combat: AC and Stats were only ever wired into the pre-combat staging list, never the live initiative tracker.** Real bug, not a design choice — once "Start Combat" ran, staging hid and the initiative list took over, and neither AC nor the Stats toggle+panel (both built for staging 2 rounds ago) ever made the jump. Both now render on every combatant row during actual combat, reusing the exact same `sccBuildCreatureStatBlockHtml()` staging already used — verified live with a real creature, mid-fight, both AC and full stat block (abilities/attacks/traits/actions) confirmed showing.
**Quick Note — global entry point, real UX complaint.** DM's own words: getting to a quick note required Session tab → Notes sub-tab → scroll past 5 fields → find the button, three steps for something meant to be instant. Added a floating "📝 Quick Note" button fixed to the bottom-right corner, visible from every tab (page-level, same placement pattern as the lookup drawer) — opens a small self-contained modal (category + title + body), posts directly to `session_note_entries`, closes. The Notes tab's own Quick Notes section (filter/search/browse all of them) is untouched — still the review surface, just no longer the only way in. Verified live: opened and submitted from the Manage tab (nowhere near Notes), confirmed the row landed in Supabase, confirmed it appeared correctly in the Notes tab's own list afterward, reverted.
**Quest Dashboard UX pass**, 4 items:
1. Stakes text was inconsistent between panels — left card showed the bare enum ("high"), right detail appended " stakes" itself ("high stakes"). One new `stakesLabel()` used by both now — "High Stakes" everywhere.
2. Suggested Level added to the left-panel card preview (a new `Lvl X-Y` badge) and moved to the top of the right panel, directly under the status/stakes badges — was previously the very last thing on the page, below the status selector.
3. Quest text (Hook/Summary/DM Notes/Reveals) now supports the same `[[type:id|Label]]` inline-link markup Field Guide already uses — same reasoning for manual-over-auto-detection as that build (short real names in this exact dataset risk false matches). New `sccLinkifyEscaped()` is a sibling of `sccFgLinkify()`, not a reuse — Field Guide content is trusted raw HTML and re-escapes its link label on the way out, quest text is already HTML-escaped by the time linkify runs, so re-escaping there would double-escape it. The DM's own example ("Osric") turned out to have no backing `world_characters` row at all — confirmed via SQL, not assumed — so linkify alone can't fix that specific case; flagged rather than fabricating a new NPC record unprompted. Verified the capability end-to-end with a real, temporary `[[character:drak|Drak]]` marker in a real quest's DM notes (Drak is real, confirmed via SQL) — clicked it, confirmed the lookup drawer opened showing his real card, then reverted the marker, matching this file's own established test-then-revert convention for Field Guide's link feature.
4. Answered directly, not just fixed: DM Notes and Reveals both being genuinely DM-eyes-only content, they used to get different visual weight (DM Notes: full border, icon, uppercase label; Reveals: thin left-accent bar, plain-text caption) purely because they were built at different times, not for any real reason. Reveals now renders through the same `.scc-dm-block` treatment as DM Notes. Related-entry tags also got a check for `collection === 'quests'` — a distinct gold "🔗 Related Quest" style, though no real quest currently links to another one to show it live; the schema also has no direction field (blocks vs. blocked-by), only a flat `related[]`, so a real quest-to-quest link would show as related but not as which way the dependency runs.
DM's own words: "Dormant implies the players worked on it and then forgot. Horrible UX." Investigated before renaming, per D2 — checked whether "blocked by completion of a separate quest" (the DM's own conditional framing) is a real mechanism anywhere in the schema: it isn't. `quests` has no prerequisite/`blocked_by`/`depends_on` column, and the 3 real dormant quests' `related[]` arrays only ever point at plain lore entities (cities/characters/organizations), never at other quests. The only real gating mechanism is `first_available` (a session number) — confirmed again against the same 3 quests from the round that first answered this question. Renamed the enum value itself, not just the display label — `status:'dormant'` → `'blocked'` on all 3 real rows, plus every code reference (filter dropdown, CSS class, `statusOrder`, `statusOptions`, `labels`) — keeping the stored value and the displayed word in sync was the whole point of this session's earlier `--cyan`-rename fix, so leaving `dormant` as the internal value while showing "Blocked" on screen would have reintroduced the exact same class of problem one message later. New color is red-orange (`#ff8066`) — reused, not invented: it's the exact hex Active used before this session recolored Active to green, freed up rather than left orphaned.

*Session Aug 15 2026 (same session, DM color-scheme rework driven directly by the DM's review of the published Color Ledger artifact — 4 hue reassignments, one status question answered, a real token rename, and a full pass of dead-code/duplicate cleanup across the color systems touched this session) — `dm.html`, `css/tokens.css`, `field_guide_sessions` (Supabase data). Full detail in Decision Log.*
Direct, itemized DM instructions, all executed as given: Quest Stakes Low/Mid/High recolored to a plain green/gold/red traffic-light scale, Campaign stakes removed entirely (dead option, 0 real quests ever used it); Quest Status Active/Available/Completed recolored to green/blue/grey (Dormant's own real meaning — gated behind a `first_available` session number that hasn't arrived yet, confirmed against all 3 real dormant quests — answered directly, kept its own muted blue-grey tone since Completed just claimed plain grey); Field Guide box colors reworked — Lore (blue) dropped entirely (9 real boxes migrated to grey/no-color), Comedy moved from pink to a genuine bright yellow, Loot & Shop moved from terracotta to gold, Purple removed outright (0 real usage, freed for religion/deity per the DM's explicit reservation); NPC Relationship Neutral/Ally swapped to grey/blue. Also done, explicitly instructed rather than optional: `--cyan`/`--cyan-dim`/`--cyan-border` (holding magenta since last round, a real "variable name doesn't represent what it is" case) renamed to `--dm-only`/`--dm-only-dim`/`--dm-only-border` across every consumer; the duplicated NPC-relationship color object (2 identical copies) and the triplicated region-health color ladder (3 identical copies) both consolidated into single shared functions (`sccRelColor()`, `sccRegionHealthColor()`) matching the `sccConditionColor()` pattern from 2 rounds ago; confirmed-dead code removed outright — 20 unused `--scc-rel-*` tokens, 3 unused `--currency-*` tokens, the unused `SCC_REL_ICONS` object (also a latent C2 violation, literal emoji in an unused JS object), and the fully-dead `.scc-flag`/`.scc-flag--vareth`/`.scc-flag--formery` CSS (re-confirmed zero consumers before deleting, not assumed from memory).
Direct payoff of the consolidation work one round earlier: this was purely a token-value edit, 8 lines in tokens.css, zero changes to dm.html — confirmed via grep that no hardcoded DM-Only hex/rgba literal exists anywhere outside tokens.css anymore. Covered `--cyan`/`--cyan-dim`/`--cyan-border` (confirmed via grep these are 100% DM-Only-scoped, no other meaning shares them), `--scc-dm-only-bg/border/label` (`--scc-dm-only-text` already derives from `--cyan`, no separate edit needed), and `--dm-reveal-bg/border` (the quest "Reveals — DM eyes only" boxes, a DM-Only-meaning surface that was never wired through the other two token families). Verified live across 3 independently-rendering surfaces — NPC-card DM-only block, organization Quick Look DM-Only badge — both confirmed `rgb(255,0,243)`/`rgba(255,0,243,...)` via computed style and screenshot.

*Session Aug 15 2026 (same session, consolidating the repeat-bug pattern instead of patching each instance) — `dm.html`, `css/tokens.css`. Full reasoning + all 5 fixes in Decision Log.*
**Architecture call: consolidate, don't just patch.** This exact session had already found and fixed the same bug shape four separate times (one meaning, several independent hardcoded copies, silently drifting) — condition colors, DM-Only, and Read-Aloud all had the shape again. Investigated feasibility before touching anything: all three were small, real-source-already-exists consolidations, not a rewrite. Did the consolidation instead of patching each spot.
**Conditions**: deleted two local hardcoded palettes (`COND_DATA` in the Live Session panel, `COND_COLORS` in the combat initiative tracker) in favor of one shared `sccConditionColor(name)` reading the real Supabase `conditions` table everything already fetches once at init. The tracker had disagreed with the real table on 10 of 15 core conditions and had no entries at all for the 3 Anavale-specific ones — now reads the same source as every other surface.
**Read Aloud**: new shared tokens (`--scc-read-aloud-bg/border/text`) in tokens.css; Field Guide's document box, its sidebar block, and the Creature/Item Quick Look card (previously gold, the one outlier) all point at them now.
**DM-Only**: the 3 newly-found stale surfaces (Manage tab's hidden badge, Field Guide sidebar's DM Notes block, Quest Dashboard's DM Notes) all repointed onto the existing shared `--scc-dm-only-*` tokens instead of getting their own fix. Quest DM Notes specifically now reuses the literal `.scc-dm-block` markup already used by the other 2 quest DM-notes surfaces, so there's one class instead of three.
Net effect: 69 lines removed, 48 added — real consolidation, not a like-for-like patch. Verified every fix live via computed style + screenshots.

*Session Aug 15 2026 (same session, building the 5 items investigated above) — `dm.html`, `css/tokens.css`. All 5 built and verified live; full detail in Decision Log.*
**Field Guide DM-Only background fixed** — was hardcoded to the old pre-recolor cyan hex; now correctly the teal token, matching the border/label that were already right.
**DM-Only made structurally distinct, not just differently colored** — applied across all 3 real DM-Only surfaces (Field Guide box, NPC-card block, lookup-drawer block): a full border instead of left-accent-only, a genuinely filled background (up from ~6-7% opacity to 15%), and a bigger/bolder warning icon (a real `::before` corner badge on Field Guide boxes since their label text is DM-authored data; a resized `<span>` icon on the NPC-card title). Verified live on all 3 surfaces via computed style + screenshots — real, visible differentiation now, not dependent on precise hue perception.
**Loot/shop icon discoverability fixed** — renamed the bare "Edit" toggle to "Edit Document", and added a small "✎ Edit Box" link directly on every rendered box (all 48, any color) that opens the editor already scrolled to and highlighting that exact box's row — expanding whichever of the 15 sections holds it automatically. For orange (loot/shop) boxes specifically, also focuses the subtype field directly. Verified live end-to-end on Mira Pont's real shop box: one click landed in edit mode with the correct row highlighted and its subtype `<select>` genuinely focused.
**Relationship badges now auto-save — real data-loss bug fixed, highest priority this round.** Cycling a badge only ever mutated in-memory state before; now saves to Supabase automatically, matching how everything else in this app already behaves. A first version fired one network request per click and hit a real race condition under rapid clicking (confirmed live: 4 quick clicks meant to land on "deity" instead saved "hostile" — whichever response arrived last, not whichever was sent last) — fixed by debouncing to one save 400ms after the last click, so only the state a DM actually lands on ever gets written. Verified: rapid 4-click cycling now saves the correct final value every time, survives a full page reload, and a sanity check across Dran/Mira Pont (in addition to Oro) confirmed multiple NPCs' relationships coexist correctly without clobbering each other. Oro's now genuinely, persistently set to "deity" — the real QA reference this was always meant to be, not just a recommendation.
**Combat stats: AC now shown by default.** Added to the collapsed staging row next to CR/DEX — no click needed. Deeper stats stay behind the "Stats" toggle, per instruction. Verified live: Grusk Enforcer's row now reads "CR 0.5 · AC 11 · DEX +0"; a creature with no AC data correctly shows no AC badge at all rather than a placeholder.
Zero console errors throughout. Pre-push checks clean: raw parse, `new Function()` on all 17 script blocks, diff-scoped literal-emoji grep empty. Supabase confirmed clean afterward — only `session_notes.npc_relationships` differs from before this round, correctly `{"oro":"deity"}` and nothing else.

*Session Aug 15 2026 (new session, 2 confirmed builds + 5 fresh investigations) — `dm.html`. 3 real bugs found this round that the last two rounds' "verified correct" checks had missed — none of them color-perception issues, all concrete code/UX gaps. Full detail in Decision Log.*
**Built — Tagged Notes conceptual fix.** DM's own words: "I do not know what it is, what it is for, and how to use it" — root cause was that nothing on the page ever explained why a second note system exists right below the 5 structured recap boxes. Added a comparison strip between the two ("the 5 fields above are for your end-of-session write-up... Quick Notes below is for jotting something down the instant it happens") and renamed the section "Quick Notes." Replaced the bare "No tagged notes yet" empty state with a real-looking, clearly-flagged, fully-disabled worked example card (dashed border, reduced opacity, gold "EXAMPLE" badge) so a DM sees a finished note before making their first one — verified the filtered-to-nothing empty state ("No notes match") still shows plain, not the example.
**Built — Suggested Level input removed entirely.** Per explicit DM confirmation. The manual number input, its save function, and its Supabase write path are all gone — the computed range + one-line reasoning (built last round) is now the only display, fully read-only.
**Investigated — DM-Only color, re-tested fresh, found a real bug the last two "verified" passes missed.** Dran's actual NPC card is genuinely, fully teal (re-confirmed via computed style + screenshot). But the *Field Guide's* own DM-Only boxes (e.g. "EXACT RECOVERY SEQUENCE") have a real bug: the border and label text are correctly teal, but the box's *background fill* is still hardcoded to the old pre-recolor cyan hex (`rgba(34,211,238,0.07)`) — never updated when the border/label were fixed in an earlier round. Not yet fixed, reported first per instruction.
**Investigated — DM-Only vs Read-Aloud color distinguishability.** Confirmed the two hues sit only ~30° apart on the color wheel (teal ~172°, green ~142°) despite being "technically distinct" — a real, quantified closeness, not an exaggerated complaint. Given the app's palette is already fully claimed (blue=neutral, red=hostile/danger, purple=deity/high-stakes, pink=comedy, orange=loot, gold=brand), recommending a non-color differentiator (solid filled background + a larger/bolder icon) over hunting for a new hue, since every unclaimed hue would either collide with an existing meaning or need yet another repaint round.
**Investigated — loot/shop subtype icon "not working."** Confirmed via a genuine cold-start click-through (not direct function calls) that the feature itself works correctly — the real problem is pure discoverability. Nothing on the page ever uses the term "block editor" (that was my own description, not UI copy); the actual entry point is a small, unlabeled "Edit" button next to the Field Guide's session selector, and once inside, the DM has to expand the right one of 15 collapsible sections and scroll to find the specific box, with no search and no visible label on the subtype dropdown beyond "Icon: coin (default)." Recommending a visible hint near "Edit" plus better in-context labeling — not yet built.
**Investigated — Oro's deity badge "still blue."** Root cause found and it isn't a render bug: `sccCycleRelationship()`/`sccCycleRelationship2()` (the relationship badge click-to-cycle) only ever update the in-memory `SCC_NPC_RELATIONSHIPS` object — there is no auto-save. The value only reaches Supabase if the DM separately visits the Setup tab and clicks "Save Setup." Confirmed live: cycled Oro to "deity," watched the badge correctly turn purple in the moment, reloaded the page, and watched it silently revert to empty/neutral — confirmed via SQL that `npc_relationships` has been `{}` the entire time. This is inconsistent with almost everything else in the app (loot, notes, quest status all auto-save on their own action) and is very likely exactly what happened to the DM. Not yet fixed.
**Investigated — Combat stats, AC "not showing."** Confirmed AC *is* correctly included in the Stats panel's output when a creature has real data (verified live: "AC 11 (leather armor)" rendered correctly for Grusk Enforcer) — no data or rendering bug there. The real gap: the default collapsed staging row shows only Name/Type/CR/DEX — no AC, no HP, nothing — until the DM clicks "Stats," which is easy to miss mid-session. Recommending core stats (AC at minimum) show by default without a click, given the DM's stated real-table use case — not yet built.
Zero console errors throughout. Pre-push checks clean: raw parse, `new Function()` on all 17 script blocks, diff-scoped literal-emoji grep empty.

*Session Aug 15 2026 (same session, building the 5 items approved from the investigation round above) — `dm.html`. All 5 built and verified live; full detail in Decision Log.*
**Loot/shop subtype icons**: new `subtype` field on orange (loot/shop) Field Guide boxes, DM-assigned via a second dropdown in the block editor (Drink/Food/General Store/Weapons/Armor/Magic, only shown when the box's color is orange) — icon in the rendered box label switches accordingly (🍺/🍽/🛎/⚔/🛡/🔮); no subtype (including all pre-existing orange boxes) falls back to the original coin icon, so nothing already migrated needed touching. Verified live end-to-end: set Mira Pont's "AVAILABLE TO PURCHASE" box to General Store, saved to Supabase, confirmed 🛎 renders in the real document view, then reverted (removed the `subtype` key entirely via SQL) since this was a test pick, not a real DM categorization decision — block count still 140, nothing else touched.
**Roll Initiative copy**: button now reads "Start Combat — Sort by Initiative" with a `title` tooltip spelling out the real behavior ("Uses any initiative you've already typed as-is; rolls a d20+DEX for anyone left blank, then sorts the full list"). No behavior changed, `sccStartCombat()` untouched.
**Per-creature combat stat-blocks**: new "Stats" toggle next to "Loot" on every creature's staging row, opening a panel that renders only the fields actually populated on that specific creature — AC/HP/Speed/CR/ability scores/saves/skills/resistances/immunities/senses/languages/attacks/traits/actions/reactions/legendary actions, each line omitted entirely when null/empty, nothing invented or estimated. Verified live with both a fully-statted creature (Grusk Enforcer — full block rendered, screenshotted) and a lore-only creature (Fluffets — clean "No stat-block data recorded for this creature yet." message, no placeholders). Combat staging is pure client-side state, cleared after testing, nothing to revert server-side.
**Tagged Notes fixes**: (a) creating a note now always resets the active category filter and search text to "All"/empty, so a freshly-created note can never be hidden by whatever filter was active — chose reset-over-auto-switch since auto-switching would itself hide whatever the DM was previously looking at. (b) `sccAddTagNote()` now requests `return=representation` (not the shared helper's `return=minimal`) to get the new row's real id back immediately, auto-focuses that exact card's title input, and shows a "Note added ✓" toast on success. Verified live against the exact reported failure mode: set the active filter to "Player Actions," picked "DM-Only" in the new-note category select, clicked the real "+ New Note" button — screenshotted before (filtered view, 0 notes, "No notes match") and after (filter auto-reset to "All (2)," new note visible at top with a focused, ready-to-type title field). Both test notes deleted afterward via SQL (matched on blank title+content, confirmed nothing else in that table matched).
**Oro logged as the deity QA reference** (no code change) — see the investigation entry below for detail; recorded here so future QA involving deity-relationship NPCs starts from a known-real example instead of re-deriving it.
Zero console errors throughout. Pre-push checks clean: raw parse, `new Function()` on all 17 script blocks, diff-scoped literal-emoji grep empty (one real near-miss caught before it shipped: a `✓` landed as a literal character in a new `showDMToast()` call via the same Edit-tool escape-normalization issue on record from prior sessions — fixed via the established Python exact-string-replacement script, confirmed via grep afterward).

*Session Aug 15 2026 (new session, structured investigate-then-report QA round) — `dm.html`. Environment check first, then 7 investigate-and-report items, 2 build items. Full findings in Decision Log; summary here for continuity.*
**Environment check, confirmed clean**: the local-dev banner from the prior round is real and renders correctly on a fresh server/reload — rules out "banner never built" as an explanation for any "still looks wrong" report this round.
**Dran's DM-Only box "yellow/gold" report — did not reproduce.** Expanded Dran's real NPC card live and read `getComputedStyle()` on the box border, title, and all 36 internal colored elements (icons/labels) — every single one resolves to `rgb(45,212,191)`, the correct teal, confirmed by screenshot too. No third hidden color rule exists in the current code. Most likely explanation: browser cache of an old `dm.html`/`tokens.css` on the DM's machine (an on-record recurring issue in this file), a stale local checkout, or the actual live GitHub Pages site (which has none of this branch's fixes, since this branch has never been merged to whatever branch Pages deploys from).
**Loot/shop category icons — no existing sub-categorization to build on.** `field_guide_sessions` block schema stores only a single semantic `color` field per box (`grey/green/cyan/blue/purple/orange/pink`) — there's no drink/food/general/weapon/armor/magic distinction anywhere in the data, and the block editor's box row is just a Label input + one Color dropdown. Recommended, not built: add a `subtype` field to orange (loot/shop) boxes specifically, surfaced as a small dropdown in the block editor next to the existing color select, DM-assigned per box — explicitly not auto-detected from box text (too unreliable to trust silently, per instruction).
**Roll Initiative — real behavior confirmed via `sccStartCombat()`.** It does NOT override manually-typed init values: for each combatant it reads the init input field, uses the typed value as-is if present, and only rolls a random d20+DEX for blanks — then sorts everyone (typed and rolled together) by initiative descending. So a DM who's typed every value gets a pure sort with nothing rolled; a DM who's typed none gets everyone rolled. Recommended clearer copy: "Start Combat — Sort/Roll Initiative" or a tooltip reading "Uses your typed values; rolls a d20+DEX for anything left blank, then sorts" — current copy ("Roll Initiative") overstates what happens when values are already filled in.
**Creature stat-block coverage — same 4/52 creatures for everything, confirmed field-by-field, not assumed.** Queried every field the DM listed: type/AC/CR/size/all 6 ability scores/languages/speed/senses/attacks/traits/HP are all exactly 4/52 (Grey Fluffets, Still Frogs, Grusk Enforcer, Grusk Color Extractor — same 4 every time). Deeper into those same 4: skill proficiencies 2/52, saving throws 1/52, actions 2/52, reactions 0/52, damage resistances/immunities/condition immunities all 0/52, legendary actions 0/52. No `spells` column exists at all — nothing in `attacks`/`actions`/`traits` jsonb for any creature contains spellcasting data. Weapon/damage data lives inside `attacks` jsonb (name/damage dice/type/attack bonus) — populated for the same 4. Also confirmed the combat encounter builder today only ever pulls `cr`/`hp`/`dexMod` from a creature into a combatant object — nothing else from the stat block reaches the UI at all currently, so a full stat-block display would be new UI work on top of the data gap, not just a data-entry task. Realistic picture for the DM: full-detail combat stat blocks are viable today for exactly 4 creatures; everything else needs real data entry first (AC/HP/type/size/ability scores at minimum) before any display work matters.
**Tagged Notes confusion — root-caused, not just "confusing."** Walked the actual click path: `sccAddTagNote()` posts a blank note and re-fetches, ordered `updated_at.desc`, so a new note *should* land at the very top — but two things make it feel broken. (1) If a category filter chip is currently active and the DM creates a note in a *different* category via the dropdown, the new note is created successfully in Supabase but immediately filtered out of view — the DM sees literally nothing happen. (2) Nothing auto-focuses the new note's title field or scrolls it into view or shows any success confirmation — it just silently appears (or doesn't, per #1) after a brief loading flash. Recommended: reset/match the active filter to the new note's category on create, auto-focus the title input immediately after render, and add a brief visible confirmation (toast or highlight) — not a cosmetic tweak, a real interaction-completeness fix.
**Deity NPC for QA reference, confirmed real.** `world_characters` has 5 real `category:'god'` rows — Oro, Nara, Thyun, Solvara, Grak — but relationship state (`ally/neutral/caution/hostile/deity`) is a per-session DM-toggled cycle stored in `session_notes.npc_relationships`, not tied to NPC identity; that field is currently `{}` (empty) for the one real session, so no NPC anywhere is *currently* set to "deity." Recommended for future QA: add Oro (most prominent, benevolent, widely-worshipped) to a session roster and click his relationship badge 4 times to cycle to "deity."
**Built (no investigation gate): Suggested Level range + reasoning.** New pure `sccSuggestedLevelRange(quest)` — never writes `quest.suggested_level`, only computes a display-only range + one-line reasoning shown next to the still-manually-editable field. Baseline from `first_available` ("session-N" → lvl N to N+1, modeling roughly one level per session), nudged +1 for `high` stakes / +2 for `campaign` stakes (`low`/`mid` unchanged), widened to include a related creature's CR-derived level when real CR data exists on a `related[]` creature entry (checked: today only one quest, The Greytalon, has any creature relation at all, and that creature's CR is null — so the CR nudge is real, wired, and correctly inert on all 10 current quests until CR data exists). Verified live on 3 real quests via direct function calls and the rendered UI (e.g. "Suggested range: lvl 1-2 (session 1, low stakes)" next to an untouched, still-blank "Not set" input).
**Built (no investigation gate): Combat tab layout.** Moved "+ Add All Players" into the top row next to the search box (was a separate row below the staging list). Added `margin-top:0.75rem` spacing both above and below the combatant list so it's no longer flush against the search row and the button row. "Start Combat — Roll Initiative" pushed to the far right of its row via `margin-left:auto` (Clear All stays left). Verified live with 2 real combatants staged, screenshotted, then cleared (client-side-only state, nothing to revert server-side).
Zero console errors throughout. Pre-push checks clean: raw parse, `new Function()` on all 17 script blocks, diff-scoped literal-emoji grep empty.

*Session Aug 14 2026 (new session, local-dev environment banner) — `dm.html`. Added a persistent, impossible-to-miss red banner at the very top of the page, auto-detected purely from `window.location.hostname` — no manual per-checkout setup, no build step. Shows "⚠ LOCAL DEV BUILD — NOT LIVE — {hostname}:{port}" whenever the hostname isn't exactly `carlosknightiii.github.io`; silent (no banner, zero layout shift) on the real live site. True git branch/folder name isn't detectable from a static HTML file served with no backend — confirmed this isn't achievable client-side rather than faking it, so the fallback is hostname:port exactly as the fallback the ask specified. Push-down handled via a `--dm-env-banner-h` CSS variable set from the banner's real measured height (not a hardcoded pixel guess), applied to both `.gate-overlay` (position:fixed, needs an explicit `top` override) and `.dm-app` (normal flow, needs `margin-top`) so neither the password gate nor the app header ever sits under the banner. Verified live: banner renders correctly on `localhost:PORT` with the exact right text, gate card and app header both cleanly offset below it with pixel-exact alignment (33px banner height, 33px `--dm-env-banner-h`, 33px `.dm-app` top — zero gap, zero overlap), zero console errors. Live-domain behavior verified by calling the exact same condition function used in the shipped code against `'carlosknightiii.github.io'` directly (returns `false`, correctly hidden) rather than by actually deploying, since this session works on a feature branch that was never merged to the branch GitHub Pages deploys from. One real near-miss caught by the standing pre-push check: the ⚠ warning-sign character landed as a literal glyph in the new inline script (Edit tool normalizing the escape on write, same recurring issue on record) — fixed via the established Python exact-string-replacement workaround before it ever reached a commit.*

*Session Aug 14 2026 (new session, DM's second QA pass — found real gaps in the first pass's own coverage, all investigated fresh and fixed) — `dm.html`, `css/tokens.css`. Explicit instruction not to trust prior "verified" claims; re-investigated everything from scratch.*
**Colors — the first purple/blue/orange audit genuinely wasn't exhaustive.** A fresh, complete grep of `var(--purple)`/`#c084fc` (not the narrower search used last time) turned up a 4th real usage the first pass missed entirely: `--dm-stakes-high-text: var(--purple)` — quest "high stakes" badges, reached through a token indirection (`.q-stakes-high` → `--dm-stakes-high-text` → `--purple`) the first audit's direct-usage grep didn't follow. Recommended and (confirmed) implemented: reuse `--orange-vivid` (same hex Combat's "high" difficulty already uses) rather than inventing a 10th distinct hue in an increasingly crowded palette — the two badges never appear in the same view, so sharing a hue for the same underlying idea ("elevated severity") is a feature, not a collision. The reported DM-Only-vs-Vareth collision **did not reproduce** — investigated every real Vareth-related visual element (the location-card VARETH badge is a plain grey `#a0a0b0`, unrelated to teal; two `.scc-flag--vareth`/`.scc-flag--formery` CSS rules exist but are dead code, never referenced by any HTML/JS) and found no actual overlap; reported this back rather than inventing a fix for a collision that isn't there.
**A much bigger gap: two entire red "DM-only" surfaces the first pass never found.** Grepping the actual red hex (`#d62b2b`, `#f87171`) instead of relying on memory of what had already been fixed turned up: (1) `.scc-npc2-dm-block` — the large "DM Only — Do Not Read Aloud" card on NPC/location panel cards, with its nested "Secret"/"Notes"/"Vareth Presence" row labels (`.scc-npc2-red`) — a completely separate rendering surface (NPC/Location panel expanded cards, not the lookup drawer) that neither of the two earlier DM-Only recolor passes had touched, still hardcoded to `#d62b2b`. (2) Quest "Reveals" boxes (`.quest-reveal-item`, explicitly labeled "DM eyes only" in its own header) — also still on the pre-recolor `rgba(248,113,113,...)` red. Both converted to the teal system. Separately, `.scc-dm-text` (the body-text rule inside the `.scc-dm-block` box fixed last round) turned out to still hardcode `color: var(--dm-red)` — the earlier fix only touched the shared `--scc-dm-only-*` token set for background/border/label, never noticing this one rule bypassed that token set entirely — exactly explaining the reported "teal border, red/brown body text" inconsistency. Fixed by pointing it at `--scc-dm-only-text` (a token that already existed, previously unused).
**Suggested Level**: confirmed genuinely interactive (real `<input type="number">`, not disabled/readonly, exposed correctly as an accessible textbox) — the "looks non-interactive" read was about visual affordance, not function. Added a dashed border while empty (`:placeholder-shown`) plus a hover cue, so an unset field visibly invites a click instead of reading as static label text.
**Field Guide bleeding into every Session sub-tab — a real, confirmed CSS bug, not intentional.** `#scc-panel-fieldguide { display: flex; ... }` was a bare ID rule with higher specificity than the base `.scc-panel { display: none; }`, so it stayed visually present (search bar, filter chips, permanently-stuck "Loading Field Guide…") underneath every other Session sub-panel regardless of which one was actually `.active`. Gated the rule on `.active` like every other panel; confirmed both that the leak is gone on Quests/Combat/Notes and that Field Guide itself still renders correctly (grid layout, real document content) when it is the active tab.
**Combat roster quick-add chips**: confirmed they hard-disabled after one click (`SCC_COMBATANTS.some(id match)` guard, plus the render logic swapping to a permanently non-interactive `<span>`) — unlike the search box, which the previous round fixed. Made the chips stay clickable indefinitely, replacing the old checkmark-and-disable with a live `×N` count, so the DM keeps the "already used" signal without losing the ability to add more.
**Tagged Notes**: moved the whole section (hint text, add-row, search/filters, list) to sit between the 5 structured recap fields and the Save/Copy/Session-Completed controls — during-session content before end-of-session actions, not after. Split the cramped single-row toolbar into a visually distinct gold-boxed "add a note" row (category select + primary-styled "+ New Note" button) sitting above a plainer search/filter row, so adding a note reads as the section's primary action instead of one more filter control.
Verified every fix live (screenshots + computed-style checks, not assumed), zero console errors, all test data cleaned up. Pre-push checks clean — the only emoji hits in the diff were 3 pre-existing characters relocated by the Tagged Notes reposition (confirmed present in the prior commit via `git show HEAD:dm.html`, not newly introduced).*

*Session Aug 14 2026 (new session, DM QA pass — 3 color collisions investigated + fixed, 6 bugs investigated + fixed, 2 combat features built) — `dm.html`, `css/tokens.css`, `field_guide_sessions` data. Full findings + all fixes in Decision Log; summary here for continuity.*
**Colors (investigated fresh, reported, confirmed before touching hex):** purple's 3-way collision (deity/Combat-high/Field Guide comedy, all literally `#c084fc`) resolved by keeping purple for deity (rarest, best semantic fit) and moving Combat-high to vivid orange `#fb923c` (freed up by the loot/shop change below — gives a real green→gold→orange→red difficulty ramp) and Field Guide comedy to pink `#f472b6` (a hue with zero prior semantic meaning). DM-Only's cyan — now confirmed reading as "blue" for the *second* time against Field Guide lore boxes and the NPC "neutral" badge — moved to a true teal (`--cyan` token redefined to `#2dd4bf`) *and* paired with a 💠 icon added to every player_facing "DM Only" text badge (character/organization/region/religion/species), not just Field Guide boxes, since hue alone had already failed once. Loot/shop's alarm-toned orange desaturated to a terracotta `#d9a66c` (kept the hue family rather than switching, per the DM's own instinct minus gold, which is already overloaded 5+ ways) — a 🪙 coin icon was added to loot/shop box labels unconditionally, per instruction, regardless of the color outcome.
**Bugs:** Suggested Level's blank input was confirmed as-designed (all 10 quests genuinely have no value set) with a weak empty state — fixed to a real "Not set" placeholder. Mira Pont's/Goldenway's missing Shop sections were confirmed to be the vendor_stock cleanup from the original build's own verification pass (0 rows for everyone) — the section and its add-stock search box both render and work correctly right now; no code bug found or needed. Nations were reachable via global search but mislabeled "Type: Location" (a real, previously-flagged-but-unfixed bug in the city/POI/nation lookup branch's `_srcType` check) and missing nation-specific fields entirely — fixed the type-label logic to a reliable direct-array-membership check and added Government/Capital/Beliefs/Gigglegloom Relationship fields. Item (and Quest/Creature/Organization) DM Notes/Secret blocks were still rendering in the pre-recolor red — the shared `--scc-dm-only-*` token set was never touched by the original cyan recolor pass (only the separate badge color was) — fixed once at the token level, fixing all 6 call sites simultaneously. Manage tab name truncation was a real CSS bug: 3 stacked hardcoded pixel caps (220px/240px duplicate `.cm-td-name` rules plus a 180px `.cm-name-link` cap) truncating names regardless of available space — removed all three, also cleaning up the genuine duplicate rule this file's own conventions forbid.
**Features:** Combat's search-based creature add was silently blocked from adding the same creature twice (`sccPickerSelect`'s dedup guard, meant for the other 4 picker types, had no real purpose for combatants — `SCC_COMBATANTS` is array-indexed everywhere, never looked up by id) — carved out a combatant-specific bypass so 3x Grusk Enforcer works via repeated search-and-click. Added a "Clear All" button next to Add All Players, confirm-gated, wired into the same show/hide logic as the other encounter-builder buttons.
Verified live end-to-end for every item (see Decision Log), zero console errors, all test data (vendor_stock rows, combat staging) cleaned up afterward. Pre-push checks clean — one real near-miss caught and fixed before it shipped: the DM-Only icon edit initially landed as a literal emoji character in 5 JS string literals (Edit tool normalized the `💠` escape into the literal glyph on write) — caught by the standard post-edit emoji grep, fixed via a Python script for exact byte-level replacement, per this file's own established C2 workaround.*

*Session Aug 14 2026 (same session as the investigation below — DM confirmed the block-based storage shape and the manual `[[type:id|Label]]` link syntax — built and shipped) — `dm.html`, new `field_guide_sessions` table. This closes out the last remaining goal from the Aug 12 2026 DM Tools UX/IA investigation. New `field_guide_sessions` table (`session_number unique`, `blocks jsonb`, RLS mirroring the anon-all-access pattern), replacing the fully static 505-line hardcoded Session 1 document with a data-driven renderer. Session 1's real content was parsed out of the old hardcoded HTML with a one-off Python/BeautifulSoup script (scratch-only, not committed) into 140 ordered blocks — 48 boxes, 30 h2, 20 standalone tables (2 more genuinely nested inside orange shop boxes, matching the investigation's own finding), 14 h1, 15 p, 4 italic, 4 lists, plus title/subtitle/tagline/meta/footer — and inserted as one row via direct SQL. **Verification was rigorous, not visual**: fetched the real *pre-migration* `dm.html` (via `git show HEAD:dm.html`) into a hidden fragment, extracted its `textContent`, extracted the new block-rendered version's `textContent` the same way, and confirmed they're identical once whitespace is stripped entirely (34,733 non-whitespace characters, exact match) — the only difference was insignificant block-boundary spacing from the old source's manual indentation, not content. New `sccFgRenderBlock()`/`sccFgRenderDoc()` reconstruct the exact same CSS classes the static markup used (`scc-fg-doc-h1`, `scc-fg-box scc-fg-box--{color}`, `scc-fg-table` + variants, etc.) so zero CSS changed. A new session selector (`<select>` sourced from the already-loaded `SCC_ALL_SESSIONS` — no new fetch needed) sits above the document; switching it also fixes the real pre-existing bug the investigation found — `SCC_FG_DATA` (the sidebar's session-scoped NPC/creature/location/item cache) is now explicitly reset to `null` on every session switch, so the sidebar actually refetches for the newly selected session instead of staying stuck on whichever session loaded first. A DM-facing editor groups blocks by their nearest preceding `h1` into collapsible sections (same shape as tonight's `session_scenarios` editor, but with a richer per-row type set: text inputs for headings, HTML-capable textareas for prose, and genuinely structured row/header editors for tables — including the 2 real box-nested-table cases — rather than raw-HTML editing anywhere). Inline entity links use manual `[[type:id|Label]]` markup (per the DM's confirmed choice over auto-detection), parsed at render time into a span wired to the existing `sccOpenLookup()` drawer — paired with a link-insert helper reusing the `.scc-picker-search`/`.scc-picker-dropdown` UI already established this session, so the DM finds the right entity by name instead of typing raw ids. Verified live end-to-end: the byte-for-byte content check above; a real link inserted via the actual search-and-click UI (linked "Pellam Voss" text to the real NPC), saved, and clicked in read view — correctly opened the real lookup drawer showing Pellam Voss's full card; the empty-session state (`SCC_FG_DOC_SESSION_NUM` pointed at a session with no row) correctly rendered "No Field Guide written for Session 2 yet" instead of erroring; the session selector correctly shows just "Session 1" today with no crash. All test edits (the inserted link) reverted afterward, confirmed via SQL the real document is back to its exact 140-block migrated state. Zero console errors throughout. Pre-push checks clean (raw parse, `new Function()` on all 16 script blocks, diff-scoped literal-emoji grep empty — the box labels' real emoji characters, e.g. 💠, are Supabase data fetched at runtime, not literal characters in `dm.html`'s own JS source, so C2 doesn't apply to them).*

*Session Aug 13 2026 (new session, Field Guide multi-session support — investigation + 2 open design questions, no code changed) — confirmed the current Session 1 Field Guide document (`dm.html` ~3236-3740) is a single, fully hardcoded 505-line HTML block with real richness: 14 numbered `scc-fg-doc-h1` sections, 30 `scc-fg-doc-h2` subsections, 48 colored callout boxes across 6 CSS color variants (green/yellow(renders cyan)/blue/purple/orange/grey), 22 tables (2 genuinely nested inside orange "shop" boxes — Mira Pont's stock, the Inn's drinks — the rest standalone 2-3 column key/value or scenario tables), 4 bullet lists, plus document-level title/subtitle/tagline/meta/footer text. The color legend is itself a real, migratable sentence inside the grey "HOW TO USE THIS GUIDE" box — not app chrome, so it isn't being hardcoded separately, it moves with Session 1's content like everything else. Confirmed the sidebar's session-scoped fetch (`sccRenderFieldGuide()`, keys off `SCC_SESSION.session_number` to filter NPCs/creatures/locations/items down to what's assigned in Setup) is real and reusable, parameterizable to any session number with a small change — but it's completely disconnected from the static document today, and a real pre-existing bug was found in the process: `SCC_FG_DATA` is cached forever and never reset to `null` anywhere, so the sidebar wouldn't actually refresh even if something did point it at a different session. Recommended storage: a new `field_guide_sessions` table, one row per `session_number`, holding the entire document as one ordered `blocks` jsonb array (title/subtitle/tagline/meta/h1/h2/italic/p/list/table/box/footer block types) rather than a handful of named columns — reasoning: the document's real content is too heterogeneous (arbitrary mixes of prose, boxes, and nested tables per section) for a clean typed-column schema, and a flat ordered block list is both the safest way to guarantee lossless migration (each block stores its real inner HTML/text close to verbatim) and a natural fit for a collapsible-by-h1-section editor mirroring tonight's `session_scenarios` pattern. Tables get genuine structured sub-fields (header row + data rows, both top-level and nested inside a box) rather than raw HTML, since they're a large, editable-worthy fraction of the real content (NPC reference cards, price lists). One open design question sent for confirmation: inline entity links — manual `[[type:id|Label]]` markup (DM-controlled, paired with a searchable link-insert helper so the DM doesn't need to memorize IDs) recommended over auto-detecting entity names in prose text, since auto-linking risks false positives on short, common-word-adjacent real names in this exact dataset (Gerald, Grey, Vel, Fess, Mira) and this app has consistently favored explicit DM-authored links (`quest.related[]`) over inferred ones everywhere else. Waiting on confirmation of the storage shape and the link mechanism before writing any schema or UI code, per instruction. Full findings in Decision Log.*

*Session Aug 13 2026 (same session as the investigation below — DM confirmed Tier/Category/Region dropdowns plus the widened text search, explicitly skipped the CR sort toggle — built and shipped) — `dm.html`. Scoped exactly to what was confirmed. New `.scc-combat-filters-bar` (Tier/Category/Region `<select>`s + Reset) added above the existing combat-add search box, options populated live from `CREATURES`/`REGIONS` each time the Combat panel opens (`sccCombatPopulateFilterOptions()`, same never-hardcode convention as the global search filter bar 2 sessions ago) — Region shows real region names (Caparia/Jugabi/Nombi/Sohot) via a `REGIONS` id→name lookup since `creatures.regions` only stores ids. Built a dedicated `sccCombatantSearch()` rather than extending the shared `sccPickerSearch()` used by 4 other picker types (location/npc/quest/item) — kept those completely untouched. New search matches name **and** `habitat`/`description`/`tags` text (previously name-only), and applies the 3 filters to creatures only — a PC has no tier/category/region so it's correctly excluded from results whenever any filter is active, no special-casing needed beyond a `filtersActive` flag. Also replicated the "browse with an empty query once a filter is active" behavior the global search got 2 sessions ago — a DM can now see e.g. "every Tier: humanoid creature" with nothing typed. No CR filter or sort was built at all, per the DM's explicit choice to skip it this pass. New `.scc-audit-mode .scc-combat-filters-bar { display:none }` rule added alongside the existing hidden-in-audit-mode controls, so the filter bar disappears the same way the search box already does when viewing a locked past session. Verified live: all 3 dropdowns populate with the real values (11 tiers, 12 categories, 4 regions); Tier=humanoid with no typed query correctly returned exactly the 2 real humanoid creatures and zero PCs; Region=jugabi returned real jugabi-tagged creatures; typing "rainforest" with no filters active correctly surfaced 3 real creatures via their habitat text, none of which match by name (confirms the widened search is doing real work, not coincidental); a filtered result (Mirrenflow Puffalumps, Category=aquatic) was clicked through the real UI and correctly landed in the real encounter roster with the difficulty badge updating, then removed; audit-mode hiding confirmed via computed style. Zero console errors, zero Supabase writes needed cleanup (combat staging is pure client-side state until "Start Combat," unchanged by this pass). Pre-push checks clean (raw parse, `new Function()` on all 16 script blocks, diff-scoped literal-emoji grep empty).*

*Session Aug 13 2026 (new session, Combat Tracker filters — investigation only, no code changed, waiting on go-ahead for a recommended filter set) — confirmed the CR sparsity is real and unchanged (4/52 creatures), and confirmed it's the *same* 4 creatures that have any real stat-block data at all (`ac`/`hp_max`/`speed_ft`/`creature_type`/`size`/`creature_alignment`/`xp_value` are all also exactly 4/52 — Grey Fluffets, Still Frogs, Grusk Enforcer, Grusk Color Extractor — everything else is a lore-only entry with no D&D mechanics). Recommended CR as a **sort** (Name/CR toggle, no-CR creatures sort last), not a filter — a CR dropdown would only ever bucket the same 4 creatures into 3 values, no real utility. Bigger finding: `tier` and `category` (already flagged as usable) are genuinely 52/52 populated with clean, well-distributed enums (tier: 11 values, common 21 down to unseen/unknown 1 each; category: 12 values, region-exclusive 9 down to the-unseen 1) — real, working dropdown filters, just never wired into the Combat tab's own `sccPickerSearch('combatant', ...)` box (they're already exposed on the *global* header search from 2 sessions ago, a completely separate UI surface — this pass is additive, not a duplicate). `regions[]` (array, 52/52 populated) resolves to exactly the 4 real region ids (caparia/nombi/sohot/jugabi) confirmed against the `regions` table, each creature tagged to 2–3 on average — confirmed as a genuinely usable "find creatures near here" filter, and confirmed as the correct substitute for "nation": nations belong to regions (15 real nations, each pinned to exactly one of the 4 regions via `nations.region`), but creatures carry zero nation-level tagging, only the coarser region level — building a nation filter would need new schema, building a region filter doesn't. `habitat` is also 52/52 populated but every single value is unique free-text lore prose (52 distinct values, confirmed via a full group-by) — useless as a dropdown, but real material to fold into the search box's own text match (currently name-only) alongside `tags`/`description`, which would make typing e.g. "rainforest" or "vareth" actually surface something without any new UI at all. `tags[]` (52/52 populated) is real but overlaps heavily with tier/category plus a long tail of one-off flavor tags (`omen`, `prophetic`, `wanderkeep-indicator`) — not clean enough for its own dropdown, but same free-text-search value as habitat. Confirmed (not re-derived from the task's own framing) that `world_characters` has zero HP/AC/CR-shaped columns — NPCs-in-combat is out of scope this pass per instruction, not touched. Recommended filter set sent for go-ahead: Tier dropdown, Category dropdown, Region dropdown, a Name/CR sort toggle, and widening the existing search box to match habitat/description/tags text — nothing invented for army/nation/race/level, since none of those exist as real, populated, filter-worthy data today. Full findings in Decision Log.*

*Session Aug 13 2026 (same investigation thread as below, DM answered all 4 design questions — fixed per-vendor list, no new vendor flag, buy-only, woven into the lookup drawer — built and shipped) — `dm.html`, new `vendor_stock` table. Scoped exactly to the 4 confirmed decisions, nothing more. New `vendor_stock` table (`entity_collection`/`entity_id`/`item_id`/`price_gp_override`/`quantity`/`notes`/`sort_order`), RLS mirroring `session_note_entries`' anon-all-access pattern, `updated_at` trigger reusing that same table's existing `update_session_notes_timestamp()` function rather than writing a duplicate. No new flag on `pois`/`organizations` — vendor-hood is purely "has rows in `vendor_stock`," per the DM's own confirmed answer to question 2. `sccInit()`'s existing `Promise.all` gained one more `sccFetch('vendor_stock', ...)` call into a new `window.VENDOR_STOCK` global, same pattern as `species`/`items`/`conditions`. New `sccVendorShopSectionHtml(collection, entityId)` renders a "Shop" section — shared buyer `<select>` (real `SCC_PLAYERS` roster), one row per stocked item (thumbnail, name, catalog price shown alongside an editable price-override input and an editable quantity input — both blank = "use catalog price"/"unlimited," matching the confirmed fixed-list-with-overrides design), a qty-to-buy input + Buy button (hidden, replaced with "Out of stock"/"No price set"/"No players" when purchase isn't currently possible), and a search-to-add-stock box reusing the exact `.scc-picker-search`/`.scc-picker-dropdown`/`.scc-picker-option` markup/CSS `sccLootItemSearch()` already uses — zero new dropdown CSS needed. Wired into `sccOpenLookup()`'s existing `city` branch (line ~13817) and `organization` branch (line ~13968), appended after each branch's existing Tags section. **Real gating bug caught before shipping, not after**: the `city` branch's own `loc` variable is pulled from a fresh `CITIES.concat(POIS).concat(NATIONS)` built inline in that function — it does **not** carry the `_srcType` tag a separate, unrelated function attaches to its own independently-built array elsewhere in the file (confirmed via grep before trusting it), so `loc._srcType === 'poi'` — the check the branch's own pre-existing (and, it turns out, already-silently-broken) "POI" type-label logic relies on — would never actually be true here. Used a direct, reliable check instead: `POIS.some(function(p){ return p.id === loc.id; })`, scoping the Shop section strictly to real POI rows and correctly excluding cities/nations that happen to render through the same branch. Buy transaction (`sccVendorBuy()`) mirrors `sccAwardCoins()`'s gold-arithmetic pattern and `sccAwardItem()`'s inventory-insert shape exactly, composed together with an affordability check (blocks and toasts if the player's real `gold_cp` can't cover it, zero writes) and a stock-quantity check (blocks if buying more than what's left) before either `sccPatch`ing gold down or `sccPost`ing the real inventory row (`dm_granted:false`, since this is a real purchase, not a DM grant) — decrements `vendor_stock.quantity` afterward only when it isn't null (unlimited stock never depletes). Add/remove/edit-stock and the buy transaction all end by re-invoking `sccOpenLookup()` on the same entity so the drawer reflects the fresh state immediately, matching the re-render-in-place pattern this file already uses elsewhere (tag notes, scenario editor). No sell-back path built at all, per the confirmed buy-only scope. Verified live end-to-end against real Supabase data via a fresh local server (a stale server was already running on the old dev port from an earlier session — started a new one on a new port and confirmed via `curl` before trusting it, per the standing lesson): opened Mira Pont's Sundry Goods POI (the same entity whose informal `"shop"` tag drove the original investigation) through the real search-and-click UI, added a real Torch to its stock via the real search box, confirmed the row landed in Supabase with `price_gp_override:null, quantity:null` (catalog price, unlimited). Bought 1 as Hana Sable through the real Buy button — confirmed via SQL her `gold_cp` dropped 900→899 (1cp, matching Torch's real 0.01gp price) and a real `inventory` row was inserted. Set a deliberately unaffordable price override (10000gp) and clicked Buy again — confirmed via SQL zero writes happened (gold and inventory both unchanged), toast correctly blocked it. Set quantity to 0 — confirmed the row correctly flipped to "Out of stock" with the Buy control hidden. Opened a real Organization (Goldenway) and confirmed its own independent Shop section renders correctly scoped to `entity_collection:'organizations'`. Opened a real City (Gobblewump Crossing, confirmed via `POIS.some()` that it is NOT a real POI) and confirmed the Shop section correctly does not render — the POI-scoping fix holds. Zero console errors throughout. All test data (the stock row, the inventory row, Hana's gold) reverted via SQL afterward, confirmed clean via a final read. Pre-push checks clean (raw parse, `new Function()` on all 16 extracted script blocks, diff-scoped literal-emoji grep empty).*

*Session Aug 12 2026 (new session, Vendor/Shop — investigation only, no code changed, waiting on 4 real design questions) — confirmed fully net-new as expected: zero vendor/shop flag or field anywhere on `pois`/`organizations` (`poi_type` real values are town/dark-zone/site/landmark/waystation — nothing shop-like), Mira Pont's real shop stock exists only as hardcoded HTML prose in the Field Guide document, not linked to the real `items` catalog at all. One soft signal found: her POI row already carries a free-text `"shop"` tag in its `tags` array — informal, not structured, but a real existing DM convention worth reusing rather than inventing a parallel one. Confirmed `items.cost_gp` (127/139 non-null, 18 of those are genuinely 0 — mostly cosmetic accessory-slot rows from the appearance-catalog migration, not shop-worthy — and 12 carry real fractional PHB-accurate prices like 0.01/0.05/0.5 gp) and `character_sheet.gold_cp` (integer copper-pieces, `renderGold()`/`onCurrencyBlur()` in `sheet/index.html`) as the two real quantities a purchase would need to reconcile. Bigger finding than expected: the low-level transaction mechanics already exist in `dm.html`, just never composed into a priced flow — `sccAwardCoins()` already does `character_sheet.gold_cp` arithmetic via `sccPatch()`, `sccAwardItem()` already POSTs a real `inventory` row (`character_id, token, item_id, quantity, dm_granted`) — a "buy" action is these same two calls with a cost check and subtraction instead of addition, not new plumbing. Neither call is wrapped in a real DB transaction (two independent REST calls) — a pre-existing pattern, not a new risk this would introduce, but worth having agreed with the DM going in. `sheet/index.html`'s `runInventorySearch()`/`selectCatalogItem()` (the +Add Item modal) is the closest reusable browse-and-buy UI pattern — name+category search against `items`, qty input, one-click add. 4 real design questions sent to the DM (stock model, whether a new vendor flag is needed, buy-only vs. buy+sell, and UI placement) — waiting on answers before writing any schema or code, per instruction. Full findings in Decision Log.*

*Session Aug 12 2026 (new session, Session Setup — nav restructure + 2 related gaps) — `dm.html`. Investigated first: confirmed Setup was one of 8 sub-panels (`sccSwitchPanel`) nested under the Session top-level tab, sharing its header/party-bar/history-dropdown with NPCs/Locations/Quests/Combat/Notes/Live Session/Field Guide. Gave it a real top-level nav spot (new `#form-setup`, `switchTab('setup')`) at the same level as Manage/Quests/Players/Session — relocated the panel's markup verbatim (session number/date, prep notes, all 5 entity pickers, save button) with zero internal changes, confirmed by grep that no other code referenced `#scc-panel-setup` by id/class before moving it. NPCs became the new default-active Session sub-panel. Found and fixed a real dependency the move exposed: `.scc-audit-mode` (the read-only history-view lockdown) was only ever applied to `#form-session` — now applied to `#form-setup` too in both `sccLoadHistorySession()`/`sccLoadCurrentSession()`, or a completed/audited session's setup would have stayed live-editable on the new page. Added a real `session_scenarios` editor (group/action/response, matching the exact shape confirmed against live data — 8 groups, 44 rows) inside the new Setup page, previously Supabase-only; saves as one PATCH via a local working copy, not per-keystroke. Wired Combat's creature-add to surface `SCC_SELECTED.creature` (Setup's assigned creatures) as one-click quick-add chips above the existing full-text search, which stays available for anything not pre-assigned. Verified live: Setup reachable and populates real data from its own nav spot (screenshotted); a real scenario edit saved to Supabase and was independently confirmed picked up by the Live Session panel's Scenarios search via a fresh fetch (not just client-side state); a creature already assigned in Setup (Grey Fluffets) added to a real encounter with one click, no search typed; audit-mode lockdown confirmed applying to the new Setup page. Zero console errors. Committed and pushed to this branch (`anavale-wiki-scc-ux`).*

*Session Aug 12 2026 (new session, Note-taking — tagging + filterable view, @mention explicitly deferred) — investigated `sheet/index.html`'s `character_notes` Notes tab first, per instruction, before touching `dm.html`. Found it's one-row-per-note (`character_id, tab, title, content, sort_order, is_favorite`), while `session_notes` is NOT a notes table so much as the master per-session record — one row per session_number, also carrying Session Setup's location/npc/creature/quest/item assignment arrays, session_status lifecycle, session_scenarios (read by Field Guide), premise/prep_notes. Recommendation (stated, not silently picked): don't restructure `session_notes` — it would break Setup/Field Guide/session-history/audit-lock, all unrelated to note-taking. Added a new one-row-per-note table instead, `session_note_entries` (mirrors `character_notes`' shape: category/title/content, scoped by `session_number`), and built a new "Tagged Notes" section inside the existing Notes panel, below the 5 structured recap fields (kept as-is, still real and load-bearing — read by `sccCopyPostSessionSummary()`/`sccLiveSaveNote()`). 7 tag categories: the ask's 4 (DM-Only, Player Actions, Quest-Related, Follow-Up) folded together with the 5 existing textarea themes, merging the two real near-duplicate pairs (Player Decisions→Player Actions, Follow-Up Needed→Follow-Up) rather than keeping two near-identical tags — net new: Unexpected Moment, Loose Thread, Milestone. DM-Only badge reuses the cyan token from 3 sessions ago, Quest-Related reuses gold, matching those established meanings instead of inventing new colors. Filter chips (category + count) + free-text search (title+content) + copy-to-clipboard + delete, no pin/favorite/drag-reorder (not asked, character_notes has them but this pass didn't). Verified live: added notes across 4 categories (one via full real click-and-type, others via the same insert path the button itself uses), category filter narrowed 4→1 correctly, text search "Gerald" narrowed 4→2 correctly (matched via content, not just title), delete removed a real row via the actual button (4→3, confirmed via SQL), copy-to-clipboard confirmed carrying the exact right text (intercepted `navigator.clipboard.writeText` directly, since the sandboxed test browser blocks real OS clipboard access — not a code issue). All test notes cleaned up afterward. Zero console errors. Committed and pushed to this branch (`anavale-wiki-scc-ux`).*

*Session Aug 12 2026 (new session, Quest Manager small-gap closure — 4 independent fixes, no new architecture) — `dm.html`, `quests` table: (1) `quest.related` tags on the Quest Dashboard detail view are now clickable — reuse `sccOpenLookup()` via a new `sccRelatedCollectionInfo()` map covering every real collection name, resolve and show the entity's real name instead of a raw id. This required fixing the still-open lookup-drawer tab-visibility bug from 2 sessions ago as a hard dependency — `#scc-lookup-panel`/`#scc-lookup-backdrop` moved from inside `#form-session` to page level (`.dm-main`, before `.dm-forms-panel`), so the drawer now paints correctly from any tab, not just Session. (2) Added `quests.suggested_level` (integer, nullable) via migration — Content Manager turned out to have no quest edit form at all (only `status` was ever editable, via the quest detail view's own inline selector), so the new field was added there instead, matching that same pattern, rather than building a new CM form (would be new architecture). (3) Fixed the 2 real quests with malformed `stakes` data: `follow-the-dran-lead` free-text → `mid`, `the-letter-for-vel` free-text → `low` — reasoning in Decision Log. (4) Confirmed accurate, re-verified not just recalled: all 10 quests are still `player_facing:false`, `js/router.js`/`js/wiki.js` still have zero quest-route references — flagged as a known, unaddressed gap, not silently left as debt. Verified live: 2 different entity-type tags clicked (character/Torv Bassle, creature/Greytalons) on a real quest with 6 related entries, both opened correct real content; suggested_level saved to Supabase, survived a fresh reload, then reverted (test value, not a real DM call); stakes fix confirmed via the Stakes filter picking up both corrected quests. Zero console errors. Committed and pushed to this branch (`anavale-wiki-scc-ux`).*

*Session Aug 12 2026 (new session, Search+Preview "easy slice" — filters + missing entity coverage, "open in new tab" explicitly deferred) — `dm.html`: (1) added filter dropdowns to the global search — Type selector (NPC/Location/Creature/Organization/Item/Quest) plus per-type secondary filters (NPC: category/player_facing; City: region/nation/city_type/player_facing; Creature: category/tier/player_facing; Organization: alignment; Item: category/rarity/equip_slot; Quest: status), all option lists computed live from the in-memory collections each time the filter bar opens — never hardcoded — so they can't go stale like Content Manager's own color_health/item-category dropdowns did. (2) `sccGlobalSearch()` now also searches Regions/Religions/Species (previously loaded live but never searched), same name+summary substring pattern as every other type. (3) `sccOpenLookup()` gained matching `region`/`religion`/`species` render branches so clicking those new result types actually renders content instead of hitting the same "nothing renders" gap Org/Item had. Small side-fix: `.scc-ql-section-body` gained `white-space:pre-wrap` since Religion's real `practices` field is genuinely multi-paragraph and was rendering as one run-on block. Verified live: all 3 new types searched-and-clicked successfully (Region/Caparia, Religion/Brightcreed, Species/Veilstepped, screenshotted), Item+Category=weapon filter confirmed narrowing to exactly weapon-category items (verified against real `ITEMS` data, not just visual read), NPC+DM-Only filter returned exactly 17 results matching the known real count from the earlier audit. Zero console errors, zero Supabase writes (pure client-side feature). Committed and pushed to this branch (`anavale-wiki-scc-ux`).*

*Session Aug 12 2026 (new session, closing out the residual inconsistency flagged at the end of the prior session) — `dm.html`: swapped the Field Guide's hardcoded DM-Only label glyph from `&#x1F7E1;` (🟡 yellow circle — a leftover from before that box was recolored cyan) to `&#x1F4A0;` (💠 diamond), all 18 real occurrences (confirmed via grep every single one sits directly inside a `.scc-fg-box--yellow` box, no exceptions, no unrelated usage). No cyan circle emoji exists in Unicode, so a genuinely different glyph was needed rather than a like-for-like color swap — 💠 was picked since it reads as a status/callout marker the same way the old circle did, without implying a specific wrong color. Verified via `git stash` before/after (🟡 in a cyan box → 💠 in a cyan box) and `codePointAt()` confirming the live DOM text is exactly U+1F4A0, not a mis-encoded fallback glyph. Pre-push checks clean (syntax, no literal emoji introduced, only entities). Committed and pushed to this branch (`anavale-wiki-scc-ux`).*

*Session Aug 12 2026 (new session, resolving the color-collision report from 2 sessions ago) — implemented the DM's color decisions: (1) DM-Only recolored to one new cyan token (`--cyan` `#22d3ee`, `css/tokens.css`), replacing both its previous colors — `.scc-fg-box--yellow` (Field Guide) and the `player_facing` fallback badge (character + organization lookup cards) — so DM-Only is now the same color everywhere; Read Aloud's green left unchanged per the recommendation (real semantic fit, low practical collision risk — both green usages carry their own text label). (2) Added a small gold quest-related icon (`&#128220;`, `var(--gold)`, no fill/background) next to the relationship badge on the NPC panel's session-roster cards, for any NPC linked to a quest via `quests.related` — icon only, existing `SCC_REL_CYCLE` relationship-badge colors untouched. (3) Documented in this file (not a new token) that any future NPC Hostile/Neutral surface should reuse `SCC_REL_CYCLE`'s existing colors as-is. One residual inconsistency found and flagged, not fixed (out of scope — would mean touching ~20 lines of hardcoded Field Guide document text): DM-Only boxes still show a literal 🟡 yellow-circle emoji glyph prefix in their label text, now sitting inside a cyan-colored box — the box's own background/border/label color are all genuinely cyan, only the inline circle glyph itself still reads yellow. All changes verified via real git-stash before/after screenshots plus computed-style checks (not just visual glance) — see Decision Log for full detail. Committed and pushed to this branch (`anavale-wiki-scc-ux`).*

*Session Aug 12 2026 (new session, immediately after the UX/IA investigation below — fixed the 2 real bugs that investigation surfaced, plus a 3rd it exposed mid-fix) — `dm.html`: (1) `sccLiveSaveNote()` now PATCHes `session_notes.player_decisions` directly instead of only touching the Notes tab's DOM textarea; discovering this required first fixing a 409 bug in the write path it was told to reuse (`sccSaveNotes()`'s unconditional `sccPost` 409s against `session_notes`' `UNIQUE(session_number)` once a session row exists — always true in practice — since `sccPost` has no `on_conflict` target; fixed by giving it the same PATCH-if-exists/POST-if-new branch `sccSaveSetup()` already used correctly). (2) `sccOpenLookup()` gained real `organization`/`item` render branches (was: Organization always "Entry not found" via a bad city-type remap in `sccGlobalResultClick()`; Item rendered nothing, no branch existed at all). Also found and flagged, not fixed (out of the requested scope): `#scc-lookup-panel` is nested inside the Session tab's own hidden form container, so the lookup drawer only ever paints when the DM is already on the Session tab, regardless of which tab the search was run from. All three fixes tested live end-to-end (Supabase writes verified via direct SQL before/after, browser screenshots, console clean) — see Decision Log for full detail. Committed and pushed to this branch (`anavale-wiki-scc-ux`).*

*Session Aug 12 2026 (new session — large DM Tools UX/IA investigation, no code changed) — investigated 8 proposed DM Session Command Center features against the real current `dm.html` state (7 of 8 goals; Claude-integration architecture explicitly skipped, already decided elsewhere). Investigation only, per explicit DM instruction — building will happen in phased follow-up sessions. Full findings relayed to DM in chat; summary here for continuity. See Decision Log for full detail including real bugs found (not just gaps).*
*Session Aug 14 2026 (new session — Handoff B of two, Class tab: Spell Slots redesign + Spells Prepared + Spells Learned) — replaced the old pip-based Spell Slots stopgap with the real design: folded Spell Slots and a new Spells Prepared tracker into the same "Features" accordion Handoff A built, as sibling groups next to Class-Specific, plus a new full-width Spells Learned browser card — `sheet/index.html`, `css/tokens.css`, `classes.spell_slot_progression`/`classes.prepared_count_formula` (new columns), `character_sheet.prepared_spells`/`learned_spells` (new columns), and a full data-population pass on `spells.school`/`casting_time`/`range`/`components`/`duration`/`classes` (all 6 were 100% empty across all 414 rows going into this session). The doc's own 5 open questions were resolved via one round of DM answers, and one answer directly overrode my recommendation: DM chose "Category" = `school` (Abjuration/Conjuration/etc.) over my recommended Gigglegloom `types`, which meant `school` had to be populated for real before Spells Learned could filter by it at all — DM's follow-up instruction was to populate the *entire* empty-column set with real 2024 PHB data, not stub or skip it. Delegated the 414-spell research to parallel background agents (split by level, cross-verified against aidedd.org/dndbeyond.com/roll20 2024 compendium, not memory) — 411 of 414 fully populated, the remaining 3 (`dark-star`, `temple-of-the-gods`, `zeynith-s-ward`) confirmed non-core/homebrew via direct research and correctly left NULL rather than guessed. Verified the full 2024 PHB spell-slot-progression and prepared-spell-count tables myself via direct WebFetch for all 8 casting classes before writing `classes.spell_slot_progression`/`prepared_count_formula` — caught two real errors in the handoff doc along the way: Paladin was wrongly grouped with the full-level "prepare any number" casters (Cleric/Druid/Wizard) when it actually shares Ranger's half-level formula, and Ranger's own rounding was stated as "round up" when the real 2024 rule rounds down (both corrected to `level_divisor:2, round:"down"`). Max values for both Spell Slots and Spells Prepared are derived live from class + character level (same "derive, don't cache" principle as `hp-calc.js`'s HP), not stored/editable — the old stopgap's manual "Set Max" input is gone entirely. `character_sheet.spell_slots` is repurposed from "current available count" to "spent count" (matching the Class-Specific tracker's segment convention) — confirmed safe via SQL first, all 3 real characters' columns were still empty/zero, never used in real play. Spells Learned is hard-scoped to the character's own class spell list (via the now-real `spells.classes` column) — the doc's original "any spell, unrestricted" recommendation was written when that column was still empty for every row; restricting to the character's actual class list is both the more correct 5e behavior and the natural reading of the doc's own filter-column list (no separate Class dropdown was ever specified, meaning the scoping was expected to be automatic). A spell must be Learned before it can be Prepared — a shared picker modal (Prepare a Spell) lists only learned-but-unprepared spells, reusing the existing `#cs-modal-root`/`.cs-equip-results` patterns rather than inventing new modal chrome. Verified end-to-end on all 3 real characters, one per casting shape: Hana (Druid, full caster, ability-mod-plus-full-level formula, 2 level-1 slots) — full learn→prepare→unprepare→spend-slot cycle including the shared picker modal, all DB writes confirmed via SQL then reverted; Kael (Ranger, half-caster, a genuine 0-slots-at-level-1 edge case matching the real 2024 rule that Rangers don't get spells until level 2, rendered correctly as "No spell slots at this level" rather than an empty/broken bar) — hand-verified his real WIS 16 against the corrected half-level-round-down formula (0/3 prepared, exact match); Visenya (Warlock, Pact Magic's genuinely different slot-table shape, fixed-table prepared formula) — hand-verified against her real CHA-derived values. Also simulated (client-side only, no real data touched) a non-caster/no-features class to confirm the Features accordion still skips emitting entirely when empty, and a non-caster-with-limited-use-features class to confirm it correctly shows Class-Specific only with no Spell Slots/Prepared groups — extending Handoff A's "skip emitting rather than emit-then-hide" rule to the new casting dimension, not just the limited-use dimension. Zero console errors, zero horizontal overflow at 1600px desktop or 375px mobile (confirmed via `document.body.scrollWidth`, after an initial false-positive 319px reading traced to the Browser pane tool reporting `window.innerWidth:0` before an explicit resize — not a real bug). `new Function()` syntax check clean. See Decision Log for full detail.*

*Session Aug 13 2026 (continuing the same working session — Handoff A of two, Class tab restructure + Class-Specific tracker) — restructured the Class tab's Overview card into a two-column "Overview" accordion (Class details left, Key Features right) and added a new "Features" accordion holding a Class-Specific limited-use resource tracker (Rage, Channel Divinity, Second Wind, etc.) — `sheet/index.html`, new `classes.limited_use_features`/`character_sheet.limited_use_spent` columns. Pulled the real Figma frame (52:2705) directly via `get_design_context`/`get_screenshot` rather than trusting the handoff doc's paraphrase, and found several real discrepancies: the doc claimed the accordion wraps only Class details + Key Features with Basic staying outside — the actual Figma has the Accordion wrapping Basic too (collapsing would hide the class name/image); kept Basic outside per the doc's explicit, deliberate UX instruction anyway, flagged the conflict rather than picking silently. The doc's own restated field list for the Overview column silently matched what the real Figma shows (no "Hit die:" row) even though current code had one — removed it, doesn't lose data since the header's Hit Dice tracker already shows it. The doc claimed the limited-use feature name renders in Cinzel Decorative — real Figma shows plain Roboto Bold; used the real value. Verified all seed data (Rage, Channel Divinity ×2, Wild Shape, Second Wind, Action Surge, Arcane Recovery) against live web sources rather than memory, and caught real problems along the way: Bardic Inspiration's max uses is Charisma-modifier-derived, not level-derived, so it doesn't fit this schema's `max_at_level` shape at all — skipped and flagged rather than force-fit; the doc's own illustrative example table for Rage was factually wrong (claimed unlimited at level 20; real cap is 6, reached at level 18, not 17). Three real open questions resolved with the DM before writing code (Ranger's Favored Enemy has no uses in 2024 — skip it; defer all point-pool features like Lay on Hands/Sorcery Points/Ki Points to a future handoff; skip emitting the Features accordion entirely when empty rather than add a hideIfEmpty flag to the shared `accordionShell()`). Verified end-to-end on Hana (real level-1 Druid — a genuine 0/0 edge case since Wild Shape isn't unlocked until level 2, rendered correctly as an empty bar) and via an in-browser-only simulated Fighter (no real data touched) confirming multi-feature rendering, click-to-spend, click-to-un-spend, and live level-up growing the segment count while preserving spent count. Zero console errors, zero overflow at any width. Pushed live. Handoff B (Spell Slots redesign, Spells Prepared, Spells Learned) intentionally not touched. See Decision Log for full detail.*

*Session Aug 13 2026 (continuing the same session — bug fix, DM-reported) — fixed Story-tab card title clipping (`sheet/index.html`): a one-word title in an unbreakable-word title (e.g. background name "Greywitnessed") could get pushed past the card edge and clip raggedly, no ellipsis, because `.cs-story-card-headtext` (a flex child) had no `min-width:0` and neither `.cs-story-card-title` nor `.cs-past-card-title` had any overflow/wrap handling. Confirmed root cause by reading the actual CSS first, not assuming — exactly the gap the DM described. Fixed with `min-width:0` on the flex wrapper plus `overflow-wrap: break-word` on both title classes (wrap, not ellipsis-truncate — these are lore names a player should be able to read in full, per the DM's own reasoning). Checked all 16 real background names for other long single-word cases, and confirmed region/org names are naturally safe already (multi-word, wraps fine at spaces without any fix — only unbreakable single words were ever at risk). Verified against Visenya's real data (background: Greywitnessed) at three widths, including a forced worst-case 240px (`.cs-story-card`'s own min-width floor) where `scrollWidth === clientWidth` confirmed zero overflow; also confirmed all 7 Your Past cards (both the shared-headtext layout and the `--full` layout, which needed the title-level fix specifically since its container already had `min-width:0` but the text itself didn't wrap) show zero overflow. Zero console errors, zero page-level overflow at 375px mobile. Pushed live. See Decision Log for full detail.*

*Session Aug 13 2026 (continuing the same session — process addendum, no sheet/dm.html code changes) — added a new CLAUDE.md §3 "⚠ Pending / Blocked" section, separate from Next Priorities, for work incomplete specifically because of an external dependency (branch merge, open DM decision, schema someone else owns) rather than just unstarted backlog. Moved the one existing case — the Attunement checkbox deferred from `dm.html` in the previous session — out of Next Priorities and into this new section. Renumbered §3 onward through §11 (Decision Log) to make room; fixed the one internal cross-reference (C4 "See Section 3" → "See Section 4") that pointed at the old numbering. Also formalized two standing habits that were previously only in Claude Code's private memory, not in this shared file: added Start step 5 (check parallel-branch overlap *and* §3 for a known blocker before starting new work) and End step 5 (check §3 for newly-unblocked items whenever a branch merges to `main`, flag proactively, move to Next Priorities) to §1's own session checklist. See Decision Log for full detail.*

*Session Aug 13 2026 (continuing the same session — last of the 5-item placement roadmap, Inventory tab piece) — added Magic Item Attunement (`sheet/index.html`, `items`/`character_sheet` schema): new "Attunement (N/3)" box under the Equipped Gear doll, showing only currently-equipped items flagged `items.requires_attunement`, with Attune/Unattune buttons and a 3-item cap. Checked for parallel-branch overlap first (standing workflow) and found real, heavy overlap in `dm.html`'s item-form region specifically — deferred just the DM Tools checkbox (no item is flagged attunement-requiring yet, so nothing is blocked); built everything else, which lives entirely in `sheet/index.html` with zero overlap. Real bug caught and fixed before shipping, not just a clean build: the handoff's suggested "clear attunement on unequip" hook needed adding at 4 places an item leaves `EQUIPPED` (found a 4th the handoff didn't mention, `autoUnequipOffHandForTwoHanded()`), but one of the 3 the handoff implied — `selectEquipItem()`'s same-item slot-move path — would have wrongly cleared attunement on a same-item slot move (e.g. moving a ring left→right hand) rather than an actual unequip; caught via careful tracing before writing the call, not via testing after. Used `EQUIPPED_ITEMS` (always a full item row for equipped items) instead of the handoff's suggested `CATALOG_CACHE` (a mixed cache — partial rows from item search, full rows from inventory fetch), since attunement specifically needs `requires_attunement`, a field the partial rows never carry. Verified end-to-end on Kael with 3 temporarily-flagged real items (reverted after): attune-to-cap, cap correctly blocks a 4th, unequip correctly clears attunement, slot-move correctly preserves it (the exact bug the fix prevents), zero console errors, zero mobile overflow. This closes the original 5-item roadmap — Cantrips & Prepared Spells remains as its own separate, larger session per the original scope note. See Decision Log for full detail.*

*Session Aug 13 2026 (continuing the same working session from Aug 12 — third of the 5-item placement roadmap, Story tab piece) — added Speed + Size to the Your Background accordion (`sheet/index.html`, `species` schema), as a new "Speed & Size" card next to the existing Home Region card. Two real open questions resolved with the DM before writing anything, per the handoff's own instruction not to guess either: (1) placement — the handoff's two suggested options (header triple-row vs. "Who You Are" accordion) both mismatched its own stated reasoning ("near Species/Region"); DM picked a third option, Your Background (where the real Region card already lives) — the better literal fit. (2) rules edition — the handoff's own example ("Dwarves are 25 ft") was 2014 PHB, but this campaign's confirmed basis elsewhere in the code is 2024 PHB; DM confirmed 2024. Checked `species` schema first (no `speed_ft`/`size` columns existed) and cross-verified the 2024 PHB speed/size table against multiple web sources before seeding real gameplay data from memory — confirmed Dwarf/Gnome/Halfling all moved to 30 ft (from 25 ft), Goliath is the one 35 ft standout, Gnome/Halfling stay Small, everyone else Medium. Seeded all 12 real species via migration, keyed off the existing `phb_equivalent` column. Side-finding, not acted on: `species.phb_equivalent` already exists and could replace the hardcoded `SPECIES_PHB` JS mirror added in an earlier session — flagged, not fixed (out of scope here). Verified live on Kael (Veilstepped → Changeling → 30 ft/Medium): card reads "30 FT · MEDIUM", positioned correctly next to Home Region, zero console errors, zero mobile overflow. Pushed live. See Decision Log for full detail.*

*Session Aug 12 2026 (same session, second of the 5-item placement roadmap — Header piece) — added Initiative + Hit Dice to the MainInfo header (`sheet/index.html`), next to AC/HP/Temp HP in `.cs-mi-vitals`. Initiative (`DEX mod + 5 if Alert feat`) needed no async refresh — renders once, correctly, in the initial synchronous header build, since it only depends on `ABILITY_SCORES`/`ch.feats`, both already resolved by then. Verified live on Kael (a real Alert-feat case): DEX +2 + Alert +5 = **+7**, correct. Hit Dice: mid-build, discovered via the required "confirm the DB write" verification step that `character_sheet` already had unused `hit_dice_total`/`hit_dice_used` columns (populated `1`/`0` on all 3 characters, zero code references anywhere) — the handoff's premise of "no existing column" was wrong. Had already added a new `hit_dice_spent` column on that premise; dropped it and consolidated onto the pre-existing `hit_dice_used` instead, rather than ship two live "spent" trackers side by side. `hit_dice_total` stays dead/unused — max is deliberately computed live from `ch.level` per the handoff's own don't-cache-max instruction, not read from that column. Verified end-to-end: toggled Kael's pip, confirmed the write via SQL, toggled back, confirmed it reverted to 0. Zero console errors, zero mobile overflow (wraps cleanly onto its own line at narrow widths). Pushed live. See Decision Log for full detail — this is the more consequential entry of the two on record today, given the mid-build schema correction.*

*Session Aug 12 2026 (same session, first of a 5-item Class/Header/Story/Inventory placement roadmap agreed with DM — this handoff was just the Class tab piece) — added Spellcasting Modifier/Spell Save DC/Spell Attack Bonus to the Class tab (`sheet/index.html`), new card directly above Spell Slots. Investigated the data source first per the handoff's own instruction not to guess: confirmed `classes` had no structured spellcasting-ability column, and `primary_ability` is unreliable for half-casters (Paladin "Strength & Charisma", Ranger "Dexterity & Wisdom" — the real casting ability is only recoverable from `casting_summary`'s free text). Flagged this to the DM via a question rather than picking silently; DM chose "add a real column." Added `classes.spellcasting_ability` (str/dex/con/int/wis/cha, null for non-casters) via migration, seeded all 12 real classes from their `casting_summary` text (matches standard 5e rules exactly). Found mid-build that the handoff's assumed "DM Tools content manager" companion field doesn't apply — `dm.html` has no class-editing UI at all; the `classes` table has always been maintained via direct migration only (same as `hit_die_tip`/`saves_tip`/`weapons_tip` before it) — followed that existing precedent rather than building a new admin UI unprompted. Card hides entirely for non-casters (`display:none`, not an empty/zeroed state). Refresh wired to `renderLivePlay()`, not the handoff's suggested `renderClassTabDynamic()` hook — same deviation as the Saves build, for the same reason (`renderClassTabDynamic()` fires before `SHEET` loads). Verified live on Kael Evander (Ranger, real half-caster case): Spellcasting Modifier +3, Spell Save DC 13, Spell Attack Bonus +5, all hand-checked against his real WIS 16 and proficiency bonus 2. Simulated the non-caster path in-browser (no data touched) — card hides correctly, restores correctly. Zero console errors, zero mobile overflow. Pushed live. See Decision Log for full detail.*

*Session Aug 12 2026 (same session, addendum to the Saves/Passive Perception build below) — added the one item that build left open: a top-level "what's a saving throw?" explainer (`sheet/index.html`). DM's decision: one explainer, not six (the six `SAVE_TIPS` stay as-is, they explain what triggers each save specifically) — and since saves render one-per-card with no shared "Saving Throws" heading to hang a single tooltip off of, DM left the anchor choice to Claude Code. Attached it to STR's row only, as a new `SAVE_INTRO_TIP` shown via a second `.cs-ab-tip` icon on that row alone — styled distinctly (blue "i" pill, new `.cs-ab-tip--intro` class, vs. the standard gold "?") so it reads as a general concept explainer, not a Strength-specific fact. `abilitySaveRowHtml()` took a new `showIntroTip` param, `true` only when `ab === 'str'`, at both call sites (initial render and the `renderAbilitySkills()` refresh). No other changes — this was the only remaining open item from the original handoff. Verified live on Kael: intro tip present only on STR's row (confirmed via DOM read, not just visual), correct copy, zero console errors. Pushed live. See Decision Log for full detail.*

*Session Aug 12 2026 (same session, after the PHB-terms fix below) — added Saving Throws + Passive Perception to the Ability Scores tab (`sheet/index.html`), per a DM handoff doc. One row per ability card ("Save", reusing the existing skill-row visual family — same "Prof." label, same positive/negative coloring) sourced from `classes.saving_throws` (a comma-separated string, e.g. "Strength, Dexterity" — confirmed against all 12 real classes before writing the parser) via a new `parseSavingThrows()`. Passive Perception (`10 + Perception total`) renders as a small gold-bordered pill on the WIS card only, above its Save row — extracted a new `computeSkillTotal()` out of the existing skill-row renderer so the skill list and the passive badge share one proficiency/flat-bonus calculation instead of two. Both refresh once `SHEET.proficiency_bonus` and `CLASS_ROW.saving_throws` are guaranteed loaded (`renderAbilitySkills()`, already the right timing hook — `CLASS_ROW` resolves before `renderLivePlay()` calls it, confirmed from the real fetch-chain order, not assumed). No `css/tokens.css` changes — reused existing tokens (`--gold`, `--radius-sm`, etc.) directly. Left one thing explicitly undecided per the handoff doc's own instruction not to guess DM's-call items: no top-level "what's a saving throw?" explainer beyond the per-ability tooltips — flagging for DM review rather than picking a tone unilaterally (resolved same session, see entry above). Verified live on Kael Evander against his real data (Ranger: STR/DEX proficient saves +3/+4, Passive Perception 15), zero console errors, zero mobile horizontal overflow. Pushed live. See Decision Log for full detail.*

*Session Aug 12 2026 (same session, after the Skills-section review rounds below) — added real PHB (5e Player's Handbook) terms in parens next to the Anavale-flavor species and class-flavor names in the header blurb (`sheet/index.html`) — DM asked for this "so we do not forget" the mechanical identity behind the in-world flavor names. Restored the `SPECIES_PHB` lookup (dropped as dead code in an earlier session's identity-pill removal, now has a real use again) and added a class-key-to-real-name lookup inline. Blurb now reads e.g. "A Veilstepped (Changeling) The Wanderer (Ranger) who carries the Featherflow — from Nombi." instead of just the flavor names. The Class tab's own class name display (`cs-ov-class-name`) is already the real PHB term (`ch.class_id`, e.g. "Ranger") and was correctly left untouched — only the blurb's Gigglegloom-flavor class name needed the parenthetical. Verified live on Kael Evander. Pushed live. See Decision Log for full detail.*

*Session Aug 12 2026 (same session, second live-review round on the Skills section) — dropped the ring dot on flat-bonus skills (`sheet/index.html`) — DM flagged it as a second, unexplained marker now that the "(+N)" next to the total already carries that information. Only proficient rows show a marker ("Prof." label) now; flat-bonus and neither both render an empty marker slot. Pushed live. See Decision Log for full detail.*

*Session Aug 12 2026 (same session, DM live-reviewed the Skills section below and asked for one legibility fix) — replaced the proficient-skill filled dot with a "Prof." text label (`sheet/index.html`) — DM found the filled-vs-outlined dot distinction unintuitive. Flat-bonus skills keep their outline dot and now also show the bonus amount in parens next to the total (e.g. "+2 (+1)"). Verified on Kael and Hana (both real overlap cases still correctly show proficient-only, no stray parenthetical). Pushed live. See Decision Log for full detail.*

*Session Aug 12 2026 (same session, after the Speaks-color fix below) — added a Skills section to the Ability Scores tab (`sheet/index.html`): all 18 skills grouped under their parent ability card, each showing proficiency state (proficient / flat +1 / neither, three visually distinct states) and total bonus. Investigated first per DM instruction — no Figma frame exists for this section (confirmed via `get_metadata`/`get_screenshot` on both breakpoints), `character_sheet.proficiency_bonus` is a real stored column that was never read anywhere before this, and real overlap cases exist on 2 of 3 characters (Kael, Hana) where a skill is both class-proficient and has a `PAST_EFFECTS` flat +1 — DM confirmed proficiency supersedes rather than stacks, citing 2024 PHB stacking rules. Old flat "Proficient skills: X, Y, Z" row removed, fully superseded. Verified live on all 3 characters (including both real overlap cases) plus mobile. Pushed live. See Decision Log for full detail.*

*Session Aug 12 2026 (same session, live DM review continued) — fixed the Speaks block's language name text to match the glyph's per-language color (`sheet/index.html`): `renderSpeaksBlock()` already colored the rune glyph from `languages.color` but left the name text on a fixed `var(--blue)` CSS rule regardless of language — DM caught this live (Hana's "Jugabi Canopy" name not matching its own green glyph). `languages.color` is already the closest real per-region signal in the data model (no separate hex color exists on `regions` itself, only a `color_health` status word) and visibly tracks each language's origin region already (Jugabi Canopy green, Nombi Frost blue, Sohot Old Tongue gold, etc.) — applied the same inline color to both spans. Pushed live. See Decision Log for full detail.*

*Session Aug 12 2026 (same session, live DM review continued) — committed and pushed the brighter equipped-gear silhouette PNG that had been sitting uncommitted in the working tree since an Aug 10 session (flagged then as "already replaced by the DM, left untouched" pending a decision). DM confirmed live on the deployed site that the doll looked too dark; the already-present lighter-gray replacement is now live. Pushed — no code changed, asset-only commit. See Decision Log for full detail.*

*Session Aug 12 2026 (same session, after the Figma-styling pass below, live DM review on the deployed site) — doll/Special-Effects flex ratio reversed from 2:3 to 3:2 (`sheet/index.html`): DM reviewed the pushed layout live and called Special Effects "too wide" against the doll being "more important" — flipped `.cs-inv-top-col--doll`/`--effects` so the doll now gets the larger share (896px vs. 598px at 1600px viewport, confirmed). Mobile unaffected (flex-grow is moot once both columns wrap to stacked below the shared min-width). Pushed live. See Decision Log for full detail.*

*Session Aug 12 2026 — Special Effects panel's internal styling rebuilt against a fresh Figma pull (node 51:2260, re-pulled this session — the panel had been built from-scratch with no Figma reference before this) — one shared bordered box per Figma's own "list"/"row" structure (not per-item cards like Your Story), item names in Roboto Bold 26px (was Cinzel Decorative), sparkle+gold mechanical lines at 32px/24px, book+italic flavor lines with a 13px icon gap, every item bottom-divided including the last, panel `min-height:277px` that genuinely grows (verified to 405px with 3 items + a long flavor line, no clip/scroll). One new token added (`--char-ov-pl`, 24px) — everything else reused existing tokens exactly. Committed but **not pushed** — awaiting DM go-ahead. See Decision Log for full detail.*

*Session Aug 11 2026 (same session, after the Longbow investigation below) — Inventory tab layout swap: Currency moved out of the top row to share a row with Carrying instead, freeing the top row for just Equipped Gear + Special Effects at a wider, unequal 2:3 split (Effects gets the larger share). Pure reuse of the existing `.cs-inv-top-row`/`.cs-inv-top-col` flex pattern — no new CSS layout system, no new mobile media query needed (existing wrap-on-min-width behavior already stacks both new column pairs correctly at 375px). Committed but **not pushed** — awaiting DM go-ahead. See Decision Log for full detail.*

*Session Aug 11 2026 — added a "Special Effects" panel to the Inventory tab (empty space right of the equip doll): investigated first (confirmed `description`/`story_note`/`gigglegloom_relationship` are flavor-only in real usage, `grants_advantage`/`grants_proficiency`/`magic_bonus` are the only pre-existing structured mechanical fields, and `effect_type`/`effect_dice`/`effect_flat`/`effect_duration` are scoped to one-shot consumable use only — none of that overlaps with "what this item does while equipped"), then added two new optional `items` columns (`equipped_effect_mechanical`, `equipped_effect_flavor`), a matching DM Tools form section, and a read-only sheet panel that shows a card per currently-equipped item with anything to show, hidden entirely when nothing does. Committed but **not pushed** — awaiting DM go-ahead. See Decision Log for full detail.*

*Session Aug 10 2026 (same session, immediately after the split-retirement work below) — closed the root cause behind the stale-`equipped_slot` bug class (found via the ring double-equip fix, confirmed again via a Right Hand "missing" Shortsword report), added `clearInventoryEquippedSlot()` wired into every place `character_sheet.equipped` stops pointing a key at an item, swept all 3 characters for other stale rows (only Kael's Shortsword), plus removed the now-redundant combined weapon-damage summary row above the doll and fixed the equip popover listing an already-equipped item as its own candidate. Committed but **not pushed** — awaiting DM go-ahead. See Decision Log for full detail.*

*Session Aug 10 2026 (later same day, new session) — retired the mechanical/cosmetic split built earlier that same day in favor of one unified ownership model: 51 new real catalog rows for the appearance assets, all 3 characters' cosmetic picks migrated into real owned inventory + `character_sheet.equipped`, the slot popover rebuilt to one picker per slot. Investigated first, built after DM sign-off on 3 open decisions. Committed but **not pushed** — awaiting DM go-ahead. See Decision Log for full detail.*

*Session Aug 10 2026 — Equipped Gear doll rebuilt against the real Figma layout (node `68:7354`, pulled fresh this session — the Aug 9 build had shipped with no Figma reference for this node), 5 equipped-slot popover copy/UX fixes from live QC, a Chest-slot art mismatch investigated and confirmed as a real character-data inconsistency rather than a code bug (left as-is per DM decision), two weapon-slot changes — per-slot red damage text on both hands, and a real two-handed-weapon mirror image on the locked hand — which surfaced and fixed a genuine latent bug where two-handed detection only ever checked `hand_right`, the two ring selectors converted from plain `<select>` dropdowns into real slot tiles matching the other 10 gear slots, and (most recent) the "Currently Wearing" cosmetic picker itself — used by all 10 doll slots plus both rings, one shared function — converted from a native `<select>` to a clickable thumbnail list matching the mechanical picker's own row style, which surfaced and fixed a real apostrophe-escaping gap in three real option values, and (most recent) a no-code investigation into Kael's over-budget Gold ring — confirmed not a bug (see Decision Log), flagged that a planned inventory-ownership requirement for cosmetics will make this class of question moot going forward. Fluid cqi-based doll sizing, real nested flex structure matching the DM's exact geometry. Not yet pushed. See Decision Log for full detail.*

*Session Aug 9 2026 — Character Sheet Tab Redesign v1 (slices 1–4 plus a MainInfo header alignment fix), a `dm.html` SCC conditions fix (Live Session panel + Combat modal), a matching `sheet/index.html` Conditions picker, the Inspiration tooltip, a tooltip-clamping fix, a full rebuild of the "Your Past" label maps + header appearance paragraph, a tab-content cleanup (Gold/Equipped/Inventory/Starting Gear + Proficient Skills relocated, the Who You Are summary-dump bug fixed, Appearance chips repositioned, a duplicate Gold editor removed), a cosmetic/structural pass against the real Figma mockup (header AC/HP/Temp HP compact row, page titles, icon thumbnails, skill tooltips, Alignment text fix, identity pills removed, condition order fixed), a ground-truth rebuild against exact pulled Figma values (header PageTitle/AC badge/direct-edit HP/Temp HP inputs, Who You Are restructured, Your Background/Your Past layout + Benefits box, pill tab bar, Notes tab, accordion animation), a 5-item regression fix pass against the live page (duplicate logo, AC badge stacking, HP bar sizing, Speaks block structure, tab bar full-width), a real orphaned-tooltip fix plus the Moral Alignment thumbnail, and (later same day, new session) the Inventory tab built against its own real Figma pass (10-slot equip doll, DM Tools dropdown fix, currency pills, carrying bar, +Add Item modal, own-inventory filter, real item table with a 9th Actions column added after DM correction) — one open question flagged for the DM (Temp HP bar, now flagged 3 times as recurring Figma mockup filler). See Decision Log for full detail.*
- **Regression fix pass batch 2, same session, right after batch 1** (`sheet/index.html`, commit `6761c42`, also not pushed): 4 more live-vs-mockup mismatches — "Your Story" section titles were 48px instead of the spec's 32px, Your Background's shorter cards (Gigglegloom/Region/Alignment) were being flex-stretched to match the tall Background card's height instead of sizing to their own content, the shared `.cs-ab-tip` "?" icon class (Background/Your Past Benefits boxes + Ability Scores tooltips — 3 call sites) had no circle styling at all despite the spec calling for a solid gold circle with a dark "?", and "Why You Left"'s "Personal quest:" label was wrongly colored gold (reserved for real "Benefits:" labels) instead of plain text color. All four confirmed via DOM measurement, not just visual read, on both Kael and Visenya.
- **Regression fix pass batch 1, same session, after the ground-truth rebuild** (`sheet/index.html`, `css/tokens.css`, commit `0a81614`, committed but **not pushed** — awaiting DM go-ahead): 5 things the DM caught live that didn't match the ground-truth rebuild — removed the duplicate nav-bar logo (a deliberate choice from the rebuild pass, now overridden), fixed the AC badge rendering inline instead of stacked (missing `flex-direction:column`, not a values problem), enlarged the HP bar from a 120×10px sliver to a full-height (37px) flexible pill matching the real Figma bar node, restructured the Speaks block so the glyph+name row and the "spoken where" line are separate rows instead of one nesting inside the other, and moved the tab bar into the same wide `.cs-ov-breakout` wrapper every other section already uses (it was the one piece still stuck in the narrow 820px column). Flagged, not changed: the real Figma frame shows a Temp HP bar/max too, but that contradicts an explicit DM decision earlier this same session to keep Temp HP as a plain badge (5e has no real max temp HP) — left as-is pending a DM call on which one wins. Also flagged: "Noto Sans Runic" (named in the ask) isn't declared anywhere in the codebase; the rune glyphs render fine via browser font fallback, but there's nothing to "fix" since it was never wired in.
- **Ground-truth Figma rebuild, same session, after the cosmetic pass below** (`sheet/index.html`, `css/tokens.css`): DM provided exact pulled Figma values (not descriptions) across 7 areas and asked for item-by-item verification against the live page before any code — several items turned out to already be correct (condition order, identity pills, Your Background structure/title size), several were more broken than described, and a few needed DM decisions before implementing (documented below). Committed as one unit (`f2075fa`) after two rounds of DM confirmation.
  New tokens: `--blue-50/100/400/600/800`, `--char-ov-h5` (18px).
  **Header**: added the "Character Sheet" page title (logo + text) — confirmed missing entirely, not a restyle. Removed the header's weight bar (not in Figma; Inventory tab already had its own independent "Carrying: X lb" line, so nothing was lost) — deleted `renderOverviewWeight()` and its markup/CSS as dead code. AC badge rebuilt to exact values (blue/100 bg, blue/800 text, bottom-only 55px radius, split "AC" label/number typography) — was a single uniform gold pill. HP row rebuilt: current AND max are both real `<input type="number">` fields, no Edit toggle, no −/+/Apply/Set-Max-Mod panel — editing max derives `hp_max_modifier` from the typed value minus the real `hp_max` base (DM's explicit call, replacing the separate Max-Mod button rather than adding a third field). Temp HP gets the same direct-input treatment and **no longer hides at 0** — a deliberate reversal of the earlier "hides entirely at 0/null" decision, since a directly-typeable field needs to always be present to type into; flagged to the DM as an implied consequence of switching interaction models, not silently changed.
  **Who You Are**: confirmed the exact bug the DM described — `storyCard()` was shared by all three Your Story sections, so the 5(6) personality items rendered as separate `blue-700`/rounded-24px boxes, visually identical to Your Background/Your Past cards. Pulled a fresh high-res Figma crop mid-session (not relying on the earlier lower-res pull) and rebuilt into the real structure: one `cs-whoyouare-card` container with a restored "Summary" heading (stripped in an earlier pass when fixing the six-section-dump bug) + paragraph, then each item as a flush `border-left:4px solid blue-400` strip with no radius/own background. Eyebrow+title text for 5 of 6 items transcribed verbatim off that crop (not paraphrased) — First Impression/People Instantly Notice About You, Misunderstood/People Get Wrong About You, Primary Care/Care About, Fear/Deepest Fear, Quest/What You Seek. The 6th field (`personality_laugh`) isn't in that Figma frame at all — flagged to the DM rather than inventing copy; DM supplied eyebrow "Laugh" / title "What Always Makes You Laugh" directly, confirmed landing verbatim (not a paraphrase) before committing. Appearance chips section removed (redundant with the header's Appearance paragraph, confirmed still present before deleting).
  **Eyebrow styling sitewide**: `.cs-story-card-label` (shared by Who You Are/Your Background/Your Past — one class, one fix) changed from Bold/gold/0.68rem to Light 400/`--blue-100`/12px, matching the real Figma value exactly.
  **Your Background / Your Past icons**: bumped the shared `.cs-story-card-img` 48px → 75px. Your Background's card structure and 26px title size were already correct — verified before touching, not assumed broken just because Your Past was.
  **Your Past cards**: rebuilt as a new dedicated `pastCard()` (distinct from `storyCard()`, which stays as Your Background's builder) — fixed 358px width (confirmed via computed style, not eyeballed) and 18px titles via new `--char-ov-h5` (Your Background stays 26px). Real structural split confirmed against the Figma frame: 5 cards (Upbringing/Friend/Pet/Love/Organization) keep the icon beside only the header row; Left Behind/Why You Left use a `--full` row-layout variant with the icon beside the *entire* content column — previously all 7 used the same header-only layout, a genuine mismatch, not just a styling gap. Also caught mid-rebuild: none of the 7 Your Past cards had a real title line distinct from the eyebrow (one label was doing double duty) — added verbatim eyebrow+title pairs for all 7 (e.g. eyebrow "Upbringing" / title "Who Raised You"), sourced from the same Figma crop; corrected "Why Your Left" (a Figma-mockup typo) to the grammatically correct "Why You Left" already used elsewhere in the file rather than replicating the typo.
  **Benefits box**: rebuilt from rounded pill-tags (`cs-story-benefit-tag`) into a typed-line `blue-600` box (confirmed via zoomed Figma crop, not guessed) shared by Your Background's Background card and the relevant Your Past cards — ability deltas combine onto one comma-joined line ("+2 WIS, +1 STR", no suffix/tooltip), skill lines get an optional amount + "(Skill Proficiency)" + tooltip, feats get "(Ability)" + tooltip, DM-add items get "(DM adds)" with no tooltip, and a new situational-description type gets "(Situational)" with no tooltip and non-bold body text. Building Had a Pet's situational line required a new `PET_EFFECT` lookup (DM-approved) sourced verbatim from `character.html`'s `data-effect` attributes on the had_pet option cards, same pattern as the existing `WHY_LEFT_QUEST` — confirmed live (not just by reading the code) that an unrecognized `had_pet` value degrades to no Benefits box and no literal "undefined", by calling `buildYourPastHtml()` directly in the browser console with a fake value before reporting it fixed.
  **Tab bar**: rebuilt from underline tabs (uppercase, gold text+underline) to pill tabs — active `blue-400` background at 100% opacity, inactive `blue-600` at 50% opacity, rounded top corners, full-width `blue-400` bottom border, Roboto SemiBold 16px — confirmed via `getComputedStyle()` reading back the exact resolved colors/opacity, not just visual comparison.
  **Notes tab**: added as a real 5th tab (`TABS` array), existing Notes UI relocated verbatim/unstyled — same low-risk relocation pattern as the earlier Gold/Equipped/Inventory move.
  **Accordion animation**: added a `grid-template-rows: 1fr → 0fr` transition (~220ms ease) wrapping the existing `cs-ov-acc-body` in a new `cs-ov-acc-body-outer` grid container — no JS height measurement, works for any content height. `toggleSheetAccordion()`'s logic (a plain class toggle) was untouched and needed no changes.
  Verification: full page checked on Kael Evander and a second full pass on Visenya Oryn (different gigglegloom/region/background/alignment/past-answer combinations — including a coincidentally-shared `had_pet` value confirming the new lookup isn't accidentally Kael-specific), zero console errors on both across all 5 tabs. Screenshots via a tall-viewport fresh-navigate pattern (a scroll-then-screenshot approach hit the same Browser-pane rendering glitch noted in the Aug 9 Class-tab entry below — worked around the same way, not re-investigated). One environment note, not a code issue: the local dev server's `tokens.css` response kept getting served stale by the browser cache after edits mid-session — worked around per-check with a manual cache-busting query string on the `<link>` href; flagged to the DM in case it affects their own local testing.
- **Cosmetic/structural pass against the real Figma mockup, same session, after the tab-cleanup pass** (`sheet/index.html`): 7 items requested together, then 3 follow-up corrections after DM review, all committed together as one unit (`d2572ad`). Header AC/HP/Temp HP rebuilt from AC-alone-on-its-own-row + HP's always-visible raw −/+/Apply/Max-Mod controls + a Temp HP plain-text link, into one compact row (AC badge, HP badge+bar, Temp HP badge) — every editing control preserved, just moved behind small "Edit" toggles into a shared panel below the row (an inline-per-item version reflowed the AC badge unpredictably when a panel opened; moving both panels out fixed it). "YOUR STORY" page title added above Who You Are, then to Ability Scores/Inventory/Class too once confirmed none of the four tabs had one (contrary to the initial ask's premise) — one shared `pageTitleHtml()` helper. Icon thumbnails added to all 7 Your Past cards and 3 of 4 Your Background cards (Gigglegloom Affinity/Home Region/Background — not Moral Alignment, which has no image anywhere in the codebase to source from, confirmed by reading `character.html`, not fabricated), all verbatim from the exact asset paths `character.js`/`character.html` already use for these same option cards. Skill-proficiency benefit tags got `?` tooltips via new `SKILL_TIPS` (mirrored from `character.js`). Identity pills row removed (redundant with the blurb, not in Figma) along with the now-dead `SPECIES_PHB` lookup. Condition pills reordered to render after "+ Add Condition" per the mockup. Moral Alignment card fixed to pull from a new `ALIGNMENT_CARD_DESC` (real second-person text verbatim from `character.html`'s `.char-alignment-desc`) instead of the third-person `ALIGNMENT_TRAIT_SENTENCE` meant for the header blurb — wrong source, not a wording problem. **DM-reviewed follow-up**: Temp HP had briefly grown a decorative always-100%-filled bar (added to visually match the new AC/HP compact badges) — reverted to a plain number badge on DM instruction, since 5e has no real "max temp HP" to show a bar or proportion against; still correctly hides entirely at 0/null. Verified live on Kael and Visenya (different gigglegloom/region/background/alignment combinations each), zero console errors, before/after via `git stash`.
- **Structural cleanup pass, same session, after the tab shell landed** (`sheet/index.html`): four fixes requested together, all low-risk relocations/bug fixes, no redesign. (1) Gold/Equipped/Inventory/Starting Gear were still rendering inside Your Story — leftover from before the tab redesign, never relocated when Inventory got its own tab. Moved all four to the Inventory tab verbatim (same ids, same `renderGold()`/`renderEquipped()`/`renderInventory()` calls) so Inventory has real working content instead of a placeholder. (2) Proficient Skills moved from Your Past to Ability Scores — its real conceptual home, flagged as homeless when it was first built. (3) `buildSummaryCardHtml()` was dumping all six `generateSummary()` sections (Who Are You/Your Magic/Your Background/Your Past/Your Appearance/Your Compass) into the Who You Are card — confirmed via a stash/screenshot before-comparison. Fixed to show only the "WHO ARE YOU" section via the existing `extractSummarySection()` helper; the other five are already covered by their own dedicated cards. (4) Appearance chips moved from between the Your Background/Your Past accordions to directly after Who You Are, so it groups with the rest of the who-you-are content instead of floating between two unrelated accordions — exact placement flagged as DM's call to adjust after seeing it live. **Follow-up, DM-requested after reviewing the above**: the portrait card's Copper/Silver/Gold split-entry boxes (`renderOverviewMoney()`/`onOverviewMoneyChange()`) turned out to be a second live editor for the exact same `character_sheet.gold_cp` field the new Inventory-tab Gold section (`renderGold()`/`setGold()`) already owns — confirmed by reading both functions, not assumed. They were built to stay in sync when both lived on the same page (Aug 7 session); now that Gold has its own tab, keeping two editors for one field was pure duplication, not two different features. Removed the header boxes entirely (markup, both functions, both call sites, the now-dead `cs-ov-money-*` CSS) — `renderGold()`/`setGold()` in the Inventory tab remain the single editor. Left the now-unused `--currency-copper/silver/gold-coin` tokens in `tokens.css` (not asked, and tokens.css tolerates defined-but-unused tokens without harm). Verified end-to-end on Kael (before/after via `git stash`) and spot-checked Visenya's old-format-summary fallback still degrades to no card, both with zero console errors.
- **Hana and Visenya's `player_characters.summary` are still old-format** (confirmed via SQL: both 186 chars with no "YOUR APPEARANCE" section; Kael's is 1530 chars with all six sections — Kael's was regenerated post-rewrite, theirs weren't). This matters now because the header's appearance paragraph (below) reads from this field: Hana and Visenya will show the `buildAppearanceDesc(app)` fallback style (choppier, fragment-based — "Tall, lean build. Adult. Heart face...") instead of Kael's flowing generateSummary() prose until their summaries are regenerated. Not fixed — DM's explicit call to leave it on record rather than act on it now.
- **"Your Past" label maps rebuilt in full** (`sheet/index.html`) — all 5 maps flagged as stale last check-in (`RAISED_LABELS`, `PET_LABELS`, `LOVE_LABELS`, `ORG_LABELS`, `LEFT_BEHIND_LABELS`) now resolve every real `character.html` option value to its exact authored card text, not sheet's own earlier abbreviated guesses or the cap+dash-replace fallback. New `FRIEND_LABELS` added — `dearest_friend` never had a map at all before, always rendered the raw stored slug. All dead/unused keys dropped (26 of them, combined, across the old 5 maps). Verified before/after on Kael and Visenya via live DOM reads before committing, not assumed from the diff.
- **Header appearance paragraph fixed** (`sheet/index.html`) — was showing `appearance_prompt` verbatim (the raw AI-portrait-generation prompt string, e.g. "Fantasy portrait of, average height, athletic, adult, with #ffba66 skin..."), now shows the "YOUR APPEARANCE" section extracted from the same `generateSummary()` text already used in Who You Are (new `extractSummarySection()`, built on a factored-out `parseSummaryBlocks()` shared with `buildSummaryCardHtml()` so the two can't drift). Falls back to `buildAppearanceDesc(app)` when a character's summary predates the six-section rewrite (see the Hana/Visenya bullet above). Of the three original appearance-duplication copies (header paragraph / chips / Who You Are narrative), this actually reduces the duplication rather than just relocating it: the header and Who You Are now show the *same* text (when a character has a new-format summary) instead of two independently-worded retellings — down to two distinct versions (that shared text, plus the chips) from three.
- **`dm.html` Combat tab "Add Condition" modal** (`sccPromptCondition()`, third and last instance of the hardcoded-conditions gap): had its own separate local `COND_DATA` — 15 core conditions only, no Muted/Fading Stage 1/Color-Drained/Exhaustion, never referenced `window.SCC_CONDITIONS_DATA` even after the Live Session panel fix earlier this session. Replaced entirely with `window.SCC_CONDITIONS_DATA` (already fetched once in `sccInit()`) — the modal now lists all 18 real conditions with real colors/tooltips. No local fallback data left in this function at all, unlike the Live Session panel (which still has `COND_DATA` as a legacy fast-path plus this table as the fallback) — this modal only ever had the one data source to begin with, so full replacement was the right call, not a hybrid.
- **Inspiration badge tooltip + tooltip viewport clamping** (`sheet/index.html`): the Inspiration badge never got a `data-tip` — added the DM's exact rules text ("Spend before any d20 roll — roll twice, take the higher result. You can only hold one at a time.") and a `wireAllTooltips()` call inside `renderInspiration()` itself (it wasn't calling it before; relied on the caller to, which `toggleInspiration()` didn't). Testing this surfaced a real, previously-invisible bug in the shared `wireTooltip()` mechanism: it centers the bubble on the element with no viewport-edge awareness, and the Inspiration badge sits in the header's literal top-right corner (per the earlier header-alignment fix) — the bubble overflowed off-screen, unreadable. Fixed in `wireTooltip()` itself (not special-cased for Inspiration), clamping the bubble's center so it can't render closer than 8px to either viewport edge. This is a shared fix — every `data-tip` element in the file benefits, not just Inspiration; confirmed via the math that it's a no-op for elements not near an edge (the clamp only engages when the unclamped position would overflow).
- **Sheet Conditions picker** (`sheet/index.html`, follow-up to the `dm.html` SCC fix below): the MainInfo header's Conditions row was free-text (type anything, generic purple pill, no tooltip) since the slice-1 header build — flagged then as a known gap ("no canonical color source"), no longer true now that the `conditions` Supabase table exists. Replaced with a picker: new `fetchConditionsData()` (`sb.from('conditions').select('*').order('sort_order')`, fetched once in the same init `Promise.all` as `fetchBackgroundData`/`fetchRegionData`/etc.) populates `CONDITIONS_DATA`; `renderConditions()` now renders a `<select>` of conditions not already applied instead of a text input, and each pill's colors come from real `color_bg`/`color_border`/`color_text` (CSS custom properties per pill, same `--cond-bg`/`--cond-border`/`--cond-text` technique `dm.html`'s SCC Live Session panel uses) with real `tip` text wired through the existing `wireAllTooltips()` system built earlier this session — conditions now get real hover tooltips for the first time. Removed the now-inaccurate CSS comment claiming conditions have no color source. Testing also surfaced (not introduced, not fixed) a latent race in the existing `saveSheet()`-per-click pattern: two `removeCondition()` calls fired with zero delay between them (only happened in my own test script, calling the function directly twice in a row) can let an earlier PATCH complete after a later one and leave stale data in Supabase. Confirmed real click-driven usage — any perceptible delay between actions — doesn't hit this, and the fire-and-forget `saveSheet()` pattern itself is unchanged from the old free-text flow, so this is a pre-existing risk, not something this edit introduced. Left as a noted risk rather than fixed unprompted.
- **SCC Live Session conditions — added "Muted"** (`dm.html`): `SCC_CONDITIONS_LIST` now has 17 entries (was 16). More importantly, fixed the underlying gap this surfaced — `sccLiveRenderConditions()`'s `COND_DATA` is a fully local, hardcoded object covering only the 15 core D&D conditions; "Fading Stage 1" and "Color-Drained" were already in the picker list but had no entry in `COND_DATA`, so they silently fell through to a generic purple placeholder with no real tooltip (just the condition's own name). Rather than add a third hardcoded copy of Muted's color/tip (a real `conditions` table already exists in Supabase, seeded same-day, `is_custom:true` for the 3 Anavale-specific ones), added a `conditions` fetch to `sccInit()`'s existing `Promise.all` (same pattern as `species`/`items`) into `window.SCC_CONDITIONS_DATA`, and changed the lookup chain to `COND_DATA[c] || sccConditionsTableLookup(c) || genericFallback`. This fixes Muted **and** retroactively fixes Fading Stage 1/Color-Drained's missing real tooltips as a side effect of the same code path — not scope creep, the fix the DM asked for necessarily covered all three. Left the separate, unrelated `COND_DATA` in `sccPromptCondition()` (combat-tab "Add Condition" modal, line ~12421) untouched — different feature, not mentioned, out of scope. Verified: picker dropdown now lists Muted; added Muted + Fading Stage 1 + Poisoned to a test player, confirmed all three render with correct real colors/tooltips (Muted and Fading Stage 1 from Supabase, Poisoned still from local `COND_DATA` — correct precedence), screenshotted, then cleared the test state (`SCC_LIVE_CONDITIONS` is pure client-side in-memory state, confirmed via grep it's never written to Supabase, so no DB cleanup was needed).
- `sheet/index.html` now has a 4-button tab bar (Your Story / Ability Scores / Inventory / Class) with working show/hide. All four tabs hold real content now — Inventory's is a stopgap (Gold/Equipped/Inventory/Starting Gear relocated there unstyled, see the Aug 9 tab-cleanup entry below), still waiting on its real Figma pass.
- **MainInfo header alignment fix** (post-slice-4, DM-requested after the header/Figma mismatch below was flagged): re-pulled node `51:1385` directly (higher-res single-component screenshot, clearer than the full-page `51:1103` pull) and corrected exactly the three things the DM named, nothing else. Conditions now render as inline pills in a `.cs-mi-top-row` above the name (was below Appearance); Inspiration is now a small circular badge (`.cs-mi-insp-badge`, 64px, dashed border, gold-filled when active) absolutely positioned top-right of `.cs-ov-maininfo` (was a pill button next to the AC badge — `.cs-ov-maininfo` needed `position:relative` added for this to anchor); Species/Appearance/Speaks are now a labeled 3-column `.cs-mi-triple-row` (was three stacked full-width blocks) — Speaks lost its boxed-pill styling to read as a plain column like the other two. Condition pills use `--purple`/`--purple-dim`/`--purple-border` (existing tokens) rather than inventing a per-condition-name color scheme — conditions are arbitrary free text with no canonical color source, Figma's mockup colors (pink "Charmed", green "Poisoned") aren't derivable from real data. AC badge/HP bar/Temp HP/death-save swap deliberately untouched — DM confirmed those already matched. Verified via before/after screenshots on Kael (with test conditions + Inspiration toggled on to confirm both pill and active-badge styling render, then reverted) and a clean spot-check on Visenya.
- **Your Story tab** (final slice, built against a real Figma screenshot pulled via `get_screenshot` on node `51:1103` — the written spec's "6 chips" description didn't say whether these were small pills or wider cards, and the real answer data runs up to full paragraphs, so pulling the actual frame settled it: wide description cards, not pills): three accordions — **Who You Are** (Summary paragraph + 6 cards: First Impression/Misunderstood/Makes You Laugh/Care About/Fear/Quest, the last 3 relocated from the old flat story-row list, the first 3 new fields never shown before); **Your Background** (Gigglegloom/Home Region/Alignment/Background cards — Home Region is new, joins the `regions` table on `home_region` for `regions.summary`; Background's Benefits box parses `backgrounds.bonuses` via `parseBackgroundAbilityBonuses()`/`getBackgroundFeat()`, ported verbatim from `character.js` per the spec, plus `backgrounds.skills` as proficiency tags); **Your Past** (7 cards — Upbringing/Friend/Pet/Love/Organization/Left Behind/Why You Left, Benefits boxes filter `player_characters.skill_bonuses` by its `past_raised:`/`past_friend:`/`past_org:` source-tag prefixes, confirmed against real data before writing the filter). This **entirely replaces** the old pre-redesign "Overview" accordion (alignment/background/gigglegloom cards + narrative summary) — that accordion's cards move into Your Background, the summary paragraph moves into Who You Are, and the Ability-Scores-slice feat chip that landed on the old Background card moves into the new Background card's Benefits box, completing what that slice's Decision Log entry flagged as transitional.
- Two things resolved by DM decision before this slice: **"Why You Left" personal quest line** — no column existed for it; built as a static `WHY_LEFT_QUEST` mirror (DM's call), sourced verbatim from `character.html`'s real `data-effect` card attributes (only 5 real option values exist there — `ran-from-something`/`received-message`/`restlessness`/`saw-the-grey`/`someone-disappeared`; the existing `WHY_LEFT_LABELS` mirror has extra keys that don't correspond to any real character-creator option, a pre-existing staleness already flagged in the Aug 7 log, not touched here). **Spell Slots** — DM-approved minimal stopgap, see below.
- Also removed as part of this slice: the old standalone "Pending From Your DM" section — `dm_pending_items` now renders inside the Organization card's Benefits box, its real Figma-matched home, so the separate section was pure duplication once the new card existed. "Proficient skills" (class-chosen, distinct from the flat +1 `skill_bonuses`) kept as a small standalone row under the Your Past cards — no new home specified anywhere, data preserved rather than dropped.
- **Finding from Your Story slice, since fixed** (see the header-alignment entry above): pulling the real Figma screenshot for Your Story showed the MainInfo header's actual layout differed from what shipped in the tab-shell slice. The header slice was originally built from the written spec's text description without pulling this frame at the time (explicitly flagged then as "visual polish vs. Figma should be a follow-up pass") — now corrected.
- Spell Slots stopgap added to the Class tab (DM-approved, explicitly NOT the real Figma redesign): current/max pips per spell level 1–9, wired to `character_sheet.spell_slots`/`spell_slots_max` (both real jsonb columns, `{}` on all 3 characters — no prior UI ever wrote to them, confirmed by grep). Shape is `{level: count}`, invented here since nothing existed to match. Clicking a filled pip spends it, clicking an empty one restores up to it — same interaction as the death-save pips. Verified end-to-end on Visenya then reverted the test values.
- Class tab: class summary card + Hit Die/Primary/Saves/Casting/Armor/Tools/Weapons (Hit Die is a new row; `?` tooltips on Hit Die/Saves/Weapons only) + Key Features (icon table ported verbatim from `character.js`, but the name/description/level parser is genuinely new code — verified against all 72 real feature strings in Supabase before writing). New `--blue-500` token (`#28304a`) pulled from the real Figma file, not guessed.
- Four build-spec inaccuracies found this session and NOT silently worked around (full detail in Decision Log): (1) Key Features parsing wasn't "already" in the character creator — only the icon table was a real port. (2) "66 features, 7 bare" is actually 72 and 8. (3) Spell Slots described as "existing UI" that never existed anywhere. (4) The build spec doc itself isn't a file in this repo — it lives in project knowledge outside git; corrections were handed back to the DM to apply there.
- `hp_temp` investigation (DM asked to rule out a code bug after it read 5 unprompted on Kael mid-session): traced every write path — only `setTempHP()` in `sheet/index.html` ever writes it, gated on an explicit "Set" click, no default/fallback path. `js/hp-calc.js` never references `hp_temp` at all. No write-path bug found; most likely the DM or Kael's player testing independently on the shared production database. Not reverted.
- New shared MainInfo header (renders once above the tabs): species paragraph, Speaks block (joins the new `languages` table), Conditions +Add/remove, Inspiration toggle, relocated AC badge/HP bar/appearance paragraph, Temp HP badge (hides at 0/null), death-save pip tracker that swaps in for the HP bar at 0 HP (with a heal-back control — a real gap found and fixed during testing, not in the original spec).
- Confirmed `js/hp-calc.js` (untracked going into this session) is real, load-bearing code shared by `dm.html` and `sheet/index.html`, from a prior session that never committed it. Committed this session along with `dm.html`'s matching `reconcileHp()` integration.
- Known duplication, pre-existing: `appearance_prompt` (when set) already restates the same fields the Appearance-section chips show, and a third version exists in the Overview summary card (now the Who You Are summary). Left as-is, flagged for a future pass.
- Two pre-existing uncommitted items found this session, unrelated, not touched: `docs/character-sheet-redesign-handoff.md` (Aug 7 output, still untracked) and `.claude/launch.json` (Aug 7 log says "added" but never committed). `.claude/settings.local.json` should stay untracked (local-only) — repo has no `.gitignore` entry for `.claude/` yet.
- Inventory tab's real Figma pass is done (see the Decision Log entry above) — no longer a blocker. The stale `RAISED_LABELS`/`PET_LABELS`/`LOVE_LABELS`/`ORG_LABELS`/`LEFT_BEHIND_LABELS` mirrors (found stale relative to character.html's real option values while building Your Past — degrades gracefully via `label()`'s fallback formatting, not broken, but not canonical either) remain a candidate for a dedicated pass, not a blocker.

*Prior session — Aug 8 2026 — fixed "Your Past" data-loss bug, built imagined-past mechanical effects (ability/skill/gold/feat/DM-item), fixed a second starting-gold bug, fully backfilled the 3 existing characters (all Your Past answers, all mechanical effects, correct gold) including their display on the live sheet. See Decision Log for full detail.*
- All code committed and pushed, all Supabase changes live. `character.html`, `dm.html`, `js/character.js`, `sheet/index.html` all touched this session.
- `sheet/index.html` displays `skill_bonuses` ("Bonus skills" row in Your Past), `feats` (chips with rules text in Ability Scores), `dm_pending_items` ("Pending From Your DM" section). Verified against all 3 real characters' real data.
- Hana, Kael, Visenya now have all 7 "Your Past" answers (DM re-collected the 6 lost to the original bug), all mechanical effects from those answers applied, and correct starting gold (background gold − real Stage 4 shopping spend + any org gold bonus — see Decision Log for the `BG_GOLD`-was-disconnected-from-real-values bug this uncovered).
- Still not built (not requested): background feats' non-HP effects (e.g. Alert's +5 initiative, can't-be-surprised) aren't wired to anything mechanical — only the feat name + rules text display, and Tough's HP interaction, exist. No initiative field exists on `character_sheet` to wire Alert to yet. Also not built: inventory purchases don't auto-deduct `gold_cp` anywhere (confirmed absent, not a regression — flagged as a possible future feature, not built).
- Fourth data-loss bug found same day: `player_characters.skills` (class skill proficiencies) was never written by `buildCharacterEntry()` — fixed for future characters. All 3 existing characters have `skills: []` and it's unrecoverable (the draft that held it is deleted from localStorage on submit, nothing server-side ever captured it) — DM re-collection is the only path, same as "Your Past". Not yet re-collected. See Decision Log.

*Prior session — Aug 7 2026 — character sheet redesign (docs/character-sheet-redesign-handoff.md), Overview + Ability Scores pass.*
- Redesigned `sheet/index.html` Overview + Ability Scores sections per Figma `P0tcVgf02XeI13Tz0TtRaX` (desktop 36:310 / mobile 36:435). Replaced `cs-hero` / `cs-type-banner` / the Class-Species-Background-Alignment-Region-Level-Pronouns-Language rows of `cs-identity-grid` with the new design — decision confirmed with DM (replace, not duplicate-alongside). Pronouns dropped from display (derived from gender, no independent field). New wide `.cs-ov-breakout` container (max-width 1560px) used only for these two sections, breaking out of `.cs-wrap`'s 820px cap — rest of the page unchanged. Photo upload (`cs-hero-portrait-img` / `cs-photo-input` ids preserved) kept working on the new portrait card per DM confirmation.
- New tokens in `css/tokens.css`: `--currency-copper/silver/gold-coin`, `--blue-200/700`, `--ability-{str,dex,con,int,wis,cha}-glow/gradient`, `--char-ov-h3/h4`, `--char-ov-gap-2xs/xs/s/m` — reused existing tokens (`--radius-sm/lg`, `--text`, `--gold`, `--steelfist` etc.) everywhere they matched a Figma-bound value exactly.
- Currency: new cp/sp/gp inputs in the portrait card (`renderOverviewMoney()` / `onOverviewMoneyChange()`) combine/split through the same `gold_cp` field and `saveSheet()` path as the existing Gold section's `setGold()`/`renderGold()` — both stay in sync, called from `renderLivePlay()`.
- Weight bar: capacity = STR × 15, current = `computeCarriedWeight()` (factored out of `renderInventory()`, same full-inventory sum as `cs-inv-weight-total` — confirmed equipped items stay as `inventory` rows with `equipped_slot` set, so this total already included them).
- Class card in Overview reads a new `classes` table (`fetchClassData()`, `CLASS_ROW` global) — table doesn't exist yet, so it renders "—" placeholders until the migration below runs; errors are swallowed, no crash.
- Overview blurb + card subtitles reuse only real existing data (`ANAVALE_SPECIES`/`ANAVALE_BACKGROUNDS`/`ALIGNMENT_PHB`/`GIGGLEGLOOM_TYPES` mirrored into `sheet/index.html`'s own lookup tables, since sheet doesn't load `character.js`) — no invented lore. One gap found and left unbuilt: the Figma mockup's alignment-card "(Gladiator)"-style archetype subtitle has no canon source for backgrounds — only the background's own PHB-equivalent exists, which is what's shown.
- **Pending DM review, not yet run:** `classes` table migration (schema in handoff §6) — all 12 rows map directly from `CLASS_DATA` in `js/character.js`; `tools`/`casting_summary` (new fields, not in `CLASS_DATA`) were drafted and are awaiting DM sign-off before the migration executes.
- `generateSummary()` in `js/character.js` rewritten per handoff §8: six paragraphs (Who Are You / Your Magic / Your Background / Your Past / Your Appearance / Your Compass, same labels as the Stage 1 accordion), gendered pronouns (not generic "they"), organic length via per-field template functions, empty fields omitted entirely. Storage format: plain text, `LABEL\n\nparagraph`, sections joined by `\n\n\n`. Live preview element `char-auto-summary` changed from `<p>` to `<div>` (character.html) so multi-paragraph `innerHTML` can render; new `.char-summary-heading` CSS added to `character.css`.
- **Bugs found but NOT fixed (out of scope for this pass, flagging for a follow-up session):**
  - `sheet/index.html` portrait fallback (`assets/images/species/{species}-{suffix}.webp`) 404s for every character without an uploaded photo — real files are named `sp-{species}-{suffix}.png`. Pre-existing, untouched by this session.
  - `css/character.css` has two separate `.char-summary-body` rule blocks (~2623 and ~3186) — pre-existing duplicate, violates the "no duplicate rules" convention in §8 of this file.
  - `sheet/index.html`'s own (untouched) "Your Past" section still uses a different/stale set of `WHY_LEFT_LABELS`/`RAISED_LABELS` option values than the real character-creator form options — predates this session, not touched since that section is out of scope.
- Added `.claude/launch.json` (static file server on :8791) for local UI verification going forward — not previously present.

**Next priorities:**
- **Needs a real-browser confirmation, not a code check** — the Live Session strip's `position:sticky` left column (built 2026-08-19). This session's own browser tooling could not visually confirm sticky positioning at all (reproduced the same failure on a fully isolated, unrelated test element), so the underlying CSS was fixed via direct spec-based reasoning and computed-style checks, not by watching it actually stick. Have the DM open the Session tab, expand any accordion section with enough content to scroll, and confirm the left column (Party HP/Conditions/Current Beat/etc.) visibly stays in place while the right column scrolls past it.
- **BUG — character photo Replace upload fails with "Upload failed — The resource already exists."** Reported by Hema, on the character photo Replace flow (`sheet/index.html`). Not yet investigated. Worth checking first, not assumed: this error text is the classic signature of an `insert`-style storage upload hitting a path that already exists, rather than an `upsert` against the `character-photos` bucket — verify against the real upload code before acting on this guess.
- **Combat encounter persistence** — no schema exists for pre-staging an encounter (Setup) or reviewing one after the fact (history). These are one plan→run→resolve lifecycle, not two separate projects; the real minimum is persisting what `sccEndCombat()`'s Combat Summary popup already computes instead of discarding it. Closes the XP/loot session-tagging gap too (`quest_progress.xp_awarded`/`loot_awarded`, `session_notes.inspiration_awarded`/`session_summary` are all confirmed dead columns).
- `quest_progress` has no durable session history — a beat's `completed_in_session` is a single overwritable stamp, not a log. Needs a real append-only log table if "which session did X complete in" is ever meant to be a durable fact. Medium priority, nothing actively broken today.
- **Creature/NPC stat-block unification** — two independent renderers (Combat's own, the lookup drawer's) still exist; same risk shape as the already-completed NPC/Location field-union.
- **Attunement checkbox in the DM Tools item edit form** (`dm.html`) for the real `requires_attunement` field — confirmed still missing via grep. Schema, sheet-side UI, and toggle logic already shipped; this is the one remaining piece.
- **A DM Tools admin form for `classes.limited_use_features`** (add/remove a class's limited-use features, edit the level→max table) — confirmed still missing via grep; currently DM-edited via direct migration only, same precedent as `spell_slot_progression`/`prepared_count_formula`.
- **Known, unaddressed gap — quests have no path to players at all.** All real quests are `player_facing:false` and `js/router.js`/`js/wiki.js` have zero quest-route code — there is no public wiki page a quest could link to even if marked visible. Blocks Search+Preview's "open in new tab" for quests and any real DM/player-facing split for the Quest Manager. Needs an explicit decision (a real `#quest` route + renderer? a DM-only shareable view instead?) before this can move.
- @mention predictive entity search in notes — confirmed genuinely new work, no existing pattern to crib from (the existing predictive-search implementations are all single-line inputs, none do caret-aware inline insertion into a textarea).
- Cross-session Tagged Note search — everything is still scoped to one session, a real gap once a second session exists.
- 4 possibly-non-core spell flags (`cause-fear`, `aganazzar-s-scorcher`, `abi-dalzim-s-horrid-wilting`, `tasha-s-otherworldly-guise`) need a DM decision — not yet acted on.
- 5 duplicate spell catalog entries (same real spell stored under both its 2014/trademarked name and its 2024-SRD/renamed equivalent) — deduping is a structural decision outside the audit's own scope, needs a DM call.
- Tier 1 audit (level/school/classes) of the 41 already-flagged-unavailable spells — lowest priority, unavailable to any character regardless of correctness.
- Point-pool limited-use features (Monk Focus/Ki Points, Paladin Lay on Hands, Sorcerer Sorcery Points) have no tracker yet — the segmented X-of-Y bar doesn't fit a point pool, needs its own numeric-input card variant.
- Bardic Inspiration has no limited-use tracker either — its max uses is Charisma-modifier-derived, not level-derived, so it doesn't fit `classes.limited_use_features`'s `max_at_level` schema. Would need an optional `max_formula` field.
- No rest-flow (long/short rest button) exists anywhere on the sheet — Hit Dice/Death Saves/Spell Slots/Spells Prepared all restore or reset via manual clicking only.
- `species.phb_equivalent` (a real, populated column) could replace the hardcoded `SPECIES_PHB` JS mirror still in `sheet/index.html` — confirmed both still exist side by side. Candidate for a small cleanup pass.
- DM Tools has no admin UI for `classes.spell_slot_progression`/`prepared_count_formula` — both seeded via direct migration, same "edit via migration for now" pattern as every other `classes` mechanical column.
- Fix the species-portrait 404 (real fix: correct the filename pattern to `sp-{species}-{suffix}.png`).
- `sheet/index.html`: combat tab, condition tooltips, XP display — long-standing, still not built.
- `assets/images/ui/icon-x.svg`/`icon-search.svg`/`icon-arrow-dropdown.svg` are staged on disk but unwired (baked-in dark fill needs a `mask-image` recolor pass before they're usable on this page's dark theme).
- Character creator migration to Supabase `species` table
- Interactive Caparia map
- Session recap page (player-facing, post-Session 2)

---

## 3. ⚠ Pending / Blocked

*Work that's incomplete because of an external dependency — a branch merge, an open DM decision, a schema change someone else owns — not just unstarted backlog (that's Next Priorities, above). Two standing habits attach to this section:*

1. ***Check on merge.*** *Whenever any branch merges to `main`, check this section before calling the merge "done" — does anything here just become unblocked by what landed? If so, flag it to the DM directly in that session's summary (don't wait to be asked), and move the item to Next Priorities — don't leave it here once it's no longer blocked.*
2. ***Check before starting.*** *Before starting new work that touches a file/area named here, check this section first — same standing habit as the parallel-worktree overlap check, extended to also catch "is there a known blocker already sitting on this exact area."*

*(Currently empty — the one item previously logged here, a DM Tools admin form for spell grants, is resolved: see 2026-08-17 "Round 2 Revisions" in the Decision Log. The DM decided on a bigger model change than just an `extra_learnable_spells` editor — spells are now DM-assignment-only end to end, no player self-serve at all — and the DM Tools Players page (`dm.html`, on the `anavale-wiki-scc-ux` branch) now has a real "✎ Edit" → Spell Assignment flow covering it, built extensibly for future non-spell sections.)*

---

## 4. Repo & Deployment

**Local path:** `Documents/DND/Anavale/anavale-wiki/`

    Documents/DND/Anavale/
    ├── anavale-wiki/          ← this repo (public git)
    ├── anavale-dm-docs/       ← private git repo (DM spoiler files)
    └── anavale-assets/        ← non-git large files (mockups, world art, etc.)

**GitHub repo:** `github.com/carlosknightiii/anavale-wiki`

**Live URLs:**
| URL | Purpose |
|---|---|
| `https://carlosknightiii.github.io/anavale-wiki` | Player-facing wiki |
| `https://carlosknightiii.github.io/anavale-wiki/dm` | DM Tools (`anavale-dm`) |
| `https://carlosknightiii.github.io/anavale-wiki/character` | Character creator |

**Git push sequence:**

    cd Documents/DND/Anavale/anavale-wiki
    git add -A
    git commit -m "..."
    git push

**Pre-push syntax check (Node v24 — `node --check` does not work on .html files):**

    node -e "require('fs').readFileSync('dm.html','utf8')" 2>&1

Returns nothing = safe. Throws = fix before pushing.

**CDN propagation delay:** GitHub Pages can take 5–15 minutes after a successful push. Before concluding a fix didn't land:

    curl -s "https://carlosknightiii.github.io/anavale-wiki/dm.html" | grep -c "FUNCTION_OR_STRING"

Count 0 when it should be 1+ = CDN hasn't propagated. Wait 5 minutes, re-run. Never write a new fix based on browser behavior without confirming via curl first.

**Site crash recovery:**

    git log --oneline -10
    git reset --hard <last-known-good-hash> && git push origin main --force

Use `git reset --hard`, not chained `git revert`. Chained reverts create indeterminate state.

**Git lock error:** `rm -f .git/index.lock` before retrying. Never force-push to resolve a lock.

---

## 5. Architecture

**Code changes → git push.** HTML, CSS, JS files only.
**Content changes → Supabase only. Never git push for content.** The `data/*.js` files are legacy seeds — not the source of truth. Never read from them in new code.

**Key files:**
| File | Notes |
|---|---|
| `dm.html` | DM Tools + SCC. Most complex file. Always stale — Rule C1 applies. |
| `character.html` | Character creator shell. Potentially stale every session. |
| `css/character.css` | Character creator styles. Potentially stale. |
| `js/character.js` | Character creator logic. Potentially stale. |
| `sheet/index.html` | Player character sheet. Potentially stale. |
| `js/wiki.js` | Wiki render logic. |
| `js/router.js` | Client-side routing. |
| `css/tokens.css` | Single source of truth for all design tokens. No `:root` blocks anywhere else. |
| `index.html` | Player-facing wiki shell. |

**The five always-stale files:** `dm.html`, `character.html`, `css/character.css`, `js/character.js`, `sheet/index.html` — read the current file state before every edit. Never assume. Never use project knowledge as a substitute for reading these files.

---

## 6. Supabase

**URL:** `https://ebppsgaftzyvftemfeom.supabase.co`
**Anon key:** `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVicHBzZ2FmdHp5dmZ0ZW1mZW9tIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODI2MTA3ODIsImV4cCI6MjA5ODE4Njc4Mn0.C0q7wPpNjXrFPWzCzXcPuR_4n8txumOxxSvzWZkVAFg`
**Region:** us-east-1 · Free tier — kept alive by GitHub Actions every 3 days (targets `species` table, exits 1 on non-200).
**Supabase MCP** is connected to Claude Chat — query the DB directly without the DM pasting anything.

**Tables (world):** `regions` · `nations` · `cities` · `creatures` · `organizations` · `world_characters` · `pois` · `religions` · `items` · `spells` · `species` · `quests`
**Tables (player):** `player_characters` · `character_sheet` · `inventory` · `character_notes` · `character_photo`
**Tables (session):** `session_notes` · `quest_progress`
**Tables (tracking):** `active_work` · `requests`
**Storage:** `world-images` (public) · `character-photos` (private)

**`active_work`** (`id`, `item`, `location`, `status`, `notes`, `updated_at`) — real-time in-progress/blocked/approved state. General session state only, never a specific tracked item's status (that's `requests`) — check it before starting any new work. Takes priority over `requests` when the two disagree, since it's updated live during a session while `requests` reflects the last time that specific row was touched.

**`requests`** (`id`, `title`, `description`, `area`, `status`, `source`, `notes`, `size`, `priority`, `created_at`, `updated_at`) — the single, live tracker for every DM feature request and bug report (see §1 Start item 7). Both `status` and `priority` are free-text columns, not DB-enforced enums — the values below are the real, current convention, not a schema constraint.
Real `status` values: `not-started` / `in-progress` / `blocked` / `needs-decision` / `declined` / `in-qa` / `approved-pending-push` / `approved-and-pushed` / `pushed-not-qa-approved`. The last four distinguish *built* from *verified* from *approved* — never treat any of them as interchangeable with a plain "done." A merge is not DM approval; default to `pushed-not-qa-approved` unless sign-off is explicitly confirmed in conversation (see the Aug 25 `0eccbc9` merge, done on this exact point).
Real `priority` values: `blocking` / `important` / `polish`.

**RLS:** All world tables have anon SELECT enabled. Wiki filters `player_facing` at render time.
**`read_aloud` column:** Present on `world_characters`, `cities`, `pois`, `creatures`, `items`.
**`session_scenarios` jsonb:** On `session_notes` — read by SCC Live Session tab accordion.

**`field_guide_sessions_backup`** (`id`, `session_number`, `blocks` jsonb, `backed_up_at`, `reason`) — snapshot table for `field_guide_sessions`. Insert a copy of the current row here before any large batch edit to `field_guide_sessions` (not per-edit — before a batch), so a bad rewrite has a real point to revert to.
**`world_characters_backup`** (`id`, `character_id`, `data` jsonb, `backed_up_at`, `reason`) — same snapshot pattern, for `world_characters`. Insert a copy of the current row before any large batch edit.
**`quests_backup`** (`id`, `quest_id`, `data` jsonb, `backed_up_at`, `reason`) — same snapshot pattern, for `quests`. Insert a copy of the current row before any large batch edit.
**`field_guide_qa_checkpoints`** (`id`, `session_number`, `section`, `status`, `notes`, `updated_at`) — per-section Field Guide QA status. Replaces tracking QA progress as a single free-text note in `active_work`; check this table for the real current shape of QA progress before assuming a session's read-through state from a narrative summary.

---

## 7. Debugging Rules (D1–D9)

**D1 — 2-attempt hard limit (non-negotiable).** If a fix fails twice, STOP. Do not write a third variation. Something is wrong with the diagnosis, not the fix. Say: *"I have reached the 2-attempt limit. I will not write another fix until I have read the exact current state of the broken code and identified the specific line that is wrong."*

**D2 — Diagnose before touching anything.** Read the actual broken code. Identify the exact line that is wrong and state precisely why it produces the observed behaviour. If uncertain, say so.

**D3 — Fix in one shot.** Once the root cause is identified, write one targeted fix. Do not patch symptoms.

**D4 — "Stop. Think. One fix."** When the DM writes this: halt all iteration. Re-read relevant code from scratch. State the root cause in plain English. Write one fix. Do not explain previous attempts.

**D5 — "Rewrite from first principles."** When the DM writes this: discard all accumulated edits. Write a clean version from scratch.

**D6 — Never answer without evidence.** Before answering any question about the project — code, UI, file structure, feature behavior — read the file or search project knowledge first. If the answer cannot be confirmed, say "I don't know — let me check."

**D7 — When the DM corrects a factual claim:** Stop immediately. Do not defend the previous answer. Correct the record in the Decision Log before writing any code.

**D8 — When a testing tool proves structurally unable to verify something, say so the first time, then loop in the DM immediately.** Example on record: a sandboxed browser tool that converts `target="_blank"` clicks into same-tab navigation instead of a real, inspectable new tab — no amount of retrying, different click techniques, or `window.open()` variants makes it capable of that specific check. The moment a limitation like this is confirmed (not suspected — actually tried and hit), say so plainly and ask the DM for one concrete, real verification step. Do not spend a second or third round writing another fix and re-running the same approximated/partial test as if it might now pass — a tool that structurally cannot observe the thing in question won't observe it any better on attempt three. Loop in the human check as soon as the ceiling is confirmed, not after exhausting further code changes against it.

**D9 — "Verified live" or "confirmed working" must be backed by real evidence in the report, not a summary assertion.** For a UI/rendering claim, that means the literal text or state actually observed on screen — not "confirmed working." For a data claim, the actual query result — not "confirmed correct." If genuinely unable to produce that evidence for something (see D8), say so plainly instead of writing a confident-sounding summary anyway. This is the mirror-image failure to D8: D8 is about a tool that can't verify; D9 is about writing "verified" without ever actually producing the check. On record: the same box-category-chip bug was reported fixed and verified six times across one project before an actual per-item, evidence-based check (paste what you literally see for each of N things, not a paragraph claiming they all pass) finally caught that most of the "verified" instances never were.

---

## 8. Site Integrity Rules (C1–C6)

**C1 — Read dm.html before every edit. No exceptions.** Read the current state of the target function with line numbers before writing any replacement. Project knowledge is never sufficient.

**C2 — No literal emoji in JS strings.** All emoji and special characters inside JS string literals in HTML files must use `&#decimal;` entities or `\uXXXX` escapes. A single literal emoji produces `Uncaught SyntaxError: Invalid or unexpected token at dm:1:1` — crashing the entire page with no useful error message.
- Safe: `&#128203;` · `—`
- Unsafe: literal 📋 or — inside a quoted JS string

**C3 — Pre-push confirmation for dm.html.** Before any dm.html push, show the diff and confirm: no literal emoji in JS strings, no unmatched quotes.

**C4 — Site crash recovery: `git reset --hard`, not `git revert` chains.** See Section 4.

**C5 — Rejected diagnosis is confirmed wrong. Stop completely.** If the DM says a diagnosis is wrong, stop all iteration. Read the actual current code state. State the new diagnosis with the specific line and reason. Write one fix only after the new diagnosis is confirmed.

**C6 — A change to shared rendering logic must re-verify every feature that mechanism affects, not just the one it was meant to fix.** If a change touches a chip/badge system, a shared layout component, thumbnail rendering, or any other mechanism multiple already-shipped features depend on, explicitly check the *other* consumers of that mechanism too, and say in the verification report that this was checked — not just the one thing the ask named. An item already marked `approved-and-pushed` in `requests` is not immune to a later shared-mechanism change breaking it silently; approval reflects the state at the time it was checked, not a guarantee against regressions introduced afterward.

---

## 9. Code Authoring Rules

**Claude Code prompts containing code blocks:**
Always use a bash heredoc when writing file contents that include backtick code blocks. Never wrap such a prompt in a fenced code block — inner backticks will break the outer fence and content will be silently dropped.

    cat > filename.md << 'ENDOFFILE'
    ...content with any backticks, code blocks, special characters...
    ENDOFFILE

**JS strings (all files):**
- Never use literal emoji in JS strings — use `&#decimal;` or `\uXXXX` (mirrors C2)
- When running sed or regex replacements, always show the diff before committing

**CSS (`character.css`, `tokens.css`):**
- Define `:root` tokens before any component rules
- No inline `style=` attributes — all styles go in character.css
- No raw `rgba()` or hex in rules — every color references a CSS variable (exceptions: `:root` definitions and canvas JS strings)
- Semantic variable naming: `--char-card-bg` ✓ · `--char-dark-blue` ✗
- Shared elements use shared classes — no one-off cosmetic classes
- No duplicate rules — edit the existing rule, never add a second copy
- Only add a new variable when the value appears in more than one rule

**Tooltips (`character.js`, `character.html`):**
- Always `data-tip`, never `title` — `title` triggers native browser tooltip
- `initTooltips()` is an empty stub — use `wireTooltip(el)` directly
- Static HTML wires automatically on DOMContentLoaded
- Dynamic content must call `wireTooltip(el)` per element after rendering

**Positioning (`character.html`):**
- Always `position: sticky` — never JS scroll listeners for sticky behaviour
- `body.char-page` has `overflow-x: hidden` which breaks `window.scrollY` in Chrome/Safari
- Before adding any `position:fixed` or `position:absolute;inset:0` element: confirm z-index, confirm `pointer-events:none` unless interaction required, confirm it will be removed from DOM after use

**z-index ladder:**
| Element | z-index |
|---|---|
| Tooltips | 99999 |
| Confirmation overlay | 200 |
| Return banner | 100 |
| Sticky HUD (Stage 4) | 90 |
| Sticky Stage 3 panel | 50 |

**Modals (`sheet/index.html`):**
- All modals must be injected into `#cs-modal-root` — a direct sibling of `<main id="cs-main">` inside `<body>`
- Never build modals as part of `renderCharacter()`'s HTML string
- Reason: `.cs-wrap` has a `cs-fadein` animation with `forwards` fill-mode — any `animation`, `transform`, `filter`, or `perspective` on an ancestor creates a new containing block for `position:fixed` descendants, breaking them silently. Symptom: modal appears at the top of the page regardless of scroll position.

**Canvas (`welcome.html`):**
- Partition animation text is drawn via `drawText(p)` — never via HTML/CSS
- Never add HTML elements inside `#partition-sticky`

**Field Guide inline content treatments (`dm.html`):**
- Any inline visual treatment for content — a DM-only aside, stage-direction, comedy, a skill check, anything that needs to visually read differently from plain prose — must be a real, named CSS class (e.g. `.scc-fg-aside`, `.scc-fg-stage-direction`, `.scc-fg-comedy-inline`, `.scc-fg-check`), never a one-off inline `style=` attribute.
- Any label text that treatment implies (e.g. "(DM-Only)", "(Read Aloud)") must be auto-injected by that class — a CSS `::before { content: ... }`, same mechanism `.scc-fg-aside` and `.scc-fg-stage-direction` both use — never hand-typed into the content itself. Hand-typed labels drift out of sync with the styling and can't be bulk-recolored/relabeled later without touching every instance of content.
- Before building a new treatment, check `js/fg-category-schema.json` and this file's own class list for an existing one that already fits. A category/treatment with no real home yet is a signal to build one class other content can reuse, not to hand-patch this one instance.
- This governs *presentation* classes specifically (how something looks/reads). It doesn't apply to structural block types (tables, quest-beat widgets, scenario cards) that already have their own dedicated render functions and real schema-defined shape.

---

## 10. Design Tokens

All tokens live in `css/tokens.css` — single source of truth. No `:root` blocks anywhere else.

**Fonts (two only):**
- `--font-display: 'Cinzel Decorative', Georgia, serif` — all headings, Bold 700 only
- `--font-body: 'Roboto', system-ui, sans-serif` — all body text, labels, UI (300/400/700)
- `--font-mono: 'Courier New', Courier, monospace` — Formery stamps, code

**Aliases:** `--font-headers` → `var(--font-display)` · `--font-serif` → `var(--font-display)` · `--font-sans` → `var(--font-body)`

**Gold:** `#f0b429` everywhere. Old `#c8940a` retired. Do not re-add Charm or EB Garamond.

**Google Fonts load:** `Cinzel+Decorative:wght@700 + Roboto:ital,wght@0,300;0,400;0,700;1,300;1,400;1,700`

---

## 11. Content Policy

**Spell content policy**: Anavale only includes spells shown and as written in the official 2024 5th Edition Player's Handbook. Supplement material (Xanathar's, Tasha's, Fizban's, etc.) not reprinted in the core 2024 PHB is excluded — flagged via `spells.classes = '{}'`, never deleted (deletion is a one-way door; flagging keeps the row recoverable if this policy ever needs revisiting). When a spell exists under two names in the catalog (a trademarked/legacy name and a generic SRD name, or a 2014 name superseded by a 2024 rename), only the row matching the name/wording actually printed in the 2024 PHB stays available — the other is flagged the same way, not deleted.

This is a standing rule, not a one-off decision — a future "is this spell legit for Anavale" question should default to this policy rather than becoming a fresh DM ask each time. Only escalate to the DM when a case is genuinely ambiguous (e.g. sources disagree on what the physical 2024 PHB actually prints) — routine core-vs-supplement calls are already answered by this policy.

---

## 12. Field Guide Request Tracker (as of Aug 25, 2026)

Status taxonomy: Not Started / In Progress / In QA / Approved Pending
Push / Approved and Pushed / Pushed but Not QA Approved.

Content edits (direct Supabase writes) take effect immediately, no
separate push step. Code/CSS fixes go through local -> merge -> push.
Where DM sign-off isn't explicitly confirmed in conversation, default
to "Pushed but Not QA Approved" rather than assume approval, a merge is
not the same as DM approval (see the DM's own standing note on the
original Phase 2/5 merge, commit 0eccbc9).

Two merge batches: 0eccbc9 (earlier, explicitly NOT DM-approved,
technical/consolidation only) and e40def2 (tonight's later batch,
scenario distribution through the image-upload fix).

### Overall Field Guide QA status
In Progress. DM paused mid-read-through at "Beat 2 — Under the Bridge
(75 XP)" in Session 1. Tracked in the active_work Supabase table.

### Templatization goal
In Progress, tracked as its own active_work row. Audit delivered
(field-guide-templatization-audit.md, given to the DM directly, not in
this repo): closed category set identified, hardcoded colors found,
recurring patterns documented. Not yet done: turning those patterns
into something Code-enforced rather than remembered.

### Content & Narrative
| Request | Status |
|---|---|
| Full Field Guide audit + redesign (4-phase) | Pushed but Not QA Approved |
| Session Opening real content (recap, location, per-player context) | Pushed but Not QA Approved |
| A/B/C placeholders to real player names | Pushed but Not QA Approved |
| Entity links throughout the document | Pushed but Not QA Approved |
| Generic "DM ONLY" titles to real descriptive titles | Pushed but Not QA Approved |
| Remove redundant static Beat XP headers | Pushed but Not QA Approved |
| Split mixed narration/dialogue/DM-only boxes | Pushed but Not QA Approved |
| Remove "how to use the tool" instructional text | Pushed but Not QA Approved |
| Fix broken "names above" reference in Beat 3 | Pushed but Not QA Approved |
| Inline quest-beat status + skill check formatting for Beat 1/2 | Approved and Pushed |
| Clarify player-known vs DM-only for "What the Town Knows" | Pushed but Not QA Approved |
| Merge Message Board content into scenarios correctly | Pushed but Not QA Approved |
| Formery refusal content to scenarios | Pushed but Not QA Approved |
| Religion/org presence woven into arrival, not standalone section | Pushed but Not QA Approved (automated trigger version still Not Started) |
| Section renumbering removed entirely | Approved and Pushed |
| Gerald's size increased to 8ft/12ft | Approved and Pushed |
| Gerald's death: real player-interaction window before he dies | Approved and Pushed |
| Comedy content woven inline, not standalone boxes | Approved and Pushed |
| "Comedic Element" label applied to all inline comedy | Approved and Pushed |
| Comedy dialogue exchanges reformatted script-style | Approved and Pushed |
| Player/NPC dialogue color-coded | Approved and Pushed (needs real CSS variable, see Bugs) |
| Bumble Frog dialogue boxes added | Approved and Pushed |
| "The Dimming — Quick Reference" box added to World Reference | Approved and Pushed |
| Gigglegloom/Dimming questions removed (players already know) | Approved and Pushed |
| Grusk/Vareth/Formery/Brightcreed questions moved to contextual trigger points | Approved and Pushed |
| Read-aloud description added for first seeing the Grusk agents | Approved and Pushed |
| "Playing Pellam Consistently" guidance box | Approved and Pushed |
| Duplicate acorn/Fluffet scene found and removed | Approved and Pushed |
| "The Real Prize Is the Documentation" moved to correct beat | Approved and Pushed |
| Beat naming mismatch fixed (quest data + Field Guide aligned) | Approved and Pushed |
| Pellam pronoun sweep (they/them) | Approved and Pushed |
| Bridge-to-inn transition text added | Approved and Pushed |
| Tinywing's sound ("skree") established | Approved and Pushed |
| Pocketmole comedy beat split and clarified | Approved and Pushed |
| guide_meta intro box rewritten (was hours stale) | Approved and Pushed |
| Featured thumbnails + entity links across ~25+ boxes | Mostly Pushed but Not QA Approved; specific screenshotted ones Approved and Pushed |
| Scenario groups distributed to contextual beat locations | Approved and Pushed |

### Layout & Visual (Code)
| Request | Status |
|---|---|
| Sidebar cards as accordions | Pushed but Not QA Approved |
| Sidebar reorder + Timer reset button | Pushed but Not QA Approved |
| Field Guide content area height | Pushed but Not QA Approved |
| Dialogue tone note readability | Pushed but Not QA Approved |
| Visualize panel field label readability | Pushed but Not QA Approved |
| Heading/label font sizes | Pushed but Not QA Approved |
| Featured thumbnail layout (image top-left, chip+title stacked) | Pushed but Not QA Approved |
| Header-to-body vertical gap reduction | Pushed but Not QA Approved |
| Comedy chip icon size | Pushed but Not QA Approved |
| h2 margin spacing | Pushed but Not QA Approved |
| Local dev warning bar overlap (main column + sidebar) | Pushed but Not QA Approved |
| Scenario jump lands under sticky header | Pushed but Not QA Approved |
| Box category chip consistency (all 7 categories) | Approved and Pushed |
| Roll Initiative button color | Pushed but Not QA Approved |
| Skill check container styling | Pushed but Not QA Approved |
| Featured entity thumbnails zoomed 50% | Not Started |
| Small inline entity-link icon alignment | Not Started |
| Scenario distribution rendering + jump dropdown rework | Approved and Pushed |
| Featured-thumbnail rendering extended to h2 scenario headers | Pushed but Not QA Approved |
| Featured thumbnails not rendering for type "poi" | Pushed but Not QA Approved |
| Featured thumbnails not rendering for type "pc" | Pushed but Not QA Approved |

### Features
| Request | Status |
|---|---|
| Quick-attach companion button | Pushed but Not QA Approved |
| Companion attachments on real character sheet + tooltips | Pushed but Not QA Approved |
| Quick Note: "Ignores Formery" category with player tagging | Pushed but Not QA Approved |
| Real location-arrival trigger for religion/org presence | Not Started |

### Real bugs found and fixed
| Bug | Status |
|---|---|
| Image upload/removal silently failing (missing storage RLS policies) | Pushed but Not QA Approved |
| Bounce Beetles artwork accidentally overwritten during that fix's testing | Restored and confirmed byte-identical |
| Mislinked "Tumblerun" | Approved and Pushed |
| Broken entity link character:pellam-voss | Approved and Pushed |
| Off-by-one data corruption, Pocketmole edit (caught same session) | Approved and Pushed |
| Off-by-one data corruption, Questions cleanup (caught same session) | Approved and Pushed |
| Stale "READ ALOUD" literal text in two dialogue box labels | Approved and Pushed |
| Hardcoded gold hex used for Pellam's dialogue color | Approved and Pushed |
| Two remaining hardcoded hex colors, not real CSS variables yet | Not Started |

### Needs DM decision
- Situational vs Permanent attachments: should Situational do anything mechanically?
- `background` category doing three different jobs with no visual distinction, worth a decision before Session 2 copies it forward.

---

## 13. Decision Log — Split Across Files

*As of 2026-08-25, the Decision Log outgrew this file (2000+ lines, 152 entries, every subsystem interleaved chronologically) and was split into per-subsystem files under `docs/` so a session looking for one specific area's history doesn't have to read through every other subsystem's entries to find it. This was a reorganization, not an edit — every original entry's content is preserved verbatim in whichever file it landed in (verified via a before/after entry-count check and a byte-for-byte presence check on all 152 entries before this split was committed). Nothing was reworded, trimmed, or dropped. One entry was added on top of that split during the merge back into `main` — a 2026-08-23 nations RLS-policy fix that had landed on `main` independently after the split branch was cut — folded into `decision-log-general.md` rather than lost, bringing the real current total to 153.*

**Convention.** Each entry was assigned to the single file that's the closest fit for its primary subject — judged from the entry's own content, not just which file(s) it happened to touch (`dm.html`/`sheet/index.html` each host several unrelated subsystems, so "touched dm.html" was never sufficient on its own). `docs/decision-log-general.md` is not a leftover bucket — it holds two real, deliberate categories: entries that genuinely span multiple of the 4 named subsystems as co-equal subjects (a cross-cutting audit, a global color-token pass, a broad UX/IA restructure spanning several DM Tools panels at once), and DM Tools feature areas that are neither Field Guide nor Combat (Quest Manager, NPC/Location Manager, Notes, Session Setup, Vendor/Shop, Search+Preview, Currency admin, color-system-wide passes, etc.) — plus every entry that's about this project's own process (CLAUDE.md itself, git/worktree workflow, Debugging/Site-Integrity rules). It's the largest file for a real reason, not because classification was lazy: most of this project's actual logged work is DM Tools feature areas outside the two specifically-named DM Tools subsystems.

Within each file: append-only, most recent entry at top — the identical convention this section used before the split, unchanged.

| File | Covers | Entries |
|---|---|---|
| `docs/decision-log-field-guide.md` | The Field Guide document/box system itself — block storage, box categories, featured thumbnails, scenario distribution, Session 1 content build. Field Guide work folded into a broader cross-cutting DM Tools audit lives in `-general.md` instead, not here. | 6 |
| `docs/decision-log-combat.md` | The Combat Encounter Tracker (`dm.html`) specifically — staging, initiative order, conditions, loot award, filters. | 7 |
| `docs/decision-log-character-sheet.md` | The player-facing character sheet (`sheet/index.html`, `character.html`, `js/character.js`) — tab structure, Inventory/Equipped Gear, Ability Scores, Your Story/Background/Past, MainInfo header, class features that aren't spells. | 54 |
| `docs/decision-log-spell-system.md` | Spell slots/prepared/learned, the spell catalog audit, spellcasting stats, and the DM-side Spell Assignment panel — wherever in the codebase it lives, since spell mechanics were carved out as their own subsystem regardless of file. | 14 |
| `docs/decision-log-general.md` | Everything genuinely cross-cutting, every other DM Tools feature area, and all process/CLAUDE.md-itself entries. | 72 |

**Adding new entries going forward**: append to the one file matching what the session actually worked on, per §1 End item 1. If the session's work is genuinely cross-cutting (touches 2+ of the 4 named subsystems as co-equal subjects, or is process/infrastructure), it goes in `decision-log-general.md` — don't split one entry across files, and don't create a new subsystem file without discussing it with the DM first (the 4 named subsystems plus general is the convention now, not a starting point to keep subdividing).

---

