# Decision Log — Character Sheet

*Split out of `CLAUDE.md`'s single Decision Log on 2026-08-25 — see that file's "Decision Log — Split Across Files" index for the full convention. This file holds only entries classified as **Character Sheet**; entries genuinely spanning multiple subsystems, or that don't fit one area, live in `decision-log-general.md` instead. Append-only, most recent entry at top — same convention as before the split, unchanged.*

---

**2026-08-19 (same session, follow-up — Primal Order moved from a standalone Details-column note into a Key Features row, per direct DM instruction) — `sheet/index.html`.**
Supersedes item 3 of the entry directly below — the standalone `.cs-ov-class-primal-order` note under Class > Details (its own function `primalOrderNoteHtml()`, its own CSS, a "?" tooltip) is gone entirely, not kept alongside the new placement. Primal Order now renders as the first row in the Key Features list, matching the exact `.cs-class-feature-row` shape every other feature there uses (icon + name + short description, no tooltip — Key Features rows don't have tooltips anywhere else, so adding one just to this row would have been a new, unexplained inconsistency).
Real constraint driving the implementation: Primal Order is per-character (`player_characters.primal_order`), while the rest of Key Features comes from `classes.features` — per-class, shared by every Druid. It can't be spliced into that array without corrupting shared class data, so `buildClassFeaturesHtml(features)` now builds it as a separate prepended row (`primalOrderFeatureRowHtml()`, reading `CH.primal_order` directly) rather than trying to inject a synthetic string into the `features` array before mapping it. This also fixed a real ordering/timing gap the old `if (!Array.isArray(features) || !features.length) return ''` guard had: on the very first synchronous paint (before `CLASS_ROW` resolves), `features` is `null` and the old code returned nothing at all — now Primal Order alone renders on that first paint (`CH` is already populated by then), and the real class features join it once `CLASS_ROW` loads, with no flicker or missing-then-appearing item.
Icons reuse existing `FEAT_ICONS` glyphs rather than inventing new ones — sparkles (`&#10024;`, already used for "spellcasting"/"beast spells") for Magician, crossed swords (`&#9876;`, already used for "extra attack"/"fighting style"/smite features) for Warden. `PRIMAL_ORDER_TIPS` (the old tooltip-text-only lookup) replaced with `PRIMAL_ORDER` (icon + description per path, one object instead of two near-duplicate ones once the tooltip use was gone) — description text is the same real, paraphrased 2024 PHB mechanics from the prior entry, just with the "Magician:"/"Warden:" prefix dropped since the row's own name already says "Primal Order: Warden".
Verified live on Hana (real Druid, `primal_order:'Warden'`): "Primal Order: Warden" now renders as the first Key Features row with the crossed-swords icon and "You gain proficiency with martial weapons and medium armor.", positioned above the real Spellcasting/Wild Shape/Druidic/Druid Circle/Timeless Body/Beast Spells rows from `classes.features`, screenshotted. Confirmed the old Details-column note is gone (`cs-class-details` no longer contains `primal-order` anywhere). Confirmed Kael (Ranger, no `primal_order`) shows Key Features starting directly with Favored Enemy, unaffected. Zero console errors on both. `new Function()` syntax check clean; diff-scoped literal-emoji grep confirmed both new icon entities landed as `&#10024;`/`&#9876;` text, not re-normalized into literal glyphs by the Edit tool.


---

**2026-08-19 (new session, `anavale-wiki-class-features` worktree/branch — 3 small class-feature builds, closes 3 real `requests` rows) — `sheet/index.html`, `character.html`, `js/character.js`, `player_characters` schema.**
Ask covered 3 independent items; checked the `requests` table mid-task (per §1's own standing habit) and found real, matching DM entries for 2 of the 3 — "Kael: Pre-Partition Script language" and "Kael: Expertise in Survival" — which turned my earlier synthetic test picks (Caparian Deep / Perception) into the wrong real values; caught before finishing, corrected to the real ones. A third, "Hana: Warden path", confirmed the Primal Order ask's own framing was already the real fact, not a hypothetical.
**1 — Ranger's second language.** New `player_characters.language_extra_2` (text, nullable, same shape as `language_extra`). `sheet/index.html`: new `LANGUAGE_ROW_2`, fetched in `fetchLanguageData()` alongside the existing `language_extra`/Druidic fetches, rendered in `renderSpeaksBlock()` via the exact same row markup `language_extra` already uses (glyph/name/spoken_where, no tooltip) — purely "show it if it's set," no class check, per instruction. `character.html`: duplicated the language-card picker verbatim as a new `#char-language-section-2` (hidden by default), gated on `CHAR_STATE.draft.class_id === 'ranger'` via a new `updateSecondLanguageVisibility()`, called both from `selectClass()` (class is chosen in Stage 2, *after* the language section in Stage 1, so this has to be reactive) and from `restoreStage2Selections()` (fires every time Stage 1 is re-entered, covering "picked Ranger in Stage 2, came back to Stage 1"). Found and fixed a real bug surfaced by duplicating the card markup: `restoreStage2Selections()`'s primary-language restore used an unscoped `document.querySelectorAll('.char-lang-card')`, which — once a second section with the same class and the same `data-value`s existed — would incorrectly mark matching cards `.selected` in *both* sections at once; scoped both restores to their own `#char-language-section`/`#char-language-section-2` container. `buildCharacterEntry()` gains `language_extra_2`, gated on `d.class_id === 'ranger'` (not just the picker's own visibility) so a stale pick survives a class change back off Ranger. `js/character.js`'s dev-only `randomizeDraft()` left untouched — out of scope, not part of the real player flow. Kael backfilled to the real value (`pre-partition`), not the synthetic test pick — confirmed via `requests`.
**2 — Expertise.** New `player_characters.expertise_skills` (text[], nullable, default `'{}'` — same shape as `skills`). `computeSkillTotal()` now also checks `expertise_skills`, doubling `profBonus` only when the skill is *also* in `skills` (Expertise requires proficiency first — enforced in the check itself, not just documented as an invariant). Skill-row marker: for an Expertise skill, the "Prof." label is fully replaced (never shown alongside) by a brain icon (`&#129504;`, HTML entity per C2) + the shared `.cs-ab-tip` "?" circle, carrying its own `data-tip="Expertise doubles your proficiency bonus for this skill."` — the skill name's own, unrelated `SKILL_TIPS` tooltip is untouched (that's a different tooltip, for a different thing; conflating the two was the "old, confusing placement" the ask called out). Widened `.cs-ab-skill-marker` 26px→34px to fit both icons without crowding. Kael backfilled to the real value (Survival, matching the real `requests` row) rather than the synthetic Perception test pick.
**3 — Primal Order.** New `player_characters.primal_order` (text, nullable). Displayed as a small labeled note (`primalOrderNoteHtml()`) directly under the Class tab's Details column (Primary ability/Saves/Casting/Armor/Tools/Weapons) — real class info/traits, not a new location — shown only when set, no class gate (a non-Druid simply never has it set). "?" tooltip gives the real 2024 PHB mechanics, paraphrased rather than quoted verbatim (copyright — confirmed via 2 independent sources, CBR and a WebSearch synthesis, that Magician grants one extra Druid cantrip plus a Wisdom-mod-based bonus (min +1) to Intelligence (Arcana/Nature) checks, and Warden grants proficiency with martial weapons and medium armor; a third source's claims about shields and terrain-based bonus spells didn't independently corroborate and were dropped as likely conflating with the Circle of the Land subclass). Hana set to `Warden` — the real, DM-confirmed fact this session's own ask was built on, not a test value; matches the real `requests` row found mid-task.
Verified live end-to-end on Kael Evander and Hana Sable via a fresh local server (`8791` was already serving the `anavale-wiki` main worktree, not this one — confirmed via `curl`+`cwd` before touching anything, started a new one on `8820` instead, per the standing "may be serving another session" lesson). Kael: Speaks block shows Nombi Frost + Pre-Partition Script (2 rows, correct colors/sub-text); WIS card shows Survival with the brain+tooltip marker at `+7` (WIS mod +3, profBonus 2 doubled to 4) and Perception still plain `Prof.`/`+5`. Hana: Class tab shows "Primal Order: Warden" with the correct tooltip text, Druidic row unaffected. `character.html`'s new picker verified via direct JS calls (not just visual): `selectClass('ranger')` toggles the section visible immediately; picking cards in both sections keeps `.selected` correctly scoped to their own section (confirming the restore-bug fix); a full page reload with a saved draft correctly restores both cards' selected state and the section's visibility; `buildCharacterEntry()` correctly nulls `language_extra_2` for a non-Ranger draft carrying a stale `language_2` pick. Zero console errors throughout. `new Function()` syntax check clean on `sheet/index.html` (1 block), `character.html` (3 blocks), and `js/character.js`. Diff-scoped literal-emoji grep clean — the one new glyph (🧠) is the `&#129504;` entity, confirmed not silently re-normalized to a literal character by the Edit tool (a recurring risk on record in this file's own history). All 3 matching `requests` rows (`Kael: Pre-Partition Script language`, `Kael: Expertise in Survival`, `Hana: Warden path`) marked `done` with notes. A 4th, unrelated `requests` row surfaced in the same search (`Hana: free Speak with Animals spell`, `needs-decision`) was left untouched — not part of this ask.
DM QA'd and approved on the live local build; all 4 checks passed. Merged into `main` and pushed. Worktree removed per the standing worktree-hygiene rule (§1, Start item 6) once merged and clean.


---

**2026-08-17 (same day, new session — Round 3 Follow-up: Add Item modal + Currency lockdown) — `sheet/index.html` (this worktree/branch) + `dm.html` (built on `anavale-wiki-scc-ux`, per §3's note that the Players page/Spell Assignment feature lives there, not on `main`).**
Habit checks per §1: §3 confirmed empty but its own text names the branch this feature lives on; `git diff --stat main...anavale-wiki-scc-ux -- dm.html` re-checked and had grown to 2824 lines since the last check (from 509) — real, heavy, active work continuing there, confirming (not just trusting the note) that the Players page/Spell Assignment code genuinely doesn't exist on `main`'s `dm.html` at all (grepped and confirmed 0 matches vs. 11 on the branch) before touching anything.
**1a — +Add Item button relocated.** Was in `.cs-inv-header-row`, next to the "Inventory" page title, well above the doll/Currency/Carrying rows and the Search/Category filter row. Moved into `.cs-inv-filter-row` itself (Search, Category, +Add Item, one row, directly above the item table) — new `.cs-inv-additem-btn--row` modifier (`flex:0 0 auto; margin-left:auto`) since the row is `align-items:flex-end` (fields have a label row the button doesn't), pinning it to the row's right edge rather than letting it sit at the row's cross-start. Left `.cs-inv-header-row` as a single-child flex/space-between wrapper rather than restructuring it — harmless, not worth a separate cleanup pass.
**1b — close icon added, scoped to Add Item only.** Confirmed via grep this file has zero Escape-key handling anywhere and zero existing `.cs-modal-close`-style class — every modal in the file (Remove Item, Slot picker, Prepare a Spell, Add Item, Create New Item) relies on click-outside alone, or (Prepare a Spell only) an explicit "Close" text button. A real, sitewide gap, but scoped the fix to Add Item specifically per the ask rather than retrofitting every modal unprompted. New `.cs-modal-title-row`/`.cs-modal-close-btn` (× icon, same visual language as `.cs-prepared-pill-x`'s existing icon-button treatment) — only applied to Add Item's title bar; every other modal's plain-block `.cs-modal-title` is untouched.
**2 — Currency, a real model change, not a bug fix.** DM's reasoning: too easy for a player to accidentally change a value and lose track of what it was — same category of decision as Round 2 Revisions' Spells Learned shift (player views, DM edits).
- **Player side**: `renderGold()`'s 3 pills (`cs-currency-gp/sp/cp`) gained `readonly` + `aria-readonly="true"`, `onblur`/`onkeydown` save handlers removed, `onCurrencyBlur()` deleted outright (zero other callers, confirmed via grep before removing). Same visual treatment — the earlier currency-styling fix (real Figma-sourced pill colors, `.cs-mi-hp-num-input`-pattern input bg) is completely untouched, just no longer editable. Scoped the focus-glow CSS rule to `:not([readonly])` rather than deleting it, so it can't visually imply the field is still live, without breaking the rule for any future non-readonly reuse of the same class. Verified via a real click + real typed input (not just checking `.readOnly` in JS) that the value genuinely doesn't change.
- **Investigated, not touched**: the Add Item modal's own "Create New Item" sub-form has GP/SP/CP fields too (`ci-gp`/`ci-sp`/`ci-cp`) — confirmed via reading the actual save code (`cost_gp`/`cost_sp`/`cost_cp`, written onto the new catalog item row) that these define a custom item's *price*, not the player's own wallet — a different concept entirely, with no "before" value to accidentally lose (it's being set once, on a brand-new entry). Confirmed no auto-deduct path exists anywhere connecting item creation to `gold_cp` (re-confirmed via grep — only 2 real occurrences of `gold_cp` in the whole file: `renderGold()` and the initial character-sheet-seed line). Left player-editable — locking it would break "add a custom item" for players with no real currency-loss risk to justify it.
- **DM side — `dm.html` (`anavale-wiki-scc-ux`)**: new "Currency" `.scc-pd-section`, sibling to Spell Assignment inside `renderPlayerDetailBody()`, per the extensible-by-design note already on that function — no restructuring of Spell Assignment or the panel/toggle mechanism needed. `renderPlayerCurrencySection()`/`sccPlayerCurrencyBlur()` mirror the sheet's own retired `renderGold()`/`onCurrencyBlur()` shape exactly (3 fields, blur-to-save, recompute-whole-total-from-all-three), but reuse the Combat tab's existing `.scc-loot-coin-row`/`.scc-coin-input-wrap`/`.scc-coin-input`/`.scc-coin-label` (already gp/sp/cp color-coded, from the loot-award UI) rather than inventing new input styling for the same 3-coin shape. Writes via `sccPatch('character_sheet', ..., {gold_cp: newTotal})` — the identical pattern `sccAwardCoins()` already uses elsewhere in this file.
Verified end-to-end, real click/type interactions where practical, not direct function calls: opened Hana's real detail panel via the actual "✎ Edit" button, confirmed the Currency block renders her real 9/0/0; set GP to 17 via a real focus+type+blur cycle, confirmed via SQL `gold_cp` landed at 1700; reloaded the real player-facing sheet (separate port, same Supabase project) and confirmed it now reads Gold 17 — then attempted a real click+triple-click+type on that same field and confirmed the value stayed at 17, proving the lock isn't just a JS property check. Reverted Hana's `gold_cp` to 900 via SQL, confirmed via a follow-up read. Add Item button confirmed on the Search/Category row via screenshot on both desktop and mobile; close × confirmed via a real click collapsing the modal (`overlay.style.display === 'none'`). Mobile check (375px, the item left untested last round) completed across Ability Scores/Inventory/Class on Hana, cross-checked the two new pieces (button placement, currency readonly) on Kael — zero horizontal overflow on any tab, zero console errors throughout on both files. `new Function()` syntax check clean on `sheet/index.html` (1 block) and `dm.html` (7 blocks). Diff-scoped literal-emoji grep empty on both.


---

**2026-08-17 (same day, new session — Round 3: Ability Scores/Inventory/Class Tab fixes, 7 items) — `sheet/index.html`, `css/tokens.css`.**
Habit checks per §1: §3 Pending/Blocked confirmed empty; `git diff --stat main...anavale-wiki-scc-ux -- sheet/index.html css/tokens.css dm.html` showed real overlap on `dm.html` only (509 lines, unrelated "Color Health UI drift" work) — flagged and used directly below (item 2b) as the reason `dm.html`'s own input/select conventions were left out of scope this round, not touched.
**1a — Ability card centering reverted.** Last round's `justify-content:center` on `.cs-ab-card` (added to balance shorter cards like CON's "No skills" against taller siblings) removed on direct DM instruction after live review — cards go back to natural top-aligned flow. Confirmed the equal-row-height behavior it was layered on top of (`.cs-ab-cards`' default Grid `align-items:stretch`) is untouched and unaffected by the reversal — that was never the `justify-content` rule's doing.
**1b — Proficiency/Saving Throw explainer restyled as two blocks.** Checked the real live render before assuming work was needed, per instruction — `outerHTML` showed the whole explainer was still one flat `<p>` with both concepts run together, not matching the DM's target (two sections, each a bold gold sub-heading + its own paragraph, clearly separated). Split `ABILITY_EXPLAINER` into `ABILITY_EXPLAINER_BLOCKS` (two `{heading, body}` pairs, same verified 2024 PHB wording, broken at its natural seam) and rebuilt the render as two `.cs-ab-explainer-block`s, each an `.cs-ab-explainer-heading` (bold, `var(--gold)`) + `.cs-ab-explainer-body` paragraph. Verified live (screenshot) — matches the target exactly.
**2a — Carrying icon moved into the bar.** Was prefixing the "Carrying" section header text; moved into `renderCarryingBar()`'s own label string, directly before the "X / Y LBS" number, per the DM's explicit placement ask. Verified via a DOM read of the bar's real `innerHTML` (`⚖ 20 / 210 LBS`) since the section sits below the fold and this environment's Browser pane has a known, on-record scroll-then-screenshot rendering glitch — worked around via a fresh tall-viewport tab (established pattern) for the confirming screenshot.
**2b — Sitewide input/select height, scoped and flagged.** Surveyed all ~14 distinct input/select classes in `sheet/index.html` before picking a scope, per the task's own warning that a blind sitewide sweep could be bigger than expected. Found 3 genuinely distinct "search field" conventions co-existing for the same functional control: `.cs-inv-filter-input/-select` (Inventory's own filter row, ~59px — the DM's literal "too tall" complaint), `.cs-inv-input/-cat-select` (item search modals, ~33px), `.cs-notes-search` (Notes tab search, ~38px) — confirming the DM's broader "sitewide" framing was correct, not just an Inventory-specific issue. New `--char-field-height: 40px` token (`tokens.css`), applied to all 5 of those classes (universal `box-sizing:border-box` already in place at line 16, so no per-rule box-sizing needed — just `height` + zeroed vertical padding). Deliberately did NOT touch compact badge/pill controls embedded in already-tuned fixed-height rows — HP number inputs, the header Conditions select, currency pills, inventory-table qty steppers (all ~37px, part of calibrated header/table layouts with their own extensive Decision Log history) — forcing those to 40px would break those rows' own tuning, not unify anything; a genuinely different UI language from a "field." Confirmed the Story tab has zero input elements at all (grep, not assumed) — nothing there to bring in line. **Flagged, not silently partial**: `dm.html` (18 more distinct input/select classes, its own separate design conventions, and 509 lines of active unmerged changes on `anavale-wiki-scc-ux` per the branch-overlap check) and `character.html`/`css/character.css` (a different creation-flow page with its own parchment-themed visual language, not part of this round's "Character Sheet Round 3" scope) were both left untouched — real, deliberate scope boundaries, not an oversight. A cache-busted `tokens.css` reload was needed mid-verification (the same recurring browser-caching issue on record from many prior sessions) before the new token's value showed up live.
**3a — Tools/Weapons row, investigated before touching anything.** Queried the real Druid row in Supabase first, per instruction not to assume a data bug: `tools:"Herbalism Kit"`, `weapons:"Simple (no metal)"` — both correctly populated, correctly mapped to their own fields. Not a data bug. Root-caused via `getBoundingClientRect()` on every key/value `<p>` in the live DOM: `.cs-ov-class-detail-keys`/`.cs-ov-class-detail-vals` were two *independent* flex columns relying on every row sharing an identical fixed 24px line-height to stay aligned — Druid's `armor` value ("Light, Medium, Shields (no metal)") wraps to 2 lines in this column width while its own key stays 1 line, so every value below it in that column silently shifts down one row relative to its key: Tools' key ends up beside the tail of Armor's wrapped text (reads as no value), Weapons' key ends up beside Tools' real value, and Weapons' own real value dangles in an extra row with no key beside it at all — exactly the DM's screenshot. Fixed structurally, not by rewording: converted `.cs-ov-class-detail` from two flex-column siblings to a single 2-column CSS Grid (`grid-template-columns: auto 1fr`) with key/value `<p>`s emitted as direct interleaved children — grid's row-major auto-placement locks each pair to the same row regardless of wrapping, so a wrapped row just grows instead of desyncing everything after it. A second, compounding bug surfaced during verification (measured a consistent extra 32px per row after the grid fix, not flush as expected): `.cs-ov-class-details` (the outer flex row holding both detail blocks side by side) had no `align-items`, defaulting to `stretch` — forced the shorter Armor/Tools/Weapons grid to match the height of its sibling block (Casting Spells' prose is much taller), and the grid's own `align-content:normal` (behaves as stretch) spread that leftover height evenly across the 3 auto rows. Fixed with `align-items: flex-start` on the outer flex row — the same root-cause shape (and same one-line fix) as an earlier session's identical `.cs-story-cards` bug. Verified live on both Hana (Druid, the real wrapping case — confirmed flush via `getBoundingClientRect()`, 0 gap between rows) and Kael (Ranger, shorter values, confirmed no regression).
**3b — Features accordion spacing tightened further.** Re-measured live rather than trusting the prior round's "fixed" claim, per instruction: `.cs-limuse-group`'s own padding (26px on every side, a real Figma-sourced value shared with `.cs-effect-panel` — left untouched, not a token to shrink unilaterally) plus the 26px inter-group gap (`.cs-ov-acc-body--tight`, itself an ad-hoc non-Figma value from an earlier round meant to address this exact complaint) compounded into real, measurable dead space around short groups like Class-Specific's single "Wild Shape / Unlocks at level 2" card. Dropped the inter-group gap from `--char-ov-gap-s` (26px) to `--char-ov-gap-2xs` (8px, the smallest existing token) — verified via `getBoundingClientRect()` across all 4 groups (Class-Specific/Spell Slots/Spells Prepared/Spellcasting) post-fix: exactly 8px between every pair, down from 26px, with each group's own padded/backgrounded box still reading as clearly distinct at the tighter gap.
**3c — Spells Prepared: one pill per empty slot.** `renderPreparedSpells()` rendered exactly one "+ Prepare" button regardless of `max - prepared.length` — fixed to loop and emit one pill per genuinely empty slot (Hana at 0/4 → 4 pills, Kael at 0/2 → 2 pills, both confirmed live). Every pill opens the same shared `openSpellPicker()` modal; `prepareSpell()` already self-limits at `max`, so no per-pill slot-index tracking was needed.
**3d — Spellcasting relocated into the Features accordion.** Was a separate standalone `.cs-ov-class-card` (its own `id="cs-spellcasting-stats-card"` + manual `style.display` show/hide toggle) sitting between the Features accordion and Spells Learned. Removed that wrapper entirely (caught and fixed a dangling orphaned `+ '</div>'` left over from the removal before it shipped, via a careful before/after read of the surrounding template) and added a new `hasCasting`-gated group to `renderClassFeaturesAccordion()`'s template, directly after Spells Prepared — same gate Spells Prepared itself already uses, so a non-caster gets neither. `renderSpellcastingStats()` simplified to just target the inner div directly (no more card-visibility toggle to manage, since inclusion is now template-gated) and is now called from `renderClassFeaturesAccordion()` itself (alongside the other sibling-group renders) as well as `renderLivePlay()`'s existing refresh call. Final section order, confirmed sensible once assembled and left as the natural DM-implied ordering (Class-Specific → Spell Slots → Spells Prepared → Spellcasting → Spells Learned, the last one still its own card below the accordion, unchanged) — not second-guessed since the ask's own wording ("directly below Spells Prepared") already specified the one placement that mattered.
**Verification**: all 7 items checked live on Hana (Druid — the real Armor-wraps case for 3a, 0/4 Spells Prepared for 3c) and cross-checked on Kael (Ranger — shorter Armor/Tools/Weapons values confirming no 3a regression, 0/2 Spells Prepared, and the pre-existing "Class-Specific correctly absent for a class with no limited-use features" rule confirmed still holding inside the now-restructured accordion). Mobile-checked (375px, cache-busted `tokens.css` reload after the viewport resize triggered the same recurring caching issue) on all 3 tabs — zero horizontal overflow throughout (`document.body.scrollWidth - window.innerWidth === 0` on every check), Ability Scores cards stack one-per-row, Class tab's Details grid and Features accordion both reflow cleanly. Zero console errors across every check on both characters. `new Function()` syntax check clean on the one extracted inline script block. Diff-scoped literal-emoji grep empty (the one new icon, ⚖, was already the pre-existing `&#9878;` entity from last round, just relocated — no new literal character introduced).


---

**2026-08-17 (same day, new session — 4 small independent QA fixes, worked in order, shipped together) — `sheet/index.html`, `css/tokens.css`.**
Habit checks per §1: §3 Pending/Blocked confirmed empty, `git diff --stat main...origin/anavale-wiki-scc-ux -- sheet/index.html css/tokens.css` re-checked (that branch has since added an unrelated region-health token block to `tokens.css`, confirmed via full diff to sit at a completely different line range from anything touched this round — no overlap).
**1 — Ability Scores: tooltip consolidation + card alignment.**
Read the real current markup before changing anything, per instruction, rather than assuming from memory: `abilitySaveRowHtml(ab, modNum, profBonus, isProficient, showIntroTip)` rendered a per-ability `SAVE_TIPS[ab]` "?" tooltip on every card's Save row, plus a `showIntroTip`-gated blue "i" pill (`SAVE_INTRO_TIP`) on STR's row only, both wired through the shared `.cs-ab-tip` mechanism. Removed both — `abilitySaveRowHtml()` dropped its 5th parameter entirely (both call sites in `buildAbilityScoresHtml()`/`renderAbilitySkills()` updated to match), `SAVE_TIPS`/`SAVE_INTRO_TIP` deleted (confirmed via grep zero other references before removing), `.cs-ab-tip--intro`'s now-fully-dead CSS rule removed too.
Copy verified against real 2024 PHB sources before writing a word, not from memory, per instruction — cross-checked 2 independent sources (5e24srd.com's Proficiency page via WebFetch, Roll20's 2024 "Resolving Outcomes" compendium page) and confirmed they agree on substance: Proficiency Bonus "reflects the impact that training has on the creature's capabilities... applied... when the creature has proficiency in a skill, saving throw, or item... also used for spell attacks and spell save DCs"; a Saving Throw "is an instant response to an effect and is almost never made by choice... makes the most sense when something bad threatens a character and the character has a chance to avoid or resist it." New `ABILITY_EXPLAINER` is a close paraphrase of both, 2 sentences, rendered via a new `<p class="cs-ab-explainer">` inserted at the top of `buildAbilityScoresHtml()`'s returned markup, above `.cs-ab-cards` — new CSS reuses the existing "muted helper paragraph" visual weight already established by `.cs-spellslot-explainer`/`.cs-ov-blurb` rather than inventing a new type treatment.
Card alignment: confirmed the real current layout before picking a technique, per instruction — `.cs-ab-cards` is `display:grid` with no `align-items` override, meaning cards already default to `align-items:stretch` (equal height per row, confirmed this was already true before touching anything). The actual gap was `.cs-ab-card`'s own `flex-direction:column; align-items:center` having no `justify-content`, so content stacked from the top of that already-equal-height box instead of centering within it — CON's "No skills" case and the other 5 cards next to WIS's taller natural content both read as bottom-heavy for the same underlying reason. Added `justify-content:center` — one line, no equal-height machinery needed since Grid was already providing it for free. Re-checked after the tooltip removal (1a) per the task's own explicit instruction not to trust the pre-change screenshot, since removing a tooltip icon changes each card's natural content height slightly.
**2 — Currency section styling, root cause found via computed style, not assumed from the screenshot's own framing.** The screenshot's premise was "Gold's label is broken, Silver and Copper read fine" — `getComputedStyle()` disproved the second half of that: Gold's and Silver's pill-label colors resolved to the exact same near-black `rgb(10,10,15)`, on the exact same fully transparent pill background. Traced to the actual cause: `.cs-currency-pill--gold/--silver/--copper` all reference `var(--currency-gold-coin)`/`var(--currency-silver)`/`var(--currency-copper)`, none of which exist anywhere in `tokens.css`. `git log -S` found why: these 3 tokens had real, Figma-sourced values (`#daa318`/`#babcc5`/`#9c5b18`) from the 2026-08-07 commit that first built this Overview redesign, but were deleted in a 2026-08-15 "dead code cleanup" pass that documented them as "3 unused --currency-* tokens" — a real miss, since the Inventory tab (built 2026-08-09, between those two dates) had been referencing them the whole time; the cleanup session's own dead-code check apparently never grepped `sheet/index.html` for these specific names, or ran before fully accounting for the Inventory build. Restored all 3 with their original values rather than inventing new ones — once restored, the pre-existing (and, it turns out, already-correct) label-color logic just works: Gold/Silver's default dark label color is legible against their now-real light/medium backgrounds, and Copper's pre-existing `.cs-currency-pill--copper .cs-currency-pill-label` override to light text was already correctly anticipating its own darker background — nothing else needed changing there. Separately, `.cs-currency-input` was styled `background:white; border:1px solid black` — literal unstyled-browser-default look, confirmed by reading the file rather than assumed, per instruction. Checked how other direct-edit number fields on the sheet are styled (per the explicit instruction) and found `.cs-mi-hp-num-input` (HP current/max in the header): `background: var(--blue-50); border:none; color: var(--blue-800)`. Reused that exact pattern for `.cs-currency-input` rather than inventing a new one, plus its `::-webkit-*-spin-button` removal for the same clean look.
**3 — Carrying bar icon.** Checked the existing icon system(s) before picking one, per instruction: neither sibling section title on this tab (Currency, Equipped Gear) carries an icon, so there's no established "every section title gets one" rule specific to this tab to match — the DM's framing ("every other similarly-styled header... uses an icon") holds at the whole-app level (Key Features rows, feat chips) but not for this tab's own immediate siblings. Chose the sheet-wide `FEAT_ICONS` emoji-HTML-entity convention (general-purpose, used for "this concept gets a small symbolic icon" broadly across the file) over `SPELL_TYPE_ICON_SRC`'s custom-SVG system (narrowly scoped to the 4 Gigglegloom spell types specifically, not a general icon system) — no new image asset needed. Added `&#9878;` (⚖, scales/balance — U+2696, verified via `ord()`) before "Carrying" in the `<h3>` text.
**4 — Redundant Search button, investigated per instruction before touching it.** The task's own framing named "the add-item modal" specifically — checked that exact surface (`cs-additem-modal-overlay`) and confirmed it has no Category dropdown and no Search button at all, just a live-as-you-type input (`onInventorySearchInput()`). The described symptom (Search input + Category dropdown + a Search button, given everything already live-filters) matches a different, nearby control instead — the own-inventory filter row (`applyInventoryFilter()`) sitting just above the item table. Reported this mismatch rather than silently guessing which one was meant, then investigated that one: confirmed `applyInventoryFilter()` is a single self-contained function reading both the search input's and category select's `.value` and re-rendering, called identically by the input's `oninput`, the select's `onchange`, and the button's `onclick` — no branch, no extra parameter, no hidden behavior gated on the button specifically. Since the input/select already invoke it live on every keystroke/selection, the button is provably 100% redundant, not just probably. A prior session (found via the code's own comment) had already reached this exact same technical conclusion and chose to keep the button anyway as a "harmless deliberate re-apply affordance" — this round's instruction was explicit ("if fully redundant, remove it"), so removed it, noting the reversal of the earlier call rather than silently overwriting it. `.cs-inv-filter-btn`'s CSS (now with zero remaining usages anywhere, confirmed via grep) removed alongside it as fully dead code.
**Verification, all live.** Ability Scores: screenshotted at desktop (6-per-row) and the 610px wrap width (2-per-row) — `getBoundingClientRect()` confirmed every card's top/bottom content gap is exactly equal at both widths, and confirmed all cards in a shared row have identical height via Grid's default stretch. Currency: `getComputedStyle()` confirmed all 3 pill backgrounds now resolve to their real hex values and all 3 labels are legible against them; tested the actual input via a real click-and-type (not a direct function call — a first attempt using scaled screenshot coordinates failed to focus the field, root-caused to a coordinate-mapping mismatch between the automation tool's ref-based and raw-coordinate click paths, not an app bug; switched to `read_page`'s element refs, which focused correctly) — typed 17 into Gold, confirmed `gold_cp` became 1700 in Supabase, reverted to 900. Carrying: `⚖ Carrying` confirmed via `textContent` read, not just a visual glance. Search button: confirmed `document.querySelectorAll('.cs-inv-filter-btn').length === 0`; typed "quarter" into the remaining search input and confirmed the item table live-narrowed to just the Quarterstaff row with no button involved, then cleared it. Mobile-checked all 4 items at 375px — Currency pills stack in a row and stay legible, "⚖ Carrying" doesn't crowd the "20 / 210 LBS" text (which renders inside the bar, not beside the label), Search/Category fields stack cleanly with no orphaned button-shaped gap, ability cards wrap 1-per-row per the existing 200px floor — zero horizontal overflow throughout. Zero console errors across every check, on both the Ability Scores and Inventory tabs. `new Function()` syntax check clean, raw parse clean on both files, diff-scoped literal-emoji grep empty (⚖ was written as the `&#9878;` entity, not a literal character, so C2 doesn't even apply, done anyway as the established safe form).


---

**2026-08-17 (new session, Character Sheet Round 2 — visual polish, responsive system, Hit Die UX; the 8 QA items Round 1 explicitly left uncovered) — `sheet/index.html`, `css/tokens.css`.**
Habit checks per §1: checked §3 Pending/Blocked (only item is an unrelated DM Tools admin form for `extra_learnable_spells`, no overlap) and `git diff --stat main...anavale-wiki-scc-ux -- sheet/index.html css/tokens.css` (zero diff — that branch never touched either file) before starting.
**Part 1 — Class tab polish, 5 items, done as one pass since they share overlapping CSS.**
1. **Accordion differentiation.** DM: "confusing as to what is expanded and what isn't... we should have the dark blue card style for the entire background of all accordions." Confirmed via `getComputedStyle()` before touching anything that `.cs-ov-acc` had genuinely zero background of its own across all 5 real accordion instances (Who You Are/Your Background/Your Past/Class Overview/Features) — only nested content (`.cs-ov-card`/`.cs-story-card`/`.cs-limuse-group`) carried the `--blue-700` "card" look, the accordion shell itself (header+body) was always transparent. Gave `.cs-ov-acc` the same `--blue-700`/`--radius-lg`/padding every other card already uses — reused, not invented, per instruction. One scoped exception: the Class tab's Overview accordion is nested *inside* `.cs-ov-class-card` (already blue-700, wrapping Basic info + the accordion as one unit) — a second identical background there would be pure invisible padding, so `.cs-ov-class-card .cs-ov-acc { background:none; padding:0; }` resets it; that whole card is already one distinct block against the page on its own.
2. **Spacing.** "Not enough space between sections." `.cs-ov-acc-body`'s internal gap bumped `--char-ov-gap-m` → `--char-ov-gap-l` (the largest existing gap token, no new value introduced). Bigger finding: the Class tab's top-level cards (Class/Overview, Features, Spellcasting, Spells Learned) had *zero* gap between them at all — they're plain sibling `<div>`s inside one shared `.cs-ov-inner`, unlike Your Story's accordions, which each get their own `.cs-ov-breakout` wrapper and inherit *that* wrapper's `margin-bottom:2rem`. New `.cs-class-tab-stack` (flex column, `gap: var(--char-ov-gap-l)`) wraps the Class tab's top-level children — confirmed hidden cards (`style="display:none"` until their data loads) correctly contribute no gap under `display:none`, so this doesn't create a phantom gap before Spellcasting/Spells Learned appear.
3. **"Overview" written twice.** Accordion header title and the inner-left column heading were both literally "Overview." Renamed the column heading to "Details" (the doc's own suggested option) — the accordion header already establishes context, the column heading now says what it actually holds.
4. **0/0 unlock message.** Wild Shape (and any limited-use feature) below its first level threshold rendered a bare "Used: 0/0" with an invisible zero-segment bar — "looks broken without" an explanation. New `firstUnlockLevel(feature)` walks `max_at_level`'s real thresholds (skipping any explicit 0-value entries) for the lowest one with real uses; `limitedUseCardHtml()` now shows "Unlocks at level N." in place of the bar whenever `computeLimitedUseMax()` is genuinely 0 for that reason. Generalizes to any feature/level combination via the same lookup already used for the real max, not hardcoded to Wild Shape.
5. **Spellcasting card too sparse.** Was 3 stacked plain `.cs-ab-skill` rows (the generic skill-row component, reused for lack of anything better at the time) — "too sparse... text too small... too far apart... needs to be more visually intriguing." Rebuilt as a 3-column row of badges (new `.cs-spellcast-stat*`), deliberately matching the header's own AC/Init badge visual language (label-above-value, `--blue-600` fill against the card's `--blue-700`, big Cinzel Decorative number) per the task's own guidance to follow an existing multi-stat pattern rather than invent a new one — `renderSpellcastingStats()`'s data logic is completely unchanged, only the render function (renamed `spellStatRowHtml`→`spellStatCardHtml`) and its markup changed.
**Part 2 — responsive system, 2 items.**
6. **Ability score card wrapping.** `.cs-ab-cards` already had `flex-wrap:wrap` — the real bug wasn't a missing property. With `flex:1 0 0` on each card, a card left alone on a wrapped row has no sibling on its own line to share flex-grow with, so it stretches to fill that entire row's width while the cards above it sit cramped at their 100px floor. Confirmed live via `getBoundingClientRect()` before assuming a fix, not just visually: at 610px viewport, 5 cards rendered at 102px each and a lone 6th stretched to 560px alone on row 2 — the exact "squishes illegibly" complaint, with an extra "then one card blows up huge" twist that's specific to flexbox's independent-per-line growth. Root-caused, not patched with a bigger min-width: switched `.cs-ab-cards` from `display:flex` to `display:grid; grid-template-columns: repeat(auto-fit, minmax(100px, 1fr))` — grid tracks are shared down every row, so a leftover card on the last row now keeps the identical column width as every card above it (empty space next to it, not a stretched card). Verified via the same `getBoundingClientRect()` check post-fix: 610px viewport now renders 125px×4 on row 1, 125px×2 on row 2 — consistent sizing across both rows. Re-verified at 375px (clean 3+2+... grid, no orphan-stretch), 800px (single row, 6 cards), and 1400px (single row) — zero horizontal overflow at any of them.
7. **Global font scaling.** DM, generalized beyond the original report: "There should be a global CSS font scaling from desktop > tablet > mobile, not just for this instance" (originated on the Your Story tab's "Your Background" accordion title). Per the task's own scoped recommendation (headers first via `clamp()`, confirm before widening to body text), converted `--char-ov-h3`/`--char-ov-h4`/`--char-ov-h5` in `tokens.css` — these ARE the site's real header/heading type-scale tokens (feed `.cs-ov-acc-title` plus 9 other selectors: card titles, class name, ability value, etc.) — to `clamp(min, preferred, max)`, max = the original fixed px value, min ≈75% of it, linear scaling by viewport width between. `--char-ov-pl` and body-text tokens deliberately untouched, matching the task's own explicit scope boundary. `.cs-page-title`/`.cs-ov-name` already had their own ad hoc vw-based clamps predating this token system — left as-is, unaffected, not migrated onto the new tokens (out of scope, already fluid). Verified the actual curve via computed style at 3 widths, not just eyeballed: `.cs-ov-acc-title` resolves 24px at 375px, 28px at 800px, 32px at ≥1200px — confirms smooth interpolation, not a broken clamp() falling back to one bound. Caught and worked around the project's own on-record browser-tokens.css-caching issue mid-verification (first read showed the stale fixed `2rem` value despite the file being correctly updated on disk, confirmed via `curl`) via the established cache-busting `<link href>` query-string swap.
**Part 3 — Hit Die redesign.** DM feedback was only "not intuitive, determine better UX/UI" — no specific direction. Took the task doc's own suggested direction, reasoned as the right call rather than blindly followed: brought Hit Dice's visual language in line with the label/"Used: X/Y"/segmented-bar pattern already established by Spell Slots and the Class-Specific tracker, replacing its original plain round pips (which predate that pattern entirely) — a player who's already learned to read Spell Slots shouldn't need a second visual language for Hit Dice. Segment fill/click convention switched to match those exactly too (available fills left, spent fills right; clicking a segment spends up through it, clicking the current boundary un-spends it) — Hit Dice previously used the opposite spent-left/available-right convention, so this also resolves a real inconsistency between the two "spend a segment" widgets on the page, not just a visual mismatch. Death Saves' pips deliberately left untouched and not converted — confirmed via re-reading their own logic that they're a genuinely different concept (marking a fixed 3-of-3 toward death, not spending/restoring a renewable pool), so forcing them into the same pattern would misrepresent what they track. Compact sizing (not `.cs-limuse-*`'s full-card treatment) since this widget lives inline in the header's vitals row alongside AC/Init/HP/Temp HP, not a full-width Features accordion card — segments wrap onto additional rows for high-level characters rather than shrinking illegibly. Simulated a level-12 character entirely client-side (no real data touched, `CH.level` reassigned in-browser only) to verify 12 segments render and wrap cleanly, since all 3 real characters are still level 1 and can't exercise this path — confirmed zero horizontal overflow at both 1400px and 375px with 12 segments present.
**Verification, all live.** Hana (Druid, level 1 — the real Wild Shape 0/0 case) and Kael (Ranger — zero limited-use features at all, since 2024 rules dropped Favored Enemy's uses) both checked on the Class tab: confirms the pre-existing "skip emitting the Class-Specific group entirely when empty" logic (no features at all) still coexists correctly with the new "show an unlock message" logic (a real feature, just not unlocked yet) — two different empty-states, both handled correctly, neither regressed by the other. Hit Die's actual spend/un-spend interaction tested through the real rendered button (not a direct function call) on Hana: clicked to spend (confirmed `Used: 1/1` in the DOM, confirmed `hit_dice_used=1` landed in Supabase via SQL), clicked again to un-spend (confirmed `Used: 0/1`, confirmed via a second SQL read it reverted to `hit_dice_used=0` — no residual test data left behind). Ability score grid re-verified at 375px/610px/800px/1400px, zero overflow at every width. Font-scaling curve verified via computed style at 3 widths (375/800/1200+). Zero console errors throughout, on both characters, at every width checked. `new Function()` syntax check clean on the one extracted inline script block, raw parse clean on both files, diff-scoped literal-emoji grep empty.


---

**2026-08-13 (continuing the same session — Handoff A of two, Class tab) — Restructured the Overview card into a two-column accordion + added a new Features accordion with a Class-Specific limited-use tracker (`sheet/index.html`, `classes`/`character_sheet` schema). Pulled the real Figma frame directly rather than trusting the handoff doc's paraphrase — found several real discrepancies, all resolved by trusting the pulled frame over the doc.**
**Verified against `get_design_context`/`get_screenshot` on node 52:2705 (file `P0tcVgf02XeI13Tz0TtRaX`), not the doc's description, per this project's standing rule.** Three real conflicts found:
1. **Accordion scope.** The doc said "Basic row stays where it is (top of card)... wrap Class details + Key Features into an Overview accordion" — implying Basic sits outside the collapsible region. The real Figma's `Accordion` component wraps the ENTIRE "info" box, Basic row included — collapsing "Overview" would hide the class name/image too. Kept Basic outside per the doc's own explicit, deliberate instruction anyway (a real UX call, not an oversight — hiding character identity on collapse is worse UX than the literal mockup), but flagged the conflict rather than silently picking one. Built by hand (not calling `accordionShell()` verbatim) since that helper's own `.cs-ov-breakout` wrapper (`width:100vw; left:50%; margin-left:-50vw`) would double-offset nested one level inside the Class tab's own already-existing outer breakout — same `.cs-ov-acc*` classes and `toggleSheetAccordion()` reused directly, just without the outer wrapper.
2. **Hit Die row.** `renderClassDetailsHtml()` has rendered a "Hit die:" line since the Class tab's original build. The real Figma frame has no such row — and the doc's own restated field list ("Primary ability / Saving throws / Casting Spells / Armor / Tools / Weapons") silently agreed, without the doc author flagging the removal as a change. Removed it to match the real frame — not a data loss, the header's Hit Dice tracker (built in an earlier session) already shows `hit_die` directly. `classes.hit_die_tip` is now unused by this function but left in the schema, same precedent as `hit_dice_total`.
3. **Feature name font.** The doc's own code comment claimed the limited-use card's feature name renders in Cinzel Decorative Bold. Direct pixel comparison against "CLASS-SPECIFIC" (genuinely Cinzel Decorative) confirmed "Favored Enemy" is plain Roboto Bold. Used the real value (`--char-ov-pl`, 24px, matching `page/fonts/pl` from the node's own variable dump) — CSS comments correct this explicitly rather than silently matching the doc.
**Other exact values confirmed via `get_variable_defs`/generated code rather than eyeballed**: two-column gap 42px (new `--char-ov-gap-l` token — no existing token matched), column `min-width:171px` (Figma-exact, one-off, no token), segment bar 3px gap + 1px `--blue-200` border + `border-radius:999px` + `overflow:hidden` (so individual segments need no corner-rounding of their own), active segment `--green` bg + new `--blue-900` text color (`#06070b` — genuinely darker than the existing `--blue-800`, not a rounding of it), spent segment `rgba(34,197,94,0.25)` bg + `--text` at 50% opacity. Segment fill direction (available left, spent right) confirmed against the real node — opposite of the Hit Dice pip convention (spent left, available right) built in an earlier session; left inconsistent with Hit Dice on purpose, since this matches what the actual design shows for this specific component, not a generic pip pattern to standardize.
**Data model**: new `classes.limited_use_features` (jsonb array of `{name, max_at_level, recharge}`) and `character_sheet.limited_use_spent` (jsonb, keyed by feature name). Max is derived live from `ch.level` at render time — same "derive, don't cache" principle `hp-calc.js` already uses for HP — so leveling up just reveals more segments with the spent count preserved, no migration needed later.
**Seed data verified against live web sources, not memory** — same discipline as the Speed/Size build. Real findings, not assumed:
- **Rage's own doc-provided example table was wrong**: claimed `{1:2, 3:3, 6:4, 12:5, 17:6, 20:999}` (unlimited at 20). Cross-checked against 3 independent sources: real progression is `{1:2, 3:3, 6:4, 12:5, 18:6}` — capped at 6, reached at level 18 (not 17), never unlimited.
- **Bardic Inspiration doesn't fit this schema at all** — its max uses is Charisma-modifier-derived (`max(1, CHA mod)`), not a level lookup. Skipped seeding it (Bard stays `[]`) and flagged in Next Priorities as a real schema gap (would need an optional `max_formula` field), rather than force an incorrect level-keyed approximation.
- **Paladin has two separate limited-use mechanics** the doc only partially separated: Channel Divinity (discrete X-of-Y, fits this schema — seeded `{3:2, 11:3}`, verified) and Lay on Hands (an HP pool, correctly identified by the doc as not fitting the segmented-bar UI). Seeded Channel Divinity; Lay on Hands deferred with the other pools per the DM's decision.
- Final verified seed: Barbarian Rage `{1:2,3:3,6:4,12:5,18:6}`, Cleric Channel Divinity `{2:2,6:3,18:4}`, Druid Wild Shape `{2:2,6:3}`, Fighter Second Wind `{1:2,9:3,17:4}` + Action Surge `{2:1,17:2}`, Paladin Channel Divinity `{3:2,11:3}`, Wizard Arcane Recovery `{1:1}`. Ranger (2024 rules dropped Favored Enemy's uses — DM confirmed skip), Rogue (nothing use-limited by default), Warlock (Pact Magic is spell slots, not tracked here), and the three deferred pool classes (Monk, Paladin's Lay on Hands specifically, Sorcerer) all correctly stay `[]`.
- All 6 features' real recharge is a hybrid (1 use back per short rest, full recovery on long rest) that the doc's binary `short_rest`/`long_rest` enum doesn't capture precisely (Action Surge is the one clean exception — genuinely full recharge on either rest type). Used `"short_rest"` for the hybrid ones since that's the more frequent/functional trigger point, noted the imprecision in the column comment for whoever builds the actual rest-flow later — the field is explicitly descriptive-only until then, per the handoff's own scope note.
**Three DM decisions resolved before writing any code**, per the handoff's own instruction not to guess: (1) skip seeding Ranger's Favored Enemy (2024 rules dropped its uses) — DM confirmed. (2) Defer all point-pool features (Monk Focus/Ki Points, Paladin Lay on Hands, Sorcerer Sorcery Points) to a future handoff rather than build a second card variant now — DM confirmed. (3) Skip emitting the Features accordion entirely when a class has no limited-use features, rather than add a `hideIfEmpty` flag to the shared `accordionShell()` (which every other accordion on the site also calls) — DM confirmed. Handled the resulting async-timing wrinkle correctly: `renderClassTabDynamic()` (which has `CLASS_ROW` but not yet `SHEET`, confirmed via the real fetch chain) builds the accordion shell into an always-present empty anchor div and does an initial safe-default paint; `renderLivePlay()` (guaranteed `SHEET` loaded) re-renders the real spent counts into the now-existing shell — same two-phase pattern every SHEET-dependent Class tab card in this series has used.
Verified live: Kael (Ranger) correctly shows no Features accordion and no Hit Die row. Hana (real level-1 Druid) shows Wild Shape at a genuine `0/0` — the feature isn't unlocked until level 2, confirmed as correct behavior not a bug, renders as an empty bar rather than something broken-looking. Simulated a level-9→17 Fighter entirely in-browser (`CLASS_ROW`/`CH.level` reassigned client-side, never persisted) to test the parts no real character could currently exercise: two simultaneous limited-use cards render correctly, clicking a segment spends it and everything to its right, clicking the boundary segment again un-spends one, leveling up grows the segment count while preserving the spent count exactly. The one real DB write from this test (`character_sheet.limited_use_spent` on Hana, from testing the actual click-through-to-Supabase path) confirmed via SQL then reverted to `{}`. Both new accordions' collapse/expand toggle confirmed working via direct `toggleSheetAccordion()` calls, despite being hand-built rather than routed through `accordionShell()`. Zero console errors, zero horizontal overflow at 375px mobile, two-column layout confirmed actually stacking (not just fitting) via `getBoundingClientRect()` at mobile width. `new Function()` syntax check clean. Pushed live.
Handoff B (Spell Slots redesign folded into the same "Features" accordion as a sibling group, Spells Prepared, Spells Learned) is intentionally not started — logged as the next Class tab priority.


---

**2026-08-13 (continuing the same session — DM-reported bug fix) — Fixed Story-tab card title clipping (`sheet/index.html`, CSS-only).**
DM screenshotted Visenya's Your Background accordion: the Background card's title "GREYWITNESSED" was visibly cut off mid-word at the card edge, no ellipsis, just a hard clip.
**Root cause, confirmed by reading the real CSS before touching anything (not assumed from the bug report's own diagnosis, though it turned out to be exactly right)**: `.cs-story-card-head` is a flex row (image + `.cs-story-card-headtext`); `.cs-story-card-headtext` had no `min-width:0`, so its default `min-width:auto` resolved to its content's min-content size — for an unbreakable single-word title at the card's 26px display font, that min-content width can exceed the available row space, pushing the whole row wider than the card. Separately, `.cs-story-card-title` itself had zero overflow/wrap handling at all — no `overflow-wrap`, `white-space`, or `text-overflow` of any kind, unlike `.cs-equip-slot-name` elsewhere in the file which already has the equivalent ellipsis pattern for this exact class of problem.
**Fix**: `min-width: 0` added to `.cs-story-card-headtext`. `overflow-wrap: break-word` added to both `.cs-story-card-title` and `.cs-past-card-title` — wrap, not the ellipsis pattern `.cs-equip-slot-name` uses, per the bug report's own reasoning: these are lore/proper-noun names (background names, etc.) a player should be able to read in full, not a secondary label where truncation is an acceptable tradeoff. `overflow-wrap` only engages when a word would otherwise overflow its line, so normal multi-word titles keep wrapping at spaces exactly as before — confirmed this isn't a behavior change for the common case, not just assumed.
**Verified `.cs-past-card-title` needed the same fix for a genuinely different reason, not just for consistency** — traced `pastCard()`'s two layouts: the default (header) layout reuses `.cs-story-card-head`/`.cs-story-card-headtext` verbatim, so it shares the exact same bug. The `--full` layout (Left Behind, Why You Left) doesn't use `.cs-story-card-headtext` at all — its title sits directly in `.cs-past-card-body`, which already had `min-width:0` — but that alone doesn't prevent unbreakable text from visually overflowing its own rendered box; `min-width:0` controls how much space a flex item is allocated, not whether its own content wraps within that space. So `.cs-past-card-title` needed the `overflow-wrap` fix regardless of which layout it's used in.
**Checked all real title-bearing data, not just the one reported name**: pulled all 16 `backgrounds.name` values — "Greywitnessed" (13 chars) is the longest, several others (Threadpuller, Masquerader, Ringscarred, Stillsought) are close behind and would have hit the same bug once a character with one of those backgrounds loaded this accordion. Also checked `regions`/`organizations` names (other data sources feeding these same title classes) — confirmed those are naturally safe without any fix, since they're multi-word ("The Gigglegloom Conclave," "The Prism Exchange") and already wrap correctly at spaces; only single unbreakable words were ever at risk, confirming the fix's scope matches the actual risk surface rather than being broader or narrower than needed.
Verified live on Visenya Oryn (real `background_id: greywitnessed`, confirmed via SQL before testing, matching the DM's screenshot exactly) at three widths: normal (1100px, title renders on one line, no clip), a stress width (650px), and a forced worst-case 240px (`.cs-story-card`'s own `min-width` floor, applied via a temporary inline style override, not real data) — at 240px the title correctly wraps to multiple lines and `cardEl.scrollWidth === cardEl.clientWidth` (240===240) confirmed zero horizontal overflow, the direct signature of the fix working under the true worst case, not just "happens to fit" at the tested viewport. Confirmed all 7 Your Past cards (both layouts) show zero overflow (`scrollWidth <= clientWidth` on each). Zero console errors, zero page-level overflow at 375px mobile. `new Function()` syntax check clean (no JS touched, CSS-only change, checked anyway per habit). Pushed live.


---

**2026-08-13 (continuing the same session — last of the 5-item placement roadmap, Inventory tab piece) — Added Magic Item Attunement (`sheet/index.html`, `items`/`character_sheet` schema). Real overlap-avoidance call, a real deviation from the handoff's suggested data source, and a real bug caught before shipping — not a routine build.**
**Parallel-branch check, per the standing workflow agreed earlier this session.** This handoff's own plan touches `dm.html` (a checkbox on the item form). `git diff --stat main...anavale-wiki-scc-ux` showed that branch had grown to 1423 changed lines in `dm.html`, with hunks covering exactly the item-form region (~2200–3700) I'd need to edit. Rather than add to an actively-churning region of someone else's in-flight work, deferred just the DM Tools checkbox — confirmed nothing is blocked by this, since the handoff's own "Why" section already states no item is flagged attunement-requiring yet. Everything else (schema, all rendering/interaction logic) lives entirely in `sheet/index.html`, confirmed untouched by that branch, so built normally on `main`.
**Schema**: confirmed via `information_schema` that neither `items.requires_attunement` nor `character_sheet.attuned_items` existed (this time the handoff's "no existing column" claim held up, unlike the Hit Dice build two entries ago) — added both via migration, matching the handoff's suggested shapes exactly (boolean default false; `text[]` default `'{}'`, same key format as `character_sheet.equipped` already uses).
**Data source — deviated from the handoff's suggested `CATALOG_CACHE`.** Traced how that cache actually gets populated before trusting it: `runInventorySearch()` (the +Add Item search modal) writes it with a *partial* select (`id,name,category,cost_gp,weight_lb,summary,base_ac,damage_dice,damage_type,image` — no `requires_attunement`), while `fetchInventory()` writes it with a full `select('*')` for owned items — meaning `CATALOG_CACHE` is genuinely mixed depending on which path touched an entry last. `EQUIPPED_ITEMS` (populated by `fetchEquipped()`/`selectEquipItem()`, both always `select('*')`, and scoped to exactly the items in `EQUIPPED`) is the correct, purpose-built source for "full data for a currently-equipped item" — already used this exact way elsewhere in the file (AC calc, Special Effects). Used that instead.
**Real bug found and fixed before shipping, not after.** The handoff said to hook "clear attunement on unequip" into "whichever function currently handles unequip," naming `unequipSlot()`/`clearInventoryEquippedSlot()` as candidates to verify. Traced the real code: `clearInventoryEquippedSlot()` only manages an inventory row's own column, not `EQUIPPED`/attunement state — the real hook points are the three (then found a fourth, `autoUnequipOffHandForTwoHanded()`, which the handoff never mentioned) places that already compute "is this item still equipped anywhere else" right before cleaning up `EQUIPPED_ITEMS`. Added the new `clearAttunementIfUnequipped()` call at all four candidate sites initially — then, re-reading `selectEquipItem()`'s own comment ("Same physical inventory row moving from a different slot entirely — not... this row moving from elsewhere") realized one of the four, the same-item slot-move path, is NOT a real unequip: the item gets re-equipped into `targetKey` a few lines later in the same function call. Calling the new clear-on-unequip function there would have wrongly dropped attunement every time a player moved an already-attuned item to a different slot (e.g. swapping a ring from right hand to left) — a real regression, not a hypothetical. Removed that one call before it shipped; kept the other three (the true sibling-clear, the two-handed off-hand auto-clear, and explicit `unequipSlot()`).
**Rendering**: `getAttunableEquippedItems()`/`renderAttunement()`/`toggleAttunement()` per the handoff's shape, wired into `renderEquipped()`'s own end (alongside `renderACBadge()`/`renderRingsRow()`/`renderSpecialEffects()`) rather than a separate hook — guarantees the Attunement list refreshes exactly when the doll does, on load and on every equip/unequip, with no risk of drifting out of sync. Placed under the doll (inside `.cs-inv-top-col--doll`, after the ring row) rather than as a 3rd top-row column, to avoid re-tuning the doll/Special-Effects 3:2 flex ratio a DM live-review already tuned once. New `.cs-attune-*` CSS reuses `.cs-effect-panel`'s color tokens (`--blue-700`/`--blue-300`) for the outer box, `--gold` for the Attune/Attuned button (matches the existing "active resource" gold-fill convention).
**Verification, with 3 of Kael's real equipped items temporarily flagged `requires_attunement` (reverted after, including his `equipped_slot` inventory columns and `character_sheet.equipped`/`attuned_items`, confirmed via a final SQL read matching his real pre-test state exactly)**: attuned to all 3 with realistic delays between actions, confirmed the DB write landed correctly (`["leather-armor","longbow","braided-cord-ring"]`); separately reproduced this codebase's already-documented zero-delay `saveSheet()` race by firing all 3 toggles with no delay, confirmed it's the same pre-existing risk (not a bug introduced here) by rerunning with realistic delays and getting a correct result. Confirmed the 3-item cap blocks a 4th attempt with zero DB write. Confirmed unequipping Longbow correctly dropped it from `attuned_items` (both locally and via SQL) while leaving the other two attuned items untouched. Confirmed moving the still-attuned Braided Cord Ring from `ring_right` to `ring_left` preserved its attunement — the exact scenario the bug fix above protects. Zero console errors throughout, zero horizontal overflow at 375px mobile, attunement box confirmed hidden (not an empty box) once all test flags were reverted. `new Function()` syntax check clean. Pushed live.
This closes the original 5-item placement roadmap. Cantrips & Prepared Spells remains, explicitly scoped to its own future session.


---

**2026-08-13 (continuing the same session — third of the 5-item placement roadmap, Story tab piece) — Added Speed + Size to the Your Background accordion (`sheet/index.html`, `species` schema). Two real DM decisions resolved before writing any code, per the handoff's own instruction not to guess either one.**
**Decision 1 — placement.** The handoff offered two options (header `.cs-mi-triple-row`, or the "Who You Are" accordion) but flagged its own uncertainty, since the DM's stated reasoning was "near Species/Region, not header." Checked both candidates against the real code before presenting choices: `renderSpeciesDesc()` confirmed the existing Species description genuinely lives in the header (contradicting option (a)'s "not header" framing), and `buildWhoYouAreHtml()` confirmed "Who You Are" holds only personality/narrative fields (First Impression, Fears, Quest) — nothing species- or region-adjacent, a thematic mismatch for option (b). Found a third, better-fitting option the handoff hadn't listed: "Your Background" (a different accordion), which already has a real "Home Region" card sitting in it — the literal match to "near Species/Region." Presented all three to the DM rather than force-fitting one of the handoff's two; DM picked the third.
**Decision 2 — rules edition.** The handoff's own example ("Dwarves are 25 ft/Medium") is a 2014 PHB value, but a comment already in `sheet/index.html` (from the earlier skill-proficiency-stacking build) cites "2024 PHB stacking rules" as this campaign's actual basis — and 2024 changed Dwarf/Gnome/Halfling speeds from 25 ft to 30 ft, a direct conflict for 3 of the 12 species. Flagged the discrepancy rather than silently following either source; DM confirmed 2024 PHB.
**Schema**: confirmed via `information_schema` that `species` had no `speed_ft`/`size` columns (or anything similarly named) before adding them — not assumed either way, per the handoff's explicit instruction (which also called out the prior two builds' real schema surprises as the reason to check first every time). Real side-finding while reading the schema: `species.phb_equivalent` already exists as a real column, populated for all 12 species with the exact same mapping (`veilstepped→Changeling`, etc.) as the hardcoded `SPECIES_PHB` JS mirror restored in an Aug 12 session — that mirror could be replaced by reading this column live instead. Not fixed here (out of scope on a Speed/Size handoff), flagged in Next Priorities.
**Seed data**: rather than trust memory for real gameplay-facing 5e facts, cross-checked the 2024 PHB speed/size table against several independent web sources before writing the migration. Confirmed: Dwarf/Gnome/Halfling all raised to 30 ft (from 2014's 25 ft); Goliath is the one standout at 35 ft (unchanged from raising in 2024, confirmed by two sources independently describing it as the "only species" at that speed); Gnome/Halfling remain the only two Small species; everything else (Human/Elf/Tiefling/Aasimar/Dragonborn/Orc, plus the two non-core species Changeling/Warforged, which were already stable 30 ft/Medium across every edition they've appeared in) is 30 ft/Medium. Seeded all 12 real species (keyed off the existing `phb_equivalent` column, not re-typed by hand per Anavale name) plus left the `other`/"Various" placeholder row null. Verified the full seeded table via a follow-up SQL read before writing any render code against it.
**Rendering**: new `speedSizeCard` inside `renderBackgroundCardsHtml()`, positioned right after the Home Region card (matching "near Region" literally) — reuses `storyCard()` verbatim with no image/benefits box, just a label + a single value line ("30 ft · Medium"), since the handoff explicitly said this needed no tooltip or explainer (self-evident once labeled, unlike Saving Throws/Passive Perception). No new CSS — `.cs-story-cards`' existing `align-items: flex-start` (fixed in an earlier session specifically so shorter cards don't get stretched to match taller siblings) already handles a minimal card cleanly.
**Refresh timing**: `SPECIES_ROW` resolves via `fetchSpeciesData()`, a fetch not covered by the two (`fetchRegionData`/`fetchBackgroundData`) that already gate the existing `renderBackgroundCardsDynamic()` refresh — added a call to that same function inside `fetchSpeciesData()`'s `.then()`/`.catch()` instead of building a new refresh path, per the handoff's explicit "piggyback on the same refresh call" instruction.
Verified live on Kael Evander (Veilstepped → Changeling → 30 ft/Medium under the new seed): card reads "SPEED & SIZE / 30 FT · MEDIUM", confirmed via a DOM read (not just the screenshot) that it's the 3rd of 5 cards, immediately after Home Region. Screenshotted the full Your Background row at 1100px (5 cards, correct order, no stretching) and confirmed zero horizontal overflow at 375px mobile. Zero console errors. `new Function()` syntax check clean. Pushed live.


---

**2026-08-12 (later same day, second of the 5-item placement roadmap — Header piece) — Added Initiative + Hit Dice to the MainInfo header (`sheet/index.html`, `character_sheet` schema). Real mid-build schema correction, not just a straightforward build.**
**Initiative**: `computeInitiative(ch)` — `DEX mod + 5 if 'Alert' in ch.feats, else +0` — verbatim from the handoff. Confirmed `Alert` is the only initiative-affecting feat in `FEAT_TIPS` and that `ch.feats` is the same field/pattern `computeHpBase()` (`js/hp-calc.js`) already reads for the Tough-feat HP check, per the handoff's own instruction to reuse that source rather than re-deriving it. Checked the real render order before assuming "no async refresh needed" was actually true (not just trusting the handoff's claim) — `CH = ch` and `ABILITY_SCORES = scores` are both set synchronously in `renderCharacter()`, well before `buildMainInfoHtml()` is called, so the claim held up: computed once, inline, directly into the initial HTML string (`<div class="cs-mi-init-badge">`, styled as a smaller/secondary variant of the AC badge — `--blue-600`/`--blue-100`, same token family as AC's `--blue-100`/`--blue-800` but a dimmer step so it doesn't compete for attention). No separate render/refresh function needed or built, unlike every other header stat so far.
**Hit Dice — the handoff's data-model premise was wrong, caught via the standing "verify the DB write" step, not before starting.** The handoff said "no existing column tracks this — needs a new one" and gave a migration for `hit_dice_spent`; that migration ran and the sheet was built against it before the routine post-write SQL check (done for every build this session) surfaced that `character_sheet` already had `hit_dice_total`/`hit_dice_used` columns — populated (`1`/`0` across all 3 characters) but referenced by zero code anywhere in the repo (confirmed via grep across `sheet/index.html`/`dm.html`/`js/*.js`/`character.html`). This should have been caught by pulling the full column list before writing the migration, not after — noting the process gap plainly rather than glossing over it. Fixed immediately, before shipping: dropped the just-added `hit_dice_spent` column, consolidated all code onto the pre-existing `hit_dice_used` (added the column comment it never had), rather than leave two live, functionally-identical "spent" trackers in the schema for a future session to be confused by. `hit_dice_total` (the sibling column) stays dead/unused on purpose — the handoff's "don't cache max, derive it live from `ch.level`" instruction is the better design (matches `js/hp-calc.js`'s own derive-don't-cache principle for HP), so max is computed live in `renderHitDiceHtml()`, not read from that column. Flagged `hit_dice_total` as a known-inert column in Next Priorities so a future session doesn't wonder whether it's the real source.
**Rendering**: `renderHitDiceHtml()`/`toggleHitDie()`/`renderHitDice()` per the handoff, pip count = `ch.level` (currently 1 for the whole party — confirmed the single-pip case renders cleanly, not just the eventual multi-pip case). Own CSS (`.cs-mi-hd-pip`/`.spent`) rather than reusing `.cs-mi-ds-pip` — deliberately inverted semantics from Death Saves (available = bright gold fill, spent = dim hollow) since an available hit die is the *good* state here, the opposite of a filled death-save pip. No rest-flow exists anywhere on the sheet to auto-restore spent dice (same gap Death Saves/Spell Slots already have) — left as manual pip-clicking, per the handoff's explicit instruction not to scope-creep into building one; flagged in Next Priorities as a real future gap.
**Refresh timing**: wired `renderHitDice()` into `renderLivePlay()` (needs both `SHEET.hit_dice_used` and `CLASS_ROW.hit_die`) — verified against the real fetch chain rather than assumed, per the handoff's explicit instruction not to assume this time (it didn't even guess a hook, having been corrected twice already this session on this exact point).
Verified live on Kael Evander (Ranger, level 1, `d10` hit die, real Alert feat): Initiative badge reads "+7" with tooltip "2 Dex + 5 (Alert) = 7", hand-checked against his real DEX 15 (mod +2). Hit Dice shows one available `d10` pip. Clicked it — pip turns spent, `SHEET.hit_dice_used` updates to 1 locally; confirmed via a follow-up SQL read that `hit_dice_used=1` landed in Supabase (`hit_dice_total` unchanged at 1, confirming it's genuinely inert). Clicked again — reverted to available, confirmed via a second SQL read that it's back to 0. Screenshotted both desktop (1100px — AC/Init/HP/Temp HP all fit one row, Hit Dice wraps to its own line) and mobile (375px — wraps to 3 rows, zero horizontal overflow). Zero console errors throughout. `new Function()` syntax check clean. Pushed live.


---

**2026-08-12 (later same day, addendum to the Saves/Passive Perception build below) — Added the saving-throw explainer tooltip that build left open (`sheet/index.html`).**
DM's addendum closed the one item the original handoff explicitly deferred: add a single top-level "what's a saving throw?" explainer, distinct from the six per-ability `SAVE_TIPS` (which explain what triggers *that* save, not the concept itself). DM specified the copy and the constraint (one explainer, not six) but explicitly left the anchor point to Claude Code's judgment, since the doc's own two suggested options (a shared "Saving Throws" section label, or the first save row with distinct styling) depend on the real DOM structure.
Checked the real structure before picking: saves render one-per-ability-card (confirmed via `buildAbilityScoresHtml()`), not grouped under any shared heading — so there's no natural single anchor for a section-level tooltip without adding new markup the addendum's "no other changes" scope note didn't ask for. Went with the doc's second option: attached to STR's row only.
New `SAVE_INTRO_TIP` constant (copy verbatim from the addendum). `abilitySaveRowHtml()` gained a `showIntroTip` parameter — when true, renders a second `.cs-ab-tip` icon *before* "Save" (new `.cs-ab-tip--intro` modifier: blue `--blue-400` background instead of the standard gold, glyph "i" instead of "?", `margin-right` instead of `margin-left` since it precedes rather than follows text) — deliberately distinct from the standard gold "?" tips so it reads as "explaining the concept of saving throws in general," not "a fact about Strength specifically." Both call sites (`buildAbilityScoresHtml()`'s initial render, `renderAbilitySkills()`'s refresh) pass `ab === 'str'` as the flag — same boolean, no separate "is this the first card" state to track.
Verified live on Kael: read `#cs-ab-save-str`'s rendered HTML directly (not just visual) to confirm the intro tip is present with the exact addendum copy and appears nowhere else — checked DEX's row has zero `.cs-ab-tip--intro` elements. Screenshot at a narrow viewport (to render the STR card large enough to inspect clearly) confirms the blue "i" pill sits visually distinct from the gold "?" pills, positioned before "PROF. Save". Zero console errors. `new Function()` syntax check clean. Pushed live.


---

**2026-08-12 (later same day, after the PHB-terms fix below) — Added Saving Throws + Passive Perception to the Ability Scores tab (`sheet/index.html`), per a DM-supplied handoff doc.**
Built per the handoff doc's spec, verified against real code/data before implementing rather than trusting the doc's snippets verbatim (read `abilitySkillsInnerHtml()`/`buildAbilityScoresHtml()`/`renderAbilitySkills()`/`renderClassTabDynamic()` and the real `classes.saving_throws` values first, per standing instruction to read files before editing).
**Saving Throws**: `classes.saving_throws` confirmed via SQL as a comma-separated full-ability-name string ("Strength, Constitution", etc.) across all 12 real classes — matches the doc's assumed format exactly, no surprises. New `AB_FULL` (short key → full name) and `parseSavingThrows(savesStr)` map it to the `str/dex/con/int/wis/cha` keys already used everywhere else on this tab. New `abilitySaveRowHtml(ab, modNum, profBonus, isProficient)` renders one row per ability card, reusing the existing `.cs-ab-skill*` classes verbatim (same "Prof." label, same positive/negative total coloring) — no new skill-row CSS, just a small `.cs-ab-save` wrapper for spacing. `SAVE_TIPS` (6 plain-language tooltips, one per ability) ported directly from the doc.
**Passive Perception**: formula is `10 + Perception's total` — rather than duplicating the skill list's proficiency/flat-bonus math a second time, extracted `computeSkillTotal(name, modNum, profBonus, ch)` out of `abilitySkillsInnerHtml()` (previously all inline) so both the skill row and the new `passivePerceptionBadgeHtml()` call the same function and can't drift on the "proficiency supersedes a flat +1" rule. Renders as a small gold-bordered pill (`.cs-ab-passive`, new but minimal CSS — no `tokens.css` changes, reused `--gold`/`--radius-sm`/etc. directly) on the WIS card only, above its Save row, per the doc's explicit placement ask.
**Refresh timing**: doc suggested a `CLASS_ROW`-triggered refresh via `renderClassTabDynamic()`, but tracing the real fetch chain (`fetchClassData(ch).then(fetchOrCreateSheet).then(fetchEquipped)`, all inside the `Promise.all` that gates `renderLivePlay()`) showed `CLASS_ROW` is always resolved by the time `renderAbilitySkills()` runs (it's only ever called from `renderLivePlay()`, after that `Promise.all` settles) — so extending the existing `renderAbilitySkills()` refresh to also update the 6 save rows and the passive badge is correct and sufficient; no second refresh hook needed. Deviated from the doc here deliberately, grounded in the actual code's timing rather than the doc's assumption — flagging the deviation rather than silently matching the doc's suggested wiring.
**Left undecided, per the doc's own instruction not to guess a DM's-call item**: no top-level "what's a saving throw?" explainer beyond the per-ability `SAVE_TIPS` — the doc explicitly named the tone/placement of that as the DM's call, not mine to pick.
**Not built, confirmed in scope note**: Passive Investigation / Passive Insight — doc says explicitly not this pass, same formula, add later only if useful at the table.
Verified live on Kael Evander (Ranger, real data) via the local static server: STR +1 mod → proficient save "+3", DEX +2 mod → proficient save "+4", CON/INT/CHA correctly show plain (non-"Prof.") saves matching their raw modifiers, WIS Passive Perception badge reads 15 (10 + WIS mod +3 + prof +2, since Perception is one of his real proficient skills) — all cross-checked by hand against his real ability scores and `classes.saving_throws` value ("Strength, Dexterity"), not just eyeballed. Confirmed the skill list itself (Athletics "+2 (+1)", Animal Handling/Perception/Survival "Prof." "+5") is unchanged after the `computeSkillTotal()` extraction — same output as before the refactor. Zero console errors, zero horizontal overflow at 375px mobile. `new Function()` syntax check clean. Pushed live.


---

**2026-08-12 (later same day, after the Skills-section review rounds below) — Added real PHB terms in parens next to the Anavale-flavor species and class-flavor names (`sheet/index.html`).**
DM asked directly, no investigation needed: add the real D&D 5e Player's Handbook term in parens next to the Anavale in-world flavor text for species and class names, "so we do not forget" the mechanical identity behind the flavor names. Checked where these two names actually render before touching anything (`grep` for `speciesDisplay`/`classDisplay`) — both appear in exactly one place each. `speciesDisplay` (the raw `ch.species` slug, e.g. "Veilstepped") is used only in the header blurb built by `buildMainInfoHtml()`. `classDisplay` (the real `ch.class_id`, e.g. "Ranger" — already a real PHB term, not a reskin) is used only on the Class tab's class card (`cs-ov-class-name`) — that one needed no change, since a class's real name and its PHB name are the same string; the Anavale-flavor stand-in for class is a *different* value, `CLASS_FLAVOR_NAME[classKey]` (e.g. "The Wanderer" for Ranger), also only used in that same header blurb.
Restored `SPECIES_PHB` (species-slug → PHB species name, e.g. `veilstepped:'Changeling'`) — this exact 12-entry table existed before, dropped as dead code in an earlier session's identity-pill removal (the pill row was found redundant with the blurb and deleted; the PHB lookup that had fed it was deleted too, since nothing else used it at the time). Re-added verbatim from git history rather than re-deriving it, since it was previously DM-approved data, not guessed.
`buildMainInfoHtml()`: added `speciesKey` (mirrors the existing `classKey` pattern) and, inside the blurb-building block, two new local strings — `speciesWithPhb` (species display + ` (SPECIES_PHB[speciesKey])` when a mapping exists) and `flavorWithPhb` (Gigglegloom class-flavor name + ` (real class name)`, derived inline via `cap(classKey.replace(/-/g,' '))` rather than threading `classDisplay` through as a new parameter, since class IDs are always single words and the existing `cap()` helper already produces the identical string). Both substituted into the existing blurb sentence in place of the bare names — no other blurb wording changed.
Verified live on Kael Evander (local static server on :8791, restarted fresh — nothing had been listening on the port from a prior session): header blurb now reads "A Veilstepped (Changeling) The Wanderer (Ranger) who carries the Featherflow — from Nombi." Confirmed via `getComputedStyle`-equivalent DOM read that the Class tab's `cs-ov-class-name` still shows plain "Ranger", unaffected. Zero console errors (network log confirmed every resource 200 OK; console `ERR_CONNECTION_REFUSED` entries were leftover from navigation attempts before the local server was up, not from the loaded page). `new Function()` syntax check clean. Pushed live.


---

**2026-08-12 (later same day, second live-review round on the Skills section below) — Dropped the ring dot on flat-bonus skills (`sheet/index.html`).**
DM flagged the outline dot next to a flat-bonus skill (e.g. History showing a gold ring plus "+2 (+1)") as confusing — a second, unexplained marker once the parenthetical amount already said the same thing on its own. Removed the dot entirely for flat-bonus and neither states; only proficient rows render a marker now (the "Prof." label from the prior fix). `abilitySkillsInnerHtml()`'s `markerHtml` simplified to a plain `isProficient ? <label> : <empty>` branch — the `.cs-ab-skill-dot` CSS rule and its two per-state overrides (`--flat`'s gold ring, `--none`'s `visibility:hidden`) are now dead and removed rather than left orphaned. The fixed-width `.cs-ab-skill-marker` wrapper is untouched, so the name column still lines up across all three states even with an empty marker.
Verified on Kael: Athletics (his real flat-bonus skill) now reads "+2 (+1)" with no dot at all; Animal Handling/Perception/Survival (proficient) unaffected, still showing the "Prof." label. Zero console errors. `new Function()` syntax check clean. Pushed live.


---

**2026-08-12 (later same day, live DM review of the Skills section below) — Replaced the proficient-skill filled dot with a "Prof." text label (`sheet/index.html`).**
DM reviewed the new Skills section live and flagged the filled-vs-outlined dot distinction (proficient vs. flat-bonus) as not intuitive, and asked for two changes: replace the filled dot with a "Prof." text label, and show the flat-bonus amount as a number in parens next to the total for flat-bonus skills.
`abilitySkillsInnerHtml()`: proficient rows now render `<span class="cs-ab-skill-prof-label">Prof.</span>` in place of the marker dot (small caps, gold, bold) — the ring dot for flat-bonus rows and the hidden dot for neither are untouched, only the proficient state's marker changed. Wrapped both marker types in a fixed-width `.cs-ab-skill-marker` (26px) so the name column still lines up whether a row shows "Prof." or a dot. For flat-bonus rows, the total string now appends the real stored `skill_bonuses[].amount` in parens (`bonusesArr.find()` replacing the earlier `.some()` existence check, since the amount is needed for display) rather than hardcoding "+1" — the mechanic is currently always a flat +1 per `PAST_EFFECTS`, but this reads the real value instead of assuming it always will be.
Verified on Kael (Athletics: `+2 (+1)`, flat state; Animal Handling/Perception/Survival: `Prof.` label, `+5`, no parenthetical) and Hana (her real overlap case, Insight: `Prof.` label, `+5`, correctly no parenthetical — confirms the flat bonus stays fully superseded and invisible on proficient rows, not just hidden visually). Zero console errors. `new Function()` syntax check clean. Pushed live.


---

**2026-08-12 (later same day, after the Speaks-color fix below) — Added a Skills section to the Ability Scores tab (`sheet/index.html`). Investigated first per explicit DM instruction, reported findings, built after DM decisions on 2 open questions.**
**Investigation (Step 1)**: (a) Checked Figma for a Skills frame under Ability Scores — pulled `get_metadata` on the file's page tree, found the desktop (`51:1663`) and mobile (`51:1668`) Ability Scores frames, then `get_screenshot`'d both directly. Confirmed neither contains anything beyond the header + the 6 ability-score cards already built — no Skills section exists anywhere in the file, on any tab. Same situation as Special Effects: flagged clearly rather than guessing at a design that isn't there. (b) Confirmed `character_sheet.proficiency_bonus` is a real stored integer column (via `list_tables`) — populated `2` on all 3 characters (level 1, matches standard scaling), seeded once at first sheet creation (`sheet/index.html`, hardcoded `2` on insert) and never read anywhere in the codebase since. Recommended reading `SHEET.proficiency_bonus` directly rather than deriving it from `ch.level` via a hardcoded scaling table, since it's already the DM's own editable value (same pattern as `hp_max_modifier`) — confirmed correct by DM. (c) Checked real data for overlap between `ch.skills` (full proficiency) and `PAST_EFFECTS`' flat +1 `skill_bonuses`, rather than reasoning it out abstractly: Kael Evander is proficient in *and* has a flat +1 on both Animal Handling and Survival; Hana Sable is proficient in *and* has a flat +1 on Insight; Visenya Oryn has no overlap. No existing code anywhere combines the two. Recommended proficiency supersede the flat +1 (based on the existing "not full proficiency, **but still useful**" tooltip copy implying the flat bonus's purpose is specifically to help skills you *lack* proficiency in) — DM confirmed with a stronger, decisive rationale: the 2024 PHB's stacking rule (same-target bonuses don't add; the higher one applies), which lands on the identical outcome since proficiency bonus (≥+2) always beats the flat +1.
**Build (Step 2)**: new `ABILITY_SKILLS` constant — all 18 skills mapped to their parent ability (fixed D&D rules, not derived from data; CON gets `[]`). New `abilitySkillsInnerHtml(ab, modNum, profBonus, ch)` renders one ability's skill list (or a "No skills" state for CON) — each skill shows name + `SKILL_TIPS` tooltip (verbatim, no new copy) + total bonus (`modNum + profBonus` if in `ch.skills`, `modNum + 1` if not proficient but present in `ch.skill_bonuses`, else `modNum` alone) + one of three visually distinct states (proficient: filled gold dot, bold name; flat: gold-ring dot, normal weight; neither: no dot, muted name — new `.cs-ab-skill--proficient/--flat/--none` CSS, no existing classes reused since nothing like this existed). Rendered inside each `.cs-ab-card`, below the existing modifier line, with `id="cs-ab-skills-{ab}"` per card. Because `buildAbilityScoresHtml()` runs before `SHEET` (holding the real `proficiency_bonus`) finishes loading — same ordering gap `renderHeaderHP()` already works around — added `renderAbilitySkills()`, called from `renderLivePlay()`, which re-renders all 6 skill blocks in place once `SHEET` is available; initial render falls back to `2` (matches the real seed default). The old flat "Proficient skills: X, Y, Z" comma-list (`cs-story-block`/`cs-story-row`) is fully removed — the grouped list covers the same information plus bonus math and flat-bonus skills it never showed, so nothing was left rendering redundantly.
**Verification**: live on all 3 real characters via the local dev server, both breakpoints. Kael (1600px): Animal Handling/Perception/Survival correctly render `cs-ab-skill--proficient` at `+5` (WIS mod +3, prof +2) — confirmed via DOM read, not just visual, that the two real overlap skills (Animal Handling, Survival) render as proficient-only, not stacked (`+5`, not `+6`); Athletics correctly renders `cs-ab-skill--flat` at `+2` (STR mod +1, flat +1); everything else renders `cs-ab-skill--none` at ability-mod-only. To confirm the async refresh path (not just the initial fallback), temporarily set Kael's real `proficiency_bonus` to `3` via SQL, reloaded, confirmed the 3 proficient skills recomputed live to `+6` via `renderAbilitySkills()`, then reverted to `2` via a follow-up SQL read. Hana (her real overlap case, Insight): confirmed `+5` (WIS mod +3, prof +2), not stacked. Visenya (no overlap): confirmed all 3 states render correctly with no false overlaps. Mobile (375px): 2-per-row wrap, "No skills" state renders cleanly on CON, zero console errors, `document.body.scrollWidth - window.innerWidth` reads 1px (sub-pixel rounding, not a real overflow). `new Function()` syntax check clean on the extracted inline script. Committed (`ba13af1`), pushed after DM review; see the "Prof." label follow-up above for the one legibility fix that came out of that review.


---

**2026-08-12 (later same day, live DM review continued) — Fixed the Speaks block's language name text color (`sheet/index.html`).**
DM flagged live on Hana Sable's sheet (screenshot from `carlosknightiii.github.io`): "Jugabi Canopy" read as blue text next to its own green rune glyph, and asked for the text to match "the color of the region it is from." Checked the data model before assuming a fix: `regions` has no hex color column at all (only `color_health`, a status word — Dire/Excellent/Faded/Good/Moderate, confirmed via `information_schema`), so there's no literal region-color field to join against. `languages.color` is the real signal — confirmed via a full table read that it already tracks each language's origin region visually (`jugabi-canopy` `#4a9a3a` green, `nombi-frost` `#2266b8` blue, `sohot-old` `#c8882a` gold, `caparian-deep` `#2a7a3a` green, `conclave-script` `#6a3aaa` purple, `pre-partition` `#8a6a2a` brown) — this is what the glyph already used. Root cause confirmed live via `getComputedStyle()` before touching anything: `.cs-mi-speaks-glyph` got `LANGUAGE_ROW.color` as an inline style, `.cs-mi-speaks-name` never did, staying on a fixed `color: var(--blue)` CSS rule regardless of language. Fix: `renderSpeaksBlock()` now applies the same `LANGUAGE_ROW.color` inline to both spans; the CSS rule's `var(--blue)` is now just an unreachable fallback for the (currently nonexistent) case of a character with no `language_extra` row resolved. Verified on both Hana (Jugabi Canopy, glyph+name both `rgb(74,154,58)`) and Kael (Nombi Frost, both `rgb(34,102,184)`) via computed style, not just visual read. Zero console errors.


---

**2026-08-12 (later same day, live DM review continued) — Committed and pushed the brighter equipped-gear silhouette PNG (`assets/images/character-sheet/equipped-gear/equipped-gear-silhouete.png`).**
DM reported the doll looking too dark on the live deployed site and asked to confirm the "new, brighter version" was live. Checked before assuming anything needed building: `git status`/`git diff --stat` showed the working-tree file was already a different, smaller PNG (128,721 bytes) than the committed one (181,525 bytes) — this is the exact file an Aug 10 session had already flagged as "the DM's already-replaced version... left the image itself untouched" pending a decision, still sitting uncommitted three sessions later. Rendered both to confirm visually rather than trusting file size alone: the committed version is a near-solid-black silhouette, the uncommitted working-tree version a visibly lighter mid-gray one — genuinely brighter, matching the DM's own description exactly. No code change needed, no new asset created — just committed and pushed what was already sitting there.


---

**2026-08-12 (later same day, live DM review) — Reversed the doll/Special-Effects flex ratio from 2:3 to 3:2 (`sheet/index.html`).**
DM reviewed the previous commit's layout on the live deployed site (screenshot from `carlosknightiii.github.io`, not local) and gave direct feedback: Special Effects was "too wide," the equipped paper doll is "more important." The original 2:3 split (Effects-favored) was a judgment call from the layout-swap session, not a Figma-sourced value — no Figma reference exists for this row arrangement at all, so there was nothing to "get right" against a spec, just a call to reverse per live feedback. One-line fix: `.cs-inv-top-col--doll { flex: 2 1 0 }` → `flex: 3 1 0`, `--effects` the inverse. Confirmed via `getBoundingClientRect()` on the real deployed dimensions equivalent (1600px viewport): doll 896px vs. Effects 598px, exact 3:2. Mobile spot-checked and confirmed unaffected — flex-grow only matters before either column hits its `min-width:358px` wrap threshold, and both already wrap to fully stacked at 375px regardless of the ratio. Zero console errors, zero overflow at either breakpoint.


---

**2026-08-12 — Special Effects panel internal styling rebuilt against a fresh Figma pull, node 51:2260 (`css/tokens.css`, `sheet/index.html`).**
Ask was explicit: re-pull the node fresh rather than build from the request's own bullet list. Pulled `get_design_context` on 51:2260 (file `P0tcVgf02XeI13Tz0TtRaX`, same file as the rest of this tab) and read the real "Special Effects" subtree (`85:1765`/`91:1440`/`91:1449`/`91:1457`) directly — this caught a structural mismatch the bullet list didn't call out: Figma has **one shared bordered box** with each equipped item as a "row" inside it (Figma's own `data-name="row"` on the wrapping div, border-bottom-2 on that same div), not the per-item `.cs-story-card` cards the panel was originally built with. Rebuilt to match: new `.cs-effect-panel` is the single box (bg `--blue-700`, 1px `--blue-300` border, `--radius` 10px, `--char-ov-gap-s` 26px padding — all pre-existing tokens, exact hex/px matches confirmed against Figma's own variable dump, zero guessing); each item is a `.cs-effect-item` with the bottom divider (2px `--blue-300`) applied once per item, not per fact-line — confirmed against Figma's real DOM (`border-b-2` sits on the row wrapping name+mechanical+flavor together), and confirmed both items in Figma's own 2-item example carry the divider including the last one, matching the ask's explicit "including the last row" instruction.
**Item name**: switched from `.cs-story-card-title` (Cinzel Decorative, used elsewhere in this tab) to a new `.cs-effect-name` — Roboto Bold 26px, matching Figma's `font/body 'Roboto:Bold'` exactly, not the Cinzel Decorative the reused class had been quietly carrying over. The ask said "check current size against this and correct if smaller" (size was already 26px) but the actual gap was font-family, caught by re-pulling rather than trusting the bullet list's framing.
**Mechanical rows** (`.cs-effect-mech`): sparkle icon (`&#10024;`, HTML entity per the file's C2 no-literal-emoji rule) at `--char-ov-h3` (32px) bold, text in `--gold` (`#f0b429`) at a new `--char-ov-pl` token (24px, added to `tokens.css` — no existing token matched 24px; named to mirror the existing `--char-ov-h3/h4/h5` convention and mapped directly to Figma's `page/fonts/pl`). Replaces the old red-left-border boxed treatment entirely — DM's explicit correction, not a refinement of it.
**Flavor rows** (`.cs-effect-flavor`): book icon (`&#128214;`) at the same `--char-ov-pl` (24px, matches Figma's icon sizing exactly), 13px gap, italic text in `--text` at `--char-ov-h5` (18px, reused rather than adding a redundant `--page/fonts/pm` token since the pixel value already exists under that name).
**Height**: Figma's `h-[277px]` on the "other" column is the canvas size of its 2-item example, not a real cap — implemented as `min-height:277px` on `.cs-effect-panel` per the ask's explicit correction of Figma's literal fixed value, confirmed to actually grow (not clip/scroll) by testing 3 real items including one deliberately long flavor sentence: panel grew from the 277px floor to a measured 405px, `overflow` never engaged.
**Data model note, not a design gap**: Figma's 2-item mock shows exactly one mechanical line per item; this panel's real data can carry several (`magic_bonus`/`grants_advantage[]`/`grants_proficiency[]`/`equipped_effect_mechanical` are all independent). Kept the existing one-`.cs-effect-mech`-row-per-fact behavior (pre-existing, not part of this ask) — Figma's divider-per-item structure supports this cleanly since the border lives on the outer `.cs-effect-item`, not on each fact line.
**Test data**: Kael's Longbow already carried real DM-set effect text from an earlier session (not touched). Added two temporary items for full coverage per the ask — his equipped Braided Cord Ring got a mechanical-only `Invisibility` line (Kael doesn't own a literal "Ring of Invisibility," so this simulates Figma's own example against a real equipped item rather than inventing a fake catalog entry) and his Leather Armor got a mechanical line plus a deliberately long flavor sentence to stress-test the "grows as needed" requirement. Both reverted to null after verification via a final SQL read; Longbow's real data was left exactly as found.
**Verification**: live on Kael, both breakpoints, via `git stash`/`git stash pop` for true before/after (old red-box styling vs. new Figma-matched styling) at 1600px and 375px. A real `tokens.css` browser-cache staleness bug surfaced mid-check (already on record from an Aug 9 session: the dev server serves fresh bytes — confirmed via a cache-busted `fetch()` — but the page's own `<link>` doesn't always pick them up) — the new `--char-ov-pl` token resolved as empty on first read despite being present in the served file; worked around by reassigning the `<link>` `href` with a cache-busting query string, then re-verified every computed value (bg/border/radius/padding/font-family/size/color/gap) matched Figma exactly. Zero console errors, zero horizontal overflow at 375px, `new Function()` syntax check clean on the extracted inline script.


---

**2026-08-11 (later same day, after the Longbow investigation) — Inventory tab layout swap: Currency moved down to share a row with Carrying; Equipped Gear + Special Effects redistribute the freed top-row width (`sheet/index.html`).**
Ask: free up width for the doll and Special Effects columns by moving Currency out of their shared row and pairing it with Carrying instead, giving Special Effects the bigger share of the freed space without starving the doll. No Figma reference for this arrangement (ad-hoc on top of the already-ad-hoc Special Effects panel) — reused the tab's existing flex pattern rather than inventing anything new.
**CSS**: added `.cs-inv-top-col--doll { flex: 2 1 0; }` and `.cs-inv-top-col--effects { flex: 3 1 0; }` next to the existing `.cs-inv-top-col--currency` rule — flex-grow only, `min-width: 358px` still comes from the base `.cs-inv-top-col` class untouched, so the mobile wrap threshold didn't need to move. No new row class for the Currency+Carrying pairing either — it's the exact same `.cs-inv-top-row`/`.cs-inv-top-col` shape as the doll/Effects row, just with different children; Currency keeps its existing `max-width:480px` cap and the Carrying column is a bare `.cs-inv-top-col` (flex:1 1 0, no cap) so the weight bar grows to fill whatever Currency doesn't use.
**Markup**: split the old single 3-column `.cs-inv-top-row` into two: [doll, Special Effects] then, below it, [Currency, Carrying] — `renderGold()`/`setGold()` and the weight-bar render call are untouched, only their container markup moved.
**Verified live on Kael** (1600px desktop and 375px mobile, tall-viewport fresh-navigate screenshots to work around the same intermittent Browser-pane scroll/paint glitch on record from prior sessions — a mid-page `scrollIntoView` position wasn't repainting correctly across several retries including a fresh tab, but a full-page capture at zero scroll worked cleanly every time, so used that instead of fighting the glitch further): doll column measured 598px, Special Effects 896px — an exact 2:3 ratio, all 10 doll slots rendered without wrapping, no horizontal overflow (`document.body.scrollWidth - window.innerWidth === 0` at both breakpoints). Currency measured 480px (its cap) with Carrying at 1014px filling the rest, confirmed same row via matching `getBoundingClientRect().top`. At 375px mobile, both new pairs (doll/Effects and Currency/Carrying) wrap to fully-stacked automatically via the pre-existing `min-width:358px` threshold — no new media query needed, and the mobile screenshots before/after this change are visually identical, confirming zero mobile regression.
**Real bonus test case, not staged**: mid-session found Kael's Longbow already had real `equipped_effect_mechanical`/`equipped_effect_flavor` values set — not mine, no test data of mine had ever touched Longbow's effect fields this session before this, so this is the DM's own independent test via the live production DM Tools between sessions. Left it alone (not mine to revert) and used it as a second real card alongside a temporary Leather Armor test card for the before/after screenshots, giving a more realistic two-card test of whether the wider column helps rather than just the single card the ask assumed was already present. Reverted only my own Leather Armor test values afterward; Longbow's real DM-set values were left untouched. Zero console errors throughout.


---

**2026-08-11 — Added a "Special Effects" panel to the Inventory tab (`items` schema, `dm.html`, `sheet/index.html`). Investigated first, reported findings, built after go-ahead.**
**Investigation**: checked real populated `items` rows before proposing any new column. `description`/`story_note`/`gigglegloom_relationship` are consistently flavor-only in practice, not just by schema comment — e.g. `+1 Chain Mail`'s `gigglegloom_relationship` is pure lore ("the enchantment made visible"), while the actual mechanical bonus lives entirely in the separate `magic_bonus` column; `story_note` is location lore or literal DM-only session notes, matching its schema-comment scope exactly with no drift. Confirmed `grants_advantage[]`/`grants_proficiency[]`/`magic_bonus` are the only pre-existing structured mechanical fields — nothing else in the 47-column schema supports arbitrary homebrew effects (resistance, speed multipliers, sentience, etc.). Also checked the `effect_type`/`effect_dice`/`effect_flat`/`effect_duration`/`uses_max` cluster (not obviously relevant to the ask, but close enough in name to warrant ruling out overlap): every one of the 9 populated rows is a consumable or tool (Health Potion, Antidote Kit, River-Treated Torch, etc.) with `uses_max` set — zero rows with a non-null `equip_slot` use these fields, confirming they're scoped to one-shot consumable-use effects with no overlap with "what an item does while equipped."
**Schema**: added two new nullable `items` text columns, `equipped_effect_mechanical` and `equipped_effect_flavor`, both optional, DM-written free text, naming deliberately distinct from the consumable `effect_*` cluster above.
**DM Tools** (`dm.html`): two new textareas in the item form's Content section, directly after History (lines ~2503–2519) — "Equipped Effect — Mechanical" / "Equipped Effect — Flavor", each with help text distinguishing "a real gameplay effect while equipped" from "pure narrative flavor... no mechanical weight." Wired into both halves of the save/edit round trip: `generateItem()`'s entry object (after `history:`) and the edit-populate `sv()` calls (a separate function, easy to miss — confirmed both by testing an actual edit-save round trip, not just reading the code).
**Sheet** (`sheet/index.html`): new `.cs-inv-top-col` panel titled "Special Effects" inserted into `.cs-inv-top-row` between the doll column and the Currency column — reuses `.cs-story-card`/`.cs-story-card-title` for the card shell (no new card component). Iterates all 13 `EQUIPPED` keys (10 doll slots + both rings); for each equipped item with any of `grants_advantage`/`grants_proficiency`/`magic_bonus`/`equipped_effect_mechanical`/`equipped_effect_flavor` set, renders a card with one row per effect. Visual split reuses existing tokens rather than inventing new ones: mechanical rows get a `var(--blue-600)` inset box with a `var(--red-strong)` left border (the same red already used for weapon damage text elsewhere in this tab — not a new color); flavor rows get plain italic `var(--char-text-muted)` text, no box, deliberately calmer. Panel column hides entirely (`display:none`, not an empty box) when zero equipped items have anything to show — implemented as a single `renderSpecialEffects()` call from inside `renderEquipped()` itself (one hook point covers all 6 mutation call sites — selectEquipItem/unequipSlot/etc. — rather than touching each one).
**Verification**: live end-to-end on Kael Evander via the local dev server. Confirmed the panel is genuinely absent (not just visually empty) when no equipped item has any effect field set — checked via `getComputedStyle(col).display === 'none'`, not just a screenshot, since the Browser-pane tool hit the same intermittent scroll/paint rendering glitch already on record from earlier sessions (worked around via `scrollIntoView()` + `form_input`/direct JS reads instead of trusting coordinate-based clicks, which were landing inconsistently under the glitch). Ran the real DM Tools round trip once: opened Leather Armor's real edit form (chosen over the first test item, Leather Cord, after discovering — and leaving alone, since it's unrelated pre-existing data — that Leather Cord has a null `description`, which the form's required-field validation correctly blocked saving on), typed both new fields, called the same `generateItem()` the Save button calls, confirmed via a follow-up SQL read that both values landed in Supabase, then confirmed the sheet picked them up live: a "Leather Armor" card with a red-accented mechanical row and an italic flavor row, positioned correctly right of the doll. All test values reverted to null on both items afterward, confirmed via a final SQL read. Zero console errors throughout. `node --check`-equivalent (`new Function()` on every extracted inline `<script>` block) clean on both `dm.html` and `sheet/index.html`.


---

**2026-08-10 — Closed the root cause behind tonight's stale-`equipped_slot` bug class (`sheet/index.html`), plus two smaller equip-doll cleanups from the same review pass.**
**Removed the redundant "⚔ [damage]" combined weapon-summary row** above the Equipped Gear doll — dead weight now that each hand slot already shows its own weapon's damage directly underneath (added earlier tonight). Deleted `computeWeaponDamageHtml()`/`buildWeaponDamageHtml()` (only ever called for this row) and their now-dead CSS (`.cs-equip-damage-row`, `.cs-equipped-damage-sep/-dice/-type/-slash`); fixed two stale comments that referenced the deleted function. Per-slot red damage text (`buildSlotDamageText()`, `.cs-equip-slot-damage`) is untouched.
**Fixed `getEquipCandidates()` showing an already-equipped item as its own candidate again** — e.g. Kael's Earrings popover listed "Carved Bone Earrings" a second time below the "Currently equipped" line. Now excludes the specific row occupying the slot's active key. Regression-checked against Visenya's two real Braided Cord Rings (one per hand) — the *other* ring still correctly shows as a real candidate; only the one genuinely already in *this* slot is hidden.
**Root cause, found investigating a related report** (Kael's Right Hand popover said "No items in inventory for this slot" despite owning a real Shortsword): his Shortsword's inventory row had `equipped_slot: 'hand_right'` stale from a much older session (already flagged as odd duplicate data in an earlier investigation tonight) while `character_sheet.equipped.hand_right` was actually the Longbow. The ring double-equip fix's exclusion check matched on `equipped_slot` string equality alone — coincidentally excluding the Shortsword too. Fixed the exclusion to also require the row's own catalog/row id to match `EQUIPPED[activeKey]` (mirroring `selectEquipItem()`'s own guard). Then fixed *why* the drift exists at all: added `clearInventoryEquippedSlot()`, wired into `unequipSlot()` and `selectEquipItem()`'s sibling-clear loop — previously `equipped_slot` was only ever set, never reset, on both paths. **Verification surfaced a third, previously-unnoticed instance of the identical gap**: directly swapping the item already in a slot (e.g. Longbow→Shortsword, both `hand_right`) is neither a sibling-clear nor a cross-slot move, so the outgoing item's row went stale there too — caught live via a real swap-back through the UI, fixed the same way.
**Swept all 3 characters via direct SQL** for any other row where `equipped_slot` points at a key the live `character_sheet.equipped` doesn't actually agree with — only Kael's Shortsword was stale; cleared it. Hana and Visenya had none.
Verified end-to-end: Kael's Right Hand "before" state reproduced exactly (code + stale data both temporarily restored, screenshotted) then fixed; selecting Shortsword correctly unequipped the Longbow (moved, not duplicated) and swapping back correctly cleared Shortsword's column this time; the ring double-equip fix re-verified intact (moved Kael's ring Left→Right→Left cleanly) since this touches adjacent logic. Zero console errors throughout. All test mutations reverted to the correct baseline, confirmed via a final SQL read.


---

**2026-08-10 — Fixed a real double-equip gap in `selectEquipItem()` (`sheet/index.html`), surfaced live right after the mechanical/cosmetic-split retirement below. DM reported Kael showing the same Braided Cord Ring in both Right Ring and Left Ring simultaneously.**
**Investigated before touching anything, per DM instruction.** Confirmed via SQL: Kael owns exactly one `braided-cord-ring` inventory row (not a migration duplicate) — but `character_sheet.equipped` had `ring_left` and `ring_right` both pointing at that same catalog id, meaning the one physical row was referenced from two slots at once. Root cause read from the code, not guessed: `getEquipCandidates()` lists an owned item as a slot candidate purely by category/`equip_slot` match, with no check for whether that same row is already equipped somewhere else; `selectEquipItem()`'s mutual-exclusion logic only clears *sibling keys within one slot's own `keys` array* (e.g. `hand_left` vs `shield`, both backing the Left Hand doll position) — it has no concept of "this item is already the value of a completely different slot." Confirmed this isn't ring-specific: `handL`/`handR` both accept category `'weapon'` (two separate slots, same collision shape), so a single owned one-handed weapon had the identical exposure — just not yet hit live. Every other slot has a unique `accessorySlotKey`, so no other pair collides.
**Fix, scoped exactly per DM instruction — same physical row, not same category**: added one general check in `selectEquipItem()`, keyed off the inventory row's own `equipped_slot` column (which slot *that specific row* currently occupies) rather than the catalog id shared by `equipped[key]` values. If the row being equipped already has a *different* `equipped_slot` than the target, that old key gets cleared as part of the same action — the item moves, it doesn't duplicate. Guarded with `equipped[priorSlotKey] === equipKey` before clearing, specifically because `unequipSlot()` never resets a row's `equipped_slot` column back to null (a separate, pre-existing gap, not fixed here) — without that guard, a stale column value could falsely clear an unrelated key that was never actually holding this item. Not hardcoded to rings/hands — works off any inventory row id match across any two keys, so it also covers the pair that hasn't been exercised yet.
**Confirmed does NOT affect Visenya's legitimate case** (two separate owned rings, one in each ring slot): the check is keyed off the literal inventory row id, and her two rings are two distinct rows with two distinct `equipped_slot` values — reopening either of her ring popovers does nothing (no `selectEquipItem()` call happens on view), and her state was confirmed byte-identical before and after.
Reset Kael's `ring_right` back to empty via direct SQL (should never have been set — he owns one ring, it belongs in `ring_left` per the original migration). Verified live end-to-end: Kael's Right Ring popover correctly shows "Nothing equipped" with the one owned ring listed as a legitimate candidate (not a false "already equipped" block — the fix allows selecting it, it just moves instead of duplicating); clicked it, confirmed `ring_left` cleared and `ring_right` populated, then reversed the click to move it back, confirmed clean round-trip. Visenya's two-ring state re-verified unchanged after opening (not clicking) her Right Ring popover. Hana's Quarterstaff (one owned one-handed weapon) used as the hand-slot equivalent test — moved Right→Left (confirmed `hand_right` cleared, `hand_left` populated, screenshotted), then moved back, confirmed restored to the original migrated baseline. All three characters' `character_sheet.equipped` confirmed via a final SQL read to exactly match their pre-fix-testing correct state. Zero console errors throughout.


---

**2026-08-10 — Retired the mechanical/cosmetic split (built earlier this same session) for one unified ownership model. `sheet/index.html`, `dm.html`, real `items`/`inventory`/`character_sheet` data. Investigated first (reported back, 3 open questions), built after DM answers. Commits pending, not pushed.**
**Investigation (Step 1)**: confirmed the catalog gap was bigger than the ask described — not just `STATIC_OPTION_COSTS` (34 values: hat/cloak/shoes/ring/necklace/earrings) but also `CLOTHING_TIERS` (17, "top") and `LOWER_TIERS` (7, "lower") in `js/character.js`, 58 option-values total matching the ~57 real files in `assets/images/appearance/` (one pair, "flowing robes"/"robes", shares an asset). Found 7 of the 17 "top" armor-tier values are cost-identical to armor already in the real `items` catalog under different ids/names (studded leather, breastplate, scale mail exact; chain shirt, half-plate, plate armor, splint armor name-differs) — flagged rather than assumed duplicate-or-distinct. Found the chest slot's popover only ever accepted `category:['armor']` and DM Tools' accessory Equip Slot dropdown had no `ring` or `chest` value at all — both hard blockers for the new model, not mentioned in the original ask. Reconstructed each of the 3 characters' real Stage-4 purchases from `appearance_data`, verified against `calcGoldSpent()`'s actual logic — every character's costed picks summed to their real `starting_gold_spent_cp` exactly, confirming nothing else slipped in. Confirmed DM Tools' loot/award mechanism (`sccLootItemSearch()`/`sccAwardItem()`, dm.html) filters `ITEMS` by name only, no category restriction — works unmodified once cosmetics are real catalog rows, no fix needed.
**DM answers, then build (Step 2)**: (1) retired the 7 cost-matching top values — no new rows, future creation-flow work (Step 4) will point at the real armor ids instead. (2) Gave chest a new `equip_slot:'chest'` (category `accessory`) for non-armor clothing (arcane vestments, robes, monk's gi, etc.) — `EQUIP_DOLL_SLOTS`'s chest entry now accepts `categories:['armor','accessory']` with `accessorySlotKey:'chest'`, one mechanical key (`armor`) either way. `arms` left intentionally empty — no source-of-truth option list exists for it anywhere (unlike every other slot, which ports verbatim from a real character.html `<select>` or STATIC_OPTION_COSTS), and no character has any arms data to migrate; inventing one from scratch is Step 4 (creation-flow) scope. (3) Kael's stray "scale mail" cosmetic value (conflicting with his real, inventory-backed, DM-granted Leather Armor) dropped entirely rather than migrated — his real Leather Armor stays canonical, per the DM's explicit resolution of the Chest-slot mismatch investigated earlier this session.
**Catalog**: 51 new `items` rows inserted (34 STATIC_OPTION_COSTS as-is; 6 new chest-clothing `accessory`/`chest` rows for the unarmored CLOTHING_TIERS values; 4 new `armor`/`armor` rows — Leather Jerkin/Padded Gambeson/Traveller's Coat/Ranger's Mail — for the 4 non-matching light/medium tier values, given provisional AC by tier since they're chosen via the same armor-tier eligibility system real armor uses, flagging these ACs as a judgment call for DM balance review, not a source-verified value; 7 new `accessory`/`legs` rows for LOWER_TIERS). All images point at the real GitHub Pages asset URL (`.../anavale-wiki/assets/images/appearance/*.webp`) rather than a page-relative path, since `items.image` is read directly by multiple pages at different directory depths. `dm.html`'s accessory Equip Slot dropdown gained `ring` and `chest` options — `generateItem()` already read this dropdown generically (`val('item-equip-slot-accessory')`), so no JS change needed beyond the two new `<option>`s.
**Migration**: Hana (cloak→Hooded, ring_left→Braided Cord, legs→Trousers, plus her already-owned Quarterstaff newly equipped into hand_right — it was cosmetically "worn" before but never mechanically equipped), Kael (feet/neck/earrings/ring_left/legs from his real Stage-4 picks, Gold ring excluded per the earlier investigation, chest stays Leather Armor), Visenya (back/feet/earrings + both rings — same Braided Cord Ring item in two separate inventory rows/equip keys since a ring is a physical instance per hand — plus her already-owned Light Crossbow newly equipped into hand_right). Real inventory rows (`dm_granted:true`, matching the existing convention that all bootstrap/non-live-purchase rows use), `character_sheet.equipped` set directly.
**Sheet rebuild**: `EQUIP_DOLL_SLOTS` extended with `ring_right`/`ring_left` entries (same shape as every doll slot, `accessorySlotKey:'ring'`) so rings now go through the exact same `getEquipCandidates()`/`selectEquipItem()`/`renderEquippedSlot()` path as the 10 doll positions — `renderRingsRow()` collapsed to one line calling `renderEquippedSlot()` per ring, the bespoke `renderRingSlot()` deleted. `openSlotPopover()`/the modal markup collapsed from two sections ("Currently Equipped" + "Currently Wearing") to one — deleted `renderPopoverCosmeticRow()`, `selectWearingOption()`, `buildWearingResultThumb()`, `escJsString()`, `WEARING_HEADING_HTML`, `SLOT_APPEARANCE_KEY`, `APPEARANCE_OPTIONS`/`APPEARANCE_OPTIONS_KEY`, `APPEARANCE_IMG_OVERRIDE`, `appearanceImgSlug()`/`appearanceImgSrc()`, `realCosmeticValue()`, `equipArtFallback()`, `ringSlotByKey()`, the sparkle "Mechanical item equipped" badge, and their CSS (`.cs-equip-slot-badge`, `.cs-wearing-section-solo`, `.cs-slot-popover-heading/-subtext`, `.cs-wearing-empty`). `saveAppearance()`/`APPEARANCE` stay live — appearance_data's non-equip fields (height/build/skin tone/hair/etc., read by `buildAppearanceDesc()`) are unrelated to the equip system and still need it; the equip-related subkeys left in old characters' `appearance_data` JSON (cloak/top/lower/etc.) are now inert, harmless, not cleaned up.
**Real bug found and fixed during verification, not shipped**: `EQUIPPED` (the module-level doll state) and `fetchEquipped()`'s rebuild of it both still used the pre-ring hardcoded 11-key shape (`armor/shield/hand_right/hand_left/head/neck/earrings/arms/back/legs/feet`) — predates tonight, never updated when rings got real doll-slot-shaped data. `ring_right`/`ring_left` were silently dropped on every page load even though `character_sheet.equipped` had them and `EQUIPPED_ITEMS` had the resolved catalog rows — rings would have rendered permanently empty despite correct data underneath. Caught live on Kael (Left Ring showed empty despite a real equipped Braided Cord Ring) before reporting done, not assumed working from the code alone. Fixed by adding both keys to both places.
Verification: live on Kael/Hana/Visenya via the local dev server, desktop (1440px) and mobile (390px). Kael: Chest correctly shows real Leather Armor (not the dropped "scale mail"), Left Ring shows Braided Cord Ring, Right Ring correctly empty (Gold ring excluded), popover confirmed single-section for both an empty slot (Head) and a filled one (Chest, listing only the one real owned armor item with its real AC/weight stats). Hana: Quarterstaff now shows equipped in Right Hand with real damage text, Back/Legs/Left Ring show the migrated cosmetics. Visenya: both rings show independently, Right Hand shows Light Crossbow equipped. Zero console errors across all 6 checks (3 characters × 2 breakpoints). `node --check` clean on both `dm.html` and the extracted `sheet/index.html` script block.
**Not yet done, explicitly deferred**: Step 4 (character.html/character.js creation flow granting real inventory instead of writing `appearance_data`) — sizeable separate work, sequenced after this verification per the DM's explicit instruction, not started.


---

**2026-08-10 — Investigated why Kael Evander has a Gold ring equipped (25gp) against a 10gp starting budget (Greywitnessed). Not a bug — logged here, no code changed.**
Checked all three possible explanations before concluding: (1) Stage 4 shopping overspend is a genuine hard block in `js/character.js` (`validateStage(4)` computes `getStartingGold() - calcGoldSpent()`, blocks `goNext()`/`jumpToStage()` on negative remainder) — not a soft warning, ruled out. (2) The ring's price (`cost_gp: 25` in `STATIC_OPTION_COSTS`) hasn't changed since May 27 2026, before Kael's July 6 creation, and isn't DM-Tools-editable at all (it's hardcoded in creator JS, not a Supabase `items` row) — ruled out. (3) The validation itself isn't buggy either — the ring alone (25gp) exceeds Greywitnessed's entire 10gp budget, so it's mathematically impossible for it to have passed the real Stage 4 hard block under any combination of other purchases. Real explanation: `appearance_data` (which is what actually stores "a gold ring") can only be written from two places — Stage 4's validated shopping flow (ruled out above) or the character sheet's own "Currently Wearing" cosmetic picker (`saveAppearance()`/`selectWearingOption()`), which has zero cost or ownership validation by design, since post-creation appearance edits are meant to be free. DM Tools never touches `appearance_data` at all (confirmed via grep), so that's not the path either. Conclusion: two intentionally separate systems — a one-time budget-gated shop and a permanently free-form cosmetic picker — both working as built.
**Flagging forward, not acting**: the DM confirmed a bigger change is planned (not yet built) requiring cosmetic items to be actually owned in inventory before they can be equipped, matching how weapons/armor already work via `getEquipCandidates()`. That would make this exact scenario structurally impossible going forward. Worth remembering when next touching `APPEARANCE_OPTIONS`/`STATIC_OPTION_COSTS`/`renderPopoverCosmeticRow()` — check whether that ownership-gating has landed before assuming the current free-form static-list behavior (hat/cloak/ring/necklace/earrings/shoes/lower/top, all unfiltered by real inventory) is still current. The hand-slot branch already partially does this (weapon-name cosmetic options are sourced from real inventory rows) — the other 8 option lists don't yet.


---

**2026-08-10 — `sheet/index.html`: the "Currently Wearing" cosmetic picker (`renderPopoverCosmeticRow()`) replaced its native `<select>` with a clickable thumbnail list matching the "Currently Equipped" mechanical picker's own row style — one shared function, so this fixes all 10 doll slots plus both rings at once. Same session, right after the ring-tile conversion below.**
Reused the mechanical picker's existing markup/CSS as the base rather than inventing new styling, per the ask: `.cs-equip-results`/`.cs-equip-result`/`.cs-equip-thumb`/`.cs-equip-result-info`/`.cs-equip-result-name` (and, for the currently-selected row, `.cs-equip-result-flag`, reusing the same "Also equipped: X" flag language for a new "Current" flag) — zero new CSS classes for the row itself, only a new `buildWearingResultThumb()` (cosmetic art has no category to fall back to the way `buildEquipResultThumb()`'s armor/weapon/shield icons do, so it falls back to a plain "—" swatch instead) and the now-dead `.cs-wearing-select` rule removed.
**No search box added** — checked real option counts first rather than guessing: the static lists run 5–18 entries (`top` is the longest at 18, every other list is 5–8), hand slots are bounded by the player's actual weapon inventory (typically 1–3), and the existing `.cs-equip-results` scroll cap (`max-height:320px`) the mechanical picker already uses handles the longest case the same way it already handles longer mechanical-item lists — no new component needed.
**Real escaping gap found and fixed, not a hypothetical**: switching from `<select onchange>` (reads `.value` off the DOM element) to `<div onclick="fn('...')">` (embeds the option's value as a literal JS-string argument) introduced a genuine risk the old picker never had — three real `top` option values contain apostrophes (`"monk's gi"`, `"traveller's coat"`, `"ranger's mail"`), and hand-slot options can be arbitrary player-typed custom item names. Added `escJsString()` (backslash then single-quote) and ran every embedded value through it — confirmed live by actually selecting "Monk's gi" through the real UI (not just eyeballing the generated HTML) and checking it saved correctly with no console error, since a broken escape here fails silently as a JS syntax error inside the inline handler, not a visible crash.
**Verified all three requested cases, both breakpoints**: Left Ring (7 options, no mechanical section at all — confirmed the previous round's `cs-equip-mechanical-section` hide/`cs-wearing-section-solo` still holds with the new list markup inside it), Back (both sections present together, mechanical unaffected), and a slot with an effectively-empty cosmetic list — no real character currently has zero weapons in inventory, so this was tested by temporarily clearing the live `INVENTORY` array in the browser (not Supabase — confirmed via `getEquipCandidates('handR').length === 0`) to exercise the true zero-candidate path (list collapses to just the "— none —" row), then restoring the array from a saved copy afterward. Clicking a real option (Signet ring, then back to Braided cord ring) end-to-end confirmed: modal closes on select (matching the mechanical picker's own close-on-equip behavior), the tile updates immediately, and the write lands in Supabase — checked via a follow-up SQL read that `ring_left`/`ring_right`/`hand_right`/`cloak` all match their pre-test values exactly, nothing left mutated. Zero new console errors; every real option's thumbnail loaded a real image (confirmed via network log, not assumed) except the same pre-existing, already-documented `appearance/longbow.webp` 404 (hand slots have no weapon art at all, graceful "—" fallback, unrelated to this change).


---

**2026-08-10 — `sheet/index.html`: ring selectors converted from plain `<select>` dropdowns into real slot tiles matching the other 10 gear slots. Same session, right after the weapon-slot changes below.**
`renderRingsRow()` rebuilt to emit the exact same `.cs-equip-slot-wrap`/`.cs-equip-slot-box`/`.cs-equip-slot-label`/`.cs-equip-slot-name` markup the 10 doll slots use (new `renderRingSlot()`) instead of `.cs-ring-item`/`.cs-ring-select` — clickable, opens the same popover via `openSlotPopover(ring.key)`. `RING_SLOTS` labels shortened to match the other slots' terse style ("Right Ring"/"Left Ring", was "Right Hand Ring"/"Left Hand Ring") — used both on the tile and as the popover title now that rings open a real popover instead of just an inline `<select>`. Dropped the now-fully-dead `.cs-ring-item`/`.cs-ring-label`/`.cs-ring-select` CSS (confirmed via grep no longer referenced anywhere).
**Sizing had a real trap, caught before shipping, not after**: `.cs-equip-slot-wrap`'s width comes from `cqi` (container query units) relative to `.cs-equip-doll`'s own `container-type:inline-size` — but `#cs-ring-row` is a *sibling* of `#cs-equipped-block` (i.e. of the doll), not a descendant, so it can't inherit that containment. Per spec, `cqi` with no qualifying container resolves to 0, which would have silently pinned every ring tile to its `clamp()` floor (~50px) forever, regardless of viewport — visually smaller than the other 10 slots at any width above the floor, the opposite of "matching." Fixed by giving `.cs-ring-row` the *identical* sizing formula as `.cs-equip-doll` (`width:100%; max-width:61.08rem; container-type:inline-size`) rather than trying to share containment — both derive their width from the same `.cs-inv-top-col` parent, so their `cqi` math always resolves to the same absolute px at any viewport. Verified via `getBoundingClientRect()`, not assumed: ring tile and doll tile boxes measured pixel-identical at both 1440px desktop (128.7×131.0px) and 375px mobile (70.4×71.7px, the mobile touch-target floor).
**Popover — rings skip the "Currently Equipped" section entirely**, not just its search box (the existing empty-candidates fix from an earlier round hides only the search input, for slots that *can* be mechanical but currently aren't — Back with nothing equipped). Rings can never be mechanical (`items.equip_slot` has never had a ring value), so this is a different case. Added `id="cs-equip-mechanical-section"` to that block's static markup; `openSlotPopover()` now branches on whether the target is a doll slot or a ring (new `ringSlotByKey()` lookup) and toggles that section's `display` accordingly, skipping `renderPopoverMechanicalRow()`/`renderEquipResults()` entirely for rings rather than calling them into an empty state.
**Real bug caught during this, not shipped**: the `.cs-slot-popover-section + .cs-slot-popover-section` adjacent-sibling CSS rule (the divider between the two sections) still matched `#cs-wearing-section` even with the mechanical section `display:none`, since CSS sibling combinators walk the DOM tree, not the render/paint tree — `display:none` removes an element from paint, not from the DOM the selector matches against. Caught live via a fresh screenshot before writing it off as done (would have shown a border/margin floating above "Currently Wearing" with nothing above it). Fixed with a new `#cs-wearing-section.cs-wearing-section-solo` override (ID+class specificity beats the two-class combinator rule), added/removed alongside the mechanical section's `display` toggle.
**Cosmetic picker reused, not rebuilt**: both rings now route through the exact same `renderPopoverCosmeticRow()`/`onWearingChange()` every other slot uses — required generalizing two lookups, not adding a parallel path. `SLOT_APPEARANCE_KEY` gained `ring_right`/`ring_left` self-mappings (their doll "area" and their `appearance_data` storage key are the same string, unlike e.g. `handL`→`hand_left`). Both rings share ONE options list (`APPEARANCE_OPTIONS.ring`) despite having two distinct storage keys, which the existing `appKey`-doubles-as-options-key convention couldn't express — added a small `APPEARANCE_OPTIONS_KEY` map (`ring_right`/`ring_left` → `'ring'`) and changed the one options-lookup line to check it first, falling through to `appKey` itself unchanged for every other slot (verified: 7 real ring options loaded in the popover, matching the shared list, not a truncated/wrong one).
Verification: live on Kael Evander (real Gold ring / Braided cord ring on both hands) at 1440px desktop and 375px mobile — tiles render the correct ring art/name, popover shows only "Right Ring"/"Left Ring" + "Currently Wearing" + its cosmetic explainer + a real 7-option select, zero "Currently Equipped" section, zero orphaned border. Cross-checked a normal slot (Back) immediately after closing a ring popover to confirm no regression — mechanical section correctly restores (`display:block`, "Nothing equipped", real search box). `document.body.scrollWidth - window.innerWidth === 0` at both breakpoints. Zero new console errors (ring art itself loaded clean — `gold-ring.webp`/`braided-cord-ring.webp` both 200 OK; the only 404 is the same pre-existing, already-documented `appearance/longbow.webp` fallback, unrelated).


---

**2026-08-10 — `sheet/index.html`: two Equipped Gear doll weapon-slot changes — per-slot red damage text, and a real two-handed-weapon mirror image on the locked hand (was just an empty grey box). Same session, right after the popover copy/UX fixes below. Also fixed a real, previously-latent bug the second change surfaced: two-handed detection only ever checked `hand_right`.**
1. **Per-slot damage text**: `renderEquippedSlot()` now appends a `.cs-equip-slot-damage` line ("1d8 (piercing)") under the item name for any hand slot holding a weapon with `damage_dice` — new `buildSlotDamageText()`, a plain-text sibling of the existing `buildWeaponDamageHtml()` (not a reuse of it: that one's spans are hardcoded gold for the combined summary row above the doll, `.cs-equip-damage-row`/`computeWeaponDamageHtml()`, which is untouched and still renders as before). Color is `var(--red-strong)` (`#d62b2b`, confirmed via computed style, not assumed) — the same red already used for the HP bar fill, not a new one. No area check needed to scope this to hands only — `damage_dice` is only ever present on weapon items, and only hand slots' categories ever admit weapons.
2. **Two-handed mirror image**: the locked "other hand" slot (`.cs-equip-slot-locked`, existing class, reused verbatim — still just `opacity:0.4`, no new grayscale/filter invented) now renders the two-handed weapon's own image instead of an empty box, with no name or damage line (that text only belongs on the hand actually holding it). Confirmed via `getBoundingClientRect()`/computed-style checks, not just a screenshot glance: the mirrored `<img src>` is the identical `longbow.webp` URL on both hands, opacity 0.4, zero `.cs-equip-slot-name`/`.cs-equip-slot-damage` children.
   **Real bug found and fixed while generalizing this, not scope creep** — the DM's ask ("derive it from whichever hand is actually locked, don't hardcode always-right") required reading `renderEquipped()`'s two-handed detection first, which turned up that it only ever checked `EQUIPPED.hand_right` — a two-handed weapon equipped into the *Left* Hand slot (fully possible via `selectEquipItem()`, which routes a Left Hand pick to `hand_left` same as a Right Hand pick routes to `hand_right`, no hand privileged there) would never have triggered the lock at all, silently allowing both hands to hold weapons simultaneously. Replaced the hardcoded check with a new `detectTwoHandedLock()` that checks both hands and returns `{activeArea, lockedArea, item, lockedKeys}` — `lockedKeys` is `['hand_left','shield']` when Right Hand is active (Left Hand's doll position has two backing keys) or just `['hand_right']` when Left Hand is active (Right Hand only ever has one). `autoUnequipOffHandForTwoHanded()` now takes `keys` as a parameter instead of hardcoding `['hand_left','shield']`, so the same function clears whichever side is actually locked.
Verification: live on Kael Evander (real Longbow, two-handed, in `hand_right`) — Right Hand shows "1d8 (piercing)" in red, Left Hand shows the same bow image at 0.4 opacity with no text. Temporarily added a real Shortsword (1d6, one-handed) to Kael's own inventory via direct SQL, equipped it into Right Hand through the actual UI (replacing the Longbow) to verify the one-handed path shows its own damage text (confirmed "1d6 (piercing)") *without* triggering the mirror/lock on Left Hand (confirmed via class inspection: plain `cs-equip-slot-box cs-equip-slot-empty`, still clickable, no `cs-equip-slot-locked`) — then re-equipped the Longbow through the same UI flow and deleted the temporary inventory row, confirmed via a follow-up SQL read that `character_sheet.equipped` matches the original `{armor:"leather-armor", hand_right:"longbow"}` exactly. Zero new console errors (only the same pre-existing, already-documented `appearance/longbow.webp` cosmetic-fallback 404).


---

**2026-08-10 — `sheet/index.html`: 5 equipped-slot popover copy/UX fixes from live QC, same session right after the doll rebuild below.**
All 5 confirmed live on Kael Evander's Back slot (empty mechanical + no cosmetic chosen — a clean before/after test case since it exercises both empty states at once) before/after, desktop viewport.
1. **Title** — `openSlotPopover()` dropped the `' — Equipped Gear'` suffix; now just the slot name (e.g. "Back", was "BACK — EQUIPPED GEAR"). The old title implied the whole popover was about mechanical gear, which is why the cosmetic row below read as out of place.
2. **Duplicate "Currently equipped"** — `renderPopoverMechanicalRow()` no longer repeats the section heading's own words in the row below it; the row now reads just the item name (`<strong>Leather Armor</strong>`) or `Nothing equipped`, not `Currently equipped: nothing`.
3. **Search box hidden when nothing to search** — `renderEquipResults()` now toggles `#cs-equip-search-input`'s `display` based on `getEquipCandidates().length`, hiding it entirely for slots with zero inventory candidates instead of showing an empty search box above the empty-state message (a real live-QC question: "what is the search item field for?"). Confirmed the box still renders normally for slots that do have candidates (Chest, on Kael) — not a regression, this only fires on the zero-candidate path.
4. **Empty-state contrast** — `.cs-inv-empty` (shared by this popover's "No items in inventory for this slot"/"No matches." and the +Add Item modal's own "No matches.") was `rgba(255,255,255,0.2)`, close to unreadable. Changed to `var(--char-text-faint)`, the same muted tone already used for `.cs-wearing-empty` and other secondary copy in this file — a consistent existing tone, not a new one.
5. **"Currently Wearing" explainer** — added a new shared `WEARING_HEADING_HTML` (heading + a small `.cs-slot-popover-subtext` line, "Purely cosmetic — doesn't affect your stats") used by both branches of `renderPopoverCosmeticRow()` (the no-appearance-option slot and the normal picker), so every slot's cosmetic row explains itself at a glance — copy-only, the section's own logic/behavior is untouched per the DM's explicit instruction that this wasn't a redesign.
Verification: JS syntax validated via `new Function()` on the extracted inline script block. Zero new console errors (the only 404s are the pre-existing, already-documented `appearance/longbow.webp` cosmetic-art-fallback path from the entry below, unrelated to this change).


---

**2026-08-10 — `sheet/index.html`: Equipped Gear doll rebuilt against the real Figma layout (node `68:7354` "equipped-doll", pulled fresh this session via `get_design_context` — the Aug 9 build had shipped with no Figma reference for this specific node at all, DM has since designed the real layout there).**
DM supplied exact structure/geometry from the pull directly (flex row, gap-10 items-end justify-center; a Left Hand slot at gap-16 beside a 4-slot Head/Chest/Arms/Feet column, mirrored on the right with Neck/Earrings/Back/Legs + Right Hand; each stacked slot's 177.249px row bottom-justifies its 140.691x143.249 frame + label; silhouette 362.553x759.268 centered between the two groups) — re-pulled it myself before writing any code rather than building from the description alone, confirmed it matched verbatim.
**Silhouette asset check (done first, per explicit instruction not to assume)**: confirmed `equipped-gear-silhouete.png` is the DM's already-replaced version, not the original — its mtime (this session, after the last commit touching it) and its working-tree bytes (128,721) differ from HEAD's committed bytes (181,525) per `git diff --stat`, and its pixel dimensions (363x760) already match the Figma spec almost exactly. Left the image itself untouched.
**Structure**: old grid-template-areas layout (max-width:480px, silhouette absolutely overlaid via grid-column span + object-fit:contain) replaced with real nested flex markup mirroring the Figma node one-to-one — `.cs-equip-doll` > two `.cs-equip-doll-side` groups (each a flanking hand slot + a `.cs-equip-doll-col` of 4 `.cs-equip-slot-row`s) flanking `.cs-equip-doll-silhouette`, all `align-items:flex-end` for the shared bottom baseline the DM described. `renderEquipped()`/`renderEquippedSlot()` rewritten to build this nesting explicitly (using the existing `equipSlotByArea()` lookup, called earlier in the file now — safe, it's a hoisted function declaration) instead of relying on CSS `grid-area` placement; the `grid-area` inline styles are gone entirely. Slot order unchanged, per the DM's explicit instruction — only sizing/spacing/anchoring moved.
**Fluid sizing, not raw px**: build spec (below, then Aug 9 entry above) already flags that hardcoding Figma's canvas px onto a differently-shaped container was exactly what broke the silhouette twice before. Used CSS container query units (`container-type:inline-size` on `.cs-equip-doll`, sized via `width:100%; max-width:61.08rem` off its own flex parent so there's no circular sizing dependency) — every descendant dimension, including heights, is a `cqi` percentage of the doll's own resolved width, so the whole layout scales as one unit exactly like Figma's proportions instead of one fixed breakpoint. `clamp()` floors stop things shrinking below a usable size.
**Real bug caught during verification, not shipped**: first pass added `flex-wrap:wrap` on `.cs-equip-doll` as a "safety net." Since the cqi percentages are tuned to sum to ~100% of the container by construction, this landed within a sub-pixel of the wrap threshold at common widths and browsers rounded one side group onto its own line below the item table — confirmed live via `getBoundingClientRect()` (the two side groups' rects were ~700px apart vertically) before catching it, not from a visual glance. Fixed by switching to `nowrap` and instead sizing the `clamp()` floors low enough (~35.8% of reference size, ~350px total doll width) to guarantee fit within this column's own pre-existing 358px architectural minimum (`.cs-inv-top-col { min-width: 358px }`, unrelated pre-existing CSS, not touched) — worked through the actual flex-wrap/flex-grow arithmetic of the shared Equipped-Gear/Currency row to find the real narrowest case (~397px column width around an ~850px browser window, currency sharing the row without wrapping) rather than assuming the mobile breakpoint was the only risk.
**Mobile (<=600px, existing DM-approved deviation, unchanged in principle)**: silhouette still hides — the real Figma mobile frame reuses this same desktop component with no mobile layout of its own (a gap already flagged in `docs/inventory-tab-build-spec.md`). Added a mobile-only override raising the slot-size floor for a better touch target now that the silhouette's gone and there's room to spare, rather than inheriting the tight ~350px-total floor sized for the silhouette-visible case.
**Label styling**: color changed to `--white` (was `--char-text-faint`, a muted gray) and size changed to a `clamp()` capped at the existing `--char-ov-h5` token (18px) — both explicit in the DM's spec ("Cinzel Decorative Bold, 18px, white"). Left the existing uppercase transform/letter-spacing alone — not mentioned in the ask, and consistent with the Inventory tab's other section headers (EQUIPPED GEAR/CURRENCY/CARRYING all render uppercase the same way).
Verification: JS syntax validated via `new Function()` on the extracted inline script block. Checked live via the local dev server (started fresh this session — `.claude/launch.json` only attaches to an already-running server, wasn't running) on Kael Evander (real leather armor/longbow/worn boots etc.) and Hana Sable (no mechanical items, but real cosmetic-only values on Back/Right Hand/Legs — a better edge-case test than a truly-blank character, confirmed the cosmetic-vs-mechanical badge distinction still renders correctly post-rebuild) at both 1440px desktop and 375px mobile. `getBoundingClientRect()`-verified the bottom-alignment and exact 10px gaps between groups and silhouette, and `document.body.scrollWidth - window.innerWidth === 0` (no horizontal overflow) at all four combinations. Zero new console errors — the only 404s (`appearance/longbow.webp`, `appearance/quarterstaff.webp`) are the pre-existing, already-documented weapon-cosmetic-art-doesn't-exist fallback path, unrelated to this change.


---

**2026-08-09 — `sheet/index.html`, `dm.html`, `css/tokens.css`: Inventory tab built against the real Figma pass (`docs/inventory-tab-build-spec.md`, nodes 51:2260 desktop / 51:2265 mobile). New session, later same day as the entries below.**
Full build per the spec's 6-step order, plus one DM-requested correction after initial report-back.
**10-slot equip data model**: `EQUIPPED`/`SHEET.equipped` extended from 4 keys to 11 (`armor`, `shield`, `hand_right`, `hand_left` existing; `head`/`neck`/`earrings`/`arms`/`back`/`legs`/`feet` new) backing 10 visual doll positions (`EQUIP_DOLL_SLOTS`). Left Hand is the one doll position with two mutually exclusive backing keys (`hand_left` weapon vs. `shield`) — equipping either now auto-clears the other at that position (extended the existing two-handed-weapon lock pattern), and a two-handed main-hand weapon clears both. DM Tools' accessory Equip Slot dropdown fixed to the exact 7 new values (was offering `finger`/`wrist`/`waist`, which have no doll position, and missing `earrings`/`arms`/`back`). Checked the spec's suspected `equipSlotMap`-overrides-back-to-null bug in `generateItem()` — confirmed it does **not** exist; the category-specific block runs after the base entry and already sets `equip_slot` correctly from the dropdown.
**Doll visual**: built with CSS `grid-template-areas` echoing the humanoid layout rather than the Figma frame's literal absolute px positions — those broke the mobile frame outright (slots rendered off-canvas, confirmed via screenshot) and wouldn't reliably reflow on desktop either, since the design canvas width doesn't match any real container width in this responsive page. Mobile falls back to a wrapping flex grid (same slot visual, no silhouette), per the spec's own pre-approved mobile deviation. Mid-build, found real Figma-exported assets (`assets/images/character-sheet/equipped-gear/gear-slot.jpg` frame + `equipped-gear-silhouete.png` body silhouette) already staged untracked on disk — missed on the first pass (shipped a CSS-only approximation initially), caught before finalizing by checking `git status` for untracked files and correcting to use the real assets. Left three further staged-but-unused assets (`assets/images/ui/icon-x.svg`/`icon-search.svg`/`icon-arrow-dropdown.svg`) unwired — their fill color is baked-in dark, so dropping them in as-is against this page's dark theme would be a real contrast regression without a `mask-image` recolor pass; flagged rather than risked.
**Actions column — DM correction after first report-back**: initial build tucked the quantity input/edit/delete controls into the Description cell since Figma's 8 columns don't show a slot for them. DM correctly called this out as breaking the spec's own explicit decision that Description is flavor text only. Fixed: real 9th "Actions" column (`cs-inv-th-actions`/`cs-inv-td-actions`, 132px, centered), reusing the existing `.cs-inv-qty-input`/`.cs-inv-edit`/`.cs-inv-del` classes — no new styling invented. `colspan` on the loading/empty-results rows bumped 8→9, mobile table `min-width` fallback bumped 760px→860px. Re-verified on both characters/breakpoints after the fix.
**Select checkbox → bulk delete only**: no bulk action shown in the mockup; built as multi-select → "Delete selected" (bar appears above the table, reuses the existing single-item delete-confirm modal rather than a new one) since that's the standard reason a table gets row checkboxes — this was the spec's own documented default, not a fix, recorded here since it's part of what shipped.
**Favorites/drag-reorder dropped from this view**: no equivalent in the new table design. Removed `toggleFavorite()`, `wireInventoryDragDrop()`, `getDragAfterElement()`, `persistDragOrder()`, `toggleInvDesc()` entirely (dead code, not stubbed) — confirmed via grep that none are referenced elsewhere before deleting. `is_favorite`/`sort_order` columns themselves are untouched and still written on insert (stable ordering, just no longer player-reorderable here); `is_favorite` also stays live and unrelated on the Notes tab (`.cs-inv-section-label` CSS class is shared with Notes' Pinned/Notes group labels — kept for that reason even though Inventory's own favorites grouping that originally used it is gone).
**Currency/Carrying/+Add Item/filter**: currency rebuilt as 3 real pill inputs (Gold/Silver/Copper) recomputing `gold_cp` on blur, replacing the old single "Set Gold" flow. Carrying bar added (STR×15 capacity — verified 195lb on Kael real STR 13, 210lb on Hana real STR 14), track/fill using `--blue-700`/`--blue-300` per the spec's prose (the raw Figma export read the opposite way, but prose was unambiguous and this is the conventional progress-bar reading). `--blue-300` was genuinely missing from `tokens.css` despite the spec's token table claiming 50–800 all existed — added it. +Add Item modal relocates the existing catalog search unchanged behind a new trigger button. New own-inventory Search/Category/Search-button row filters the already-loaded `INVENTORY` client-side (click-to-search, distinct from the catalog search's live-filter). Category list (Accessory/Armor/Consumable/Misc/Shield/Tool/Weapon) verified against real `items.category` via direct SQL — exactly 7 distinct values, matching the spec exactly. That check also surfaced that the *old*, unchanged-and-relocated catalog-search dropdown was already missing "Misc" (10 real items use it) — added it there too since already touching that exact `<select>`.
**Flagging again, third time now**: the Figma pull for this Inventory frame reuses the same MainInfo header component (`51:1385`) and once again shows a Temp HP bar with a `/8` denominator. This is the third time this exact placeholder has surfaced in a Figma pull for this component this session (cosmetic pass, MainInfo regression-fix pass, now here) — each time correctly identified as mockup filler data, not a real design change, and left alone per the standing decision that there's no real `hp_temp_max` column to show a bar/proportion against. Recording this explicitly so it reads as a known recurring artifact of this Figma component rather than something to re-investigate from scratch next time it shows up.
Verification: JS syntax parsed clean via `new Function()` on the extracted inline script block (same check throughout, both before and after the Actions-column fix). Zero console errors on Kael Evander (real leather armor + longbow equipped) and Hana Sable (fully empty `equipped: {}`) at both 1600px desktop and 375px mobile. Confirmed the item table's `overflow-x:auto` wrapper contains its own horizontal scroll without the page itself overflowing (`document.body.scrollWidth === window.innerWidth`, both 378). All 5 tabs (Your Story/Ability Scores/Inventory/Class/Notes) still switch correctly post-change. Not bundled with any other tab's work per the DM's explicit instruction.


---

**2026-08-09 — `sheet/index.html`: fixed a real orphaned-tooltip bug (condition pill's own remove button); gave the Moral Alignment card the shared 5-alignments illustration as its thumbnail. Commits `9d0660e`, `c1c808b`, same session as the two regression-fix batches above.**
DM reported a stuck tooltip bubble screenshot (hover text for "Invisible" still on screen after interacting with conditions) plus, mid-investigation, a second report that the Moral Alignment card's "Pragmatic" value has no icon/thumbnail like its sibling cards do.
**Tooltip bug — root cause found via D2, fixed in one shot per D3**: `wireTooltip()` shows a bubble on `mouseenter` and only removes it on `mouseleave` from the source element, both listeners bound once per element. The condition pill's "×" button lives *inside* the pill span that carries `data-tip` — clicking it never fires `mouseleave` (the mouse never leaves the pill's bounds to reach a child), and the click's handler (`removeCondition()` → `saveSheet()` → `renderConditions()`) immediately replaces the whole conditions row via `innerHTML`, destroying the pill without the event that would have cleaned up its bubble ever firing. The bubble, parented to `document.body` (not the pill), is orphaned permanently. Fixed centrally in `wireAllTooltips()` — which every re-render on the page already calls immediately after rebuilding its DOM — by sweeping and removing any stray `.cs-tooltip` elements before rewiring. Reasoned this is safe with a single mouse cursor: a legitimately-open tooltip on some *other*, unrelated element would already have received its own `mouseleave` before any click could reach a different part of the page, so the only case where a bubble survives to see this sweep is exactly the bug case (element destroyed while still hovered). Verified by scripting the exact repro (`mouseenter` then `.click()` on the pill's own remove button, no `mouseleave` in between) — bubble count went 1→0 across the click — and separately confirmed normal hover-then-actually-leave behavior is untouched (1→0 on a real `mouseleave` too, on the AC badge). Test mutation (removing then re-adding "Invisible" on Kael) reverted and confirmed via a follow-up SQL read.
**"Pragmatic" thumbnail — D7 correction**: first reported this as an unresolved asset gap ("no per-alignment icon exists, needs a DM decision") after confirming `character.html`'s 5 `.char-alignment-card` elements have no `data-img`/`background-image`/icon of any kind, and that the only alignment-related asset in the repo, `assets/images/img-alignment.webp`, is one combined illustration of all 5 alignments together rather than 5 separate per-value icons. That factual read was correct, but the conclusion was wrong — the DM corrected it on the spot: they'd already decided this combined image *is* the intended thumbnail, used as-is (same image regardless of which of the 5 alignments the character has), not cropped per-value like Gigglegloom Affinity/Home Region/Background's real per-value icons. Fixed per D7 (stopped, corrected the record, then wrote one fix): `alignCard = storyCard(...)` in `renderBackgroundCardsHtml()` now passes `'../assets/images/img-alignment.webp'` as `imgSrc` unconditionally, no per-alignment branching. Verified the image loads (`naturalWidth:1024`, `complete:true`) and renders inside the existing 75×75 `object-fit:cover` thumbnail frame correctly on Kael, zero console errors.


---

**2026-08-09 — `sheet/index.html`: batch 2 of the regression fix pass — Your Story section-title size, Your Background card stretching, tooltip icon styling, "Why You Left" label color. Commit `6761c42`, right after the batch-1 MainInfo fixes (`0a81614`).**
Four more items from the same live-page-vs-mockup comparison as the batch-1 entry above, fixed and verified as a separate batch per the DM's explicit request (screenshot before/after each item, commit each batch separately). Used the `git stash` before/after pattern (established earlier this session) to get a clean "before" state for screenshots after realizing the first edit (section titles) had already landed before any screenshot was taken — stashed, reloaded or Kael to capture "before," restored the stashed change by hand (a plain `git stash pop` refused because the working tree already had the batch's other 3 edits in it by that point — re-applied the 2-line diff manually from `git stash show -p` instead of fighting the pop, then dropped the stash) rather than losing it.
**Section titles**: `.cs-ov-acc-title` (shared by all three "Your Story" headings) was `font-size: var(--text-4xl)` = 48px — should have been 32px per spec. Changed to `var(--char-ov-h3)` (already a 2rem/32px token, used elsewhere for exactly this kind of heading) and added explicit `font-weight:700` (Cinzel Decorative only ships as 700 anyway, but making it explicit removes any doubt). Also removed a now-obsolete mobile override (`font-size: 2.25rem` at max-width:600px) that existed only to shrink the old 48px down for narrow screens — at the new 32px default it's already smaller than that override was, so keeping it would have made mobile headings *bigger* than desktop.
**Your Background card stretching**: `.cs-story-cards` (the shared flex row for both Your Background's and Your Past's card grids) had no `align-items` set, so it defaulted to `stretch` — every card in a row was forced to the height of the tallest one, which is why Gigglegloom Affinity/Home Region/Moral Alignment (a few lines of text) were rendering with a huge dead zone at the bottom to match the Background card's real height (it has a whole Benefits box). Fixed with one line, `align-items: flex-start`, on the shared class — confirmed via `getBoundingClientRect()` that the four Your Background cards now render at four different real heights (204/228/208/497px on Kael) instead of all matching the tallest.
**Tooltip icons**: `.cs-ab-tip` (the shared "?" class used by the Background card's Benefits box, every Your Past card's Benefits box, and separately the Ability Scores tab's per-ability tooltips — three call sites, not two) was plain gold text with no circle/background at all, despite looking superficially like it might be styled. Rebuilt as a real 13×13px circle (`border-radius:50%`, `background:var(--gold)`, `color:var(--blue-800)`, bold, centered) — both `--gold` (#f0b429) and `--blue-800` (#0a0a0f) already matched the DM's spec values exactly, no new tokens needed. Verified via a DOM sweep, not just one card: all 15 instances on Kael's Your Story tab (Background + 5 Your Past cards with benefits) render identically, and separately confirmed the Ability Scores tab's 6 tooltips (one per ability) picked up the same fix automatically since it's the same shared class.
**"Why You Left" quest label**: `.cs-story-quest-label` had `color: var(--gold)` — a real mismatch with the spec, which reserves gold (`--currency-gold-coin`, a different token, used by `.cs-story-benefit-label` for "Benefits:") for Benefits-box labels only. Changed to `var(--text)` (#e8e8f0, matches spec exactly) — "Personal quest:" now renders as bold body-color text, matching the rest of that card, not mistaken for a second Benefits-style label.
Verification: DOM-measured (not just visually eyeballed) on Kael Evander — computed `font-size`/`font-weight`/`font-family` on all three section titles (32px/700/Cinzel Decorative), computed `color`/`font-weight` on the quest label (rgb(232,232,240)/700), computed size/bg/color/radius on 5 sample tooltip icons (13×13/gold/dark/50%), and real per-card heights in the Your Background row. Full spot-check repeated on Visenya Oryn (different background/past answers — Flamerage/Sohot/Measured vs. Kael's Featherflow/Nombi/Pragmatic) confirming none of the fixes are Kael-specific. Zero console errors on both characters throughout.


---

**2026-08-09 — `sheet/index.html`: fixed 5 MainInfo regressions found by comparing the live page directly against the real Figma header (node `51:1385`) — duplicate logo, AC badge not stacking, HP bar too small, Speaks block wrong structure, tab bar not full width. Commit `0a81614`.**
DM flagged these against a mockup (no image was actually attached to the request — pulled the real Figma frame instead, same source used for the ground-truth rebuild the entry above this one). Loaded the live page (Kael Evander, local dev server on :8791) before touching any code, per D2/C1, and confirmed all 5 were real, not stale-cache artifacts from last session's `tokens.css` caching issue (used a `?cb=` query-string bust each reload).
**Duplicate logo**: root cause was a deliberate choice from the ground-truth rebuild session, recorded in that session's own comment ("Duplicates the sticky nav's logo deliberately — this is a real Figma component distinct from the site-wide nav bar, not a redundant copy of it"). The DM's read of the mockup overrides that — the real Figma frame for this page shows exactly one logo, in the PageTitle component, with no separate nav-bar logo at all. Removed `.cs-nav-logo`/`.cs-nav-title` (markup + CSS) entirely; `.cs-nav` now holds only "← Return to Wiki", right-aligned (`justify-content: flex-end`). Updated the now-false comment above `sheetPageTitleHtml` that claimed the duplication was intentional.
**AC badge**: `.cs-mi-ac-badge` was `display:inline-flex` with no `flex-direction` set, defaulting to row — "AC" and "14" were laid out side by side despite the comment above the rule correctly describing the intended stacked look. This was a real gap between what a prior session's computed-style check confirmed and what the CSS actually declared — the rule never had `flex-direction:column` to begin with. Fixed: `flex-direction:column; align-items:center; justify-content:center;` plus `line-height:1` on both the label and number spans (their default line-heights were adding visible gap between the two lines that the tight Figma stacking doesn't have). Verified via `get_metadata` on the real Figma node — label and number are true stacked/centered elements, confirming the direction of the fix, not just its visual plausibility.
**HP bar**: was a fixed `120px × 10px` sliver. Pulled `get_design_context` on the real Figma bar node (`55:5981`) — it's `border-radius:999px`, height **37px** (full row height, matching the input boxes beside it), width flexible (fills whatever space is left in a 564px-wide row after the label/inputs/fraction text). Implemented as `.cs-mi-hp-slot` growing (`flex:1 1 320px`, was `0 0 auto`) so `.cs-mi-hp-compact-bar` (now `flex:1 1 auto; height:37px`) has room to expand into. Also fixed a real, separate, lower-priority violation of this file's own CSS rule (§8, "no raw hex outside `:root`") caught while touching this rule anyway — the bar's unfilled-track color was a bare `#371212`; added `--red-track` to `tokens.css` and pointed the rule at it. The fill color (`--red-strong`) was already correct — confirmed it matches the Figma fill hex (`#d62b2b`) exactly, no change needed there.
**Found but deliberately NOT changed, flagging for a DM decision**: the real Figma frame's Temp HP block also has a bar + "/ max" fraction (bicolor, cyan `#0d9ace`/`#042937`, same structure as the HP bar) — but this session's earlier cosmetic pass explicitly built and then *reverted* a Temp HP bar on the DM's own instruction, on the grounds that "5e has no real max temp HP to show a bar or proportion against," landing on a plain number-only badge instead (recorded twice in the Decision Log above). Since there's no `hp_temp_max` column and no data model for it, and the DM's own prior call was deliberate rather than a mistake, I did not reinstate a Temp HP bar just because the Figma frame happens to show one — that would silently reverse a discussed decision rather than execute the batch of fixes actually asked for. Left Temp HP as a plain input, unchanged. Flagging the conflict here rather than picking a side.
**Speaks block**: markup nested the "spoken where" sub-text inside the same flex column as the language name, with the rune glyph as a sibling centered (via `align-items:center`) against that whole two-line column — so the glyph visually landed at the seam between the two lines instead of next to the name, and the sub-text was indented to start after the glyph rather than spanning full width. Restructured to match the real Figma layout: a `.cs-mi-speaks-row` (glyph + name, inline) as one row, `.cs-mi-speaks-sub` as a second, separate, unindented full-width row below it. Also checked whether "Noto Sans Runic" (named in the DM's ask) is loaded anywhere in the codebase — it isn't, in this file or any other (`character.js`/`character.html`/`dm.html`), so there's nothing to "fix" there; it was never wired in. The rune glyphs (e.g. `ᚱᚢᚾ`) do render as real glyph shapes, not tofu/placeholder boxes — confirmed by temporarily blowing the glyph up to 80px in the live DOM and screenshotting it — via the browser's own system-font fallback for the Runic Unicode block, not a font this codebase declares. Left as-is since it's visually correct; noting the "Noto Sans Runic" name doesn't correspond to anything actually in the repo, in case the DM wants that font declared explicitly for consistency across browsers/OSes rather than relying on fallback.
**Tab bar width**: `.cs-tabbar` was rendered as a direct child of `.cs-wrap` (the page's narrow, 820px-capped default container), while literally every other section on the page (MainInfo header, page titles, Who You Are / Your Background / Your Past accordions, Ability Scores cards) already renders inside the wide `.cs-ov-breakout > .cs-ov-inner` wrapper built for the Aug 7 Overview redesign. The tab bar was the one piece never migrated into it, which is why it visibly sat inset ~290px per side relative to everything else at this viewport. Fixed by wrapping `tabBarHtml` in the same breakout markup — no new CSS needed, just adopting the existing pattern.
Verification: full reload cycle (cache-busted query string each time, not relying on HMR) against Kael Evander for all 5 fixes with DOM `getBoundingClientRect()`/`getComputedStyle()` checks (not just visual screenshots) confirming exact pixel results — AC badge `flex-direction:column` confirmed computed, HP bar 557×37px pill confirmed, Speaks row/sub rects confirmed non-overlapping and unindented, tab bar `x:20 w:1360` confirmed matching the Who You Are card's own rect exactly. Spot-checked Visenya Oryn (different species/HP-at-full/multi-glyph Conclave Script language) — bar renders correctly fully green... fully red-filled at 10/10 HP, multi-character glyph row renders correctly. Checked mobile viewport (375px) — HP bar and tab bar both reflow correctly, no overlap or overflow. Zero console errors throughout on both characters, both viewports. Committed but **not yet pushed** — awaiting DM confirmation before it goes live on GitHub Pages.


---

**2026-08-09 — `sheet/index.html`: cosmetic/structural pass against the real Figma mockup — header AC/HP/Temp HP compact row, page titles on all 4 tabs, Your Past + Your Background icons, skill tooltips, Alignment card text fix, identity pills removed, condition order fixed. Final pass this session, after the tab-content cleanup entry below. Commit `d2572ad`.**
DM handed over 7 lower-risk cosmetic/structural fixes from a mockup comparison, explicitly framed as lower-risk than the same-day tab-cleanup pass, with a request to report back (not pre-clear) since it touched more visual surface area.
**Header AC/HP/Temp HP**: was AC alone in its own row, HP's raw −/+/Apply/Max-Mod-row controls always visible taking a full-width row, Temp HP a plain text link ("+5 TEMP HP Edit"). Rebuilt into one compact row per the Figma header component (`51:1385`, re-pulled at high res for exact layout) — AC badge (untouched, only relocated), HP as a small white number badge + "/max" + a narrow colored bar, Temp HP as the same badge shape with no bar. Every editing control (`adjustHP`/`applyHPDelta`/`setMaxHPModifier`/`setTempHP`) preserved exactly, just moved behind "Edit" toggles (`HP_EDIT_OPEN`/`TEMP_HP_EDIT_OPEN`, module-level so they persist across the `renderHeaderHP()` re-render every mutation triggers) instead of always-visible. Real bug caught and fixed during this same build, before it ever shipped: an initial version rendered each toggle's panel inline inside its own flex item inside `.cs-mi-vitals`, which made the AC badge visibly jump to a different position when a panel opened (flex-wrap fighting a flex item that had just grown taller) — fixed by routing both HP's and Temp HP's edit panels into one shared `#cs-mi-hp-edit-panel` block rendered *below* the compact row instead of inline within it.
**Page title**: "YOUR STORY" added above Who You Are per the Figma frame. Checking whether Ability Scores/Inventory/Class "already" had one (per the DM's framing) turned up that none of the four tabs did — added a shared `pageTitleHtml()` helper and wired it into all four once this was confirmed, rather than leaving three tabs still missing it.
**Your Past + Your Background icons**: DM's ask was Your Past only, on the belief Your Background already had icons — reading the live code showed zero `<img>` tags in *either* section. Investigation found `character.html`'s Your Past option cards each carry a real `data-img` attribute pointing at `assets/images/past/*.webp` (26 files, one per real option value across all 7 fields, already existed, never wired into the sheet) — built `PAST_CARD_IMAGES` sourced verbatim from those attributes and a `pastCardImg()` helper, wired into all 7 `storyCard()` calls via a new optional `imgSrc` param. For Your Background (added as a DM-requested follow-up after reporting the Past/Background discrepancy back), found real sources for 3 of 4 cards by reading `character.js`: Gigglegloom Affinity from `renderGigglogloomAffinity()`'s `assets/icons/icon-{typeId}.svg`, Home Region from the region cards' own inline `background-image: assets/images/regions/img-{region}-landscape.webp`, Background from `renderBackgroundCards()`'s `assets/images/backgrounds/bg-{id}.png` — all confirmed against real files on disk before wiring in, and unlike Your Past's per-value lookup maps these three are direct filename patterns (no map needed, one template string per field) since the real key already matches the filename exactly. Moral Alignment got no thumbnail — `character.html`'s `.char-alignment-card` has no `data-img`, no `background-image`, no icon of any kind, confirmed by reading the full card markup, not assumed. Left it without a fabricated image rather than invent one.
**Skill tooltips**: new `SKILL_TIPS` (18 D&D skills) mirrored verbatim from `js/character.js`'s `renderBackgroundGrid()` (a function-local var there, not previously exposed anywhere sheet-accessible) — wired onto every skill-proficiency benefit tag via the same `.cs-ab-tip`/`data-tip` mechanism the feat tag already used, both in the Background card and in `buildYourPastHtml()`'s `skillTag()`.
**Alignment card text**: was showing `ALIGNMENT_TRAIT_SENTENCE[alignKey]` — third-person text ("They are Pragmatic — they do what works for them...") written for the header blurb, reused on the Moral Alignment card by mistake. Real second-person text exists verbatim in `character.html`'s `.char-alignment-desc` for all 5 alignments ("You do what works for you, and you try to be honest about that...") — added as a new `ALIGNMENT_CARD_DESC` dict and pointed the card at it instead. Fixed at the source per the DM's explicit instruction not to just reword it manually. The header blurb's own use of `ALIGNMENT_TRAIT_SENTENCE` is untouched — that usage was already correct.
**Identity pills + condition order**: the "ADULT, MALE, VEILSTEPPED (CHANGELING)" / "FROM NOMBI" pill row under the name was removed — confirmed genuinely redundant with the blurb sentence directly below it and absent from the Figma frame; dropped the now-fully-dead `SPECIES_PHB` lookup and `.cs-ov-pill`/`.cs-ov-pill-strong` CSS it only fed. `renderConditions()`'s pill/add-button order was reversed to put "+ Add Condition" first, matching the mockup.
**DM-reviewed follow-up, before committing**: DM asked three confirmation questions after seeing the above. (1) Temp HP — confirmed it had grown a decorative always-100%-filled bar during the compact-row rebuild (added to visually echo the new AC/HP badges); reverted to a plain number badge per the original decision recorded earlier this session ("5e has no max temp HP" — a bar implies a proportion that doesn't exist in the data model), hide-at-0/null behavior was never broken. (2) Page titles — extended to the other three tabs as described above. (3) Your Background icons — added for 3 of 4 cards as described above, Moral Alignment correctly left bare.
Verification: full before/after via `git stash`/`git stash pop` on Kael Evander (confirmed every "before" complaint was real, not misremembered — the pills row, the AC-alone row, the raw HP controls, the third-person alignment text, the reversed condition order, all present in the stashed version), plus live spot-checks on Visenya Oryn (different gigglegloom/region/background/alignment — Flamerage/Sohot/Greywitnessed/Measured vs. Kael's Featherflow/Nombi/Greywitnessed/Pragmatic — confirming the icon/text lookups aren't hardcoded to one character's data). Zero console errors throughout on both characters. JS syntax validated via `new Function()` on the extracted inline script block.


---

**2026-08-09 — `sheet/index.html`: tab-content cleanup (Gold/Equipped/Inventory/Starting Gear + Proficient Skills relocated to their correct tabs), Who You Are summary-dump bug fixed, Appearance chips repositioned, duplicate Gold editor removed. Later same-day pass, after the label-map/header-appearance entry below.**
DM flagged four structural problems left over from the tab redesign, all higher-risk than cosmetic polish, and asked for a report back before any further (lower-risk) work: (1) Gold, Equipped, Inventory, and Starting Gear were still rendering inside Your Story — never relocated when the tab shell gave Inventory its own tab, so Inventory was an empty placeholder while real content sat in the wrong tab. Moved all four verbatim to the Inventory tab (`inventoryTabHtml`, new) — same markup, same ids (`cs-gold-block`, `cs-equipped-block`, `cs-inv-*`), same `renderGold()`/`renderEquipped()`/`renderInventory()` calls, no redesign, same stopgap treatment as Spell Slots under Class. (2) Proficient Skills moved from Your Past to Ability Scores (`buildAbilityScoresHtml()` now takes `ch` as a second param) — flagged as homeless in the Aug 8 entry that built it ("no new home specified anywhere"), Ability Scores is its real conceptual home. (3) `buildSummaryCardHtml()` was rendering all six `generateSummary()` sections (Who Are You/Your Magic/Your Background/Your Past/Your Appearance/Your Compass) back-to-back in the Who You Are card — confirmed via a `git stash` before/after screenshot comparison on Kael, not assumed from reading the code alone. Fixed to show only the "WHO ARE YOU" section, reusing the existing `extractSummarySection()` helper (built earlier this session for the header's appearance paragraph) rather than adding a second parsing path — the other five sections are already fully covered by their own dedicated cards (Gigglegloom Affinity, Background, Organization, header appearance paragraph, Alignment), so this is a straight duplication fix, not a content change. (4) Appearance chips (structured height/build/age/etc.) had a real `cs-section-label` heading already, but sat as a bare `.cs-section` sandwiched between the Your Background and Your Past accordions — moved to render directly after Who You Are so it groups with the rest of the who-you-are content instead of floating between two unrelated accordions. Exact placement flagged as the DM's call to adjust after seeing it live, per their own framing.
**Follow-up finding, DM-requested before committing**: DM asked specifically whether the portrait card's Copper/Silver/Gold split-entry boxes (built in the Aug 7 Overview slice) were still live alongside the relocated Gold section, and if so whether they were the same data or something different. Read both `renderOverviewMoney()`/`onOverviewMoneyChange()` (header boxes) and `renderGold()`/`setGold()` (Inventory tab) before answering — confirmed both read/write the identical `character_sheet.gold_cp` field via the same `saveSheet()` path, and were built in the Aug 7 session to explicitly stay in sync with each other (each one's edit handler called the other's render function). Not two different features — the header did a three-field cp/sp/gp split entry, the Inventory tab does a single-total-gp entry plus a formatted read-only display, both editing the same number. That syncing-by-design made sense when both lived on the same page as a header-quick-edit / full-detail pair; once Gold moved to its own tab, the two were no longer visible together and the header boxes became a second, disconnected editor for a value now owned by Inventory. Removed the header boxes entirely per the DM's "don't leave both live" instruction: markup (`cs-ov-money-row` and its three inputs), both JS functions, both remaining call sites (`renderLivePlay()`, `setGold()`), and the now-fully-dead `cs-ov-money-*` CSS block. Left `--currency-copper/silver/gold-coin` in `tokens.css` as unused-but-defined — removing tokens wasn't asked and tokens.css tolerates having them defined without any component referencing them.
Verification: full before/after via `git stash`/`git stash pop` on Kael Evander (confirmed the Who You Are dump bug was real, not a misreading) plus live checks post-fix — Inventory tab renders real gold (8gp 8sp)/equipped Leather Armor+Longbow with correct AC math/real inventory list; Ability Scores tab shows "Perception, Survival, Animal Handling" under Proficient Skills; header money boxes confirmed gone via DOM query, Inventory Gold block confirmed still reading the correct live value afterward. Spot-checked Visenya Oryn (old-format summary, no "WHO ARE YOU" section) — confirmed the summary card still correctly renders nothing rather than erroring, matching pre-existing fallback behavior. Zero console errors throughout on both characters. JS syntax validated via `new Function()` on the extracted inline script block (equivalent to `node --check`, which doesn't work on `.html`).


---

**2026-08-09 — `sheet/index.html`: full rebuild of the 5 stale "Your Past" label maps + new `FRIEND_LABELS`; header appearance paragraph now sources from `generateSummary()` instead of the raw AI portrait prompt. Two Aug 7 files resolved per DM decision.**
**File resolution** (DM decided, not mine to call): `docs/character-sheet-redesign-handoff.md` deleted — DM confirmed it's superseded by `character-sheet-tabs-build-spec.md`. `.claude/launch.json` `git add`ed and committed as-is — DM confirmed it's the active config, no changes needed. Committed separately (`1d44a53`) before the label-map work below, since it was a fully separate, already-decided action.
**Label maps**: quantified the prior check-in's "stale" flag into real numbers before touching anything (reported to the DM first, no fixes until confirmed) — of the 5 named maps, `PET_LABELS`/`LOVE_LABELS`/`ORG_LABELS` matched **zero** of their real `character.html` option values (every key in all three was fictional), `RAISED_LABELS` matched 2 of 5 (`kind-parents`/`strict-religious` missing, plus a `streets`/`the-streets` key typo), `LEFT_BEHIND_LABELS` matched 3 of 5 (`promise`/`self` missing). `dearest_friend` had no map at all, ever — always rendered its raw stored slug. None of this looked broken on screen because `label()`'s fallback (`cap(key.replace(/-/g,' '))`) produces plausible-reading text for slug-shaped values ("wanderkeep" → "Wanderkeep") — confirmed this exact mechanism live on Kael's real card values before writing a word of the fix.
Rebuilt all 5 maps plus new `FRIEND_LABELS`, sourcing every value from `character.html`'s real `.char-option-card-label` text for each question's actual option cards (not sheet's own earlier abbreviated versions, and not guessed) — e.g. `single-parent` changes from sheet's old "A single parent" to the real card's "A single parent working too hard". Dropped 26 combined dead keys across the old 5 maps once every real value resolved correctly. `label()` itself untouched — the maps were the problem, not the lookup function.
**Header appearance paragraph**: was `ch.appearance_prompt` rendered verbatim — a raw AI-portrait-generation prompt string ("Fantasy portrait of, average height, athletic, adult, with #ffba66 skin...") with no relation in tone or content to the rest of the header. Replaced with the "YOUR APPEARANCE" section extracted from `ch.summary` — the same `generateSummary()` narrative text already shown in Who You Are — via a new `extractSummarySection()` built on a `parseSummaryBlocks()` factored out of the existing `buildSummaryCardHtml()` parser (shared logic, not a second copy that could drift from the first). `buildAppearanceDesc(app)` stays as the fallback for characters whose summary predates the six-section rewrite.
That fallback path turned out not to be theoretical: checking it live on Visenya surfaced that her `summary` (and, checked further, Hana's too) is still the *old* one-line pre-rewrite format — 186 characters, no section structure at all — while Kael's is the real 1530-character six-section version. So Visenya's header now correctly shows the structured-fallback style ("Tall, lean build. Adult. Heart face...") rather than Kael's flowing prose, not because of a bug but because there's no "YOUR APPEARANCE" block in her data to extract. DM's explicit call: leave both characters on old-format summaries for now, record the fact rather than regenerate. Logged in Current Focus so this isn't rediscovered as a mystery later.
Net effect on the appearance-triple-duplication finding from the header-alignment fix earlier this session: reduced, not just relocated. Header and Who You Are now show the *same* authored text (for characters with a regenerated summary) instead of two independently-worded retellings of the same facts — three distinct copies down to two (that shared text, plus the structured chips).
Verification: full before/after text captured live via DOM reads on both Kael and Visenya for all 6 "Your Past" cards plus the header appearance paragraph, reported to the DM in full before committing anything (per explicit instruction — no commit until confirmed). Zero console errors on either character both before and after.


---

**2026-08-09 — `sheet/index.html`: added the Inspiration badge's tooltip; fixed a real tooltip-clipping bug it surfaced (shared `wireTooltip()`, not Inspiration-specific).**
DM asked a direct status question — did the Inspiration badge get its tooltip when it was rebuilt during the header-alignment fix? No: `renderInspiration()` never set `data-tip` on the badge at all. Added it, verbatim DM-specified text: "Spend before any d20 roll — roll twice, take the higher result. You can only hold one at a time." Also added a `wireAllTooltips()` call inside `renderInspiration()` itself — every other `render*()` function that introduces `data-tip` elements this session already does this internally (`renderConditions()`, `renderClassTabDynamic()`, etc.); `renderInspiration()` was the one holdout, previously relying on whichever caller happened to run `wireAllTooltips()` afterward, which `toggleInspiration()` never did.
Verifying the fix (hover screenshot, not just a DOM `_tipWired` check — screenshots catch what DOM inspection can't) surfaced a second, real bug: the tooltip bubble rendered but its text ran off the right edge of the browser window, unreadable. Root cause: `wireTooltip()`'s positioning (`tipEl.style.left = rect.left + rect.width/2`, centered via CSS `transform: translate(-50%, ...)`) has never accounted for viewport edges — it just happened not to matter until now, because no `data-tip` element had ever sat this close to a screen edge before. The Inspiration badge does, by design, per the header-alignment fix (top-right corner of the whole header). Fixed in `wireTooltip()` itself, not special-cased per element: after appending the bubble to the DOM (so `offsetWidth` is measurable), clamp its center so the rendered bubble can't come within 8px of either viewport edge. This is a shared fix, not an Inspiration-only patch — every `data-tip` element in the file benefits from it, and the clamp is a no-op for anything not already near an edge (confirmed via the clamp math: `Math.max(half+8, Math.min(maxLeft, left))` only changes `left` when the unclamped value would already be out of bounds).
Verification: reloaded, confirmed via DOM inspection the badge carries the exact requested `data-tip` text and `_tipWired: true`. Hovered it and screenshotted — full tooltip text now visible and fully on-screen (previously cut off mid-sentence at the viewport edge). Spot-checked no console errors. Did not exhaustively re-verify every other tooltip in the file pixel-by-pixel, but the clamp logic is provably a no-op for centered elements away from an edge, so this isn't a speculative "should be fine" — it's a direct consequence of the math.


---

**2026-08-09 — `sheet/index.html`: Conditions free-text input replaced with a real picker sourced from the `conditions` Supabase table, matching the `dm.html` SCC fix from earlier the same session.**
DM asked a direct status question first — did the sheet's own Conditions UI get updated when `dm.html`'s Live Session panel was fixed to pull from Supabase? No: the MainInfo header's Conditions row (built in this session's tab-shell slice) has been free text since it shipped — type anything, one generic purple pill style for every entry, no tooltip. That slice's own CSS comment said as much at the time ("conditions are arbitrary free text so there's no per-name color source, kept to one consistent accent rather than inventing one") — true when written, no longer true now that a real `conditions` table exists.
Implementation mirrors the `dm.html` fix's pattern exactly, per the DM's explicit instruction to use it as the reference: `fetchConditionsData()` (`sb.from('conditions').select('*').order('sort_order')`) added to the same init `Promise.all` that already fetches `backgrounds`/`regions`/`species`/`languages`, populating a new `CONDITIONS_DATA` global — fetch-once-at-boot, same shape as `dm.html`'s `sccInit()` → `window.SCC_CONDITIONS_DATA`, just using this file's own `sb.from()` Supabase-client style rather than `dm.html`'s raw-REST `sccFetch()` helper (different file, different existing convention, same underlying pattern). `renderConditions()` rewritten: the free-text input + Add flow is now a `<select>` populated from `CONDITIONS_DATA` filtered to conditions not already applied (so a character can't double-add the same condition, and the picker naturally shrinks as more are added); each pill sets `--cond-bg`/`--cond-border`/`--cond-text` inline from the matching row's real columns (same CSS-custom-property technique `dm.html`'s SCC panel uses) and a real `data-tip` from the row's `tip` column, wired through `wireAllTooltips()` (the tooltip sweep built earlier this session) — conditions get real hover tooltips for the first time, not just color. Updated the CSS comment that was now factually wrong instead of leaving it to mislead the next read. `CONDITIONS_DATA` came back with 18 rows (all of `dm.html`'s 17-entry `SCC_CONDITIONS_LIST` plus `Exhaustion`, which was never added to the DM picker list) — the sheet's picker is a straight reflection of the whole table, not a hand-maintained list, so it didn't need its own separate "add Muted" step at all.
Testing surfaced a pre-existing, unrelated latent race: calling `removeCondition()` twice with zero delay between them (only possible via direct function calls in a test script, not real clicks) can let two `saveSheet()` PATCH requests complete out of order and leave stale data — confirmed this doesn't happen under normal click-driven use (any perceptible gap between actions resolves in order), and the fire-and-forget `saveSheet()` pattern itself predates this change (the old free-text `addCondition()`/`removeCondition()` had the identical risk). Not fixed — out of scope for what was asked, flagged in Current Focus as a known risk rather than silently left undiscovered.
Verification: full add/remove cycle on Kael Evander with a real wait between each action (not the zero-delay pattern that produced the race above) — added Charmed, confirmed via direct SQL read it persisted, removed it, confirmed via a second SQL read it cleared correctly. Separately added Muted + Poisoned together, confirmed via DOM inspection both pills carry correct distinct real colors and tooltips (`_tipWired: true`), confirmed the picker dropdown excludes both once applied, then removed both and confirmed the DB read clean before moving on. Clean spot-check on Visenya Oryn (fresh page load, zero console errors, `CONDITIONS_DATA` populated, no residual conditions). Before/after screenshots captured by stashing the change (`git stash`), screenshotting the live old free-text-input state, then `git stash pop` to restore — not by mentally reconstructing what "before" looked like.


---

**2026-08-09 — MainInfo header alignment fix: Conditions, Inspiration, Species/Appearance/Speaks corrected to match the real Figma frame (DM-requested follow-up to the Your Story slice's discovery below).**
Re-pulled node `51:1385` directly via `get_screenshot` rather than reusing the earlier `51:1103` full-page pull — the dedicated component node rendered at higher resolution (1024×301 vs. the full page's 802×1024 with the header as a small fraction of it), clear enough to read exact pill/badge/column treatment. Fixed exactly the three things named, nothing else:
1. **Conditions** — moved from below the Appearance/Speaks blocks into a new `.cs-mi-top-row` above the name, matching Figma's placement. Pills changed from the red danger-tint they'd inherited (visually implying "this is bad," which conditions aren't uniformly) to `--purple`/`--purple-dim`/`--purple-border` (existing tokens, not new). Considered matching Figma's per-condition mockup colors (pink "Charmed", green "Poisoned") but conditions are arbitrary free text typed by the player — there's no data source to derive a real color-per-name mapping from, and inventing one wasn't asked for, so kept to one consistent accent instead. "+ Add" button copy changed to "+ Add Condition" to match the frame's label.
2. **Inspiration** — was a pill button living inside `.cs-mi-vitals` next to the AC badge; is now a standalone 64px circular badge (`.cs-mi-insp-badge`) absolutely positioned top-right of the whole `.cs-ov-maininfo` block, dashed border when off, solid gold-tinted border + fill when on, two stacked micro-text lines ("Inspiration" / "None" or "Yes") matching the Figma circle's two-line layout. Required adding `position: relative` to `.cs-ov-maininfo` for the absolute positioning to anchor correctly instead of escaping to the page body. `renderInspiration()`/`toggleInspiration()` logic itself is unchanged — only the markup/CSS the same function outputs.
3. **Species/Appearance/Speaks** — were three stacked full-width blocks (Speaks additionally boxed in a bordered card); now a `.cs-mi-triple-row` with three `.cs-mi-triple-col` columns, each with a bold `.cs-mi-triple-label` header ("Species"/"Appearance"/"Speaks") matching the frame. Speaks lost its `--char-card-bg-subtle` box/border entirely so it reads as a plain column like the other two, not a distinct card — glyph+name+sub layout inside is otherwise unchanged. `renderSpeciesDesc()`/`renderSpeaksBlock()` async logic untouched, only their containing markup moved.
AC badge, HP bar, Temp HP badge, and the death-save swap were explicitly named by the DM as already matching — none of their markup, CSS, or JS logic was touched; `.cs-mi-vitals` now holds only the AC badge (Inspiration's removal from it is the only change to that row).
Verification: before/after full-page screenshots on Kael Evander — "before" showed the original stacked/pill-button/vitals-adjacent layout, confirming the mismatch was real and not a misreading of the earlier screenshot. For "after," added two test conditions (`Charmed`, `Poisoned`) and toggled Inspiration on via direct `SHEET`/`saveSheet()` calls to confirm both the pill row and the active-badge gold-fill state render correctly (the default clean state has neither, so neither would otherwise be visible in a screenshot) — screenshotted both states, then reverted `conditions: []` and `has_inspiration: false`, confirmed via a direct SQL read. Cross-checked the clean (no conditions, inspiration off) state on Visenya Oryn as a read-only spot check — correct 3-column layout with her own species/appearance/speaks data, zero console errors on either character.


---

**2026-08-09 — Character Sheet Tab Redesign v1: Your Story tab (fourth and final slice this session). Real Figma screenshot pulled mid-task to resolve an ambiguity, which also surfaced a header/Figma mismatch from an earlier slice.**
Before writing any HTML, pulled the actual Figma screenshot for node `51:1103` (`get_screenshot`) rather than building from the written spec's text alone — the spec's "6 chips" description for Who You Are didn't specify pill-vs-card, and the real answer data (`personality_immediate`, `seeking`, etc.) runs from short phrases to full paragraphs (e.g. Kael's `seeking` is ~250 characters); a literal small pill chip would have overflowed badly. The screenshot showed wide description cards, not pills, for all three accordions — confirmed the actual design before committing to a component shape, not guessed.
Built three accordions replacing the old pre-redesign "Overview" accordion entirely: **Who You Are** (`buildWhoYouAreHtml()` — narrative Summary card, relocated as-is from the old Overview accordion, plus 6 new/relocated cards: First Impression/Misunderstood/Makes You Laugh from `personality_immediate`/`personality_wrong`/`personality_laugh`, never displayed anywhere before; Care About/Fear/Quest relocated from the old flat story-row list). **Your Background** (`buildYourBackgroundHtml()`/`renderBackgroundCardsDynamic()` — Gigglegloom/Alignment cards relocated from the old Overview cards; Home Region is new, joins `regions` on `home_region` for `regions.summary`, async since it comes from a fetch — placeholder until `fetchRegionData()` resolves, same pattern as `CLASS_ROW`; Background card's Benefits box parses `backgrounds.bonuses`/`backgrounds.skills` via `parseBackgroundAbilityBonuses()`/`getBackgroundFeat()`, ported verbatim from `character.js` per the spec's explicit instruction — not the data, just the two parsing functions — verified against Kael's real Greywitnessed row (`bonuses: ["+2 Wis","+1 Str","Alert"]`, `skills: ["Arcana","Survival"]`) before trusting the port). **Your Past** (`buildYourPastHtml()` — 7 cards, Benefits boxes filter `player_characters.skill_bonuses` by `source` prefix (`past_raised:`/`past_friend:`/`past_org:`), confirmed against Kael's real row (e.g. `source: "past_raised:single-parent"`) before writing the filter, not assumed from the spec's description).
Two DM decisions from the check-in before this slice, both implemented as directed: **Why You Left's "Personal quest" line** — built as a static `WHY_LEFT_QUEST` mirror (DM chose static-mirror over new-column or skip). Sourced verbatim from `character.html`'s real `data-effect` attributes on the why-left option cards — discovered while looking for the source text that only **5** real option values exist there (`ran-from-something`/`received-message`/`restlessness`/`saw-the-grey`/`someone-disappeared`), while the existing `WHY_LEFT_LABELS` mirror in this file has 7 keys, including `called-to-wander`/`forced-out`/`opportunity` which don't correspond to any real character-creator option. This is the same "stale label mirror" issue the Aug 7 log already flagged for `RAISED_LABELS` — pre-existing, predates this session, not fixed here (degrades gracefully via `label()`'s cap-and-format fallback rather than breaking, so it's a polish gap, not a bug). **Spell Slots** — see the entry below, done earlier in this same session.
**New finding from pulling the real screenshot, flagged not silently fixed**: the MainInfo header's actual Figma layout differs from what shipped in the tab-shell slice earlier this session. Real design: Conditions render as inline colored pills near the top of the card (not below Appearance where they landed); Inspiration is a small circular icon top-right (not the pill-button toggle built); Species/Appearance/Speaks render as a labeled 3-column row (not stacked full-width paragraphs). The header slice's own Decision Log entry already flagged this as a risk ("visual polish vs. Figma should be a follow-up refinement pass") — this confirms it concretely rather than leaving it a suspicion. Not reworked here: three already-shipped, already-reviewed commits' worth of header weren't today's ask, and Your Story's own three accordions render correctly independent of the header above them. Flagged in Current Focus as a candidate for a dedicated pass.
Also removed: the old standalone "Pending From Your DM" section, now fully superseded by `dm_pending_items` rendering inside the Organization card's Benefits box (its real Figma-matched location) — kept both, would have been visible duplication. "Proficient skills" (`player_characters.skills`, class-chosen at Stage 2 — distinct from the flat +1 `skill_bonuses` now shown per-card) has no specified home in the spec's card table, so kept as a small standalone row under the Your Past cards rather than dropping real data or inventing a new card design for it unprompted.
New CSS: `.cs-story-card`/`.cs-story-cards`/`.cs-story-benefits`/etc., reusing only existing tokens (`--blue-700`, `--radius-lg`, `--gold`, `--char-ov-gap-*`, `--char-accent-tint`) — no new tokens needed beyond `--blue-500` already added for the Class tab.
Verification: full-page screenshots (tall viewport, fresh tab — the established working pattern from the Class tab slice) against Kael Evander (Ranger) and Visenya Oryn (Warlock) — 6/4/7 cards each, correct Benefits boxes, `BACKGROUND_ROW`/`REGION_ROW` both resolving correctly post-fetch, zero console errors on either. Read-only display work — `backgrounds`/`regions` fetches, no new writes — so no test-data cleanup was needed.


---

**2026-08-09 — Character Sheet Tab Redesign v1: Class tab (third slice, same session as the two entries below). Two build-spec inaccuracies found and flagged rather than silently worked around.**
Moved the class summary card (image/name/level + Primary/Saves/Casting/Armor/Tools/Weapons) out of Your Story's Overview accordion into the real `#cs-tab-class` content (`buildClassTabHtml()`), same relocation pattern as the Ability Scores slice. Added a Hit Die row (new — `classes.hit_die` wasn't displayed anywhere on the sheet before) and `?` tooltips on Hit Die/Saves/Weapons only (`hit_die_tip`/`saves_tip`/`weapons_tip`), reusing the `.cs-ab-tip` element/style built for Ability Scores rather than inventing a second tooltip-icon pattern. Primary and Armor stay tooltip-free, matching `character.html`'s `.char-class-trait` exactly, per the spec's explicit "don't invent tips for them."
Key Features: ported `FEAT_ICONS` verbatim from `character.js`'s `renderClassGrid()` (~49 regex→icon pairs plus fallback; literal emoji converted to `&#decimal;` entities per C2, since this file's convention forbids literal emoji in JS strings). Wrote a new `parseClassFeature()` — split on `" — "` into name/description, then strip `" subclass at level N"` or `" at level N"` off whichever string currently holds the name (handles both bare entries like "Ranger Archetype subclass at level 3" and combined ones like "Otherworldly Patron subclass at level 1 — your patron defines..." where the level phrase sits on the name side of the dash). Verified this against all 72 real feature strings pulled from Supabase (not the character.js mirror, and not just eyeballed — ran it through Node) before writing any HTML around it; every entry parsed cleanly, including the trickiest dash+subclass+level combination. Alternate-row shading is pure `.cs-class-feature-row:nth-child(even)` CSS against a new `--blue-500` token — pulled the real hex (`#28304a`) from the Figma file itself via `get_variable_defs` on node `52:2705` rather than guessing or interpolating between the two blues already in tokens.css.
Two things the build spec got wrong, caught by verifying against real code/data instead of trusting the doc (CLAUDE.md's standing "if what you find conflicts with project knowledge, stop and say so" rule): (1) the spec says Key Features' icon+level parsing is derived "via the same pattern the character creator already uses" — false; `character.js` only assigns an icon and renders the raw string whole, it has never split name/description/level apart. Only the icon table was an actual port; the parser is new code written for this session, just conforming to the algorithm the spec described. (2) the spec says there are "66 features... 7 of the 66" bare subclass entries — real count from Supabase is 72 total (12 classes × 6, not 11 × 6) with 8 bare entries, not 7 (missed Wizard's "Arcane Tradition subclass at level 2"). Neither error changes anything about the implementation since the parser handles all 72 correctly regardless of what the spec counted, but both are now on record so the spec doc itself doesn't keep getting cited as ground truth on these two points.
Confirmed NOT building, not a silent gap: the spec describes the Class tab's Spell Slots section as relocating "existing `character_sheet.spell_slots`/`spell_slots_max` UI... as-is, no redesign yet." Grepped the entire codebase (`sheet/index.html`, `dm.html`, `character.html`, `js/character.js`) for `spell_slots` — zero matches anywhere. Both Supabase columns exist (confirmed via `information_schema`), but no UI was ever built to read or write them, on this sheet or anywhere else. There is nothing to relocate. Building one from scratch is real design work outside "no redesign yet" and needs a product call, not an engineering guess — left undone, flagged here and in Current Focus rather than either skipping silently or inventing a design unprompted.
Verification: full-page screenshots (not DOM-only — the DM specifically asked for this after the Ability Scores slice hit a Browser-pane rendering glitch on scroll) against two different classes on two real characters — Kael Evander (Ranger: mostly non-subclass dash entries plus one bare subclass line) and Visenya Oryn (Warlock: every subclass-tier feature is the combined dash+level case) — both rendered correctly, tooltips confirmed wired via DOM inspection (`_tipWired: true` on all three). The earlier screenshot glitch didn't recur once each check used a fresh tab and a tall (1400×2200) viewport instead of scrolling a fixed one; noted as the working pattern for future full-page checks in this environment, not a code issue. No Supabase writes in this slice (read-only display), so no test-data cleanup was needed.


---

**2026-08-09 — Character Sheet Tab Redesign v1: Ability Scores tab (second slice, same session as the tab shell/header entry below).**
Moved `buildAbilityScoresHtml()`'s output from being embedded inside Your Story into the real `#cs-tab-abilities` content block — it's now the Ability Scores tab's actual content, not a placeholder. Stripped the collapsible-accordion chrome it had when it was squeezed among other Your Story content (a dedicated tab doesn't need a "Collapse" toggle); the 6 ability cards render directly.
Two changes required by the build spec, both done: (1) added a `?` tooltip icon per ability card, text pulled verbatim from `character.html`'s `.char-ability-name` `data-tip` copy — richer than anything the sheet had before, so this is new UI, not a merge of an old mirror. New `wireAllTooltips()` (`document.querySelectorAll('[data-tip]').forEach(wireTooltip)`) replaces the old pattern of manually calling `wireTooltip(el)` at each individual call site (the AC badge was the only other user); `wireTooltip()` itself is unchanged and still idempotent via `el._tipWired`, so old and new call styles can't double-wire. (2) Feats no longer render on this tab — moved to the Background card in Your Story's Overview accordion (`buildOverviewCardsHtml()`'s `bgCard`), per the spec's explicit "Feats have moved off this tab" instruction. Reused the existing `.cs-feat-list`/`.cs-feat-chip` CSS as-is (was about to go dead after the move — now has a new home instead) and `FEAT_TIPS`, `ch.feats` — no new data plumbing needed, `ch` was already in scope in `buildOverviewCardsHtml()`.
Verification: live reload against Kael Evander, confirmed via direct DOM/JS inspection (not just visual screenshot, which hit an unrelated Browser-pane rendering glitch mid-session on scroll) — 6 ability cards present with correct per-ability tooltip text, all 7 `[data-tip]` elements on the page wired (`_tipWired: true`), zero `.cs-feat-list` elements left in the Ability Scores tab, exactly one Alert feat chip now on the Background card, tab-switch state (`hidden` attribute, active button class) correct. No new console errors — the 4 stale `ERR_CONNECTION_REFUSED` entries present throughout this session are leftover from before the local static server was started, not a code issue. No Supabase writes involved (this slice is read-only display), so no test-data cleanup needed.


---

**2026-08-09 — Character Sheet Tab Redesign v1: tab shell + shared MainInfo header (first slice). `js/hp-calc.js` confirmed real and committed.**
Built per the new Character Sheet Tab Redesign v1 build spec (Figma `55:5629`), scoped to exactly two pieces per DM instruction: the 4-tab shell (Your Story / Ability Scores / Inventory / Class — `ACTIVE_TAB` state, click handlers, show/hide, no data changes) and the shared `MainInfo` header (Figma `51:1385`, built once via `buildMainInfoHtml()` and reused above all four tabs rather than duplicated per tab). Only Your Story carries real content this pass — everything that already existed on the page moved into its content block unchanged; the other three tabs render a "coming soon" placeholder until their own build slices land.
Header additions, each backed by real data: species paragraph (`species.description`, populated 12/12 in the Aug 9 Supabase session below), Speaks block (new `languages` table, joined on `language_extra` — glyph/name/spoken_where), Conditions (+Add/remove-×, `character_sheet.conditions`), Inspiration toggle (`has_inspiration`), Temp HP as a plain badge that hides at 0/null (5e has no max temp HP), and a death-save pip tracker (`death_save_successes/failures`) that swaps into the HP bar's slot at 0 HP rather than appending below it. AC badge and HP bar are relocations of existing logic — `computeAC()`/`computeWeaponDamageHtml()` factored out of the old `renderEquippedAC()` so the math isn't duplicated between the header and the (still-present) Equipped section; HP math in `adjustHP()`/`setMaxHPModifier()`/`setTempHP()` is unchanged, only its render target moved.
Bug found and fixed during required browser verification, not in the original spec: the death-save view had no HP control at all, so a character at 0 HP had no way to ever heal back up through the sheet. Fixed by adding a minimal heal input next to the pips (`applyHPDelta()`, unchanged); crossing back above 0 now also resets both death-save counts to 0, matching the 5e rule and required for the bar/death-save swap to make sense the next time HP hits 0.
`js/hp-calc.js`: found untracked at session start, not mentioned anywhere in this log. Traced it fully before touching anything HP-related (per the DM's explicit ask) — confirmed via `git diff` on `dm.html`/`sheet/index.html` that it's a real, load-bearing shared module (`computeHpBase`/`reconcileHp`) extracted from logic that used to live duplicated inline in `sheet/index.html`, referenced by `<script src>` in both files, and actively called by both. Verified live against Supabase (not assumed) that every column it touches — `hp_con_mod_cached`, `hp_temp`, `hp_max_modifier`, `conditions`, `has_inspiration`, `death_save_successes/failures` — already exists, so nothing here is blocked on a pending migration. Root cause of it being untracked: a prior session did the refactor but ended without running `git add`/commit or writing a Decision Log entry. Committed tonight together with `dm.html`'s matching `reconcileHp()` call in `sccLoadPlayers()` (SCC player bar), since neither file works correctly without `hp-calc.js` and the two files' changes are one coherent unit.
Verification: live end-to-end against Kael Evander's real Supabase data, including a full HP→0→death-save→heal-back cycle (confirmed pip persistence, swap-back, and the reset via direct SQL read), then all test mutations (conditions, inspiration, HP) reverted to original values, confirmed via a second SQL read. Cross-checked Speaks/species rendering against Visenya Oryn (different species/language) as a read-only spot check.
Two things found but explicitly NOT touched, flagged instead: (1) pre-existing appearance-content duplication — `appearance_prompt` (when set) already restates the same fields the Appearance-section chips show, and a third version exists in the Overview summary card; relocating the paragraph into the header made the redundant copies more visually distant, not more redundant — left for the real Your Story tab build. (2) `docs/character-sheet-redesign-handoff.md` and `.claude/launch.json` are both still untracked from the Aug 7 session (the latter despite that session's Decision Log entry claiming it was "added") — unrelated to tonight's work, left uncommitted pending a separate decision.


---

**2026-08-08 — Backfilled `skills` for the 3 characters; added its display to the live sheet.**
DM provided each player's final chosen class skills directly (no re-collection needed, unlike the other backfills — these were simply given). Casing verified against `CLASS_DATA[cls].skills_list` (same Title Case convention `skill_bonuses` already uses) before writing, not guessed. Backfilled: Hana (Druid) Nature, Insight; Kael (Ranger) Perception, Survival, Animal Handling; Visenya (Warlock) Arcana, Deception.
Display: `sheet/index.html` had no UI for `skills` at all (confirmed dead column in the entry above) — added a "Proficient skills" row in the Your Past story block, right next to the existing "Bonus skills" row, same `.cs-story-row` pattern, no new CSS. Verified against all 3 real characters' real data.


---

**2026-08-08 — Full character-creator field audit: no further data-loss bugs found; 3 dead fields identified.**
After four data-loss bugs found one at a time this session (six `past_*` fields, then class skills), did a systematic pass instead of waiting for the next one to surface by accident. Enumerated every `<input>`/`<select>`/`<textarea>`/checkbox in `character.html` (grep-verified complete), traced each into `CHAR_STATE.draft`, confirmed every key `buildCharacterEntry()` reads matches a real `player_characters` column in both directions (no orphaned keys either way), then ran a live test — one fully-populated draft covering every field, through the real `buildCharacterEntry()`, 32 assertions, all passed. Result: the class-skills fix was the last one: every other field across all 5 stages is confirmed correctly reaching Supabase.
Found 3 unrelated dead fields — different failure mode, not data loss, since no UI ever collected them so there's nothing to have lost: `player_email` (no email input exists anywhere in the creator), `alignment_trait` (`selectAlignmentTrait()` and `.char-alignment-trait` exist in `js/character.js` but nothing in `character.html` ever calls/renders them — looks like a per-alignment-card sub-choice that was designed but never wired up), `appearance_data.glasses` (collected by `collectAppearanceData()`, but no `app-glasses` element exists). All three are always empty/null for every character, past and future, until someone builds the missing UI — not a bug fix candidate, a product decision if ever wanted.


---

**2026-08-08 — Fourth data-loss bug: class skill proficiencies never persisted.**
DM noticed Kael Evander's `skills` array was empty in Supabase despite ranger requiring 3 skill picks. Confirmed `validateStage(2)` in `js/character.js` blocks progression until `skills_count` skills are chosen, so this wasn't skipped — same failure class as the three `past_*` bugs (data collected correctly in the draft, never reaches the DB), but a different specific mechanism: `CHAR_STATE.draft['skills_' + classId]` (e.g. `skills_ranger`) is correctly read by the live preview, Stage 3 panel, and Stage 2 validation, but `buildCharacterEntry()` never read it at all — not a wrong-key typo, a missing field entirely. Confirmed systemic: Hana and Visenya also have `skills: []`. Also confirmed `player_characters.skills` is a fully dead column — not read anywhere in `sheet/index.html` or `dm.html` either, so this had zero visible symptom on the live sheet.
Fix: `buildCharacterEntry()` now writes `skills: d.class_id ? (d['skills_' + d.class_id] || []) : []`. Applies to all future characters.
Recoverability (checked, not guessed): none. Unlike "Your Past," the specific skill checkboxes chosen live only in the browser's `localStorage` draft, which `submitCharacter()` deletes on success — no server-side snapshot exists at any point, in the DB, the GitHub-dispatch payload, or anywhere else. This happened at Session Zero (2026-07-06); nothing to query now. Only path to recovery is asking each player directly what they picked (same remediation as "Your Past") — DM has not yet decided whether to re-collect. Per-character options if they do: Kael (Ranger, choose 3): Animal Handling, Athletics, Insight, Investigation, Nature, Perception, Stealth, Survival. Hana (Druid, choose 2): Arcana, Animal Handling, Insight, Medicine, Nature, Perception, Religion, Survival. Visenya (Warlock, choose 2): Arcana, Deception, History, Intimidation, Investigation, Nature, Religion.


---

**2026-08-08 — UI clarity fix; "Your Past" fully backfilled from DM-recollected answers; second starting-gold bug found and fixed.**
UI: `dm.html`'s pending-items "Mark Added" button read as if it might auto-grant the item. Renamed to "I Added This" with a `title` tooltip spelling out that it's a manual checklist clear, not a grant — matches dm.html's existing pattern of pairing action buttons with `title=` hints.
Your Past backfill: the DM re-collected all six previously-lost answers directly from Hana, Kael, and Visenya. Matched each to its exact `data-value` from `character.html`'s option cards (not guessed), cross-checked against each character's already-correct `organization_joined` as a sanity check. Applied the resulting mechanical effects via the existing `PAST_EFFECTS`/`computeSkillBonuses`/`computeFinalAbilityScores` system from the entry above — each character's already-applied organization bonus was left untouched, only the newly-unlocked `raised`/`friend` effects were added on top. Verified every number via the real live functions in a local browser (fed each character's actual stored data through `computeSkillBonuses()`, not hand-derived) before writing to Supabase, then re-verified the live sheet rendered the final values correctly for all three.
Gold bug #2 (found during the required pre-fix audit, not previously known): `sheet/index.html`'s `BG_GOLD` table — used to seed `character_sheet.gold_cp` on first sheet load — was a flat 50gp/75gp placeholder completely disconnected from `ANAVALE_BACKGROUNDS[bg].starting_gold` in `js/character.js` (the real 5–25gp PHB-style values shown to players during creation). Combined with the already-known "Stage 4 shopping spend never subtracted" bug, every character's seeded gold was significantly higher than intended, regardless of shopping. Audited all 5 creation stages (class, species, ability scores, gear, review) confirmed no other stage affects starting gold — only background `starting_gold` and Stage 4 shopping do, plus the imagined-past `merchant-guild` org bonus (already correctly isolated). Also confirmed: adding an inventory item on the live sheet never auto-deducts `gold_cp` anywhere in the codebase — this path doesn't exist yet, so there's nothing to reconcile there today.
Fix: renamed `BG_GOLD` → `BG_STARTING_GOLD` with the real per-background values (mirrored from `character.js`, same pattern as `FEAT_TIPS`). New `player_characters.starting_gold_spent_cp` column, written by `buildCharacterEntry()` as `calcGoldSpent()` at submission (that function only exists in `character.js`'s live DOM-driven form — the persisted value lets `sheet/index.html` use it without duplicating the shopping-cost logic). Seed formula is now `(real background gold) − starting_gold_spent_cp + starting_gold_bonus_cp`, applies to all future characters.
Backfill: DM confirmed none of the three characters have real play history on their gold yet (Kael's low balance was a manual DM test value, not session spending) — so all three got a clean direct set to their true correct amount, verified via the same live-function replay used for the original gold audit: Hana 900cp (9gp), Kael 880cp (8.8gp), Visenya 5400cp (54gp, preserving her already-correct +50gp merchant-guild bonus).


---

**2026-08-08 — Fixed "Your Past" data-loss bug; built imagined-past mechanical effects; backfilled 3 existing characters.**
Bug: `collectStage1Data()` in `js/character.js` stores the seven "Your Past" answers as `past_raised`/`past_friend`/`past_pet`/`past_love`/`past_org`/`past_left-behind`/`past_why-left`, but `buildCharacterEntry()` read them from `who_raised_you`/`dearest_friend`/etc., which were never set — every submitted character silently lost six of seven answers (confirmed via Supabase: all 3 real PCs had identical null pattern). Same wrong-key bug also found and fixed in two preview-only functions (`renderSummaryCard()`, `renderStage3Panel()`) that had never shown their "+1 skill from imagined past" chips as a result.
Feature: the "Your Past" cards' effect text (e.g. "+1 Wisdom, +1 History proficiency", "+50gp starting gold") was preview-only — nothing applied it. Built `PAST_EFFECTS` (single source of truth in `js/character.js`, replacing the old `PAST_SKILL_LOOKUP`/`PAST_SKILL_GRANTS` dicts, which disagreed with each other and with the card copy — `kind-parents` now correctly grants Insight) plus shared helpers `computeFinalAbilityScores()`, `computeSkillBonuses()`, `computeStartingGoldBonus()`, `computeDmPendingItems()`, `computeFeats()`, used by both the live creator preview and `buildCharacterEntry()` so they can't drift. New `player_characters` columns: `skill_bonuses` (flat +1s, explicitly not full proficiency — matches existing tooltip copy), `starting_gold_bonus_cp`, `dm_pending_items`, `feats`. `ability_scores` now stores the *final* value (raw + background bonus + imagined-past bonus) at submission — previously background bonuses were computed live in two duplicate places and never persisted or reached `sheet/index.html` at all; this also fixes that for all future characters. `sheet/index.html`'s `fetchOrCreateSheet()` now folds `starting_gold_bonus_cp` into the initial `gold_cp` seed and adds the Tough feat's +2 HP/level to the initial `hp_max` calc. New "mentor" friend-answer skill choice (History/Medicine) built as a picker modeled directly on the existing kept-to-myself org picker (`friend-mentor-skill-picker` in `character.html`, `selectMentorSkill()` in `character.js`). Minimal DM-facing "pending items" list added to `dm.html`'s SCC player bar (`sccMarkPendingItemAdded()`) for DM-add items like "Wanderkeep Field Kit" — nothing auto-grants these.
Backfill (DM-approved, one-time): Hana Sable, Kael Evander, Visenya Oryn each got their background ability bonus + feat applied (all 3 are the entire `player_characters` table) and their surviving `organization_joined` answer's skill bonus/feat/DM-item applied — the other six lost past-answers were NOT guessed and remain unset until each player re-picks. Visenya's merchant-guild "+50gp" was added on top of her current `character_sheet.gold_cp` (mid-campaign, not a reset). Hana's `hp_max`/`hp_current` recomputed +2 for Tough. All values verified end-to-end in a local browser session (no test data written to production) before the real Supabase writes.
Sheet display (same session, requested as a follow-up before this was considered done): `sheet/index.html` now shows all three new fields, since storing them correctly isn't enough if a player can't see them. `skill_bonuses` → new "Bonus skills" row in the existing Your Past story block (`.cs-story-row`, no new CSS). `feats` → new chip list (`.cs-feat-chip`, new CSS) in the Ability Scores accordion, each with its rules text from a new `FEAT_TIPS` lookup mirrored from `character.js` (sheet doesn't load that file — same mirroring pattern as `ANAVALE_BACKGROUNDS`/`ALIGNMENT_PHB`/etc.). `dm_pending_items` → new "Pending From Your DM" section reusing the existing `.cs-gear-item`/`.cs-gear-grid` styling, positioned right after Starting Gear, only rendered when non-empty. Verified against all 3 real characters' actual post-backfill data (Hana: Tough chip + HP tooltip text, +1 Athletics, "Common weapon of choice"; Kael: Alert chip, +1 Survival, "Wanderkeep Field Kit"; Visenya: Alert chip, +1 Persuasion, no pending section, 100gp).


---

**2026-08-07 — Character sheet redesign: Overview + Ability Scores (docs/character-sheet-redesign-handoff.md).**
Implemented per Figma `P0tcVgf02XeI13Tz0TtRaX` (36:310 desktop / 36:435 mobile). Replaced `cs-hero`/`cs-type-banner`/most of `cs-identity-grid` in `sheet/index.html` with the new Overview (portrait+money+weight card, name/pills/blurb, alignment/background/gigglegloom cards, class card) and Ability Scores accordions — DM confirmed replace-not-duplicate, wide breakout container scoped to just these two sections, photo upload kept. New tokens added to `css/tokens.css` only where no existing token matched. Currency (cp/sp/gp inputs) and the weight bar both wired to existing `gold_cp`/inventory-sum logic, verified live against real Supabase data (token test against "Kael Evander"). Class card reads a new `classes` table that doesn't exist yet — renders placeholders until the migration (drafted, schema in handoff §6, tools/casting_summary content awaiting DM sign-off) is approved and run. `generateSummary()` in `js/character.js` rewritten to the six-paragraph narrative spec in handoff §8 (gendered pronouns, organic length, empty fields omitted) — required changing `#char-auto-summary` from `<p>` to `<div>` in `character.html` to hold multiple paragraphs. Found but did not fix (pre-existing, out of scope): species-portrait 404 (`sheet/index.html` portraitSrc uses the wrong filename pattern — real files are `sp-{species}-{suffix}.png` not `{species}-{suffix}.webp`), a duplicate `.char-summary-body` CSS rule in `character.css`, and stale `WHY_LEFT_LABELS`/`RAISED_LABELS` option values in `sheet/index.html`'s untouched "Your Past" section. Added `.claude/launch.json` for local static-server preview (new, wasn't there before).


---

**2026-07-23 — Equipped section added to sheet/index.html.**
New Equipped section (armor/shield/main hand/off hand) rendered between Gold and Inventory in `renderCharacter()`, populated live via `renderEquipped()` (called from `renderLivePlay()` alongside `renderHP()`). Reads/writes `character_sheet.equipped` jsonb; item stats fetched live from `items` by id into `EQUIPPED_ITEMS`. AC computed dynamically in `renderEquippedAC()`: base 10, armor `base_ac` + dex-by-weight-class (heavy = none, medium = capped at `max_dex_bonus` ?? 2, light/none = full dex), shield `ac_bonus` (default 2), plus `magic_bonus` from armor/shield. New equip-search modal injected into `#cs-modal-root` (never inside `.cs-wrap`, per the modal rule), filtered by category per slot. All new symbols use HTML entities (`&#8230;`, `&#10005;`), not literal emoji — existing literal emoji elsewhere in the file (📝, 🗺, etc.) predate this and were left alone. HEAD after push: `be4ecec`.


---

**2026-06-30 — Character sheet built. Keep-alive workflow created.** HP, Inventory, Notes in `sheet/index.html`. Keep-alive pings Supabase every 3 days.

