# Decision Log — General / Cross-Cutting / Process

*Split out of `CLAUDE.md`'s single Decision Log on 2026-08-25 — see that file's "Decision Log — Split Across Files" index for the full convention. This file holds only entries classified as **General / Cross-Cutting / Process**; entries genuinely spanning multiple subsystems, or that don't fit one area, live in `decision-log-general.md` instead. Append-only, most recent entry at top — same convention as before the split, unchanged.*

---

**2026-08-21 (new session, process note — two "how an NPC should sound" systems, not a naming collision) — `CLAUDE.md`.**
Confirmed with the DM: `[Voice: ...]` bracketed text inside `read_aloud` is pre-existing DM performance direction — how to act the line out loud at the table, nothing to do with audio. The new `elevenlabs_voice_id`/`voice_notes` columns (built on the still-uncommitted `npc-voices` branch, confirmed via a read-only investigation of that worktree's diff — not modified, per explicit instruction not to touch active work in progress) pick an actual synthesized voice for TTS playback, a different concept entirely. The branch's own Preview Voice feature (`sccStripVoiceDirection()`) already strips `[Voice: ...]` text before sending it to ElevenLabs, so the two never actually overlap. Both stay — genuinely different purposes, not something that needs reconciling.


---

**2026-08-20 (continuing the same session — process addendum, a removed worktree's session can't take further prompts, no code changed) — `CLAUDE.md`.**
Repeated mistake tonight: suggesting a follow-up prompt go back to a session whose worktree had already been removed — the folder was gone, so there was nowhere for that prompt to land. Added a rule right after the "Feature branches don't touch CLAUDE.md" one, since it's the same underlying shape of mistake (routing work to the wrong place): once a Code session removes its own worktree, that session is done and cannot receive any further prompts, no matter how small the follow-up. Anything that comes up afterward — deleting the now-unused remote branch, any other cleanup — goes to the designated `Anavale-wiki-MAIN-3` worktree instead, never back to the session that just removed itself.


---

**2026-08-20 (new session, process addendum — feature branches stop touching CLAUDE.md, no code changed) — `CLAUDE.md`.**
Several branches landed close together tonight and CLAUDE.md conflicted on the merge every single time — not because any two branches' real work overlapped, but because every branch was independently inserting a Decision Log entry at the top of the log and a Current Focus update at the top of that section, the same two anchor points, regardless of what file the actual feature touched. Added a new rule to §1: feature-branch sessions skip End items 1–4 entirely (no Decision Log entry, no Current Focus edit, no self-correction edits, no bug-pattern logging) — the session's final report to the DM carries that same content instead, just not committed to the file yet. A single consolidation pass, run afterward on the designated `Anavale-wiki-MAIN-3` worktree once a batch of parallel branches has finished merging into `main`, adds the whole batch's entries in one commit directly on `main` — nothing else is touching the file at that point, so there's nothing left to conflict with.


---

**2026-08-19 (new session — Live Session accordion converted to horizontal tabs, closes `requests` row `a7759f51`) — `dm.html`.**
Ask: convert the Live Session tab's right-column accordion (NPCs/Locations/Quests/Scenarios/Field Guide) from stacked expand/collapse sections into horizontal tabs at the top of that column — one section's content visible at a time, switching via the tabs, same content per section as today. Left column (Party HP/Conditions/Current Beat/Die Roller/Timer/DM Safety Net) explicitly out of scope, confirmed untouched.
Read the current accordion markup/JS/CSS first, per C1: 5 `.scc-collapsible-toggle.scc-live-acc-toggle`/`.scc-collapsible-body.scc-live-acc-body` pairs inside `.scc-live-accordion`, toggled by `sccLiveToggleSection(name)`/`sccLiveOpenSection(name)`, with `sccLiveApplyAutoExpand()` auto-opening Quests once per session load when a real in-progress beat exists (guarded by `SCC_LIVE_ACC_AUTO_APPLIED`, reset on every session load/completion). Confirmed via grep this accordion mechanism is fully independent of `.scc-tab-btn` (the top-level Live Session/Combat/Notes sub-nav one level up) and of `sccSwitchPanel()` — no shared state to conflict with.
**Markup**: 5 toggle+body pairs replaced with one `.scc-live-tabs` bar (5 `<button class="scc-live-tab-btn" data-live="...">`) followed by 5 `.scc-live-tab-panel` divs — same ids (`scc-live-acc-body-*`) and same inner content, byte-for-byte untouched between each panel's open/close tags. NPCs starts `.open`/`.active` as the default tab (the old accordion started with none expanded, which isn't a valid resting state for tabs — picked the first tab as the sensible default, not asked separately since it's the only reasonable reading of "one section's content shows at a time").
**JS**: `sccLiveToggleSection()`/`sccLiveOpenSection()` replaced with one `sccLiveSwitchSection(name)` (toggles `.active` on the matching button, `.open` on the matching panel, closes every other one — exclusive by construction, not by comparison to the old accordion's multi-open capability, which nothing depended on). Field Guide's lazy-load-on-first-view (`sccRenderFieldGuide()`/`sccFgInitDocument()`) is preserved verbatim inside the new function, confirmed idempotent/cached (`SCC_FG_DATA`/`SCC_FG_DOC_CACHE`) so re-switching to it repeatedly is cheap. `sccLiveApplyAutoExpand()` kept its exact guard/trigger logic, just calls `sccLiveSwitchSection('quests')` instead of opening a section — verified live via a safe client-side-only simulated in-progress beat (no Supabase write) that this correctly switches the active tab away from wherever the DM was.
**CSS**: new `.scc-live-tabs`/`.scc-live-tab-btn`/`.scc-live-tab-panel` reuse the exact token set `.scc-tab-btn` (the pattern one nav level up) already established — `var(--dm-radius-sm)`, `var(--dm-gold-dim)`, `var(--scc-border-gold)`, etc. — no new tokens invented. Field Guide's mint-green modifier kept the accordion toggle's original hex (`#6ee7b7`/`#a7f3d0`) for continuity. The Field Guide panel's `height:70vh`-when-active rule (needed for its own 2-pane internal layout) was re-pointed from the old `.scc-collapsible-body.scc-live-acc-body--fieldguide.open` selector to `.scc-live-tab-panel--fieldguide.open` — verified via computed style it still resolves correctly (504px = 70vh of a 720px viewport, `display:flex; flex-direction:column`). Updated two now-stale code comments (the sticky-overflow fix's own reasoning, and the removed-audit-mode-pointer-events-block explanation) that referenced "accordion sections" to describe the new tab reality instead, rather than leave them misleading for a future read.
**Audit mode**: confirmed nothing needed changing — the existing `.scc-audit-mode` rules never targeted `.scc-collapsible-toggle`/`.scc-live-acc-toggle` (removed on purpose in an earlier session, since a toggle/tab never writes, only the actions inside each panel are individually gated by `SCC_VIEWING_HISTORY`) — same reasoning now applies to the tab buttons unchanged.
Verified live end-to-end against real Session 1 data on a fresh local server (started on a new port after confirming several already-running servers on this machine belonged to other sessions, not this worktree): all 5 tabs render their real content (11 real NPCs, real locations with health badges, the real "Something Is Wrong With Gerald" quest card, all 8 real scenario groups, the real Field Guide document) with exactly one panel/button active at a time on every click, confirmed via DOM inspection not just screenshots. Auto-expand-to-Quests confirmed via a safe simulated in-progress beat (client-side only, reverted after, zero Supabase writes). Audit/history mode confirmed via a safe synthetic session (pushed into `SCC_ALL_SESSIONS` client-side only, never touching Supabase) — entered audit view, cycled through all 5 tabs successfully while locked, then returned to the current session and confirmed `SCC_ALL_SESSIONS`/`SCC_SESSION` were back to exactly the one real session with `scc-audit-mode` removed. Zero console errors throughout. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty. Matching `requests` row (`a7759f51-1244-407c-8c6d-45247bafa99c`, "Live Session accordion → horizontal tabs") marked `done`.


---

**2026-08-19 (new session, `anavale-wiki-live-session` branch — 5 direct DM edits to the Session Command Center: Quick Actions removed, Inspiration control retired, two-column Live Session layout, Quick Note FAB scoped to the SCC page, Quests dropped from the Manage tab) — `dm.html`.**
All 5 items were real, already-tracked `requests` rows (`fa2250b7`/`dd959055`/`895c6924`/`33e593f2`/`2b979ea5`, area `scc-edits`) — confirmed via SQL before starting, all `not-started`, all updated to `done` at the end of this session as part of finishing the task.
**1 — Quick Actions removed.** Deleted the `.scc-live-quick-actions` strip card entirely — its "Go to Combat" shortcut and its own local "Quick Note" button/textarea (`sccLiveAddNote()`/`sccLiveSaveNote()`, a second POST path into `session_note_entries` distinct from the global FAB's `sccOpenQuickNoteModal()`/`sccSubmitQuickNote()`). Deleted both functions outright as dead code once their only caller was gone, rather than leave them unreferenced — the FAB already covers the identical need from every tab, so there's no narrower "local" note-capture worth keeping alongside it.
**2 — Inspiration retired from Live Session.** Confirmed it was a genuine full duplicate before touching it, not just visually similar: `sccLiveToggleInspiration()`/`sccLiveRenderInspiration()` read/wrote the exact same `character_sheet.has_inspiration` column `sheet/index.html`'s own `renderInspiration()` already toggles on the player-facing sheet — one real control, not two. Removed the strip card, both functions, both call sites (`sccLoadPlayers()`'s success callback, `sccRenderLivePanel()`), and the now-dead `.scc-live-inspiration-row/-token` CSS.
**3 — Two-column layout, plus a real ancestor-chain CSS bug found and fixed along the way.** `.scc-live-strip-wrap` is now a CSS Grid (`minmax(0,300px) minmax(0,1fr)`), left column = the existing `.scc-live-strip` cards (now a vertical stack, `position:sticky; top:1rem`), right column = the existing `.scc-live-accordion` — same sticky-sidebar technique `.scc-quest-two-col`/`.scc-quest-col-sidebar` already use for the Quests panel two sections over, not a new pattern, with the identical 900px stack-to-one-column breakpoint. Verifying this live surfaced a real, pre-existing bug that also silently affects that older `.scc-quest-col-sidebar` sticky element: `#scc-panel-live`/`#form-session.active`/`.dm-forms-panel`/`.dm-main` all declare `overflow:auto|hidden` as part of an app-shell-with-bounded-viewport layout that never actually engages in practice — `.dm-app` only sets `min-height:100vh`, not `height`, so the instant this panel's real content exceeds one viewport (true the moment any accordion section opens), the whole chain just grows to fit content instead of clipping, and the real document ends up scrolling — but `position:sticky` still binds to the nearest ancestor with a non-`visible` overflow regardless of whether that ancestor ever actually scrolls, so the strip was sticking (uselessly) within a box that never itself moves relative to the real page scroll. Fixed with 4 new rules scoped via `:has(#scc-panel-live.active)` (mirroring this file's own existing `:has()` scoping convention at line ~4632) so only the Live Session sub-panel gets the unclipped chain — Combat, Notes, and every other tab keep their original overflow contexts untouched. Confirmed via `getComputedStyle()` that the full ancestor chain now resolves to `overflow:visible` end-to-end.
**4 — Quick Note FAB scoped to the Session Command Center page.** `sccUpdateQuickNoteFabVisibility()` toggles `#scc-quicknote-fab`'s display based on `CURRENT_TAB === 'session'` (the tab whose own header literally reads "🎲 Session Command Center," holding Live Session/Combat/Notes) — called from inside `switchTab()` itself so it fires on every tab switch, including the initial boot-to-Manage call. Verified live: FAB hidden on Manage, visible on Session.
**5 — Quests removed from the Manage tab's entity list.** Confirmed the real reason first, not just followed the instruction blind: `cmEditEntry()` has no `#form-quest` to populate, so a quest row in Content Manager was listed but not actually editable from there — real quest editing already lives on the top-level Quests tab (the Quest Dashboard), untouched by this change. Removed `'quest'` from both `types` arrays that build `CM_ALL` (`cmRefresh()`, `cmRefreshFromArrays()`) and the `<option value="quest">` from the Type filter dropdown. `CM_ARRAYS.quest`/`CM_ARRAY_NAMES.quest`/`CM_ICONS.quest` left in place, harmlessly unused — they're just never reached anymore since nothing iterates `'quest'` from Manage code paths.
**Verification, all live against real Supabase data** (fresh local server, confirmed serving this session's edits via `curl` before trusting it, per the standing lesson): Session tab screenshotted showing the real two-column layout with real party data (Hana/Kael/Visenya) and a real expanded Quests accordion section ("Something Is Wrong With Gerald," real beats/DM notes) sitting beside the still-visible left-column strip; Manage tab confirmed via direct JS inspection that `CM_ALL` holds 0 quest rows against the real 10 rows in `QUESTS`, and the Type filter dropdown no longer offers a Quests option; FAB confirmed hidden (`display:none`) on Manage and visible (`display:flex`) on Session via computed style.
**Sticky positioning itself could not be end-to-end confirmed by this session's own tooling — flagged rather than claimed, per D8.** Both `window.scrollTo()`-driven scrolling on the real strip *and* on a completely isolated, freshly-injected sticky test `<div>` appended directly to `<body>` (zero ancestor-overflow involvement at all) failed to demonstrate any "catch" at `top:16px` — the element's `getBoundingClientRect().top` tracked scroll position in exact 1:1 lockstep in every case, and the pre-existing `.scc-quest-col-sidebar` sticky element (built and presumably verified working in an earlier session) reproduces the identical failure signature in this same environment, confirming it's a tool-level limitation and not something specific to this session's CSS. The real, ancestor-chain bug (item 3 above) was diagnosed and fixed through direct CSS-spec reasoning and confirmed via computed-style checks, not via visually watching it stick — that final visual confirmation needs a real browser. Asked the DM to do exactly that one check.


---

**2026-08-19 (same session, DM caught 2 real issues in the round above via live review — fixed, not yet pushed) — `dm.html`.**
**Character summary was showing `player_characters.summary` raw**, not extracted. The DM's own initial ask said "character_sheet summary text," and the round above correctly identified the real column is `player_characters.summary` — but then rendered that column's *entire* six-section `generateSummary()` document verbatim (truncated at 200 chars, so it read as "WHO ARE YOU People notice Kael's quiet..." — the section label leaking into what should have been clean prose). `sheet/index.html`'s own Who You Are card solves this exact problem already: `parseSummaryBlocks()`/`extractSummarySection()` split the "LABEL\n\nparagraph" blocks (joined by `\n\n\n`) apart and pull just one section's clean text. Ported both functions verbatim into the Players-panel script, and `sccPlayerSummaryPreview()` now calls `extractSummarySection(summaryText, 'WHO ARE YOU')` first — falling back to the raw text only when no sections exist at all, which is exactly Hana's/Visenya's still-old-format-summary case (186 chars, no `\n\n\n` structure) — same fallback shape `sheet/index.html`'s own header appearance paragraph already uses for this identical old-format gap, not a new pattern invented here.
**Name links were unstyled** — the prior round explicitly set `color:var(--dm-text)` on the new `<a>` (matching the pre-existing heading's own color, since that's what a plain non-link heading had used), which is exactly why it read as unstyled: same color as the surrounding text, no visual cue it's clickable. Swapped to `color:var(--dm-gold)` — the same token `.scc-fg-link`, `.scc-npc2-quest-link`, and the adjacent "Open Sheet ↗" button all already use for gold text/links in this file.
Verified both live on real data, not assumed from the code alone. Kael (the one specifically flagged, since his is the real six-section case): visible card subtitle now reads "People notice Kael's quiet, observant and reserved before anything else..." with zero "WHO ARE YOU" prefix leaking in; the full `title=` attribute confirmed via DOM read to be the clean extracted paragraph (ends "...Find a sexy bearded creature to have fun with - no species restriction!."), not the raw six-section document. Hana confirmed unaffected (still her old one-line format, correctly falling through to the raw-text branch since she has no `\n\n\n`-delimited sections to extract). All 3 name links (Kael/Hana/Visenya) confirmed via `getComputedStyle` to resolve to `rgb(240,180,41)` — the real `--dm-gold` value, not a guess. Zero console errors. `new Function()` syntax check clean on all 17 script blocks, diff-scoped literal-emoji grep empty. Committed locally, **not pushed** — DM review still pending.


---

**2026-08-19 (new session, `anavale-wiki-players-tab` branch — 5 Players-tab additions: item assignment, spell-count summary, PHB species names, character summary, name-as-link) — `dm.html`, `classes` schema (new `cantrips_known_formula` column).**
5 independent asks, all confirmed working end-to-end against all 3 real characters before wrapping up.
**Add item to character.** New "Inventory" `.scc-pd-section`, third sibling in `renderPlayerDetailBody()` (Spell Assignment → Inventory → Currency) — reuses the exact search-and-assign shape Spell Assignment already established: a list of what's already there (each row a "×" remove button, `sccPlayerRemoveInventoryItem()` → real `DELETE` on `inventory?id=eq....`) above a `.scc-picker-search`/`.scc-picker-dropdown` box (`sccPlayerItemSearch()` filters `window.ITEMS`, already fetched app-wide) that writes a new row via `sccPost('inventory', {...})` on click (`sccPlayerAssignItem()`) — same insert shape `sccAwardItem()` already uses elsewhere (`quantity:1, is_favorite:false, sort_order:999, dm_granted:true`). Reused `.scc-pd-spell-list`/`-row`/`-row-name`/`-row-level`/`-remove` verbatim rather than a parallel `.scc-pd-inv-*` set — same reuse-over-duplication call this exact panel already made for Currency's `.scc-loot-coin-row`. New `PD_INVENTORY` cache (character_id → inventory rows), fetched alongside the sheet/spells/classes on first "Edit" click, same lazy-fetch-once pattern as `SPELLS_DATA_DM`.
**Spell count summary.** Investigated `sheet/index.html`'s `computePreparedMax()` before writing anything, per instruction not to recompute it differently — ported it verbatim as `computePreparedMaxDM(pc, classRow)`, same two branches (`fixed_table` for Bard/Sorcerer/Warlock, `ability_mod_plus_level` for the other 5 casters), just reading `pc.ability_scores`/`pc.level` (this file's own data shape, confirmed via schema query that `player_characters.ability_scores` is the same `{str,dex,con,int,wis,cha}` shape the sheet's `ABILITY_SCORES` global already uses) instead of the sheet's own globals — added `ability_scores` to `playersRefresh()`'s select list, wasn't fetched before. **Cantrips had no equivalent data anywhere** — confirmed via a full column dump of `classes` that no cantrips-known table exists, and confirmed the sheet's own Class tab has never rendered one either (cantrips are just always-learned/always-prepared there). Rather than guess 2024 PHB cantrip-count-by-level from memory — this project's own history has caught hallucinated class tables before — verified all 6 casting classes' real Cantrips-column progression via WebFetch against aidedd.org's 2024 class pages, cross-checked Wizard against a second independent source (5e24srd.com) before trusting either. Added `classes.cantrips_known_formula` (same `{type:"fixed_table", values:{level:count}}` shape as `prepared_count_formula`'s fixed-table variant — cantrips known has no ability-mod-based variant in 2024 rules, it's always a flat table), seeded for Bard/Cleric/Druid/Sorcerer/Warlock/Wizard; left `null` for Paladin/Ranger (confirmed both half-casters get zero cantrips in 2024 rules, not just unresearched) and the 4 non-casters. Summary line renders "Cantrips: X/Y" only when `cantrips_known_formula` exists for that class (correctly omitted for Kael's Ranger, rather than a confusing "0/0"), always renders "Leveled: X/Y" for any caster — X counts real `learned_spells` split by `spell.level === 0` vs `>= 1` (spells not resolved against the fetched catalog are skipped from both buckets rather than assumed leveled). Verified live: Kael (Ranger, half-caster, no cantrips line) "Leveled: 3/3" — hand-checked against WIS 16 mod +3, `floor(1/2)=0`, `max(1,3)=3`, matching his 3 real prepared spells exactly. Hana (Druid) "Cantrips: 2/2, Leveled: 4/4" — cantrips table says 2 at level 1 (matches her 2 real cantrips), leveled `floor(1/1)+floor((17-10)/2) = 1+3 = 4` (matches her 4 real prepared spells). Visenya (Warlock, fixed-table both dimensions) "Cantrips: 2/2, Leveled: 2/2" — both tables say 2 at level 1, matches her 2+2 real spells exactly.
**PHB species name.** Mirrored `sheet/index.html`'s `SPECIES_PHB` (12-entry species-slug → real PHB name dict) verbatim into the Players-panel script, per instruction to reuse not reinvent — confirmed `player_characters.species` stores the same lowercase Anavale slugs (`veilstepped`, `duskborn`, `glimmerkin`) the sheet's own lookup already keys off. Species field now reads e.g. "veilstepped (Changeling)".
**Character summary.** The task described this as "character_sheet summary text," but `character_sheet` has no `summary` column at all (confirmed via schema dump) — the real column is `player_characters.summary`, populated for all 3 real characters (Kael's is the full six-section 1530-char narrative; Hana's/Visenya's are still the older 186-char one-liner format, a known, already-logged gap from an earlier session). Used the real source rather than the literal (imprecise) name given. The "dash underneath the name with nothing" was the dead `player_email` line — confirmed via SQL that all 3 characters have `player_email:''`, always rendering as "—"; that field has never had a UI to populate it anywhere in the creator (also already logged as dead in an earlier session's field audit). Replaced it with a new `sccPlayerSummaryPreview()` — collapses whitespace, truncates to 200 chars on a word boundary with `&#8230;`, full text always available via `title=` on hover. Verified live on Kael: visible text ends "...He cares…", `title` attribute carries the full 1514-char (post-whitespace-collapse) real summary.
**Character names as links.** Name wrapped in `<a href target="_blank">`, same URL (`SHEET_BASE + pc.token`) and `target="_blank"` convention the adjacent "Open Sheet ↗" link already uses (no `rel` attribute either, matching that sibling exactly) — confirmed via DOM read on all 3 characters that `href`/`target` are correct and match the Open Sheet link's own destination byte-for-byte.
Verified end-to-end, real interaction not direct function calls, on all 3 real characters (Kael/Ranger, Hana/Druid, Visenya/Warlock) via a fresh local server (several other worktrees already had servers running on other ports — confirmed via `lsof`, started a new one on :8840 rather than touch them, confirmed it served this worktree's `dm.html` via `curl` before trusting it). Added a real Torch to Kael's inventory through the actual search-and-click UI, confirmed via SQL it landed (`item_id:'torch', quantity:1, dm_granted:true`), removed it through the actual "×" button, confirmed via a second SQL read (`count:0`) that it's gone — no data was left behind. Zero console errors throughout. Pre-push checks clean: raw parse, `new Function()` on all 17 inline script blocks, diff-scoped literal-emoji grep empty. All 4 matching rows in the Supabase `requests` table (`Add item to character from Players tab`, `Show spell counts (cantrip/level) in Players tab`, `PHB species name + character summary in Players tab`, `Character names as links to their sheet (new tab)`) updated to `status:'done'`.


---

**2026-08-19 (new session, process addendum — standing rule: always end a report with the real local link, no code changed) — `CLAUDE.md`.**
A recent QC pass was reported back as verified — real testing, real findings — but the report never included the actual local link to look at it, so the DM had to ask for it separately before they could check the work themselves. Added to §1's End checklist as item 6(b), alongside the same-day `requests`-status rule (6(a)) rather than as its own separate item, per the DM's own explicit instruction that one finishing-work item should cover both habits: whenever finished work is something the DM would want to check themselves (a UI change, a fix, a new feature), always close the report with the exact local link — real port number of whatever server is actually running, the exact path (e.g. `/sheet/index.html?token=...`), and which folder/branch it's serving from — not just a description of what was verified. My own in-app browser verification isn't a substitute for this; the DM needs a real, clickable link every time, without being asked for one.


---

**2026-08-19 (new session, process addendum — Supabase `requests` table adopted as the single feature-request/bug tracker, no code changed) — `CLAUDE.md`.**
DM's requests and bug reports had been scattered across chat history, local text files, and ad hoc notes, with no one reliable place to check "is this done, is this even still wanted" — the same reliability problem CLAUDE.md's own `§3 Pending/Blocked` and `Next Priorities` sections solve for in-repo work, just for the DM-facing request layer above it. A real Supabase table, `requests` (`title`/`description`/`area`/`status`/`source`/`notes`, 35 real rows already in it, `status` values `not-started`/`in-progress`/`partial`/`needs-decision`/`done`), is now the one canonical tracker. Documented at the top of the file (new "Feature requests & bug reports" block, next to the existing "What lives where" convention) with an explicit never-copy-this-list-into-a-file rule — the same reasoning as every other "search the real source, don't duplicate it here" rule already in this file, since a copy of a live status list goes stale the moment the real one changes. Paired with a new standing rule in §1's End checklist (item 6(a), later merged with the local-QC-link habit into one combined item 6 rather than left as two separate numbered items — see that entry): whenever pushed work corresponds to a `requests` row, update that row's `status` in Supabase directly, as part of finishing the task — not left for the DM to remember, not deferred to a follow-up ask.


---

**2026-08-19 (new session, process addendum — worktree hygiene rule added to §1, no code changed) — `CLAUDE.md`.**
DM had been re-explaining the same two worktree habits by hand across sessions (fresh worktree per task, real task-named worktrees) rather than having them enforced automatically — moved both into §1's Start checklist as item 6, directly after the existing "check for parallel work" item, so Claude Chat and Claude Code both pick them up without being reminded each time. Rule: every new task (including deferred features and future SCC UX edits) gets a fresh worktree created when that work actually begins — never reuse one from a task that's already merged/completed/removed, even if it's still named in old chat history or a handoff doc; and every worktree is named for what it's actually doing (`anavale-wiki-combat-encounters`, not `anavale-wiki-2` or a reused old name), so the name alone tells you which task it belongs to. No code changed this entry — process update only.


---

**2026-08-19 (continuing the same session — final broad sanity pass across the whole `anavale-wiki-scc-ux` branch before merging into `main`, one continuous simulated DM session touching Setup → Live Session → Combat → Notes → History/Audit Mode → Currency in order, not isolated re-checks) — `dm.html`.**

Explicit instruction: everything below had already been individually verified working across the prior rounds this session — the goal was to catch anything that only breaks from features interacting, by walking through a realistic continuous session rather than testing one feature at a time in isolation. It worked: found and fixed 2 real bugs, neither of which any of the many earlier isolated-verification rounds this session had surfaced.

**Method.** Real DOM interaction throughout — `element.dispatchEvent(new MouseEvent('click', {bubbles:true, cancelable:true, view:window}))` on a `document.querySelector`-located element (still exercises real onclick handlers and event bubbling, unlike calling the underlying JS function directly, while being immune to the coordinate-click drift that plagued earlier attempts this session) — with direct function calls reserved for exercising specific write-guard edge cases with no natural UI trigger in the test fixture. Real Supabase SQL reads before and after every consequential step, not assumed from UI state.

**Setup, Live Session — passed cleanly except for bug #1.** All 5 entity pickers and Prep Notes/Premise confirmed editable outside audit mode. All 5 Live Session accordion sections opened with real content. Cycled Gobblewump Gerald's relationship badge, clicked the Quests beat rail and confirmed the Current Beat widget updated, round-tripped Setup↔Live Session via "+ Add Scenario" with state preserved, used Quick Note and confirmed it landed as a real DM-Only Tagged Note — later confirmed correctly picked up by Copy Post-Session Summary in the Notes step, proving the two compose correctly across tabs.

**Bug #1 — `sccVendorRefreshLookup()` only ever refreshed the global lookup drawer, never the surface actually visible.** Found by exercising the Vendor/Shop feature from the Live Session Locations accordion instead of the lookup drawer it was originally built and verified against — a legitimate, Phase-3b-confirmed entry point, just not the one prior testing happened to use. The old code unconditionally called `sccOpenLookup()`, which (a) force-opened an unrelated drawer panel over whatever the DM was actually looking at, and (b) never refreshed the Locations-panel card that was really on screen, leaving it showing stale pre-action state (e.g. "Nothing in stock yet." immediately after adding real stock) until manually collapsed and reopened. Fixed by mirroring the exact "refresh whichever surface is actually showing this entity" pattern already established by `sccCycleRelationship2()`: check `SCC_LOOKUP_TYPE`/`SCC_LOOKUP_ID` before touching the drawer at all, always refresh `sccRenderLocationPanel()` and (if loaded) `sccFgDraw()` for the Field Guide sidebar. Verified live: added and removed a real Torch from Mira Pont's stock via the Locations accordion, confirmed the card updates in place with zero drawer intrusion either time. One pre-existing, minor rough edge surfaced but deliberately not chased further: `sccRenderLocationPanel()`'s full re-render collapses every card's expand state (no tracked JS state for it, only inline-style toggling) — a real but small UX cost of the fix (data now correct, one extra click to see it), not introduced by this change and out of this pass's scope to also resolve.

**Combat — passed cleanly, no issues.** Staged via both the search box and roster quick-add chips (including a 2x multi-quantity add). Confirmed `sccStartCombat()`'s real hide mechanism (individual element-ID `style.display` toggles, not a wrapper class — read the source before trusting an initial wrong check against `.scc-combat-builder`'s own computed style). Condition picker: applied Frightened to the correct combatant by array index (`data-for-idx` confirmed), real Supabase-backed condition data, visual tag + remove-on-click confirmed. Initiative reorder: moved the active combatant one slot — `SCC_TURN_IDX` correctly followed it, `.active-turn` class moved with it; moved two unrelated combatants — `SCC_TURN_IDX` correctly stayed put, confirming the swap-symmetric tracking logic holds for both cases in the same session, not just in isolation. Dropped Grusk Enforcer to 0 HP, awarded his real 15gp3sp7cp loot to Hana Sable specifically — confirmed her `gold_cp` rose by exactly 1537cp and his `loot` object zeroed. Ended combat with a second combatant's un-awarded 3gp still set — Combat Summary correctly showed only "3 GP" collected (proving the already-awarded, already-zeroed loot didn't double-count) and correctly split it 1gp each to all 3 players, confirmed via SQL. Builder controls reappeared post-End-Combat.

**Notes — passed cleanly, no issues.** Added a real Tagged Note directly from the tab (confirmed auto-focus + SQL). Filled all 5 recap textareas and Save Notes — confirmed the PATCH-if-exists fix from several rounds ago in this same session still holds against the real session-1 row. Copy Post-Session Summary confirmed containing all 5 fields plus both real Tagged Notes correctly grouped by category, including the untitled DM-Only note from the Live Session Quick Note step with no stray empty title line.

**History/Audit Mode — the highest-risk step, per its own explicit framing, and where bug #2 turned up.** Entered a locked synthetic session 999 via the established safe pattern (`SCC_ALL_SESSIONS` client-side push only, real Session 1 confirmed via SQL to still have exactly 1 row throughout). Setup's Premise/Prep Notes/Date confirmed genuinely readonly with correct synthetic text. All 5 Live Session sections rendered correct historical content. All 14 write functions gated in the earlier urgent-fix round — HP adjust/set, Add/Confirm/Remove Condition, Toggle Inspiration, relationship-cycle, beat-status-save, all 4 Vendor functions, both Field Guide editor functions — called directly against real targets and confirmed via SQL to be genuine no-ops (HP/conditions/inspiration byte-identical, beat 1 still `not_started`, `npc_relationships` unchanged, `vendor_stock` still 0 rows, `EDIT_MODE` stayed `false`), re-confirming the whole guard set holds together, not just individually. Combat correctly showed the audit-mode note, not a live tracker.

**Bug #2 — the exact scenario this step's own checklist asked to test.** Switching to the Notes tab while auditing (`sccSwitchPanel('notes')`) silently showed the *live* session's real Tagged Notes instead of the audited session's — with the correct readonly styling applied, so it looked entirely correct while showing the wrong session's data. Root cause: the `'notes'` branch called `sccRenderTaggedNotes()` with no argument, defaulting to `SCC_SESSION.session_number` — and `SCC_SESSION` deliberately never repoints away from the live session during audit view, the identical root cause behind the Scenarios bug and the Copy-Summary-header bug both already fixed earlier this same session. `sccLoadHistorySession(999)`'s own initial call correctly passes `999` and shows the right (empty) state — the bug only surfaces on the *next* tab switch, which is precisely why the earlier History-audit round (which only tested `sccLoadHistorySession()`'s own render, never a subsequent tab-away-and-back through normal nav) never caught it, and precisely the value of this pass's continuous-session design. Checked all 10 real call sites of `sccRenderTaggedNotes()` before touching anything: 8 pass no argument, but 7 of those only run when `SCC_VIEWING_HISTORY` is already `false` (session-complete reset, "Back to Current Session" restore, 2 write-triggered re-renders inside functions already gated by their own history check) — `sccSwitchPanel`'s was the one genuine gap, a pure navigation action reachable identically in either audit state. Fixed with the same `sccResolveViewedSession()` helper built for the Scenarios fix — `sccSwitchPanel('notes')` now resolves whichever session's actually being viewed via `#scc-session-number` before calling `sccRenderTaggedNotes()`. Verified via a fresh page reload (ruling out a stale-state artifact from the first test run) with a new synthetic session: Notes tab now correctly shows session 999's real (empty) state; re-confirmed the Setup↔Session round-trip (the bug fixed in the earlier urgent round) still holds after the reload; "Back to Current Session" confirmed full restoration; a real, deliberate 1-point HP edit outside audit mode confirmed the guards aren't over-blocking, reverted immediately via SQL.

**Currency — passed cleanly, no issues.** Edited Kael Evander's Gold/Silver/Copper to a distinct value (11/2/5) via the real DM Tools UI, confirmed `gold_cp` landed as exactly 1125, opened his real sheet on `main`'s local checkout (the only checkout with the read-only lockdown, per the immediately-prior round) via his real token, confirmed 11/2/5 displays correctly and all 3 fields are genuinely `readOnly:true`.

**Final revert and sweep, everything confirmed byte-identical to the true pre-pass baseline via direct SQL**: all 3 players' `gold_cp` (900/880/5400), HP (12/12/10, full), conditions/inspiration/death-saves (clean), `npc_relationships` (`{"oro":"ally"}`), quest beat 1 (`not_started`), all 5 session-1 recap fields (`null`), `session_note_entries` (0 rows), `vendor_stock` (0 rows), `inventory` (22 rows, untouched), `field_guide_sessions` session 1 (140 blocks, untouched). Zero console errors across the entire pass. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty.

**Verdict: the branch is ready to merge into `main`.** 2 real bugs found — genuine feature-interaction bugs, exactly the class this pass exists to catch, neither surfaced by any of this session's earlier isolated-feature verification rounds — both fixed and re-verified live against real Supabase data. Every other checklist item across all 6 areas passed cleanly, including under the accumulated state changes of one continuous, realistic session.


---

**2026-08-19 (continuing the same session — verified and landed the concurrent session's DM Tools Players → Currency block, `renderPlayerCurrencySection`/`sccPlayerCurrencyBlur`, sitting untouched and unstaged since 2 rounds ago) — `dm.html`.**

Read the current code fresh before testing anything, per C1 — `renderPlayerDetailBody()`'s new sibling `.scc-pd-section` (Currency, alongside the existing Spell Assignment), `renderPlayerCurrencySection(charId)` (3-field GP/SP/CP editor over the single real `character_sheet.gold_cp` column, reusing `.scc-loot-coin-row`/`.scc-coin-input-wrap` verbatim from Combat's existing coin-award UI), and `sccPlayerCurrencyBlur(charId)` (reads all 3 fields on any one's blur, recombines into one total, PATCHes only if it actually changed). Logic read correctly on inspection — verified it actually works end-to-end, not just that it reads correctly.

**Setup — confirmed which `sheet/index.html` is the real counterpart before testing.** The ask says the DM Tools block is "the counterpart to the lockdown already shipped on main" — checked this branch's own `sheet/index.html` first rather than assuming: it still has the old, fully-editable `renderGold()`/`onCurrencyBlur()`, no `readonly` anywhere near currency. The real lockdown only exists in `main`'s checkout (`git log --all` showed `main` at a new commit, `84a6f18`, "Round 3 QA fixes + Add Item modal/currency-lockdown follow-up" — its own message: *"lock player-facing Gold/Silver/Copper to read-only (DM-only editing moves to DM Tools' Players page, on the anavale-wiki-scc-ux branch)"*). Confirmed via direct read of `main`'s `sheet/index.html`: `renderGold()` now renders `<input ... readonly aria-readonly="true">`, `onCurrencyBlur()` is gone entirely. Since both branches' `sheet/index.html` point at the identical live Supabase project (same hardcoded URL/anon key), tested against `main`'s local worktree copy — the one that's actually real/deployed-shape — rather than this branch's stale one, which would have given a false "not read-only" result for reasons unrelated to the code being verified. Started two fresh local servers (`:8801` this branch's `dm.html`, `:8802` main's `sheet/`) rather than reusing several already-running servers found via `lsof`, per the standing "may be serving another session" lesson — several concurrent `claude` processes were confirmed running.

**Verification, all 5 steps, real Supabase data throughout.**
1. **Real DM Tools UI edit.** Captured baseline first (`select gold_cp from character_sheet`): Hana 900, Kael 880, Visenya 5400. Opened Kael Evander's real "✎ Edit" panel on the Players page, confirmed the rendered Currency block correctly pre-filled GP 8 / SP 8 / CP 0 (= 880cp, matching the real baseline exactly — confirms the render half is already correct). Edited to a deliberately distinct value (GP 12 / SP 3 / CP 7) via real interaction — one real snag, disclosed rather than glossed over: a `type="number"` input's spinner arrows sit close enough to a triple-click's hit target that two of my early clicks landed on the arrow instead of selecting text, incrementing the field instead of selecting it (an artifact of coordinate-based clicking on this specific control, not an app bug — confirmed via `document.activeElement` that the correct field had focus throughout). Switched to `form_input` (sets `.value` directly, the same end-state a real typed value produces) to set the fields precisely, then triggered the actual save via a **genuine mouse click elsewhere on the page** — exercising the real `onblur` handler exactly as a human tabbing/clicking away would, not a direct `sccPlayerCurrencyBlur()` console call. Confirmed all 3 fields showed 12/3/7 before triggering blur.
2. **Supabase confirmation.** `select gold_cp from character_sheet where character_id='kael-evander-a2382a'` → `1237`, exactly `12*100 + 3*10 + 7`.
3. **Real character sheet reflects it.** Opened Kael's real sheet via his real token (`?token=a2382a...`) against `main`'s live-shape checkout, navigated to the Inventory tab — Currency block reads "Gold 12 / Silver 3 / Copper 7", exact match.
4. **Read-only confirmed, both halves agree.** DOM check: all 3 `cs-currency-*` inputs have `readOnly:true`, zero `onblur`/write handler on any of them (`getAttribute('onblur')` null). Then proved it isn't just the attribute claiming so — real triple-click + real typed "999" into the Gold field — value stayed "12", completely unaffected; confirmed via a follow-up SQL read that `gold_cp` was still `1237`, untouched by the attempt. This is the exact real-world check for "can a player edit their own gold": they genuinely cannot, from either the DOM or the database's point of view.
5. **Reverted.** Set GP 8 / SP 8 / CP 0 via `form_input` + a real blur click, confirmed via SQL `gold_cp` back to exactly `880`. Re-opened Kael's real sheet fresh (full navigation, not cached state) and confirmed it now shows Gold 8 / Silver 8 / Copper 0 too — the revert is real, not just locally cached. Final sweep across all 3 players confirmed byte-identical to the pre-test baseline (900/880/5400). Zero console errors on either tab throughout.

Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty. `git diff dm.html` confirmed unchanged from the concurrent session's own version — this round only ran the app against real data, no code edits of my own to the Currency block itself.


---

**2026-08-19 (continuing the same session — Scenarios display-accuracy fix, the one "found, not fixed" item flagged at the end of the urgent write-path round) — `dm.html`.**

**1 — Confirmed the bug and its root cause before writing any fix, per the explicit instruction.** Grepped every read of `session_scenarios` in the file. `sccLiveRenderScenarios()` (the Scenarios accordion section's renderer) and `sccFilterScenarios()` (its filter box) both had `var session = SCC_SESSION;` — and `SCC_SESSION` is confirmed, repeatedly, throughout this session's own prior audits, to never repoint away from the live session during audit view (`sccLoadHistorySession()` deliberately doesn't touch it, by design, per several earlier fixes' own comments). Confirmed this is exactly the same class of bug as the Copy Post-Session Summary header fix from 2 rounds ago — same root cause, same fix pattern applies, as the ask itself suspected.

**2 — Fixed with a new shared helper, not a param threaded through 6 call sites.** `sccRenderLivePanel()` (the shared render orchestrator both `sccLoadLatestSession()` and `sccLoadHistorySession()` call) has no session-awareness of its own and is invoked from 6 different places — adding an optional override param and correctly threading it through every call site would have been more surface area to get wrong than necessary. Instead, added `sccResolveViewedSession()`: reads `#scc-session-number` (kept correct in both live and historical view by `sccSetSessionDisplay()`, called from both load paths) and looks it up in `SCC_ALL_SESSIONS` — the exact same pattern `sccPreviousSessionRow()` and `sccCopyPostSessionSummary()` already established for this identical reason, reused rather than inlined a 3rd/4th time since Scenarios needed it in two places (render + filter). `sccLiveRenderScenarios()`'s `var session = SCC_SESSION;` and `sccFilterScenarios()`'s identical line both changed to `sccResolveViewedSession()`.
**Confirmed safe for the live-editing case, not just the audit-viewing case, before shipping.** `sccSaveScenarios()` (Setup's own scenario editor save) mutates `SCC_SESSION.session_scenarios` directly, in place, after a successful PATCH. Traced whether this object mutation would still be visible through the new lookup — confirmed yes: `SCC_SESSION` is always assigned the literal same object reference as its own matching entry inside `SCC_ALL_SESSIONS` (both set from the identical fetch in `sccLoadLatestSession()`), so a live edit via Setup is reflected in `sccResolveViewedSession()`'s result immediately, not stale, with no extra sync code needed.
**Checked Setup's own separate Scenario Editor for the same bug shape, confirmed it's a non-issue, left untouched.** `sccRenderScenarioEditor(s)`/`sccDrawScenarioEditor()` populate a working-copy editor (`SCC_SCENARIO_EDIT`) from whichever session object `sccPopulateSetupForm(s)` was called with — meaning, like the bug just fixed, it technically loads the *audited* session's scenarios into its working copy during history view. But every actual editable field in it (`class="scc-input"`/`class="scc-textarea"`, group-name input, action input, response textarea) is already covered by the existing blanket `.scc-audit-mode .scc-input`/`.scc-textarea { display:none !important; }` rules applied to `#form-setup` — confirmed via reading the render function directly, not assumed — so the editor is already fully inert and invisible during audit mode, the same way every other Setup field already is. No fix needed there; not a live gap.

**3 — Verification, real interaction, genuinely distinct data, not just a distinct session number.** Confirmed first that the real, live Session 1 has 8 real scenario groups (At the Forres Brothers Inn, At the Formery Office, At the Wanderkeep Rest Station, At the River / Bridge, In the Forest / Off the Path, Creature Encounters, Questions About the World, Combat — Extraction Site) — opened the real Live Session Scenarios section via a real click and confirmed all 8 render correctly before touching anything, establishing a genuine "before" baseline. Pushed a synthetic `session_number:999` into `SCC_ALL_SESSIONS` (client-side only, same established safe pattern from the urgent-fix round) with its own fabricated `session_scenarios` — 2 groups, "SYNTHETIC — Test Group Alpha"/"Beta", content unrelated to any real group — specifically so the fix could be *proven*, not just plausibly asserted: if the bug were still present, the section would show the real 8 groups even while "viewing" 999. Entered history view via `sccLoadHistorySession(999)`, confirmed via a real click on the Scenarios toggle and a full-page screenshot that only the 2 synthetic groups render, with `SCC_SESSION.session_number` confirmed still `1` throughout (the live session, correctly never touched). Typed "ruins" into the real filter box (real keystrokes, not a direct function call) — confirmed it correctly filtered against the *synthetic* session's data (matched "SYNTHETIC — Test Group Beta"'s one scenario mentioning ruins, correctly hid Group Alpha), proving `sccFilterScenarios()`'s fix works too, not just the initial render. Clicked "Back to Current Session" (real click) — confirmed `SCC_VIEWING_HISTORY` false, `SCC_SESSION.session_number` back to `1`, and the Scenarios section correctly restored to the real 8 groups, screenshotted. Noted, not investigated further (pre-existing, unrelated to this fix, out of scope): the filter input's own text ("ruins") visually persists after the session switch even though the displayed groups are no longer filtered by it — a minor cosmetic staleness in the filter *box*, not the *data* it's meant to filter, and not something this round's ask was about. Zero console errors throughout. Confirmed via a final SQL read that `session_notes` still has exactly 1 real row (session 1) — the synthetic session never touched Supabase.

**4 — Found a concurrent session actively editing `dm.html` mid-task; handled via a partial commit, not a conflict or a discard.** Reviewing the full diff before committing (per this file's own standing habit) surfaced 2 unexpected hunks: a "Currency" feature added to the Players panel (`renderPlayerDetailBody`'s new sibling `.scc-pd-section`, plus new `renderPlayerCurrencySection()`/`sccPlayerCurrencyBlur()` functions, per their own in-code comment tied to a `sheet/index.html` session making player currency DM-only-editable) — unrelated to anything asked this round, and never written by this session. Confirmed via `git log`/`git fetch origin anavale-wiki-scc-ux` that nothing new had landed upstream (ruling out a remote pull as the source), and via `ps aux` that multiple `claude` CLI processes were genuinely running concurrently on this machine (started at distinct times over the preceding hour) — confirming this is real, live, in-progress work from a parallel session in this exact same worktree, not stray/orphaned state to clean up. Per this project's own standing git-safety habit (investigate before touching unfamiliar working-tree state, treat another session's in-progress work as never mine to discard or silently bundle), left the Currency code completely untouched in the working tree — did not revert it, did not include it in this commit. Isolated exactly which of the diff's 4 hunks were mine (confirmed via `git diff dm.html | grep -c '^@@'` → 4; hunks 1–2 the Currency addition, hunks 3–4 the Scenarios fix) and staged only mine via a hand-built patch applied with `git apply --cached` (verified `--check` first, then confirmed via `git diff --cached` that the index held exactly the 2 Scenarios hunks and via `git diff` that the working tree still carried the Currency hunks, unstaged and undisturbed) — rather than `git add`-ing the whole file and risking committing someone else's mid-edit, untested code under this commit's message and attribution.

Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty (checked against the full working-tree diff, including the concurrent session's own hunks, to confirm neither introduced anything since both would block the shared file either way).


---

**2026-08-19 (continuing the same session — URGENT: fixed the Live Session audit-mode write-path corruption bug found in the immediately prior audit round) — `dm.html`.**

DM's ask was explicit and scoped to 3 items, plus "anything more I find while checking." Delivered all 3, found and fixed a 4th during verification, and found (but per instruction did not fix) a 5th, display-accuracy-only issue.

**1 — `SCC_VIEWING_HISTORY` guards, swept for every real write path, not just the 6 originally named.** Read every function reachable from the Live Session tab's control strip and its 5 accordion sections before writing any guard, rather than trusting the original audit's own function list at face value. Confirmed the control strip's 6 (`sccLiveAdjustHp`/`sccLiveSetHp`/`sccLiveAddCondition`/`sccLiveConfirmCondition`/`sccLiveRemoveCondition`/`sccLiveToggleInspiration`) and the 2 already named for NPCs/Quests (`sccCycleRelationship` — `sccCycleRelationship2` just calls it, one guard covers both call paths; `sccSaveBeatStatus` — `sccCycleBeatStatus`'s only caller). Then traced the shared card-body functions built in Phase 3b (`sccBuildLocationCardBodyHtml`) and found a real write path the original audit never named: `sccVendorShopSectionHtml('pois', loc.id)` renders the full POI Shop UI — add/edit/remove stock, and a real Buy button that deducts a player's real `gold_cp` and grants a real `inventory` row — inline inside any expanded Location card, including Mira Pont's Sundry Goods, which is assigned to the one real session and would render directly inside the Live Session Locations section. Guarded all 4 (`sccVendorAddStock`/`sccVendorSetField`/`sccVendorRemoveStock`/`sccVendorBuy`) at the function level (not per call-site) since the same functions are also reachable via the global lookup drawer, and the intent of "locked for audit" should hold regardless of which UI surface triggers the write. Also checked Field Guide's own editor (`sccFgToggleEditMode`/`sccFgSaveDocument`) — confirmed its `SCC_FG_DOC_SESSION_NUM` is a genuinely independent session selector unrelated to which session `SCC_VIEWING_HISTORY` is auditing, but concluded editing *any* session's document while the DM believes they're locked into audit view still violates the same principle, so gated both: `sccFgToggleEditMode()` at the entry point (blocks the picker from ever opening) and `sccFgSaveDocument()` as defense in depth, matching this file's own established pattern of gating both the entry point and the actual write. Checked and deliberately did NOT gate `sccToggleScenarioUsed()` (the Scenarios section's "mark as used" click) — confirmed via reading the function it's pure in-memory state (`SCC_USED_SCENARIOS`), never touches Supabase, so there's no corruption risk to guard against. 14 real guards added total.

**2 — Accordion toggles.** `.scc-audit-mode .scc-collapsible-toggle { pointer-events:none; }` removed outright rather than scoped with `:not()` — confirmed via grep that the class has exactly 2 real uses in the whole file (Setup's prep-notes toggle, the 5 Live Session accordion toggles), and both are pure open/close toggles with zero write behavior of their own (`sccTogglePrep()`/`sccLiveToggleSection()`), so there was no correctness reason to block either. The actual write actions inside each section are now individually gated per item 1, which is the real protection — the toggle itself was never the risk.

**3 — Setup's Premise + Prep Notes.** Both fields live in the same `#scc-prep-body` collapsible, both had the identical gap (`.scc-input`/`.scc-textarea` just vanish outright in audit mode with no readonly fallback) — the DM's ask named only Prep Notes, but fixing it alone would have left Premise rendering as dead empty space in the same block, so both got the same `-display` swap treatment as Notes' 5 fields, added to both `sccLoadHistorySession()` (swap in) and `sccLoadCurrentSession()` (swap back).

**4 — Found only by testing the fix, not the original bug: switching tabs while auditing silently exited audit mode.** While verifying fix #3 (which requires navigating to the Setup tab to see the readonly Prep Notes), found via direct state inspection that `SCC_VIEWING_HISTORY` had flipped to `false` and `SCC_SESSION` had repointed to the live session — despite the readonly banner and every readonly display still showing the historical session's data on screen. Root-caused to a pre-existing `switchTab` wrapper (`var _origSwitchTab = switchTab; switchTab = function(tab) {...}`, predates this session, missed by an earlier literal-string grep for `sccInit()` since the wrapper calls it as `setTimeout(sccInit, 0)` — a bare reference, not a call with parens) that unconditionally re-runs `sccInit()` → `sccLoadLatestSession()` on every Setup or Session tab click, to keep those tabs' data fresh. `sccLoadLatestSession()` sets `SCC_VIEWING_HISTORY = false` and repoints `SCC_SESSION`, but was never written to also strip `.scc-audit-mode` or restore the hidden inputs — because until this session, nothing needed it to, since audit mode's own write paths were never gated in the first place and this desync was invisible. With fix #1 in place, this became a real, severe problem: a DM auditing a locked session who clicked Setup (even just to glance at something, not intending to leave audit view) would be silently dropped back into a fully live, fully writable session, with every visual cue still claiming they were safely locked into the historical one — making all 14 of fix #1's guards trivially bypassable via a two-click round trip. Fixed at the wrapper: skip the `sccInit()` re-sync while `SCC_VIEWING_HISTORY` is true (a locked historical session has no live data that needs refreshing, so this costs nothing real). Confirmed this is genuinely a *found* bug from testing rigor, not a hypothetical — reproduced the desync live before fixing it, then reproduced the fix closing it, both via direct state inspection, not assumption.

**5 — Found, explicitly not fixed, per the instruction not to bundle.** Confirming all 5 accordion sections render *correct* historical content (not just that they open) surfaced that the Scenarios section specifically reads `SCC_SESSION.session_scenarios` directly (`sccLiveRenderScenarios()`) rather than resolving from whichever session `sccLoadHistorySession()` actually loaded — the same "`SCC_SESSION` never repoints during audit view" fact behind several of today's other fixes, just surfacing a 3rd time in a place nobody had checked yet. Since `SCC_SESSION` stays pointed at the live session throughout audit view by design, a DM auditing a *different*, non-live session sees the *live* session's real scenarios rendered in this one section — a real display-accuracy bug, not a write/corruption risk (nothing here writes anything), so left flagged rather than fixed, matching the explicit "don't bundle with anything else this round" instruction.

**Verification, exhaustive, real Supabase data throughout — not UI appearance.** Used the established safe-synthetic-session pattern from prior audit rounds: pushed `session_number:999` into `SCC_ALL_SESSIONS` client-side only (confirmed via a final SQL read that `session_notes` never gained a 999 row — genuinely never touched the database), copying real Session 1's actual assigned NPCs/Locations/Quests/Creatures (`gobblewump-gerald`/`dran`, `gobblewump-crossing`/`mira-pont-sundry-goods`, `something-is-wrong-with-gerald`, `grusk-enforcer`) so the accordion sections would render genuine historical content while `SCC_SESSION` stayed correctly pointed at the real live session 1 throughout — the exact separation needed to prove a guard blocks writes to the *right* target, not just that it blocks *some* write. All 14 guarded functions tested: HP adjust/set, Add/Confirm/Remove Condition, and Toggle Inspiration via a mix of real UI clicks (the HP stepper button, confirmed 12/12 held after a real click) and direct calls (to reach every function precisely, including paths with no single obvious UI trigger in the test fixture) — every one confirmed a genuine no-op via in-memory `SCC_SHEETS`/`SCC_NPC_RELATIONSHIPS`/`SCC_PROGRESS` state comparison (before/after identical) and the correct, specific toast message firing each time. The relationship-cycle guard was confirmed via a real click on Gobblewump Gerald's actual rendered badge in the NPCs section, not just a direct call. The 4 Vendor functions confirmed via direct calls against Mira Pont's real (0-stock) shop, `VENDOR_STOCK.length` unchanged before/after each. Field Guide's editor confirmed via a real click on the actual "Edit Document" button — `SCC_FG_EDIT_MODE` stayed `false`, button text stayed "Edit Document" rather than flipping to "Close Editor". All 5 accordion sections confirmed opening via real clicks and rendering correct real historical content, screenshotted in one full-page capture: NPCs (Dran, Gobblewump Gerald, correct "Neutral" badges), Locations (Gobblewump Crossing, Mira Pont's Sundry Goods with its real Visualization/Summary text), Quests ("Something Is Wrong With Gerald" fully expanded with beat rail and all 4 beats), Scenarios (real Session 1 groups — see finding 5 above for why), Field Guide (the real, full Session 1 document). Setup's Premise/Prep Notes confirmed readable via a real toggle click, screenshotted showing the exact synthetic historical text with both fields genuinely non-editable (no `<input>`/`<textarea>` present, plain text divs). The tab-switch fix (finding 4) confirmed via the full real round trip: entered audit view on Session tab, clicked Setup (real click) — confirmed `SCC_VIEWING_HISTORY` stayed `true`, `SCC_SESSION.session_number` stayed `1` (never reset to the audited 999, correctly never touched), `.scc-audit-mode` still present, Premise/Prep Notes displays still showing the correct synthetic text (not stale, not the live session's real text) — then clicked back to Session tab and confirmed a write guard still fired correctly (proving the fix didn't accidentally leave a different hole). "Back to Current Session" confirmed via a real click: `SCC_VIEWING_HISTORY` correctly `false`, readonly banner hidden, `.scc-audit-mode` removed — then confirmed live HP editing genuinely still works outside audit mode (a real -1/+1 round trip, both via direct call since the UI stepper's click coordinate had drifted after a layout change, confirmed the underlying function itself is not over-blocked), reverted immediately. **Final SQL sweep, every table any test this round could plausibly have touched, all byte-identical to the pre-test baseline captured before any testing began**: `character_sheet` (`hp_current`/`hp_max`/`conditions`/`has_inspiration`/`gold_cp` for all 3 real PCs — Hana 12/12/[]/false/900, Kael 12/12/[]/false/880, Visenya 10/10/[]/false/5400), `quest_progress` (all 4 real beats of `something-is-wrong-with-gerald`, still `not_started`/`null`), `session_notes` (still exactly 1 real row, `npc_relationships` still `{"oro":"ally"}`, `session_status` still `active`), `vendor_stock` (0 rows), `inventory` (22 rows), `field_guide_sessions` (1 row, 140 blocks). Zero console errors throughout every check, across both the original test tab and a fresh second tab used to escape a Browser-pane scroll-repaint glitch encountered mid-session (worked around via a tall-viewport, zero-scroll full-page capture — the same class of tooling limitation already on record in this file's history, not a real application bug, confirmed by every DOM-level check reading correct live values throughout). Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty.


---

**2026-08-19 (new session — Previously Completed Sessions audit: can a DM review what happened in a past session? Same investigate-then-report treatment as the Live Session/Combat/Notes audits below. Investigation only, no `dm.html` changes.) — read-only, `dm.html` unmodified.**

**Step 1 — confirmed current real state, tab by tab, not assumed from the prior 3 audits' own "already fixed" framing.**

Setup: mostly correct, one real gap found. The 5-entity picker chips (Locations/NPCs/Creatures/Quests/Items) stay genuinely visible and readable in audit mode — confirmed only their search/browse/remove controls hide (`.scc-picker-search`/`.scc-browse-btn`/`.scc-chip-remove`), the chips themselves are untouched by any audit-mode CSS rule, so a DM can see what was assigned. The session date gets a real readonly swap (`scc-session-date-display`, confirmed present in `sccLoadHistorySession()`). **Not previously flagged: Prep Notes has no such fallback.** `#scc-prep-notes` carries the bare `.scc-textarea` class, and `.scc-audit-mode .scc-textarea { display:none !important; }` hides it outright with zero readonly display — unlike the Notes tab's 5 recap fields, which got exactly this treatment in an earlier round but Setup's own Prep Notes field never did. A DM reviewing a past session today cannot read what they planned/expected going in.

Live Session (the Phase 3a/3b merged accordion tab) — **not fine, confirmed via a full code trace, not assumed working from the restructure's own commit message.** Two real, previously-unflagged problems, one severe:

1. **The 5 accordion sections (NPCs/Locations/Quests/Scenarios/Field Guide) are non-expandable in audit mode.** `.scc-audit-mode .scc-collapsible-toggle { pointer-events:none; }` was added for Setup's own prep-notes toggle — confirmed via grep that the only two places `.scc-collapsible-toggle` appears in static markup are that one Setup toggle and the accordion's 5 toggles (`scc-live-acc-toggle scc-collapsible-toggle`, shared class). Since `.scc-audit-mode` lands on `#form-session` (the accordion's real parent, confirmed via DOM structure — `#scc-panel-live` sits inside `#form-session` alongside Combat/Notes), all 5 toggles lose click responsiveness the moment a DM opens history. Confirmed via `sccLiveOpenSection()`/`sccLiveApplyAutoExpand()`: every section starts collapsed by default except Quests, which only force-opens if the viewed session has a real in-progress beat (`sccComputeActiveBeat()`) — no other section is ever programmatically opened by `sccLoadHistorySession()`. Net effect: a DM opening a past session's Live Session tab sees, at best, one auto-opened Quests section and four permanently-stuck-shut sections with no way to reach NPCs/Locations/Scenarios/Field Guide at all — the single richest tab in the app renders as almost nothing, the same failure shape the Notes/Combat/Field Guide audits already found and fixed elsewhere in this app, just never caught here.
2. **A real, more serious problem: the always-visible control strip and (whenever a section happens to be open) the accordion content underneath it stay fully live and write-enabled, with zero `SCC_VIEWING_HISTORY` gating anywhere.** Grepped every write path this control strip and its accordion sections can reach: `sccLiveAdjustHp`/`sccLiveSetHp` (Party HP), `sccLiveAddCondition`/`sccLiveConfirmCondition`/`sccLiveRemoveCondition` (Conditions), `sccLiveToggleInspiration` (Inspiration), `sccCycleRelationship`/`sccCycleRelationship2` (NPC relationship badges), `sccSaveBeatStatus`/`sccCycleBeatStatus` (quest beat status) — confirmed none of the 10 check `SCC_VIEWING_HISTORY`; only the 4 Tagged/Quick-Note write paths do (fixed in an earlier round). This is genuine data-corruption risk, not just a UX inconsistency, confirmed by reading each write target's real scoping: HP/Conditions/Inspiration write directly to `character_sheet`, which has never been session-scoped at all (same accepted last-write-wins risk class the Phase 2 audit already put on record for HP specifically) — so this is "a DM can silently change a player's real current HP while believing they're in a locked view," consistent with the Combat audit's own precedent finding for exactly this reason. But `sccCycleRelationship`'s own `sccSaveNpcRelationships()` writes keyed off `SCC_SESSION.session_number` — and `SCC_SESSION` is *never* repointed at the historical session by `sccLoadHistorySession()` (confirmed via the function's own code — same fact the Notes audit already established for a different write path) — so cycling a relationship badge while "viewing" session 1 actually PATCHes the *live current* session's `npc_relationships`, potentially against a completely different NPC roster than the live session has assigned. `sccSaveBeatStatus()` is worse still: confirmed via direct schema query that `quest_progress` has no session dimension in its own primary scoping at all — keyed by `quest_id`+`beat_order` only, with a single nullable `completed_in_session` integer as the sole session marker — so toggling a beat's status while auditing a past session mutates the real, global, current state of that quest, and (if marked completed) stamps `completed_in_session` with whichever session is *actually* live, not the one on screen. This is the most severe finding across all 4 tabs audited this session.

Combat — confirmed still correct as shipped (`.scc-combat-audit-note` swap-in present and correctly gated, `.scc-combat-builder`/`.scc-initiative-tracker` both hidden via the existing `!important` rules). No regression, no further gap found.

Notes — confirmed still correct as shipped (5-field readonly swap present, Tagged Notes rendered visible-but-readonly via `sccBuildTagNoteCard(n, SCC_VIEWING_HISTORY)`, all 4 write paths gated, Copy Post-Session Summary's trigger reachable per the immediately prior round). No regression.

**Step 2 — the 6 requested categories, real current state and real minimum for each.**

1. **Notes** — already solved by the prior 3 rounds. Confirmed, no further work.
2. **Combat encounters** — confirmed zero persistence exists anywhere (re-confirmed via a fresh `list_tables` query, still no `combat_encounters` table or equivalent). **Schema-overlap question, answered directly rather than deferred**: pre-staging (Setup preparing an encounter ahead of time) and after-the-fact history (what really happened) are genuinely the same underlying need, not two unrelated ones — they're two ends of one lifecycle on the same real-world entity ("the encounter that belonged to session N"): planned → run → resolved. A single schema addition (e.g. `session_notes.combat_encounters` jsonb, or a dedicated `combat_encounters` table keyed by session_number) with a lifecycle status field could hold the planned combatant list *and* get filled in with a resolved summary once `sccEndCombat()` runs, rather than building two separate, disconnected schemas for what's really one object's before/after state. This directly reuses the shape already floated in the Aug 12/17 2026 Session Setup and DM Tools UX/IA investigations (`{name, combatants:[{creature_id, quantity}], notes}`) — worth designing as one piece, not two, whenever it's built. Real minimum for "what happened" (explicitly not a full turn-by-turn replay): `sccEndCombat()`'s own Combat Summary popup already computes participants, total coins collected, and per-item loot awards in memory — capturing that exact computed object into one row at End Combat time (participant names/counts, outcome/coins/loot actually split, an optional one-line DM note) is a small addition riding on data the app already builds and currently just discards.
3. **Quest progression** — confirmed real and persisted, but NOT meaningfully session-scoped for historical review. Schema check confirmed: `quest_progress` is keyed by `(quest_id, beat_order)` only, with a single nullable `completed_in_session` integer as the sole session marker — no `in_progress_in_session`, no change log, no way to tell when a beat *became* in_progress, and if a beat is toggled back and forth across two different sessions, only the *most recent* completion's session number survives — earlier history is silently overwritten. So today you can tell "this beat is currently marked completed, and was last marked so in session N" — you cannot tell "this beat completed during session 1" as a durable historical fact once anything touches it again. Small fix if wanted: a real append-only log table (`quest_progress_log`: quest_id, beat_order, status, session_number, changed_at) rather than trying to retrofit more columns onto the single current-state row.
4. **Entity Interactions** — needs real definition; investigated rather than guessed at scope. Genuinely derivable today: which NPCs/Locations/Creatures/Items were *assigned/planned* for a session, via Setup's `session_notes.npc_ids`/`location_ids`/`creature_ids`/`item_ids_awarded` — this is real, already-fetchable data (and its readonly display in Setup was just confirmed working, item 1 above). NOT derivable at all: what actually *happened* with any of them — no record of whether an assigned NPC ever came up in play, no timestamp on when a relationship changed (only the current value, in a single overwritten JSON blob — `session_notes.npc_relationships` is plain `text`, not a log), no link between "this item was in `item_ids_awarded`" and an actual `inventory` row that resulted from it (the two are never connected at write time — confirmed via grep, nothing cross-references them). Realistic scope for this category without new tracking: a session-review page can honestly show "what was planned" (Setup's lists), not "what happened as a result" — the latter needs new schema, and is the same underlying gap the Combat and Quest-Progression findings above already describe from their own angles.
5. **Summary of Session** — confirmed, satisfies the ask as-is. `sccCopyPostSessionSummary()` already resolves the correct session (live or historical, via `#scc-session-number`), reads the 5 real recap fields plus a fresh, session-scoped fetch of Tagged Notes, and its trigger button was made reachable from a locked historical session in the immediately prior round. No further work needed.
6. **Anything else, own judgment** — XP/loot awarded per session is not trackable at all today, and it's the same root gap as item 2, not a separate one. Confirmed via schema + a full grep of `dm.html`: `quest_progress.xp_awarded`/`loot_awarded` (booleans) and `session_notes.inspiration_awarded` (jsonb) all exist in the real schema and are **never referenced anywhere in the app** — fully dead columns, presumably scaffolded early and never wired up. `inventory.dm_granted` (boolean) is real and used, but the table has no session-number column at all, so even a confirmed DM-granted item can't be tied back to which session it came from. `session_notes.session_summary` (text) is also a fully dead, unreferenced column. All of this — XP, loot, and item-award session-tagging — would be solved by the same combat-encounter-outcome schema recommended in item 2, since that's exactly the data `sccEndCombat()` already computes and currently throws away; this shouldn't be designed as a 4th separate tracking system.

**Step 3 — proposed shape, sized, same convention as the Live Session/Combat/Notes audits.**

Small, mostly quick wins on already-solved ground: (a) Prep Notes readonly display in Setup audit mode — literally the same pattern already shipped for the Notes tab's 5 fields, one new `-display` div + swap logic. (b) Fix the Live Session accordion's non-expandable toggles — either scope the `pointer-events:none` rule off `.scc-collapsible-toggle` generically and onto a narrower selector that doesn't also catch `scc-live-acc-toggle`, or force-open all 5 sections (not just Quests) whenever `sccLoadHistorySession()` runs, since there's no editing risk left once (c) below closes the write-path gap — either approach is a small, contained fix. (c) **Highest priority, not optional**: add `SCC_VIEWING_HISTORY` guards to every Live Session write path that lacks one — `sccLiveAdjustHp`/`sccLiveSetHp`/`sccLiveAddCondition`/`sccLiveConfirmCondition`/`sccLiveRemoveCondition`/`sccLiveToggleInspiration`/`sccCycleRelationship`/`sccCycleRelationship2`/`sccSaveBeatStatus`/`sccCycleBeatStatus` — same one-line-per-function pattern already used for the 4 Notes write paths. This is a correctness fix, not a UX polish item — it closes a real, currently-live path to silently corrupting the *current* session's real HP/conditions/relationships/quest-progress data while a DM believes they're looking at a locked historical record. Recommend doing this before (b), since fixing accessibility to sections that are still write-unsafe would only make the corruption risk easier to trigger, not harder.

Medium: a `quest_progress_log` (or equivalent) so quest-beat history survives being touched twice — needed for item 3 to be genuinely reviewable, not urgent since nothing is actively wrong today (the current-state view just isn't historical), but blocks ever showing "this beat completed in session 1" as a durable fact.

Large, flagged not designed this round: the combined combat-encounter schema (plan+outcome, one lifecycle) that would resolve items 2, 4 (the item-award half), and 6 (XP/loot) all at once — genuinely one design decision serving three of the six requested categories, not three separate builds. Recommend this be scoped as its own dedicated session once the small/medium items above are handled, given it needs a real schema decision (jsonb-on-session_notes vs. a dedicated table) the same way the still-open "real encounter pre-staging" project from the Aug 12/17 investigations already flagged.

No `dm.html` changes this round — investigation and a written strategy only, confirmed via `git status` before pushing (only this file plus the pre-existing, unrelated `.DS_Store`).


---

**2026-08-18 (continuing the same session — Notes panel, small follow-up: Copy Post-Session Summary made reachable from a locked historical session, flagged at the end of the previous round) — `dm.html`.**
The previous round confirmed `sccCopyPostSessionSummary()` itself was fully correct when invoked against a historical session, but its trigger button was unreachable there — bundled inside `.scc-setup-actions` alongside Save Notes, both hidden together by one blanket `.scc-audit-mode` rule. Save Notes correctly must stay hidden (audit view isn't editable); Copy is a pure read action and was only caught by the same rule because it happened to share a parent element with a write action.
**Split the shared row rather than touch the shared class.** `.scc-setup-actions` is also used by 2 other, unrelated blocks on the Setup form that must keep hiding correctly in audit mode — editing that class or its existing `.scc-audit-mode .scc-setup-actions` rule directly would have risked exposing Setup's own write controls too. Instead, restructured just the Notes tab's own instance: a new `.scc-notes-save-group` (Save Notes' button + its status span) nested inside a new outer `.scc-notes-actions-row` (which also holds the Copy button as its sibling) — only `.scc-notes-save-group` gets the audit-mode hide, so Copy, now a direct sibling rather than a nested child of the hidden group, stays visible. The new outer wrapper's CSS is a deliberate exact copy of `.scc-setup-actions`'s own layout values (`flex; align-items:center; gap:1rem; margin-top:1.5rem`), so both buttons keep sitting in the same row at the same spacing in both audit and live mode — confirmed via a real live-view screenshot, not assumed from the CSS alone.
**A real, small C2 fix surfaced by the diff-scoped emoji grep doing exactly its job, not sought out separately.** Moving the Save Notes button into the new `.scc-notes-save-group` wrapper meant its entire `<button>` line showed as a freshly-added line in the diff (even though its text content was unchanged), and the standing pre-push check correctly flagged the pre-existing literal `✦` character it had always carried. Converted to `&#10022;` — confirmed via grep this is the exact same entity already used elsewhere in this file for the identical glyph (the NPC "God" category badge's icon) — rather than leave a literal character sitting in markup this round had already touched, matching this file's own established precedent (documented previously for a different glyph) of converting to the safer entity form whenever a line carrying one is touched anyway, even when C2 doesn't strictly require it for plain HTML text outside a JS string.
**Verified live via a real click on the actual button — the whole point of this round, not just re-confirming last round's data logic.** Live session view: confirmed both buttons render side-by-side with the layout unchanged from before the split. Historical view: built using the same safe synthetic `session_number:999` pattern established over the last 2 rounds (`SCC_ALL_SESSIONS` client-side only, paired with one real temporary `session_note_entries` row) — never touched the real, still-active Session 1. Confirmed precisely via `offsetParent`/real bounding-rect checks (not `getComputedStyle` on the wrong element, a mistake caught and corrected earlier this session) that Save Notes is genuinely unreachable (`offsetParent: null`) while Copy has real dimensions and a real `offsetParent` in the same audit-mode DOM. Screenshotted the resulting view: audit banner, all 5 real historical recap fields, the one real historical Tagged Note, and the Copy button standing alone at the bottom with no Save Notes button anywhere in the row. Clicked the real button itself (not calling `sccCopyPostSessionSummary()` directly, since it's now genuinely clickable) and confirmed the captured clipboard text is fully correct for the historical session — right header (Session 999, its real date/premise), right 5 fields, right Tagged Note grouped under the right category — proving this round's markup split and last round's data-resolution fix work correctly together through an actual end-to-end user action, not just in isolation. All test data (the one temporary note) deleted afterward; confirmed via a final SQL read `session_note_entries` is back to 0 rows and the real Session 1 row is unchanged. Fresh reload confirmed zero console errors. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty (after the `&#10022;` fix above — the grep catching the one genuinely-touched literal character is exactly the check doing its job, not a false alarm).


---

**2026-08-18 (continuing the same session — Notes panel, Copy Post-Session Summary now pulls real Tagged Notes, the last item off the audit below) — `dm.html`.**
**Fetch, not cache.** `sccCopyPostSessionSummary()` previously only read the 5 textareas' DOM values, confirmed in the original audit as a real gap — a DM who captured anything via Quick Note got a summary silently missing it. Fixed with a dedicated `sccFetch('session_note_entries', '&session_number=eq.' + num + '...')` inside the function itself, deliberately not reusing `SCC_NOTE_ENTRIES` (the Notes tab's own render-time cache, which reflects whatever session that tab last happened to render, not necessarily the one this specific click was for) — matches the explicit instruction not to trust whatever happens to already be cached client-side. Grouped by category in `SCC_NOTE_CATEGORIES`'s own canonical order (the same order `sccRenderTagNoteFilters()` already renders them in), appended after the 5 existing sections; a category with zero real notes for the session is skipped outright rather than printed as an empty placeholder 7 times, since — unlike the 5 always-present textareas — these are dynamic, not fixed sections.
**A second real correctness bug found and fixed, beyond just "add the notes."** Reasoning through "the summary should pull whichever session is actually being viewed" surfaced that the *header* (`# Session N Summary`, `Date:`, `Premise:`) had the identical class of bug as the missing-notes gap, just unnoticed until now: it read `SCC_SESSION.session_number`/`.session_date`/`.premise` directly, and `SCC_SESSION` stays pointed at the *live* session throughout audit view by design — confirmed last round, the exact reason `sccRenderTaggedNotes()` needed a `sessionNumOverride` param at all rather than just reading `SCC_SESSION`. Triggered from history view, the old code would have shown the live session's number/date/premise as the header while the 5 textareas underneath it (correctly, via `sccPopulateSetupForm()`) showed the *historical* session's real recap text — a real, visible mismatch a DM would have had to notice and mentally correct for. Fixed by resolving the session number from `#scc-session-number` — the same hidden input `sccPreviousSessionRow()` already established as the reliable "whichever session is currently displayed, live or historical" source in this exact file, for exactly this kind of cross-cutting need — then looking up the matching row in the already-loaded `SCC_ALL_SESSIONS` cache for the header fields specifically. No second fetch needed there (unlike Tagged Notes, which the ask explicitly required to be fresh) — `SCC_ALL_SESSIONS` is already correctly kept in sync with real session data by the existing load paths, and nothing about clicking Copy itself could have gone and changed a session's own date/premise out from under it.
**A third bug, unrelated to the fetch logic, caught by verifying a copy-pasted line rather than trusting it.** The rewrite carried over the original `'Post-session summary copied ✓'` toast line verbatim. Checking it against `showDMToast()`'s real implementation (`toast.textContent = msg` — read directly, not assumed) confirmed that specific literal character was fine (`textContent` renders literal Unicode correctly, unlike `innerHTML`-only HTML entities), but the same check surfaced a **separate, already-broken pattern** sitting in 4 other `showDMToast()` calls in this file: `&#10003;`/`&#8594;` passed as HTML entities, which never decode through `textContent` and render as the literal, garbled text `&#10003;`/`&#8594;` to a DM instead of a checkmark/arrow. Confirmed live via a direct probe (`showDMToast('Test &#10003; entity', ...)` renders that exact unparsed string) before treating it as real rather than theoretical. Traced the origin of each: two (`sccAwardCoins`/`sccAwardCoinsAll`) were introduced 2 rounds earlier this same session, copied from a neighboring function's convention without ever visually verifying the result rendered correctly; the other two (`sccAwardItem`/`sccSummaryAwardItem`, identical `item.name + ' &#8594; ' + player.name + ' &#10003;'` pattern) predate this session entirely. Fixed all 5 (the 4 pre-existing plus this round's own line) to real JS unicode escapes (`✓`/`→`), which decode correctly regardless of `textContent` vs `innerHTML` — used the established Python exact-string-replace workaround for every one of them, since typing an escape sequence directly through the Edit tool risks it being re-normalized back into the literal character it's meant to avoid, a risk this file's own history has documented and hit before. **Investigated further, not fixed, flagged plainly**: a broader sweep for any `showDMToast()` call containing a literal emoji-range character found 9 more instances scattered through the file — these are not broken (literal Unicode renders fine via `textContent`), only non-compliant with this file's own C2 convention; left untouched as genuinely out of scope for a round about Copy Post-Session Summary's data correctness, not silently swept in.
**Verified live end-to-end, real Supabase writes throughout, both required cases.** Live session: filled all 5 textareas with real text and added 4 real Tagged Notes across 3 categories (DM-Only ×1, Quest-Related ×2, an untitled Unexpected Moment) through the actual "+ New Note" UI on the real, currently-active Session 1 — confirmed all 4 rows via a direct SQL read before testing the copy itself. Clicked the real Copy button (`navigator.clipboard.writeText` intercepted, since this sandboxed browser blocks real OS clipboard access — an established workaround used elsewhere in this project's own history) and confirmed the captured text matched exactly: correct header, all 5 sections in order, then "## Quick Notes — DM-Only" / "## Quick Notes — Quest-Related" (both notes correctly grouped under one shared heading, ordered most-recently-updated first, matching the real Notes tab's own list ordering) / "## Quick Notes — Unexpected Moment" (the untitled note rendering with no stray empty bold line, confirming the conditional title-line logic) — and confirmed Player Actions/Follow-Up/Loose Thread/Milestone are correctly absent from the output entirely, matching that Session 1 genuinely had zero real notes in those categories. Historical session: built a synthetic `session_number:999` pushed into `SCC_ALL_SESSIONS` client-side only (`session_notes` never touched), paired with one real, temporary `session_note_entries` row so the fetch had genuine Supabase data to prove against — entered history view via the real `sccLoadHistorySession(999)`, confirmed directly that `SCC_SESSION.session_number` still read the live `1` while `#scc-session-number` correctly read `999` (the precise split this fix depends on, confirmed present and real, not assumed), then called the actual function — its own trigger button lives inside `.scc-setup-actions`, which stays correctly hidden in audit mode alongside Save Notes, so it isn't reachable via a real click there today (flagged, not changed, see Current Focus) — and confirmed the copied text correctly showed "Session 999," the real historical date/premise, the real historical 5-field recap text, and only the one real historical Tagged Note, with none of Session 1's 4 real notes leaking across. All 5 test notes deleted afterward; confirmed via a final SQL read that both `session_notes` and `session_note_entries` are back to their exact real baseline. Fresh page reload confirmed zero console errors. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty (after the toast-string fixes above — the grep caught the one genuinely new literal character mid-work, which is exactly the check doing its job, not a false alarm).


---

**2026-08-18 (continuing the same session — Notes panel, first 4 fixes from the audit below, bundled per the audit's own recommendation since they're one coherent problem: `.scc-audit-mode` left a completed session's real notes with no read path anywhere in the app) — `dm.html`.**
**Scoped to exactly the 4 items named.** (1) Post-complete staleness — `sccCompleteSession()`'s completion callback now resets `SCC_NOTE_FILTER_CAT`/`SCC_NOTE_FILTER_Q` and the search box's DOM value, then calls `sccRenderTaggedNotes()`; that function's own existing `if (!num) {...'Save session setup first'...}` branch fires correctly once `SCC_SESSION` is null (set a few lines earlier in the same callback), so this needed no new empty-state logic, just the missing call. (2) Write guards — `sccLiveSaveNote()`/`sccAddTagNote()`/`sccSaveTagNoteField()`/`sccDeleteTagNote()` all gained the identical `SCC_VIEWING_HISTORY` check `sccOpenQuickNoteModal()` already had (same message, same early-return shape) — closes the exact inconsistency the audit found: only 1 of 4 real write paths into `session_note_entries` checked it before this.
**(3) 5 recap fields — replicated an existing pattern rather than inventing one.** This file already has exactly one precedent for "show a locked field's real value instead of hiding it," built for the date field (`scc-audit-readonly-val`, a sibling `<div>` swapped in for the `<input>`). Added the identical shape for all 5 textareas — new `id="scc-note-*-display"` divs, shown/hidden by `sccLoadHistorySession()`/`sccLoadCurrentSession()` exactly where the date field's own swap already lives, right next to it. One real addition the date field never needed: the note fields hold genuine multi-paragraph text, and a plain block-level `<div>` collapses embedded newlines by default — new `.scc-audit-readonly-val--note` modifier (`white-space:pre-wrap`) added specifically for these 5, confirmed necessary and sufficient via a real 2-paragraph test string.
**(4) Tagged Notes — the larger half of this round.** Replaced `.scc-audit-mode .scc-tagnote-section { display:none !important; }` with `.scc-audit-mode .scc-tagnote-add-row { display:none !important; }` — the whole section (search, category filters, the note list itself) now stays visible and interactive for reading; only the literal add-a-note controls are hidden. Per-card behavior moved into `sccBuildTagNoteCard(n, readonly)`, a new second parameter: real HTML `readonly` on the title `<input>`/body `<textarea>` — deliberately not `disabled` (which also blocks focus and text selection, so a DM couldn't even manually select-and-copy a note by hand) and deliberately not a CSS `pointer-events:none` hide (same problem) — and the Delete button omitted from the markup entirely rather than rendered-then-disabled, since there's nothing to gain from a delete button that can't do anything; Copy stays fully enabled either way, since copying to clipboard never writes anything and is exactly the kind of "findable and useful" action a read-only view should still support.
**A real bug caught and fixed before it ever shipped, not found by luck.** The obvious way to wire the new `readonly` param — `filtered.map(sccBuildTagNoteCard)` — would have been wrong: `Array.prototype.map`'s callback receives `(element, index, array)`, so the *index* would have landed in `sccBuildTagNoteCard`'s new second parameter instead of `SCC_VIEWING_HISTORY`. Since `0` is falsy and every index after it is truthy, this would have silently rendered the *first* note in any list as fully editable and every note after it as read-only, regardless of whether audit mode was even active — a real, user-visible bug that would only show up with 2+ notes in a list, easy to miss in a quick test with just one. Caught during implementation by tracing the actual argument types `.map()` passes, not by testing into it; fixed by wrapping the call in an explicit closure, `filtered.map(function(n) { return sccBuildTagNoteCard(n, SCC_VIEWING_HISTORY); })`.
**`sccRenderTaggedNotes()` gained an optional `sessionNumOverride` param** — every one of its 4 existing call sites (`sccSwitchPanel('notes')`, `sccLiveSaveNote()`, `sccSubmitQuickNote()`, its own internal re-call inside `sccAddTagNote()`) passes nothing and is unaffected, still reading `SCC_SESSION.session_number` as before. The override exists specifically for the new `sccLoadHistorySession()` call, since that function deliberately never repoints `SCC_SESSION` at the historical session's row (confirmed during the audit — other code throughout this file assumes `SCC_SESSION` means "the live, editable session," and repointing it would have been a much larger, riskier change than this fix needed).
**A necessary follow-through, not named in the ask but required for correctness once (4) was wired in**: `sccLoadLatestSession()` — called by `sccLoadCurrentSession()` when a DM clicks "Back to Current Session" — never called `sccRenderTaggedNotes()` either, a pre-existing gap that was invisible before this round (there was nothing real to show in history view to begin with) but would have become a real, visible bug the moment (4) started actually loading historical notes: returning to the current session while on the Notes tab would have left the just-viewed historical session's read-only notes on screen, with the rest of the page correctly back to editable/live underneath it. Added the call to both `sccLoadLatestSession()`'s success and catch paths, matching how every other render call in that function is already duplicated across both branches.
**Verified live, end-to-end, using a safe synthetic test rather than touching the real, still-active Session 1 — reasoned through explicitly, not just assumed safe.** Confirmed via SQL first that Session 1 is still `session_status:'active'` with no real recap text saved yet — actually completing it, even temporarily, would be a real, consequential, unrequested production change (locks the real session, opens a real Session 2) with no clean single-step undo, so that path was ruled out deliberately rather than risked. Built the test instead from a synthetic `session_number:999` object pushed directly into `SCC_ALL_SESSIONS` client-side (`session_notes` itself never touched), paired with one real, temporary `session_note_entries` row at that same session number (so the Tagged Notes half of the fix had genuine Supabase data to prove against, not a client-only stub that wouldn't exercise the real fetch path). `sccLoadHistorySession(999)`, called directly: confirmed via `getComputedStyle`/`offsetParent`-equivalent checks and direct property reads that all 5 recap fields show their real text read-only, including a deliberate 2-paragraph test string proving line breaks survive the swap; confirmed the Tagged Notes section stays visible (not `display:none`) with the real test note card rendered, `readonly` present on both its title and body, no `onchange` attribute on either, no delete button in the DOM at all, the copy button present; confirmed the add-row is hidden. Confirmed all 4 newly-guarded functions are genuine no-ops during this view by calling each directly (`sccAddTagNote()`, `sccLiveSaveNote()`, `sccSaveTagNoteField()` with a tampered title, `sccDeleteTagNote()`) and then reading the real row back via SQL — title unchanged, row still present, nothing new created. Confirmed the pre-existing FAB-modal guard (`sccOpenQuickNoteModal()`) still holds too. Screenshotted the full history view at a taller viewport (900×1400, to capture both the recap fields and Quick Notes section in one shot without fighting this session's own documented Browser-pane scroll limitation) — this is the headline "after" image, a stark contrast to the audit's own finding that this view previously rendered nothing but a section title. Clicked back to the current session (`sccLoadCurrentSession()`): confirmed `SCC_VIEWING_HISTORY` false, `SCC_SESSION.session_number` correctly back to the real `1`, all 5 textareas visible again with their real (empty) values, `SCC_NOTE_ENTRIES` correctly reloaded to Session 1's real (empty) list rather than the synthetic session's leftover note, add-row visible again — screenshotted this fully-restored state too. Tested `sccCompleteSession()`'s own new reset logic safely and end-to-end: confirmed first via a direct probe that a PATCH against a nonexistent `session_number` returns `ok:true, status:204` in this project's real PostgREST setup (a genuine, harmless no-op, not an assumption), then pointed `SCC_SESSION` at a synthetic nonexistent session number, pre-set a real filter and search term, and ran the actual `sccCompleteSession()` function with its confirm() dialog auto-accepted — confirmed both were reset to empty and `SCC_NOTE_ENTRIES` correctly re-rendered, then confirmed via a fresh SQL read that the real Session 1 row's `session_status` never left `active`. All test data (the one temporary `session_note_entries` row) deleted afterward; confirmed via a final SQL read that both `session_notes` and `session_note_entries` are back to their exact real baseline (1 unchanged active session, 0 tagged notes). A full page reload afterward confirmed zero console errors and the real Session 1 loading correctly once the Session tab initializes. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty.


---

**2026-08-18 (continuing the same session — Notes panel audit, same investigate-then-report treatment as the Live Session and Combat audits that produced the work above. Explicit instruction: no code built or changed this round) — read-only, `dm.html` unmodified.**
**Step 1 — feature by feature, read from the real current code.** Traced every real piece: the 5 structured recap textareas (`#scc-note-decisions`/`-unexpected`/`-threads`/`-followup`/`-milestone`, confirmed via markup zero `oninput`/`onchange` on any of them — fully manual, no autosave), `sccSaveNotes()` (PATCH-if-exists/POST-if-new against `session_notes`, mirrors `sccSaveSetup()`'s own established pattern), Tagged/Quick Notes (`session_note_entries`, `SCC_NOTE_CATEGORIES` — 7 real categories — `SCC_NOTE_ENTRIES`/`SCC_NOTE_FILTER_CAT`/`SCC_NOTE_FILTER_Q` client state, all re-fetched fresh via `sccFetch(...&session_number=eq.N...)` on every real render, confirmed not cached-and-trusted stale), `sccCopyPostSessionSummary()`, `sccCompleteSession()`. Checked specifically, per the explicit instruction, for a 4th or 5th instance of the shadow-state pattern (real persisted data vs. a silently-drifting in-memory mirror) found 3+ times already this session in other panels — **confirmed none exists here**: every write in this panel — the 5 textareas via Save Notes, every Tagged Note field via `sccSaveTagNoteField()`'s per-field autosave-on-blur, every one of the 3 quick-note entry points — goes straight to a real Supabase write, with no intermediate JS-only state that persists data has to catch up to later. This panel's real problems are a different shape, found below.
**Step 2 — cross-compare.** (1) Checked whether Quick Note's move into Tagged Notes (built in Phase 3a) left the 5 textareas redundant or confusing — confirmed still genuinely distinct: `SCC_NOTE_CATEGORIES` has near-1:1 thematic overlap with 4 of the 5 textarea themes (a real, but pre-existing and already-documented-in-app fact, not new confusion from this session's changes — the `.scc-tagnote-compare` strip already correctly frames the real distinguishing axis as capture *mode* — bulk end-of-session write-up vs. incremental timestamped tagging — not theme). (2) **Confirmed the real gap the task explicitly asked about**: `sccCopyPostSessionSummary()` reads only the 5 textareas' live DOM `.value` — grepped the full function body, zero reference to `SCC_NOTE_ENTRIES`, `session_note_entries`, or any Tagged Notes data anywhere in it. A DM who used Quick Note during the session (any of its 3 real entry points) gets a copied summary that silently excludes everything captured that way — and since Quick Note exists specifically to make mid-session capture *easier* than the 5-textarea recap, this excludes the more-likely-used path, not an edge case. (3) **A bigger, previously-undiscovered finding, found by tracing what actually happens when a DM opens History to review a past session's notes** — directly relevant to "findable and useful later," so investigated even though not explicitly named in the ask's bullet list. `.scc-audit-mode` hides `.scc-textarea` (all 5 recap fields), `.scc-tagnote-compare`, and `.scc-tagnote-section` in full via 3 separate blanket CSS rules — not scoped to just the edit controls, the entire Tagged Notes list disappears too. Confirmed `sccPopulateSetupForm()` *does* correctly load a historical session's real 5-textarea values into the DOM on history-load (so the data reaches the page) — they're then immediately hidden by the same rule that (correctly) hides Setup's own editable fields, with no read-only fallback display the way the date field already has one (`scc-audit-readonly-val`, an existing precedent in this exact function). Worse for Tagged Notes: grepped every call site of `sccRenderTaggedNotes()` (4 total: `sccLiveSaveNote()`, `sccSwitchPanel('notes')`, `sccSubmitQuickNote()`, its own internal re-call inside `sccAddTagNote()`) — `sccLoadHistorySession()` is not among them, so even an unhidden section wouldn't show the right session's notes. Net, confirmed via full trace rather than assumed: opening History and clicking into Notes today renders a section title and nothing else, for any completed session — the single most severe finding this round against the stated "findable and useful later" goal. (4) A smaller, real staleness bug at the session boundary: `sccCompleteSession()` clears the 5 textareas' DOM values and `SCC_SESSION` but never calls `sccRenderTaggedNotes()` or resets `SCC_NOTE_FILTER_CAT`/`SCC_NOTE_FILTER_Q` — a DM completing a session while already on the Notes tab sees the just-locked session's real Tagged Notes list linger on screen. Confirmed this self-heals (not a permanent staleness) the moment the DM navigates away and back, since `sccSwitchPanel('notes')`'s own existing call to `sccRenderTaggedNotes()` re-fetches correctly for whatever `SCC_SESSION` is current at that point. (5) Confirmed 3 real, distinct entry points into `session_note_entries` — the page-level `#scc-quicknote-fab` (every tab, full modal with category+title+body), Live Session strip's inline "Quick Note" (`sccLiveAddNote`/`sccLiveSaveNote`, always `category:'dm-only'`, no title, fastest for mid-fight capture), and the Notes tab's own "+ New Note" (`sccAddTagNote`, category picker, creates an in-place editable card). Grepped `SCC_VIEWING_HISTORY` usage — only the FAB modal's `sccOpenQuickNoteModal()` checks it before allowing a write; traced whether the other two could actually corrupt a locked historical session's real data if somehow reached during audit view, and confirmed the risk is real but narrower than it first looks: `sccLoadHistorySession()` never repoints `SCC_SESSION` at the historical session's row, so a stray write from either ungated path would land against whatever session was genuinely active *before* History was opened, not the locked session being viewed — a real correctness gap (wrong-session write, no warning), but not corruption of the audited record itself. (6) Missing functionality against "findable and useful later, not just captured": no cross-session search/browse for Tagged Notes exists anywhere — every fetch is hard-scoped to the current `session_number` — not yet painful with only Session 1 real, but a real, structural gap the moment a second session exists; Quest-Related tagged notes have no actual link to their real quest (unlike the `[[type:id]]` inline-linking convention already established for Field Guide/quest text elsewhere in this app), and structurally couldn't render one even if the data existed, since note content lives inside a plain `<textarea>`.
**Step 3 — proposed strategy, reported not built, sized per the same convention as the two prior audits.** Small: fix the post-complete staleness (`sccRenderTaggedNotes()` + filter reset inside `sccCompleteSession()`'s completion callback); add the same `SCC_VIEWING_HISTORY` guard the FAB modal already has to `sccLiveSaveNote()`/`sccAddTagNote()`/`sccSaveTagNoteField()`/`sccDeleteTagNote()`; a read-only display for the 5 textareas in audit mode, replicating the exact pattern this file already uses once for the date field rather than inventing a new one. Medium: make Copy Post-Session Summary also pull real Tagged Notes — needs an explicit fresh fetch (not trusting whatever happens to already be cached client-side in `SCC_NOTE_ENTRIES`) plus a formatting/grouping decision, not a one-line addition; restore genuine Tagged Notes visibility in audit mode — keep the section visible with add/edit/delete disabled rather than hidden, and — the actually-missing half — wire a real fetch into `sccLoadHistorySession()` so it loads *that* session's notes instead of nothing. Large, flagged not built: cross-session Tagged Notes search/browse, a real "look across every session" view structurally separate from today's single-session-scoped list, needing its own dedicated pass once more than one real session's data exists to build and verify against; Quest-Related note-to-quest linking, needing a real design decision (a linked-entity field vs. reusing manual `[[type:id]]` markup rendered outside the textarea in read mode) before any schema or UI work starts.
Zero `dm.html` changes this round, confirmed via `git status` before staging — only this file changed.


---

**2026-08-18 (continuing the same session — Live Session Phase 3b, the NPC/Location field-union merge deferred from Phase 3a below as the deliberately riskiest piece — real danger of a field silently disappearing across 3 independent render sites) — `dm.html`.**
Scope held exactly as given: NPCs and Locations only. Creatures/Items in the Field Guide sidebar were never touched — confirmed via a final diff read that `sccFgCardDetail()`'s `creature`/`item`/`dm` branches and the sidebar's own list-row summary logic are byte-identical to before this session.
**Step 1 — re-verified fresh, not trusted from the Phase 2 audit's own summary, per explicit instruction.** Read all 3 real render sites for each entity type directly rather than reasoning from memory of the earlier audit. NPC: Live Session's `sccFilterNpcs()`, the lookup drawer's `sccOpenLookup('character', ...)` branch, Field Guide sidebar's `sccFgCardDetail(e, 'npc')` — full verified field union: `read_aloud`, `personality`, `summary`, `appearance`, `motivation`, `contradiction`, `player_knowledge`, `gigglegloom_relationship`, a category/pronouns/status/affiliation fields row, `associated` (world-association tag cloud), `tags`, and a DM-only block (`secret`/`dm_notes`/linked-quest chips). Confirmed via `information_schema` that `world_characters` genuinely has no `description` column — Live Session's old `c.summary || c.description` fallback was dead code, not a real fourth field being merged away. Location: Live Session's `sccRenderLocationPanel()`, the lookup drawer's `sccOpenLookup('city', ...)` branch (serves cities/POIs/nations through one type), Field Guide sidebar's `sccFgCardDetail(e, 'location')` (POI-only) — full verified union: `read_aloud`, `summary`, `description`, `strategic_importance`, `tone`, `culture`, `beliefs` (nations only), `gigglegloom_notes`, `gigglegloom_relationship`, a `formery_present`/`government`+`capital_city` fields row, Population (cities only), Landmarks (cities only), Structures (cities only), Tags, a DM-only block (`vareth_presence`+`dm_notes`), and — POIs only — the real Shop section. Confirmed via `information_schema`, not assumed from the Step 1 NPC pass's own reasoning: `pois` has none of `landmarks`/`structures`/`population_data`/`formery_present`; `nations` has none of `landmarks`/`structures`/`dm_notes`/`color_health`/`strategic_importance`/`tone`/`formery_present`/`read_aloud` — every card's real field list is genuinely conditional on per-collection schema, confirmed rather than templated-and-hoped.
**The 4 flagged complex pieces, investigated in the depth asked for before any component code was written.** NPC linked-quest chips are not a stored field — computed fresh via a new `sccNpcLinkedQuests(npcId)` scanning `quests.related` for a matching `{collection:'characters', id}` entry, so any shared card has to compute this live, not just read a column. Landmarks vs. Structures confirmed genuinely separate systems via both code and schema, not a naming collision to reconcile: `landmarks[]` performs real entity-link resolution (a real `entry_id` substitutes the linked `CITIES`/`POIS` entity's own name/description/image), `structures[]` is a category-grouped directory where an `entry_id` only ever adds a static 🔗 flag icon with no resolution behind it — kept both, unchanged, side by side, since merging them would be an unrequested redesign of two different features, not deduplication. POI Shop confirmed as real transactional functionality (live buyer select, price/qty override inputs, a wired Buy button, an add-stock search box) that had to survive with zero functional regression, not just visually — treated with the same weight as the other 3 pieces rather than assumed safe because "it's just another section."
**Step 2 build.** New `sccBuildNpcCardHeaderHtml(c, opts)`/`sccBuildNpcCardBodyHtml(c)` and `sccBuildLocationCardHeaderHtml(loc, opts)`/`sccBuildLocationCardBodyHtml(loc)` — header/body deliberately split rather than one monolithic template, since the 3 sites need genuinely different shells (Live Session's card is click-to-expand with its own chevron/Quick-Look button; the lookup drawer wraps the header in its own `.scc-ql-header` divider and shows a `player_facing` badge via `opts.showPfBadge` that no other site renders; Field Guide's sidebar only ever consumes the body, since its own card-row/expand mechanism was explicitly out of scope). Content styling is `.scc-npc2-*` (Live Session's richer per-section icon+title+body treatment) as the primary look everywhere, with `.scc-ql-row`/`.scc-ql-tag`/`.scc-ql-fields`/`.scc-ql-header` reused pragmatically where they're a better fit for the content shape (label/value rows, tag clouds, the lookup drawer's own divider) — a deliberate, judged reuse rather than an accidental blend of two competing systems. Wired into all 3 sites: `sccFilterNpcs()`/`sccRenderLocationPanel()` (Live Session — `sccRenderLocationPanel()`'s own local `healthBadge()` helper is gone, folded into the shared header), `sccOpenLookup()`'s `character`/`city` branches (each replacing roughly 120 lines of duplicated field-rendering with one call), and `sccFgCardDetail()`'s `npc`/`location` branches only.
**3 real bugs found and fixed while preserving "existing interactive behavior... correctly," not part of the original ask but required by it.** (1) The NPC card's Linked Quests chip called the dead `sccSwitchPanel('quests')` (a target Phase 3a removed) — fixed to `sccOpenLookup('quest', q.id)`, matching this file's established cross-reference pattern, verified live from all 3 sites. (2) Relationship-badge cycling only ever refreshed Live Session's own render — cycling from inside the lookup drawer updated that badge but left Live Session's copy of the same NPC stale, a real cross-site desync where a DM could see two different relationship states for one NPC depending on which panel was last touched. Fixed via new `SCC_LOOKUP_TYPE`/`SCC_LOOKUP_ID` tracking globals (set in `sccOpenLookup()`, cleared in `sccCloseLookup()`) so `sccCycleRelationship2()` can re-open the drawer on itself when it's currently showing the NPC being cycled, alongside its existing Live Session refresh. (3) A newly-exposed async race, the same shape as Phase 3a's Party HP bug found and fixed 2 rounds ago: the Location card's Shop section reads `SCC_PLAYERS` for its buyer list, but `sccLoadPlayers()`'s async fetch chain can resolve after `sccRenderLocationPanel()`'s initial synchronous render — since `sccToggleLoc()` only shows/hides pre-baked HTML with no re-render, a card built during that window showed "No players" permanently. Newly exposed specifically because Locations now assembles its full Shop UI at initial page load, where before Shop only ever built on-demand inside the lookup drawer, well after `SCC_PLAYERS` had settled. Fixed identically to the Phase 3a pattern — added `sccRenderLocationPanel()` to `sccLoadPlayers()`'s existing completion callback.
**Step 3 verification, field-by-field and site-by-site, not eyeballed or assumed from the code alone.** NPC: Dran (rich DM-only content — `secret`+`dm_notes` populated — but genuinely empty `tags`/`associated`/`gigglegloom_relationship`/`read_aloud`, confirmed via SQL before treating their absence as correct rather than a regression) checked at Live Session and the lookup drawer; Gobblewump Gerald (tags, `gigglegloom_relationship`, `read_aloud`, `secret`, `dm_notes` all populated, and — confirmed via SQL — genuinely linked to the real quest "Something Is Wrong With Gerald") checked at Field Guide sidebar for the fullest field sweep: DOM-confirmed every section title present (Visualization/Personality/Summary/Appearance/What They Know/Gigglegloom Relationship/Secret/Notes/Linked Quests), confirmed the Linked Quests chip carries the fixed `onclick="sccOpenLookup('quest','something-is-wrong-with-gerald')"` rather than the dead panel-switch call, confirmed tags render, screenshotted. Location: Mira Pont's Sundry Goods (a POI — real DM-only shop-pricing notes, real tags) checked at all 3 sites — Live Session (full card screenshotted: header/Visualization/Summary/Description/Tags/DM-only block/Shop), the lookup drawer, and Field Guide sidebar (DOM-confirmed Visualization/Summary/Description/Tags/Notes/Shop all present, screenshotted) — confirmed Landmarks/Structures correctly absent everywhere for this entity since POIs genuinely carry neither column (re-confirmed via SQL at verification time, not just recalled from Step 1). **POI Shop tested end-to-end through a real click, not a direct function call**: added a temporary real `vendor_stock` row (Torch, catalog price, unlimited qty) at Mira Pont's, confirmed the buyer select listed all 3 real players, set the buyer to Kael Evander, fired the actual Buy button's own click handler — confirmed via SQL his `gold_cp` dropped 880→879 (exactly Torch's 0.01gp price) and a real `inventory` row landed (`quantity:1, dm_granted:false`), confirmed via DOM read the card stayed open and the Shop section correctly re-rendered in place afterward (the pre-existing re-render-in-place convention, now confirmed to also hold at Live Session, not just the lookup drawer it was originally built for). All test data reverted: Kael's `gold_cp` back to 880, the test inventory row deleted, the temporary `vendor_stock` row deleted, `session_notes.npc_relationships` re-confirmed unchanged at `{"oro":"ally"}` — all via a final SQL read. Zero console errors throughout. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty (one literal 💠 caught mid-build via the same recurring Edit-tool escape-normalization issue on record in this file's history, fixed via the established Python exact-string-replace workaround before it reached this checkpoint).


---

**2026-08-18 (continuing the same session — Live Session Phase 3a, the small/medium wins from the Phase 2 audit strategy below; Phase 3b, the NPC/Location field-union, deliberately not started) — `dm.html`.**
Ask was 10 items across 4 categories (Cut / Restructure / New Widget / Fix), explicitly scoped away from the riskiest piece (the field-union merging NPC/Location detail across 2-3 render sites) — that stayed untouched, exactly as instructed.
**Investigation before any edit.** Read the full 7-panel markup (`#scc-panel-npcs/locations/quests/combat/notes/live/fieldguide`), `sccSwitchPanel()`, every render function each panel's content depends on, the existing `sccTogglePrep()` collapsible pattern (reused rather than inventing a new one), and the layout CSS chain (`#form-session.active{height:100%}` → `.scc-panel{flex:1;overflow-y:auto}`) to understand *why* Field Guide's 2-pane layout needs a definite height, before deciding how to give it one inside a new home. Grepped every `sccSwitchPanel('npcs'|'locations'|'quests'|'fieldguide')` call site (found: only the 4 button definitions themselves, no other code called them) — this single fact is what made the whole restructure low-risk: nothing else in the codebase assumes those 4 panels are separately reachable.
**Markup surgery, done via Python exact-string extraction, not hand-retyped.** Extracted the NPCs/Locations/Quests panels' `.scc-panel-inner` divs and Field Guide's `.scc-fg-layout` div verbatim from the live file (byte-for-byte, via `str.find()` boundary markers, each extraction printed and eyeballed before use) rather than retyping ~9000 characters of existing markup by hand — the established lesson from this file's own history about the Edit tool's escape-normalization risk applies just as much to *accidentally introducing a transcription error* in a huge block as it does to Unicode escapes specifically. Reassembled into the new `#scc-panel-live` structure (control strip + `.scc-live-accordion` of 5 `.scc-collapsible-toggle`/`.scc-collapsible-body` pairs) and verified via `node --check`-equivalent + a full diff read before ever loading it in a browser.
**Real reasoning behind what got cut vs. kept, not just following the list mechanically.** DM Safety Net and Quick Actions weren't named in the ask's control-strip enumeration (Party HP/Conditions/Inspiration/current-beat/Die Roller/Timer) but also weren't listed under CUT — kept both rather than silently dropping real content the ask never said to remove. Quest Progress's own full beat list (`sccLiveRenderBeats()`) *was* silently removed even though it wasn't explicitly named in CUT — justified by the audit's own finding #1 ("Quest beat-progress tracking is genuinely duplicated") and the new Current Beat widget's explicit job ("shows only the currently active beat prominently," replacing a full editable list with a compact read-only summary) — keeping all three (old full list + new widget + Quests section) would have made the duplication worse, not better, directly contradicting the round's own stated goal. Recorded as a judgment call, not silently absorbed.
**Field Guide sizing, reasoned from the actual CSS cascade, not guessed.** `.scc-fg-layout{height:100%}` previously resolved against `#scc-panel-fieldguide.active{display:flex}`, itself a flex child of the fixed-height `#form-session.active`. Once Field Guide became an accordion section inside a naturally-growing scrollable page, that percentage-height chain had nothing left to resolve against. Rather than guess at a fix, traced exactly which ancestor needed a real height and added it at the correct single point — `.scc-collapsible-body.scc-live-acc-body--fieldguide.open{height:70vh}` — so `.scc-fg-layout`'s existing internal 2-pane independent-scroll behavior keeps working exactly as before, just inside a bounded box instead of the whole viewport. Made Field Guide's data load lazily on first expand (`sccLiveOpenSection('fieldguide')` triggers `sccRenderFieldGuide()`/`sccFgInitDocument()`, both already internally cached/idempotent) rather than eagerly at page load — no reason to fire 5 parallel fetches for a section collapsed by default.
**Two real async-timing bugs found during verification, not assumed away.** (1) Live-tested the fully-collapsed default view immediately after the markup landed and found Party HP genuinely rendering 0/0 for all 3 real players despite the top Party bar showing correct 12/12 data — traced to `sccLoadPlayers()` never having refreshed Live Session's own HP/Conditions/Inspiration widgets, invisible before since `sccRenderLivePanel()` only ever ran on a manual tab click (by which point the fetch had almost always already resolved); newly exposed because Live Session now renders synchronously at page load. Fixed by adding the 3 missing render calls to `sccLoadPlayers()`'s own completion callback. (2) Tested auto-expand fresh (real in-progress beat set via SQL, hard page reload) and found Quests stayed collapsed despite the Current Beat widget correctly showing the real data — traced to `sccLiveApplyAutoExpand()` being called from inside `sccRenderQuestPanel()` itself, which runs synchronously *before* `SCC_PROGRESS` has actually loaded in the bootstrap flow; the guard's first (premature, empty-data) call latched it permanently closed before the real data ever arrived. Fixed by removing the call from `sccRenderQuestPanel()` entirely and calling it only from the 3 specific bootstrap sites, after their own `sccLoadQuestProgress()` promise has genuinely resolved — re-verified with the identical SQL-set-then-reload test, confirmed Quests now auto-opens correctly while the other 4 sections stay collapsed.
**Verification, comprehensive, every item live-tested with real data and real reverts.** All 5 sections expand/collapse independently with correct real content (11 NPCs, real Locations with health badges, the real Quests card with rail+badges, 8 real Scenario groups, Field Guide's real document). Rail click-to-cycle confirmed still fully interactive (beat 1 → in_progress, confirmed via SQL, Current Beat widget and the Quests section's own sidebar box updated identically from the one shared `sccComputeActiveBeat()`). Beat status badges confirmed via DOM inspection to be real non-interactive `<span>`s, not `<select>`s. "+ Add Scenario" confirmed landing on the real Setup scenario editor with the jump-highlight visible. Quick Note: real note typed and saved, confirmed via SQL it POSTed into `session_note_entries` with `category:'dm-only'` and left `session_notes.player_decisions` untouched (`null`), confirmed it appears in the real Notes tab's Tagged Notes list, deleted afterward. Quest Dashboard: real completed+in-progress beat pair set via SQL, opened the Dashboard in a tab that never visited the Session tab first (the exact gap being fixed) and confirmed real status icons/XP rendered — reverted. Cross-checked zero regression on Combat, Notes, and the global-search lookup drawer's quest branch (now sharing `sccBuildReadOnlyBeatsHtml()` with the Dashboard) via real interaction, not assumption. Zero console errors across every check, multiple fresh tabs, multiple reloads. Screenshotted both the fully-collapsed default state and the fully-expanded state. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty (one literal checkmark caught in my own new toast string during this same check, fixed to the `✓` escape before it shipped).


---

**2026-08-18 (continuing the same session — fixed the Conditions/Inspiration data-loss bug the Phase 2 audit below found, item 3 of "Live Session") — `dm.html`.**
Ask was explicit and 3-part: confirm the real columns, repoint the controls to them (removing the shadow state entirely), and reason through whether this conflicts with the character sheet editing the same fields.
**1 — Columns confirmed, not guessed.** Grepped `sheet/index.html`'s own Conditions picker and Inspiration toggle (`addCondition()`/`removeCondition()`/`toggleInspiration()`) — both read/write `SHEET.conditions`/`SHEET.has_inspiration` via `saveSheet()`. Cross-checked against the live schema directly: `character_sheet.conditions` (array) and `character_sheet.has_inspiration` (boolean), keyed by `character_id` — real, populated (if currently empty) columns, not a stale/dead pair.
**2 — Repointed, shadow state fully removed.** `SCC_LIVE_CONDITIONS`/`SCC_LIVE_INSPIRATION` (module-level globals, confirmed via a full-file grep to have zero references outside the 6 functions being rewritten) deleted outright, replaced with an explanatory comment rather than left as dead declarations. All 6 dependent functions (`sccLiveRenderConditions`/`sccLiveAddCondition`/`sccLiveConfirmCondition`/`sccLiveRemoveCondition`/`sccLiveRenderInspiration`/`sccLiveToggleInspiration`) rewritten to read `SCC_SHEETS[playerId]` — the exact cache `sccLiveAdjustHp`/`sccLiveSetHp` already read for Party HP, two rows above these in the same panel — and PATCH `character_sheet` via `sccPatch('character_sheet', '?character_id=eq.'+encodeURIComponent(id), {...})`, matching HP's fire-and-forget-with-toast-on-failure pattern exactly rather than inventing a new persistence style for this one panel. Confirmed via grep that Combat tab's separate `SCC_INITIATIVE[idx].conditions` (per-encounter, genuinely meant to be in-memory) is untouched — a different concept, not a second instance of the same bug.
**3 — Conflict check, reasoned through rather than assumed safe.** This now shares a write target with the player's own sheet, same as HP already does — plain last-write-wins PATCH, no locking, no realtime sync anywhere in this app. That's an already-accepted risk class here (HP has worked this way since it was built), so Conditions/Inspiration follow the identical model rather than getting a bespoke read-only mode — a DM setting a condition mid-fight and a player toggling their own Inspiration are both legitimate, both should land in the one real place either surface already looks.
**Mechanical note**: the Edit tool's string-matching repeatedly failed against this block on the first several attempts because the source uses the literal 6-character text `•` (not a rendered bullet glyph) inside a JS string literal — confirmed via `xxd` before finally getting a clean match via a direct Python exact-string replace, the established workaround this file's own history already documents for this exact class of Edit-tool mismatch.
Verified live, both directions, against real Supabase writes — not just DOM state. Set Hana Sable's condition to "Poisoned" and toggled Kael Evander's Inspiration on through the actual Live Session UI (real click-equivalent calls into `sccLiveAddCondition`→pick→`sccLiveConfirmCondition`, `sccLiveToggleInspiration`), confirmed both landed in Supabase via direct SQL immediately after. Fully reloaded the page in a fresh tab (re-authenticated, re-navigated Session → Live Session from scratch) and confirmed both persisted — Hana's "Poisoned" chip and Kael's active Inspiration token both rendered correctly from a freshly-fetched `SCC_SHEETS`, the exact case that was silently broken before. Separately opened both characters' real `sheet/index.html` (via their real tokens) and confirmed the identical state renders there — Kael's header shows the gold "INSPIRATION — Yes" badge, Hana's shows a "Poisoned ×" pill — confirming this is genuinely one shared column read by two surfaces, not two independent copies that happened to agree. Screenshotted all three surfaces. Reverted both test values afterward (`conditions:[]`, `has_inspiration:false` on both, confirmed via a final SQL read matching the real pre-test state on all 3 characters) since neither reflects real session state. Zero console errors throughout. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty.


---

**2026-08-18 (new session — Phase 2 comprehensive audit of Live Session, Field Guide, and the NPCs/Quests/Locations sub-tabs, plus the separate Quest Dashboard, ahead of a possible merge into one "Live Session" tab. Investigation and a written strategy only, no code changed.) — `dm.html` read-only; full report published as an artifact, this entry is a condensed record.**
Delegated the 3-part read to parallel research agents rather than doing it serially myself, given the scope (7 sub-tabs' worth of code across a 16,000+ line file) — each agent instructed explicitly to read the real current code and cite real function/column names, not summarize from memory or trust the task's own background guesses without verifying them. Synthesized their 3 independent reports into one cross-comparison and merge strategy myself, since that step requires holding all the findings at once, not something to also delegate.
**Live Session** (`#scc-panel-live`, `sccRenderLivePanel`): 12 real features enumerated — Quest Progress (beat cycling, mutates real `quest_progress`), Party HP (real, persists to `character_sheet.hp_current`), Conditions (in-memory only, confirmed never persisted anywhere), Inspiration (same — in-memory only), Quick Actions (nav buttons), a local global-search box (own reimplementation, narrower than the header's), Scenarios (reads `session_notes.session_scenarios`, "used" state in-memory only, no in-panel editor), Quick Note (writes into a *different* sub-panel's own DOM textarea, `session_notes.player_decisions`), NPCs This Session (thin chip list), Die Roller (self-contained), Timer (in-memory, confirmed it keeps running across tab switches since it's never torn down), DM Safety Net (static text). Confirmed only Quest Progress and NPCs read `SCC_SELECTED` at all — Locations and Items assigned in Setup have zero representation anywhere in this panel.
**Field Guide** (`#scc-panel-fieldguide`): confirmed two fully independent sub-features sharing no code. (a) Sidebar — session-scoped read-only reference cards for NPC/Creature/Location/Item/DM-Notes, filtered by Setup's assignments, falling back to unfiltered if none. (b) Document — `field_guide_sessions` table, one row per session, a `blocks` jsonb array; confirmed the real current box-color enum is 5 values (`grey/green/magenta/gold/yellow` — blue/purple fully retired, not 6 as the task's own background note guessed), confirmed `[[type:id|Label]]` link parsing (`sccFgLinkify`) supports exactly 6 entity types (narrower than the 7-type "open in new tab" set elsewhere), confirmed the editor holds a full in-memory working copy with one Save button doing one PATCH/POST of the whole array — not per-field autosave. Everything in the document is DM-authored; only the loot-icon lookup and link rendering are computed.
**NPCs/Quests/Locations sub-panels + Quest Dashboard**: confirmed the Session-tab "Quests" sub-panel and the top-level Quest Dashboard tab are two completely uncoupled surfaces with no code path linking them, despite both operating on `quests`/`quest_progress` — worth stating plainly since it would be easy to assume one is a subset of the other. For each of the 3 sub-panels, precisely answered "what can a DM do here that global search + the lookup drawer can't": NPCs — see `read_aloud`/personality/secret/dm_notes/linked-quests (none in the lookup drawer's character card), lose category/pronouns/status/affiliation/motivation/contradiction/appearance/associations/tags (all lookup-only); Locations — see `read_aloud`/landmarks(with real entity-link resolution)/dm_notes, lose a much longer list (strategic_importance/tone/culture/beliefs/gigglegloom/government/population/structures/tags/Shop) — confirmed this is the most lopsided of the three, no side is a subset of the other; Quests — the sub-panel is the *only* place in the app that writes `quest_progress` at all (the lookup drawer's identical-looking beat rail is `pointer-events:none`), while the Quest Dashboard is the only place that edits `quest.status` and the only place that shows `summary`/`reveals`/`related`/suggested-level, and confirmed via a full grep that the Dashboard never fetches `quest_progress` at all — a real, non-cosmetic gap if a DM ever tries to run a session from that tab.
**Cross-comparison**, all specific findings quoted in the report rather than repeated here in full: 4 genuine duplicates (beat-progress tracking, the local search box, the NPCs-chip stub, relationship-cycling implemented twice), 5 near-duplicates flagged as genuinely confusing (NPC detail split 3 ways, Location detail split 2 ways, Quest capability split 3 ways by *mechanism* not just fields, Conditions/Inspiration looking real but not persisting, "Quick Note" not actually being a categorized note), plus real gaps against the original stated pain points (no current-beat-to-NPC/location cross-referencing, zero location progress-tracking, no mid-session way to add a Scenario, and a genuine reliability gap — Conditions/Inspiration/scenario-used-marks/the Timer are all silently lost on a page reload mid-session).
**Recommended merge strategy**, reported not built, full detail in the artifact: one Live Session tab with an always-visible control strip (HP, Conditions and Inspiration once fixed to persist, current beat, dice, timer) plus a row of collapsible sections below it (NPCs/Locations/Quests/Scenarios/Field Guide) reusing this app's own established accordion convention rather than a vague "make it accessible" — sections default collapsed except whichever is contextually relevant, expand in place, never navigate away from the tab. NPC/Location card rendering unions the currently-split field sets into one shared component used everywhere (replacing 2-3 independent copies); the two beat-progress renderers merge into one. Quest Dashboard stays separate (genuinely non-overlapping purpose confirmed above) with one additive fix. Quick Note reroutes to the real `session_note_entries` table. Every recommendation tagged with a build-size estimate (small: search-box removal, section reorg, exposing the existing Setup scenario-editor into Live Session; medium: the current-beat widget, Quick-Note rerouting, Dashboard progress-awareness; large, flagged explicitly as needing their own dedicated passes: the NPC/Location field-union component, and migrating Conditions/Inspiration to the real persisted `character_sheet` columns, which is a real behavior/sync change, not a rewire).
No `dm.html` changes this session. Confirmed via `git status` before pushing that only this file (plus the pre-existing, unrelated `.DS_Store`) changed.


---

**2026-08-17 (same session as the investigation below — DM confirmed the recommended design, built the 5 carryover-suggestion rows, "Open in new tab"-adjacent Combat Encounters explicitly deferred) — `dm.html`.**
Scoped exactly to what was confirmed: the 5 existing categories (Locations/NPCs/Creatures/Quests/Items) get a "Suggested from Session N" row; Combat Encounters is untouched, per the investigation's own conclusion that it needs a real schema decision first, not a UI-only add.
**Markup**: one label + one chip-row div added per `.scc-picker-group`, above its search controls — reused Combat's own `.scc-combat-session-label`/`.scc-combat-session-creatures` classes verbatim rather than inventing near-duplicate CSS, since the visual/behavioral shape (label + wrapping row of pill chips) is identical to what Combat's "From Session Roster" row already does. Both start `display:none` in the static HTML so there's nothing to flash before JS decides whether to show them.
**`sccPreviousSessionRow()`**: the one piece of real design judgment in this build, not in the original investigation. Reads the hidden `#scc-session-number` input rather than `SCC_SESSION.session_number` — `SCC_SESSION` is `null` for the entire window between a session completing and the next one being saved (confirmed by reading `sccCompleteSession()`/`sccLoadLatestSession()`), which is exactly when a DM opening a fresh Setup would most want to see what carried over. `sccSetSessionDisplay()` already keeps that input correct in both the real-session and pending-next-session branches, so reading it instead of `SCC_SESSION` directly costs nothing and covers a real case the naive version would have silently gone blank for.
**`sccRenderSuggestedRow(type)`** is pure and synchronous — no fetch of its own — so it's safe to call on every chip mutation; wired into the tail of the existing `sccRenderChips(type)` (already the one function every add/remove path already calls), so the ✓-already-added state stays correct regardless of whether a chip came from the suggested row, the search box, or Browse All. Hides both the label and the row — not an empty-state message — the moment there's nothing to show, checked at 3 separate points (no previous session; previous session assigned nothing in this category; nothing it assigned still resolves against the live catalog, e.g. a since-deleted entity).
**Quest in-progress badge**: needed `SCC_PROGRESS` populated for the *previous* session's quests specifically — the existing `sccLoadQuestProgress()` call in `sccLoadLatestSession()` only ever covers the *current* session's own `quest_ids_active`, so a quest carried over from last time would never have had its beat data loaded at all. `sccRenderSuggestedRows()` (the one-time orchestrator, called from both the success and catch paths of `sccLoadLatestSession()`) issues one extra `sccLoadQuestProgress()` call for the previous session's quest ids, merging into the same shared `SCC_PROGRESS` object every other beat-status UI in the app already reads from — not a parallel/duplicate cache.
**Verification, real and synthetic, both reported honestly.** Real data: confirmed live (not assumed from the investigation, which was done in an earlier turn) that only Session 1 exists — reloaded Setup fresh, confirmed via direct DOM read that all 5 suggested-row elements exist in markup but are genuinely `display:none` with empty `innerHTML` (not just visually absent by coincidence), and `sccPreviousSessionRow()` returns `null`. Screenshotted this real, current, correct resting state. Synthetic test: pushed one fake `session_number:0` row directly into the in-memory `SCC_ALL_SESSIONS` array — never touched Supabase, so a page reload is a complete, guaranteed revert, confirmed afterward (`SCC_ALL_SESSIONS` back to exactly the 1 real row). First attempt at the injection raced `switchTab('setup')`'s own fresh data reload and got silently overwritten — not a code bug, a test-sequencing mistake, caught by re-checking `sccPreviousSessionRow()` came back `null` when it shouldn't have, then redone correctly by waiting for the real fetch to settle first. Once sequenced correctly: all 5 rows rendered with real resolved names; the ✓-already-added state correctly triggered for 3 of the 5 test picks that happened to already be in the current session's own chips (a real, not staged, exercise of that branch); a real click on the one not-yet-added suggested chip correctly added it via `sccAddChipById` and the chip itself flipped to checked, confirmed via both DOM read and screenshot. Quest badge verified as two separate, honestly-labeled halves: the fetch-and-merge path was let run for real against a quest with no actual in-progress beats (confirmed via SQL earlier this session) and correctly showed no badge — proving the merge logic isn't just trusting whatever was last injected; the render path for a genuinely in-progress beat was then confirmed separately by setting `SCC_PROGRESS` directly, since no quest in the live campaign has a real in-progress beat yet to exercise the full path end to end. Zero console errors throughout. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty.


---

**2026-08-17 (new session — Session Setup redesign investigation, 3-part report, no code changed) — investigated Combat encounter pre-staging and carryover-suggestion signals against the real current `dm.html` state and live Supabase data before any design was committed to.**
**Part 1 (combat encounters)**: confirmed via code trace (`SCC_COMBATANTS`/`SCC_INITIATIVE`, every function touching them) and via `list_tables` against the live `anavale` Supabase project (25 real tables, no `combat_encounters` or equivalent) that the entire Combat encounter builder is ephemeral client-side state — zero persistence anywhere. The existing "Setup preps it, Combat quick-adds it" pattern (`sccRenderCombatSessionCreatures()`/`SCC_SELECTED.creature`) is structurally the wrong shape to reuse: it's a deduped unique-id set (`sccPickerSelect()`'s own dedup guard blocks re-adding an id for every type except the special-cased `combatant`), while a real encounter needs quantities. Concluded: needs new schema (nothing today can express creature+qty), but *loading* a saved encounter into live Combat once that schema exists is cheap — `sccAddToCombatStaging(id, name)` already tolerates repeated calls for the same id, so "Load Encounter" is that function called in a loop, no changes to staging/initiative/loot logic at all. Flagged, not addressed: `sccAddToCombatStaging()` only ever resolves `CREATURES`/`SCC_PLAYERS` — NPC villains have never been addable to Combat, pre-built encounters inherit that same gap.
**Part 2 (carryover signals)**: queried live Supabase directly rather than reasoning from schema shape alone. Confirmed real and derivable today with zero new schema: `quests.status` (7 real `available` quests right now), the previous session's `session_notes` row (already loaded client-side as `SCC_ALL_SESSIONS` for the history dropdown — no new fetch), and `quest_progress`'s per-beat `status`/`completed_in_session` (an `in_progress` beat is a sharper "pick this up" signal than session-level assignment, since it reflects real play rather than DM intent). Confirmed NOT derivable without new tracking: `session_note_entries` (Quick Notes, including the Follow-Up category) is pure free text — `title`/`content` only, no `entity_type`/`entity_id` or any structured reference — so a "Follow-up: check on Dran" note can't become a suggested NPC chip without either new schema (an optional linked-entity field on the note) or free-text name-matching, which this project has already explicitly rejected once for this exact dataset's false-positive risk (short, common-word-adjacent real names — the reasoning on record for why Field Guide/quest links use manual `[[type:id]]` tagging instead of auto-detection). Recommended the same manual-tagging precedent here rather than reintroducing that risk. Also confirmed "assigned in Setup but never resolved" has no flag anywhere in the schema and would have to be inferred — not recommended, since the `quest_progress` `in_progress` signal already covers the sharper version of the same underlying need.
**Real-data grounding, checked rather than assumed**: queried the actual campaign state before finalizing any recommendation — only one session exists (`session_notes`, session 1, still `session_status:'active'`, not completed), every beat across all 8 real quests in `quest_progress` is still `not_started` (zero play progress recorded yet despite Session 1 having happened), and `session_note_entries` has zero real rows. The recommended signals are structurally sound and buildable, but none of them have real data to demonstrate against yet beyond Session 1's own Setup picks — stated plainly in the report rather than fabricating a demo scenario.
**Part 3 (recommendation, reported not built)**: 6 categories — existing 5 (Locations/NPCs/Creatures/Quests/Items) plus a new Combat Encounters category requiring a real migration (`session_notes.combat_encounters` jsonb, `{name, combatants:[{creature_id, quantity}], notes}`) and its own quantity-aware UI, explicitly not a reuse of the existing dedup-only `scc-picker-group` component. Carryover surface: a "Suggested from Session N" chip row per category, for the 5 existing pickers only (Combat Encounters has no history yet to suggest from) — sourced from the previous session's row, same visual/behavioral pattern as the existing quick-add chips (✓-used-×N, clickable indefinitely), wired to the already-existing `sccAddChipById()` rather than new add-logic. Quests get one extra layer: badge/prioritize any quest with a real `in_progress` beat. Combat Encounters called out explicitly as the one piece that's a real schema project requiring DM sign-off before any build, separate from everything else, which is buildable against data that already exists today.
No `dm.html` changes this entry — pure investigation, per explicit instruction to report before building.


---

**2026-08-17 (new session, process addendum — new Debugging Rule D8, no code changed) — `CLAUDE.md`.**
Prompted directly by the "open in new tab" thread two entries below: across three separate rounds, this session's sandboxed Browser-pane tool proved structurally unable to verify a real cross-tab `sessionStorage` inheritance test — every attempt (direct click, ref-based click, Cmd-modified click, `window.open()` fired from a real click handler) converted the `target="_blank"` navigation into a same-tab navigation instead of creating a second, independently-inspectable browsing context. This was tried and confirmed, not merely suspected, each round — and each round still shipped a code fix before finally saying so plainly and asking the DM to do the real check themselves, which is exactly backwards: the DM caught the second round's incompleteness only because they ran their own real-browser test, not because the tooling here ever could have.
Added **D8** to §7 Debugging Rules (renumbered the section heading D1–D7 → D1–D8): the moment a testing tool is confirmed structurally unable to observe the thing actually in question, say so immediately and ask the DM for one concrete real verification step — do not spend further rounds writing another fix and re-running the same approximated/partial test as if a different code change might make an incapable tool suddenly capable. The rule cites the exact `target="_blank"`-becomes-same-tab-navigation case on record here as the concrete example, so a future session recognizes the same shape of limitation on sight rather than re-discovering it fix-by-fix.
No `dm.html`/`sheet/index.html` changes this entry — pure process update, per the DM's explicit ask.


---

**2026-08-17 (new session, "Open in new tab" confirmed by the DM to still re-prompt for the password after the prior round's fix — that fix addressed a real cause but not the whole one) — `dm.html`.**
DM tested the previous fix directly and for real (not through this session's own sandboxed tooling): `sessionStorage.dm_auth` came back `null` in a genuine new tab, same port, ruling out an origin mismatch. Explicit instruction: don't assume the removal of `rel="noopener"` was close to right, investigate further, check for a second implementation, re-verify the live DOM directly, check for anything else that could sever the opener relationship, and get a real cross-tab test if at all possible.
**1 — Second/duplicate implementation, checked and ruled out.** Grepped every `target="_blank"` and `window.open(` in the file: exactly one `id="scc-lookup-newtab-link"` exists. The other two `target="_blank"` links (Players tab "Open Sheet ↗", a city's "MAP" link) are unrelated, pre-existing features with no shared code path. Checked all 3 document-level `click` listeners in the file for anything that could intercept or `preventDefault()` this specific link — none do. No `onclick`/`addEventListener` attached to the element itself. The link actually rendered in the UI is unambiguously the one code path that was touched.
**2 — Live DOM re-verified directly, not trusted from the source diff.** Opened a real lookup card through the actual UI and read the rendered element's `outerHTML`/`getAttribute()`/`relList` directly: `href` resolves correctly to the real same-origin URL, `target="_blank"` present, and — confirming the prior round's edit had genuinely landed, not silently reverted or shadowed — `rel` attribute absent at that point in the investigation, matching what should have been there after removing `noopener`.
**3 — What else could sever the opener relationship, researched rather than guessed.** `WebFetch`'d MDN's `Window.opener` page directly (not relying on general web-dev memory, since this exact nuance — the interaction between `target="_blank"` and default opener behavior — is one browsers have actively changed over time). Found the real gap: *"Windows opened because of links with a target of `_blank` don't get an opener, unless explicitly requested with `rel=opener`."* Cross-checked against the separate MDN `rel` attribute reference page, which independently confirms `rel="opener"` as "effectively, the opposite of noopener," needed specifically because `target="_blank"` no longer preserves the opener by default in modern browsers — a real, current browser security default, not a bug in this app's own code, and one that flips the older assumption the original "just don't add noopener" fix relied on. Removing `rel="noopener"` was a real, correct step — it just wasn't the complete fix, because the browser was already withholding the opener by default with or without that attribute present.
**Fix**: added `rel="opener"` to the link, restoring the explicit request for the auxiliary-browsing-context relationship that `target="_blank"` alone no longer guarantees. Documented the reasoning directly in the HTML comment, including the exact MDN wording, so a future session doesn't rediscover this the hard way a third time.
**4 — Real cross-tab test, attempted again, same documented ceiling hit, not glossed over a third time.** Re-verified the live DOM post-fix (`relList: ["opener"]`, read directly off the rendered element). Attempted a genuine click through the real UI once more — identical result to both prior attempts this investigation thread: this Browser-pane sandbox converts the `target="_blank"` navigation into an in-place navigation of the *same* tab rather than creating a second, independently-inspectable browsing context. `tabs_context` never showed a second tab across any attempt, this round or the last. This is now confirmed, three separate times, across two different sessions, to be a structural property of this specific tool, not something further retrying overcomes. Explicitly not claiming this is fixed based on that same-tab result, since same-tab navigation was never capable of exercising the real cross-context question in the first place — consistent with the instruction not to declare victory a second time without the DM's own real confirmation.
Zero console errors throughout. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty. Diff is the `rel` attribute change plus its explanatory comment, nothing else touched.
**Status: fix applied and well-sourced, not yet confirmed by a real cross-tab test.** Needs the DM to click the real link in a real browser and check `sessionStorage.dm_auth` in the resulting tab, same as the check that caught the prior round's incompleteness.


---

**2026-08-17 (new session, "Open in new tab" re-prompting for the DM Tools password — the real mechanism, confirmed via spec/MDN, not the sessionStorage-preseeding approximation the feature's original build relied on) — `dm.html`.**
DM's own framing was the correct hypothesis going in, and the ask was explicit: confirm it, don't assume it. Investigated in the order given.
**1 — Exact link markup.** Grepped the live file: `<a id="scc-lookup-newtab-link" ... target="_blank" rel="noopener" ...>` — a plain anchor, not `window.open()`, not JS-constructed. Confirms the real mechanism is native browser link-following, which matters because the sessionStorage-copy behavior in question is specifically an anchor/`window.open()`-triggered "auxiliary browsing context" concept, not something a different open mechanism would even be subject to.
**2 — `rel="noopener"`, present.** Added when this feature was originally built (prior session), as a routine security default for any `target="_blank"` link — reasonable in isolation, wrong for this specific link's actual job.
**3 — What the gate checks, and whether a real new tab receives it.** Grepped: the gate is exactly `sessionStorage.getItem('dm_auth') === '1'`, two call sites (initial DOMContentLoaded check, and the value set on successful password entry) — no other check, no cookie, nothing else. Confirmed via `WebFetch` against MDN's `Window.sessionStorage` documentation (chosen over relying on general web-dev knowledge, since this exact edge case — the interaction between `noopener` and storage inheritance — is a real, sometimes-misremembered browser quirk worth sourcing directly rather than asserting from memory): *"If the page has an opener, the sessionStorage is initially a copy of the opener's sessionStorage object... To prevent the sessionStorage from being copied, use one of the techniques that remove opener."* `Window.opener` docs list `rel="noopener"` as exactly one of those techniques. This is a direct, unambiguous match to the reported symptom: `rel="noopener"` severs the opener reference the browser's storage-copy algorithm depends on, so a new tab opened via this link never receives `dm_auth` at all, and the gate — correctly, from its own narrow point of view — re-prompts.
**4 — Fix.** Removed `rel="noopener"` entirely; `target="_blank"` alone preserves the default opener relationship in every current browser, no replacement attribute needed. Left an explicit comment on the tradeoff rather than silently dropping a security attribute: `noopener` exists to block reverse-tabnabbing from *untrusted* content loaded in the new tab, and this link only ever opens the same app at the same origin — the risk the attribute guards against doesn't apply to a same-origin link into your own already-trusted page.
**Verification — tried harder than last round, and honest about where that effort hit a real wall.** Attempted a genuine click through the actual live UI, not a simulation, via `read_page` + a real coordinate/ref-based `left_click` on the link — same as last round's approach, but this time without pre-seeding anything, starting from an already-authenticated tab reached the normal way. Result: no timeout this time (last round's clicks consistently hung 30s; this round's didn't, for reasons not fully explained but not the point of this investigation), but this specific Browser-pane sandbox also never surfaced a second, independently-inspectable tab in `tabs_context` — the click's destination loaded, but in the *same* tab, not a new one. Suspecting popup-blocking specifically, tried two more genuinely different trigger mechanisms to rule that out: `window.open()` called directly from script (returned `null` — blocked, confirming programmatic calls without a trusted gesture are blocked here, as expected) and `window.open()` called from inside a real `computer`-driven click event handler on an injected test button (removed after) — trusted-gesture-triggered, and *also* resulted in same-tab navigation rather than a real second tab, not a `null`/blocked result this time. Tried a Cmd-modified click on the real link too, same outcome. Four distinct approaches, one consistent result: this harness converts `target="_blank"` requests into same-tab navigation rather than creating a second browsing context I can inspect independently, regardless of how the navigation is triggered. This is the same wall the original "open in new tab" build session hit — that session worked around it by pre-seeding `sessionStorage` in a tab created via `tabs_create()` instead of a real opener relationship, which is exactly the approximation that let this bug ship undetected, since it never actually exercised the real opener-dependent code path at all.
Given that ceiling, verification rests on two legs, both stated plainly rather than papered over: the MDN documentation above (unambiguous, directly on-point, not a stretch), and confirming live that the fixed link's destination loads correctly with the gate never appearing and zero console errors when clicked for real (weaker on its own, since the same-tab navigation this harness performs was never going to touch the gate regardless of the fix) — real, positive signal that nothing broke, not proof of the cross-tab mechanism specifically. No further tool-side workaround found one after genuinely trying several; not claiming a stronger test was performed than was. Screenshotted the successful landing (Drak's edit form via the deep link, gate absent, `sessionStorage.dm_auth` confirmed still `'1'`).
Zero console errors throughout. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty. Diff is exactly one attribute removal plus its explanatory comment.


---

**2026-08-17 (new session, 3-bug QA round on the just-shipped "Open in new tab" work — Manage filters resurrecting, search dropdown covering the search bar, new-tab link reported invisible — all three investigated to a live-confirmed root cause before any fix) — `dm.html`.**
Explicit instruction: investigate each, don't guess, handle independently since they're unrelated areas. All three ended up sharing a theme — a plausible-sounding assumption (persistence, a hardcoded offset, an accessibility-tree check) turned out to be either wrong or merely insufficient once checked against real, live DOM state.
**Bug 1 — filters reappearing after removal, "stacking to 7 filters, 0 of 283 results."** Checked (a) where filter state actually lives first: grepped the whole file for `localStorage`/`sessionStorage` — zero hits touching any `cm-*-filter` id. State lives purely in each `<select>`'s own `.value`, nothing else. (b) Confirmed `cmRemoveFilter(id)` (the "×" handler) correctly does `el.value = ''` then re-applies — tested in isolation, works. (c) Confirmed it's *not* "switching tabs" in general — Manage→Quests→Manage alone never resurrects anything, reproduced and confirmed clean. The actual trigger is `switchTab('add')`, which fires not just from clicking the Add tab directly but from `cmEditEntry()` too — meaning any DM editing any Manage-tab row hits this. (d) Confirmed a genuine full page reload always gives a clean slate — no persisted state survives a reload, so there's no "was this ever supposed to persist" question to put to the DM; it wasn't, and still isn't.
Root cause, found by reading `sortAllDropdowns()` (called on every `switchTab('add')`) rather than guessed: it rebuilds each filter `<select>`'s option list by `while (sel.options.length) sel.remove(0)` then re-adding sorted. Removing options one at a time forces the native `<select>` to auto-reselect a replacement option every time the removal happens to empty out whatever was currently selected — and since the loop keeps removing from index 0 as the list shrinks, this cascades: each removed "currently selected" option hands selection to whatever's now at index 0, which then itself gets removed next iteration, and so on, until the list is empty. The option that happened to be selected *last*, right before the list emptied out, can retain a residual `selected=true` flag on its own detached node — a flag that was never explicitly reset, since `.value=''` earlier had only deselected whatever was selected *then*, not whatever this internal cascade later selects in passing. When that node gets re-added during the rebuild's second phase, the HTML spec's own rule for inserting a `selected=true` option into a select — it becomes the new selection, deselecting everything else — silently overrides whatever the DM had actually left selected (or explicitly cleared), with no visible symptom until the next real filter interaction re-renders the chips/count off the now-corrupted `.value`.
Reproduced deterministically, live, before writing any fix: set `type=item, vis=visible, category=weapon` (37 of 283), removed `category` via its own chip (confirmed cleared, 132 of 283), then `switchTab('add'); switchTab('manage')` — `category` came back as `weapon` *and*, more strikingly, `type` (never touched by the removal) flipped to `region`, an option that had never been selected at any point in the test session, illustrating the cascade isn't "restore the last real value" so much as "whatever survived the churn."
Fix: `sortAllDropdowns()` now does `var savedValue = sel.value;` before the strip-and-rebuild, and `sel.value = savedValue;` after — setting `.value` per spec deselects every option and selects only the one genuinely matching, so no leftover node-level artifact from the churn can win regardless of what the cascade did internally. One function, one fix, covers every select it touches — also closes the identical latent risk on `#q-status-filter`/`#q-stakes-filter`/`#q-session-filter` (Quests tab) and `#add-type-select`, none of which had been reported broken yet but shared the exact same mechanism.
Verified live: repeated the full repro (4 filters set, one removed via its chip, round-tripped through `switchTab('add')`+`switchTab('manage')`, *and* separately through a real `cmEditEntry()`+`cmCancelEdit()` cycle, *and* through Quests-tab round trips) — final `.value`s, chip labels, and count all matched exactly what should have remained after the one real removal, every time. Reload-gives-clean-slate behavior re-confirmed unchanged (283 of 283, every filter empty).
**Bug 2 — search results dropdown covering the search input while typing.** Investigated (a) the CSS positioning and (b) the dev banner's role, both explicitly asked. `.scc-global-results` was `position:fixed; top: 48px` — a bare number with zero reference to `--dm-env-banner-h`. Measured live: with the banner showing (33px real height, confirmed via `getComputedStyle`), the search bar itself sits at `y:57–87px` (pushed down by `.dm-app`'s own correct `margin-top: var(--dm-env-banner-h)`), while the fixed-position dropdown stayed pinned at the pre-banner `y:48px` — starting *above* the search bar's own top edge and covering it outright once opened. A first fix — `top: calc(84px + var(--dm-env-banner-h))`, 84px being the real measured `.dm-header` bottom edge at zero banner height — correctly cleared the search bar at normal desktop width, but broke again the moment the viewport narrowed enough that the header wraps to multiple lines (the title stacks "ANAVALE / DM / TOOLS" across 3 lines, the subtitle wraps too) — the header's real height at that width is nothing like 84px, and no fixed number chasing a variable-height header can stay correct.
Fixed properly, not by chasing a bigger constant: dropped the hardcoded/calc'd offset entirely. New `sccPositionGlobalResults()` reads `.scc-global-search-wrap`'s own live `getBoundingClientRect()` and sets the dropdown's `top`/`right` directly from it — called once at the top of `sccGlobalSearch()`, covering every place results get shown (the "no results" empty state and the real results list both flow through the same function). This is correct at literally any viewport width or header height, banner present or not, with zero dependency on `--dm-env-banner-h`.
Verified live via real typed input (clicked into the box, typed, screenshotted — not `sccGlobalSearch()` called directly with a query argument, which wouldn't exercise the actual `oninput` path or show real typed text): desktop width, banner genuinely present — dropdown opens a clean 30px below the search bar, input text ("gerald") fully readable throughout. Narrow/wrapped-header width — 8px gap, still no overlap, confirmed via `elementFromPoint`-style rect math (`resultsRect.top < searchRect.bottom` false in both cases). One thing surfaced and deliberately not chased: at the narrow width the 380px-wide dropdown extends a few px past the viewport's left edge (a pre-existing, fixed-width-vs-narrow-viewport cosmetic gap, not what was reported and not touched — the report was specifically about obscuring the input, which is resolved).
**Bug 3 — "Open in new tab" reported completely missing this round, contradicting last round's DOM-only verification.** Investigated in the exact order asked. (a) Dev banner confirmed rendering correctly (rules out an environment/deploy mismatch, cheap check, done first). (b) Confirmed the link exists in both the committed file and the live DOM, with the exact correct `href` for a real NPC — so (d) never applied, the element was never missing from the DOM; last round's report was accurate as far as it checked, just incomplete, exactly the theory given. Checked the *actual rendered state*, not just `href`, per instruction: `display:flex`, `opacity:1`, on-screen by its own coordinates — all looked fine by those measures alone, which is exactly what would make an accessibility-tree/DOM check report success while a real user sees nothing. The real signal came from `document.elementFromPoint()` at the link's own visual center: it returned `dm-env-banner`, not the link — something else is painted on top of it.
Root cause: `.scc-lookup-panel` (`position:fixed; top:0`, z-index 999) was never made banner-aware at all, unlike `.gate-overlay`/`.dm-app`, which already offset by `var(--dm-env-banner-h)` (an established pattern in this exact file, just never extended to this panel). The panel's own top-right controls — the new "Open in new tab" link *and* the pre-existing "✕" close button, both `position:absolute` with `top: ~0.65–0.75rem` — land inside the banner's own screen region (`y:0–33px`) at a far lower z-index (999/1 vs the banner's 10000), so the banner paints over both of them: present in the DOM, "visible" by every CSS property that isn't about paint order, and completely invisible and unclickable to a real user whenever the banner shows. This is not something introduced by the new-tab link in isolation — the close button had the identical bug the whole time, just never surfaced as a report before, since there was nothing DM-facing sitting right next to it drawing attention to that top strip until now.
Fixed at the panel, not per-control: `.scc-lookup-panel`'s `top:0` → `top: var(--dm-env-banner-h)`, matching the same pattern `.gate-overlay` already uses — the whole panel (and anything anchored to its top edge, today or later) now starts below the banner instead of the banner painting over its top strip.
Verified live via real search-and-click (not scripted `sccOpenLookup()` calls — those hit an unrelated Browser-pane test-harness timing quirk around CSS transitions/viewport settling that made the panel *appear* broken via direct JS invocation even when it wasn't; real user-driven clicks were unaffected throughout and are the trustworthy signal here) across 3 of the 7 real types — NPC (Drak), Item (+1 Chain Mail), Quest (The Defect That Isn't) — at both desktop and tablet widths. `elementFromPoint()` at both the link's and the close button's own centers now correctly returns each control itself, not the banner. Screenshotted all 3, genuinely visible and positioned correctly below the banner in every shot, not just measured via script.
Zero console errors throughout all three investigations. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty.
**Aside, not one of the 3 bugs, recorded for continuity**: this session's `dm.html`/uncommitted `CLAUDE.md` state already carried a concurrent session's Players-page Spell Assignment work (see the entry directly below), landed inside the prior commit (`3ea5fe0`) by that other session's own `git commit` sweeping up whatever was in this shared worktree at the time. Confirmed before relying on the file as a safe base: the two feature sets don't touch any of the same functions or CSS classes (Spell Assignment: `#form-players`/`.scc-pd-*`; this round: Content Manager filters, the global search dropdown, the lookup panel) — no interference either direction.


---

**2026-08-17 (new session, "Open in new tab" for search results, the two-piece version confirmed after a prior session's investigation) — `dm.html`.**
Prior session's investigation (logged 2026-08-16) found the obvious answer — link to the public wiki — doesn't work: quests have no public route at all, and ~90% of DM-tool content is `player_facing:false`. It also found the real target isn't uniform: only 6 of ~10 searchable types (NPC/City/POI/Creature/Organization/Item) have a real, working edit form, and it's Content Manager's — Quest has a separate real detail view (the Quest Dashboard, not Content Manager; Content Manager lists quests but `cmEditEntry()` on one is dead, no `#form-quest` exists), Region is explicitly non-editable in Content Manager (`item._type === 'region'` renders plain text, no edit link, no delete button), and Nation/Religion/Species aren't in Content Manager at all (not in `CM_ARRAYS`). Underneath all of that, `dm.html` had zero URL routing of any kind — confirmed via a full grep of `location.hash`/`location.search`/`URLSearchParams`, the only hit being the env-banner's hostname check — so picking a target per type wasn't even sufficient; there was no way to construct a URL that opened the app and landed on a specific entity at all.
Built exactly the two confirmed pieces.
**1 — Minimal URL routing.** New `sccHandleUrlRouting()` + two builders, `sccBuildManageEditUrl(type,id)` (`?tab=manage&edit=<type>:<id>`) and `sccBuildQuestDetailUrl(id)` (`?tab=quests&quest=<id>`) — both built off `window.location.pathname`, not a hardcoded filename, so they resolve correctly whether the page loads as `dm.html` (local) or `/dm` (the live GitHub Pages URL, per this file's own Repo & Deployment table). The manage-edit path resolves `CM_FILTERED` by `_type`+`_entry.id` (a real gap: `cmEditEntry()` had only ever taken a positional array index before this, since every existing caller was a table-row click with the index already in hand) and calls `cmEditEntry()` on the match; the quest path calls the already-id-addressable `selectQuest(id)` directly. Wired into `syncFromSupabase()`'s completion callback, immediately after its existing `cmRefresh()` call — the one point in the boot sequence guaranteed to have real Supabase data in `CM_FILTERED`/`QUESTS` — and gated on `sessionStorage.getItem('dm_auth')==='1'` so it's inert on a genuinely unauthenticated fresh load. Net effect on a real deep-linked open: the page boots to its normal Manage-tab default first, then flips to the correct edit/detail view once the async fetch resolves (typically well under a second) — a direct, accepted consequence of the existing boot sequence being async and unconditional, not worth restructuring just to hide the flash.
**2 — The link itself.** One persistent `<a target="_blank" rel="noopener">` added once to the lookup drawer's static shell (next to the existing close button), not duplicated into each of `sccOpenLookup()`'s 9 per-type branches — reuses the exact `--dm-gold`/`--dm-gold-dim` styling the Players tab's "Open Sheet ↗" link already established as this app's one precedent for this kind of button. `sccOpenLookup()` now sets a local `newTabUrl` inside each of the 7 supported branches, right after the entity is confirmed found; the function's existing tail (already the single place `content.innerHTML` gets set for every branch) shows/hides and points the one shared link based on it — hidden by default at the top of the function too, so every branch's existing early-return "not found" path correctly leaves it hidden with no extra code. City needed real care, not a blind copy of the other 5: `sccOpenLookup('city', id)` already serves real cities, POIs, *and* nations through one lookup type (an existing quirk carried over from global search), and only cities/POIs have a Content Manager form — nations don't. Reused the branch's own already-computed `isNationEntry`/`isPoiEntry` flags rather than re-deriving them: `if (!isNationEntry) newTabUrl = sccBuildManageEditUrl(isPoiEntry ? 'poi' : 'city', loc.id);`. Region/Religion/Species branches were left completely untouched — no `newTabUrl` assignment exists in any of the three, so the shared link simply never appears for them, correctly absent rather than present-but-dead.
Verified live end-to-end, not from reading the code alone. Local server: a stale server was already running on the usual `:8791` dev port serving a different checkout (confirmed via `curl` before trusting it, per this file's own standing lesson) — left it alone, started a fresh one on `:8795`. Opened the lookup drawer for one real entity of each of the 7 supported types via the actual global search UI and confirmed via DOM read that each produced the exact right href — `?tab=manage&edit=item:chain-mail-plus-one`, `...edit=character:the-vareth`, `...edit=city:gobblewump-crossing`, `...edit=poi:doopu-station`, `...edit=creature:fluffets`, `...edit=organization:gigglegloom-conclave`, `?tab=quests&quest=the-defect-that-isnt` — plus a real nation lookup (`the-auvari-remnance`, reached through the same `city` type) confirming the link correctly stays hidden (`display:none`, `href` removed) rather than pointing at a form that doesn't exist. Region and Religion also confirmed hidden on real entities (Caparia, The Brightcreed); Species has zero rows in this environment's synced dataset to test live against, so its exclusion was confirmed structurally instead — grepped and confirmed its branch has no `newTabUrl` assignment, the identical shape to Region/Religion's already-live-confirmed branches. For "opens correctly in a new tab, gate doesn't re-prompt": a real `target="_blank"` click consistently made the Browser-pane automation tool itself hang/time out — this sandboxed pane appears not to track or surface real popup/new-tab windows opened via anchor click, a harness limitation confirmed by the app itself never erroring or freezing across repeated attempts (checked via console + screenshot each time), not an application bug. Verified the equivalent real behavior instead: opened a genuinely fresh tab, pre-seeded its `sessionStorage` with `dm_auth=1` — exactly what a same-origin `target="_blank"` navigation inherits per the HTML spec, the same mechanism this app's pre-existing "Open Sheet ↗" link already relies on, previously unverified — then navigated it directly to each of the 7 real URLs above. All 7 loaded straight into the correct edit/detail view with no password gate shown, screenshotted. Zero console errors throughout, on both tabs, across every check. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty — the one new visible glyph (↗) was written as the `&#8599;` HTML entity in static markup (not a JS string, so C2 doesn't even apply), done anyway as the safer form given this file's own recurring history of the Edit tool re-normalizing literal Unicode characters on write.
Does not touch the separate, still-open "quests have no public wiki route" gap in Next Priorities — that's about the player-facing wiki having nowhere to send a quest link; this session's routing is entirely internal to DM Tools, a different mechanism for a different need.


---

**2026-08-16 (same session, Color Health build — the 3 stale UI lists fixed, the DM's new teal palette made real, and a new auto-computed region rollup) — `dm.html`, `css/tokens.css`.**
DM confirmed all 3 pieces from the prior investigation round directly. Built exactly those, no scope creep.
**Fix 1 — the 3 stale option lists.** `#city-color-health` and `#poi-color-health` (both had `dire/excellent/faded/good/moderate` — only "faded" real) and `#cm-colorhealth-filter` (had `compromised/dire/excellent/good/moderate/unknown` — a third independently-wrong list, `compromised` invented with zero backing data anywhere in the app) all replaced with exactly the real 5-value enum: Vivid/Muted/Faded/Dimmed/Unknown. No JS logic changes needed beyond the static `<option>` lists — `cmPopulateForm()`'s existing `sv('city-color-health', entry.color_health)`/`sv('poi-color-health', ...)` and `cmApplyFilters()`'s existing substring-match filter logic both already expected the real enum values; they'd just never had matching options to select from.
**Fix 2 — palette promoted from a one-off edit to the real single source.** New `--region-vivid/-muted/-faded/-dimmed/-unknown` tokens in `css/tokens.css` (`#4EFFE1`/`#35AB97`/`#206A5E`/`#334340`/`#666666`, the DM's confirmed teal-shade values), each paired with a `-text` variant picked by actual perceived-brightness math (ITU-R BT.601 weighted formula) rather than copied from the old palette's contrast choices, since the new hues have a genuinely different brightness distribution — Vivid (198.7) and Muted (133.4) land above the readability threshold and get dark text, Faded (82.5)/Dimmed (61.9)/Unknown (102) land below it and get light text. `sccRegionHealthColor()` — already the single shared function every color_health badge across the app reads from (region lookup drawer, city/POI/nation lookup drawer, location panel card) — repointed from its old hardcoded hex onto `var(--region-*)` references. Confirmed via grep that none of the old hex (`#6ee6d2`/`#f0b429` in this role/`#aaa0c8`/`#a0a0b0`) survives anywhere else in `dm.html`.
**Fix 3 — new auto-computed region rollup, additive, not replacing anything.** Investigated the linkage first, per instruction, rather than assuming: `cities.region`/`pois.region` (text, referencing `regions.id`) confirmed real and 100% populated (23/23 cities, 13/13 POIs, zero nulls, every value matching one of the 4 real region ids). New `sccBuildRegionHealthRollupHtml(regionId)` filters `CITIES`/`POIS` by region, counts real `color_health` values into the 5 real buckets, and folds any null/unset entity into the Unknown bucket rather than dropping it silently — per the explicit instruction that incomplete data should be visible, not hidden. Renders as chips (e.g. "11 Vivid · 3 Muted · 4 Faded · 6 Unknown"), each swatch color pulled from `sccRegionHealthColor()` directly — the exact same source as Fix 2, no second palette invented for this feature. Wired into the region lookup drawer directly after the Description section, as its own clearly-labeled block ("Cities & POIs (N)") — the region's own (often free-text) `color_health` badge above it is completely untouched, still reads and displays exactly as before.
Verified live end-to-end, not assumed from the code alone: **Fix 1** — Gobblewump Crossing (real `color_health:'vivid'`) opened via the actual `cmEditEntry()` flow now pre-selects "Vivid" (previously would have opened blank/unmatched); Reveltown Formery Branch Office (real `'muted'`) pre-selects "Muted"; the CM filter set to Vivid now returns the real 15 matching entries (12 cities + 3 POIs) instead of 0. **Fix 2** — a real rendered health badge's `getComputedStyle()` background resolved to `rgb(78,255,225)` (`#4EFFE1`) exactly. **Fix 3** — checked against 2 real regions with real mixed child data: Caparia (24 children) rendered "11 Vivid, 3 Muted, 4 Faded, 6 Unknown," Sohot (5 children) rendered "3 Muted, 1 Faded, 1 Dimmed" — both hand-verified against direct SQL group-by queries before building, both matched exactly. Confirmed the empty-case (`sccBuildRegionHealthRollupHtml()` called with a region that has no children) returns an empty string rather than broken markup. Screenshotted both regions' lookup drawers and both edit forms. Zero console errors throughout. Zero Supabase writes needed reverting — every check this round was read-only (opening forms/drawers, no save calls), confirmed via a final SQL read that the two real test entities' `color_health` values are unchanged. Pre-push checks clean: raw parse on `dm.html` and `css/tokens.css`, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty.


---

**2026-08-16 (same session, merged `anavale-wiki-scc-ux` into `main`, no code lost or overwritten) — `CLAUDE.md`, `css/tokens.css`, `sheet/index.html` conflicts resolved; `dm.html` merged cleanly with zero conflicts.**
Checked divergence first, per the parallel-worktree overlap habit this file itself established: `main` (checked out in the sibling `anavale-wiki` worktree) had moved 13 commits ahead independently — a Class-tab/sheet build (Spell Slots redesign, Spells Prepared/Learned, Saving Throws, Initiative/Hit Dice, Magic Item Attunement, a new "Pending/Blocked" CLAUDE.md section) touching only `sheet/index.html`, `css/tokens.css`, and `CLAUDE.md`. This branch's own 40 commits touched only `dm.html`, `css/tokens.css`, and `CLAUDE.md` — confirmed via `git diff --stat` on both sides before merging, not assumed. `dm.html` and `sheet/index.html` never overlapped at all (disjoint files), so the only real conflict risk was the 2 files both sides touched.
Merged `origin/main` into this branch (not the reverse) so any conflict could be resolved and fully verified in this worktree before touching `main` at all. `css/tokens.css` auto-merged with zero conflicts — confirmed via a full duplicate-property scan afterward that no `--token-name` got defined twice. `sheet/index.html` merged in cleanly as a straight addition (this branch never touched it). `CLAUDE.md` conflicted at 4 points — the header date line, the Current Focus section, the Next Priorities bullet list, and the Decision Log — all 4 were resolved by keeping **both sides' full content** (concatenated, nothing dropped) rather than picking one branch's version, since both represent real, distinct session work worth keeping; only the single "Last updated" date line took one side (the more recent Aug 16 over Aug 13). Verified programmatically: zero conflict markers remain, section numbering came out sequential and non-duplicated (`## 1` through `## 11`), and spot-checks confirmed specific content from both sides survived intact (this branch's `sccComputePcAC`/death-save panel in `dm.html`; main's Spell Slots/`--magic-blue` tokens in `sheet/index.html`/`tokens.css`).
Ran the full pre-push suite on every touched file before committing the merge, not just `dm.html` as usual: raw parse and `new Function()` syntax checks on `dm.html` (7 blocks) and `sheet/index.html` (1 block), both clean; diff-scoped literal-emoji grep against the content main brought in, clean. Committed as a real merge commit (both parents preserved, not squashed), pushed `anavale-wiki-scc-ux`, then fast-forwarded `main` in the sibling worktree (`git merge --ff-only`, succeeded cleanly since `main`'s old tip was now a direct ancestor of the merge commit) and pushed. Confirmed via `git ls-remote` that `origin/main` and `origin/anavale-wiki-scc-ux` point at the identical commit afterward.
**Followed the Pending/Blocked section's own "check on merge" instruction** (§3, added by the other branch): both items listed there — a DM Tools Attunement checkbox and a `classes.limited_use_features` admin form — were explicitly blocked on this exact merge landing. Moved both to Next Priorities, flagged directly in this session's summary rather than left for someone to notice later. Neither was built this session — flagged as newly-unblocked, not implemented.


---

**2026-08-16 (same session, beat-status colors flagged "Wrong" via 3 screenshots — extended the Quest Status blue/gold/red/grey scheme to per-beat status too) — `dm.html`, `css/tokens.css`.**
DM sent 3 screenshots of the beat detail list's 3 status states — Done (green pill/border), Available (grey dropdown), In Progress (blue card/dropdown) — captioned only "Wrong colors," no further detail given. Read them against the one obvious reference point: the Quest Status scheme specified earlier the same session (Available=Blue, Active=Gold, Blocked=Red, Completed=Grey). Beats have the same 3 analogous states minus Blocked (not_started≈Available, in_progress≈Active, completed≈Completed) and none of the 3 screenshotted colors matched that scheme. Applied the identical mapping rather than guessing at a fourth independent palette: not_started→blue, in_progress→gold, completed→grey.
This is the beat-progress color's 4th distinct scheme within this one session (gold → blue → "blue for everything but completed" → now blue/gold/grey) — not a mistake being corrected each time, a real sequence of direct DM instructions, each superseding the last for a stated reason. This round explicitly supersedes the "make every rail segment blue except completed" decision from earlier the same session, since that decision was itself adopted specifically to dodge a Stakes-badge collision (Mid Stakes = gold, colliding with a gold in-progress highlight) — this round's instruction reintroduces gold for in-progress regardless, which is a deliberate DM call to prioritize the Quest-Status-wide scheme over that specific collision-avoidance, not an oversight.
**Implementation, single-source via the shared tokens.** Repointed `--scc-beat-todo-bg/text/border` (blue), `--scc-beat-prog-bg/text/border` (gold), `--scc-beat-done-bg/text` (grey, `#a0a0a0` — matching Quest Status's Completed grey exactly, not a new shade) — reused `var(--blue)`/`var(--gold)`, no new hex invented. Every consumer updates from these 3 token sets: `.scc-beat-seg--not_started` (repointed off its borrowed `--prog-*` reference onto its own now-correctly-blue `--todo-*` token — no longer needs to borrow), `.scc-beat-item--completed`'s hardcoded green border-color literal (now grey), `.scc-beat-status-select--completed`'s hardcoded green border literal (now grey), `.scc-beat-status-select--not_started`'s border (now `--scc-beat-todo-border`, a new token added for symmetry with `--scc-beat-prog-border` — was previously a flat neutral `--scc-border-mid` regardless of state). `.scc-beat-item--active` (the highlighted glow box for the beat currently in progress) had its `!important` overrides — border-left, border-color, background gradient, box-shadow — moved from hardcoded blue rgba literals back to gold; its `@keyframes scc-beat-pulse` had been left pulsing gold-toned throughout the entire blue era (never updated when the base rule went blue) — it now correctly matches its own base state again instead of silently fighting it every animation cycle.
**Checked for siblings again, per this session's own established habit** — the Live Session panel's independent beat-dot renderer (`.scc-live-beat-item--completed/in_progress .scc-live-beat-dot`) was still hardcoded to `var(--dm-green)`/`var(--dm-blue)` literals, a third render surface for the same concept that's now had to be manually chased down and fixed twice this session already. Repointed both onto the shared `--scc-beat-done-text`/`--scc-beat-prog-text` tokens directly instead of re-hardcoding a third literal pair — this specific class of drift (one render site quietly falling out of sync with the others) can no longer happen here, since there's only one real source now.
**Flagged, not silently absorbed**: gold for the in-progress beat highlight reintroduces the exact Stakes-badge collision (Mid Stakes = gold) that blue was chosen earlier this session specifically to avoid — now happening at both the quest level (Active status, flagged 2 rounds ago) and the beat level (in-progress highlight, flagged here) simultaneously, on the same card, at the same time, for any quest that's both Mid Stakes and has an active beat. Implemented as instructed since both asks were explicit and direct; not resolved unilaterally since fixing it means recoloring Stakes, which wasn't part of either ask.
Verified live end-to-end via real dropdown-driven state changes (not direct function calls, matching this session's own verification standard): set beat 1 to `in_progress` and beat 2 to `completed` through the actual `<select>` elements, confirmed via `getComputedStyle()` across every affected surface — rail segments (in_progress `rgb(240,180,41)`, completed `rgb(160,160,160)`, not_started `rgb(96,165,250)`), beat item boxes and the active-highlight border, the status dropdowns, and (after switching tabs) the Live Session panel's dots — all resolve to the correct gold/grey/blue in every state. Screenshotted the Session>Quests and Live Session panels showing the gold in-progress dot and grey completed checkmark live. Reverted both test writes via the same real dropdowns and confirmed via a fresh SQL read that all 4 beats are back to `not_started`/`null`. Zero console errors throughout. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty.


---

**2026-08-16 (same session, the actual final cause of the XP/dropdown alignment complaint — a follow-up screenshot showed the previous round's fix hadn't fully closed it, plus "To Do"→"Available") — `dm.html`.**
DM sent a fresh zoomed screenshot showing the real "+50 XP" badge and status dropdown still visibly offset on the live localhost instance, captioned "Fix alignment," alongside a second ask to rename the dropdown's not-started option from "To Do" to "Available." Did not assume the prior round's fix was wrong or repeat the same approach — re-measured live via `getBoundingClientRect()` first. Confirmed the earlier `align-items:center` fix had genuinely helped (both boxes were near-equal height, centers only 3.2px apart) but two real causes remained, found by continuing to trace the render chain rather than stopping once a plausible-looking fix had shipped:
**Cause 1 — native `<select>` intrinsic height doesn't reliably equal a plain span's even with byte-identical padding.** This is a documented cross-browser quirk (selects carry their own UA-level box-model floor in some engines), not something `align-items:center` on a parent can fully control. Gave `.scc-beat-xp` and `.scc-beat-status-select` an explicit shared `height: 1.35rem`, and added `display:inline-flex; align-items:center` directly onto the select itself (previously only its span sibling had this) — this pins both elements to an identical, browser-independent box height rather than trusting native rendering to agree with a hand-written span.
**Cause 2 — the real remaining 3.2px, found only after equalizing heights and still seeing an offset.** `.scc-beat-actions` (the div wrapping the status select + Live Session button) still carried `margin-top: 0.4rem` — confirmed via grep it has exactly one render call site, always nested inside `.scc-beat-meta` as a sibling of `.scc-beat-xp`. A flex row's `align-items:center` centers each child's full margin box, not just its content box — so that leftover top margin (a holdover from before the XP badge and the actions row were consolidated onto one shared line, presumably for spacing when they were still stacked) was pushing the whole actions group down relative to its sibling even once both had identical content heights. Confirmed no other consumer of this class exists, so removed the margin outright rather than scoping an override.
**"To Do" → "Available"**: the not_started beat-status option's visible text, one render call site (grep-confirmed, `dm.html` beat-status `<select>` builder). Pure display-text change — the underlying stored value stays `not_started`, no enum/data change, since there was no signal this needed the same value-level rename treatment "Dormant"→"Blocked" got earlier this session (no cross-system consistency at stake, just wording on one dropdown).
Verified live via `getBoundingClientRect()` on all 4 real beats after both fixes, not just the one beat checked before: XP badge and status dropdown vertical centers are now exactly 0px apart on every single beat (628.24/628.24, 754.95/754.95, 881.66/881.66, 1008.38/1008.38) — not "close," identical. Screenshotted the Session>Quests panel confirming all 4 dropdowns read "Available" and sit level with their XP badges. Zero console errors. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty.


---

**2026-08-16 (same session, real fix for a recurring vertical-alignment complaint — diagnosed via computed layout, not guessed) — `dm.html`.**
DM's own words: "vertical center the damn exp text! I've complained about it multiple times" — attached a DevTools screenshot with `.scc-beat-meta` selected, showing "+50 XP" and "To Do" visibly at different vertical positions in the same row. Read the actual current CSS before touching anything (D2): `.scc-beat-meta { display:flex; gap:0.4rem; margin-top:0.2rem; flex-wrap:wrap; }` had no `align-items`, which defaults to `stretch` — its two children are `.scc-beat-xp` (a plain span) and `.scc-beat-actions` (the status `<select>` + Live Session button, `display:flex; align-items:center`, naturally taller because of the select's own padding). With `stretch` as the default, `.scc-beat-xp` was being force-expanded to match `.scc-beat-actions`'s taller natural height — but the span has no vertical-centering mechanism of its own (no flex, no line-height matching its stretched box), so its text stayed pinned to the top of that taller box while the dropdown text, centered within its own smaller natural padding, ended up at a visually different height. This explains why the complaint recurred: the XP badge's own declared styles (padding, font-size, line-height) were internally fine — the misalignment only appeared as a consequence of what its flex sibling forced onto its box height, which nothing about reading `.scc-beat-xp` in isolation would reveal.
Fixed at the root, not by eyeballing a padding nudge: added `align-items: center` to `.scc-beat-meta` so both children size to their own natural content height and align by center against each other, instead of the taller one silently stretching the shorter one. Added `display: inline-flex; align-items: center` to `.scc-beat-xp` itself as a second, defensive layer, so its own text stays centered within its own box regardless of any future height change (e.g. if it ever wraps). Also removed one exact duplicate `.scc-beat-status-select:hover` rule sitting immediately below this block (spotted while reading it, zero behavior change — matches this file's own "no duplicate rules" CSS convention).
Verified live via `getBoundingClientRect()` on real rendered content, not just a screenshot glance: the XP badge's own vertical center and the actions group's vertical center are now 3.2px apart (630.2px vs 633.4px, both sized to their own natural ~17-19px height) — down from the badge being force-stretched to the full 25.6px row height with 17px of text pinned to its top edge. Screenshotted the Session>Quests panel confirming "+50/+75/+200/+300 XP" now sit level with their "To Do" dropdowns across all 4 beats. Zero console errors. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty.


---

**2026-08-16 (new session, DM-specified Quest Status color scheme — Available=blue, Active=gold, Blocked=red, Completed=grey) — `css/tokens.css`.**
Direct, itemized instruction, executed exactly as given. Read the current 4 tokens first (C1): Available (`var(--blue)`) and Completed (`#a0a0a0`) already matched the request precisely, no change. Active moved from green (`var(--green)`) to gold (`var(--gold)`, `rgba(240,180,41,0.15)`); Blocked moved from a raw literal red-orange (`#ff8066`, itself reused from Active's own pre-recolor hex 2 sessions ago) to a real red token reference (`var(--red)`, `rgba(248,113,113,0.15)`) — consistent now with how the other 3 states already reference base palette tokens rather than hardcoded hex. Confirmed via grep that all 4 colored quest-status render sites (`renderQuestsList()` cards, `renderQuestDetail()` badge, `.quest-card--status-*` top border, `sccRenderQuestPanel()`'s Session>Quests panel) share the identical `.q-status-*` classes and these same 4 token pairs — this was a single-file, tokens.css-only edit; zero changes needed in `dm.html`, every render site picks up the new colors automatically.
**Flagged, not silently fixed**: Quest Stakes (Low/Mid/High) already uses the exact same green/gold/red traffic-light scale, and a Stakes badge sits immediately next to the Status badge on every quest card (confirmed at all 3 render sites via grep — badge order is always Status then Stakes, adjacent). Active=gold now visually matches Mid Stakes=gold; Blocked=red now visually matches High Stakes=red — a real, same-row collision, not a hypothetical one. Confirmed live on Anomaly 7-C (the one real quest that's both Blocked and High Stakes): both badges render as the identical red side by side, screenshotted. No real quest is currently `active` status, so the gold/gold case isn't visible in the live data yet, but will be the instant one is set. This is the exact one-hue-two-meanings shape this whole session has been hunting down and fixing elsewhere — implemented as instructed since the DM's ask was explicit and itemized (not a case for declining or asking first), but resolving the collision itself would mean either recoloring Stakes (not part of this ask) or changing how Status is signaled on the card (e.g. leaning on the already-present border-top color instead of a second badge) — a DM call, left open rather than guessed at.
Verified live: read real `q-status-available`/`q-status-blocked` badges off actual rendered quest cards, and constructed throwaway DOM elements for `q-status-active`/`q-status-completed` (no real quest has either status today) to confirm all 4 states via `getComputedStyle()` — exact matches: Available `rgb(96,165,250)`, Active `rgb(240,180,41)`, Blocked `rgb(248,113,113)`, Completed `rgb(160,160,160)`. Screenshotted the Quest Dashboard. Zero console errors.


---

**2026-08-15 (same session, a sixth round on the same beat list — a "beat numbers wrong color" report that didn't reproduce, then two real fixes: XP reverted to gold, status dropdown recolored per-state) — `dm.html`.**
DM's screenshot annotated "Change back to yellow-gold" on the beat number labels and "Change to blue" (repeated) on the XP badges. Investigated the numbers first, per D2: grepped `.scc-beat-num`, confirmed it was never touched by any edit this session (still `color: var(--dm-gold)`), and confirmed live via `getComputedStyle()` on all 4 rendered numbers ("1"-"4") that they resolve to `rgb(240,180,41)` — the app's real gold — matching a fresh screenshot too. Reported this back as already-correct, no code change, rather than making a no-op edit — same evidence-first discipline as two earlier rounds this session that also didn't reproduce.
DM's follow-up gave the real remaining ask directly: XP badges should be gold-yellow (undoing last round's move to blue), and the beat-status dropdown's color should reflect its current value instead of rendering the same neutral grey in every state.
**XP badges**: `.scc-beat-xp` (Quest Dashboard beat list) and `.scc-live-beat-xp` (Live Session panel's independent beat-dot renderer, checked as a sibling per this session's habit) both reverted blue→gold (`var(--dm-gold)`, `rgba(240,180,41,...)`) — undoes exactly the one XP-badge change from 2 rounds ago, nothing else touched on either surface.
**Status dropdown recolored per-state**: `.scc-beat-status-select` previously rendered identically (neutral grey) regardless of To Do/In Progress/Done. Added 3 modifier classes reusing the exact tokens the beat rail already defines for the same 3 states — `--scc-beat-todo-*` (grey, was sitting unused since the rail itself moved to all-blue-except-completed 2 rounds ago), `--scc-beat-prog-*` (blue), `--scc-beat-done-*` (green) — render code appends `scc-beat-status-select--' + st` to the `<select>` class. Deliberately did not mirror the rail's own "blue for not-started too" choice here: that reframing was a DM-driven, comprehensive instruction specific to the rail; the dropdown was just asked to "reflect current status," which only works if its 3 states are visually distinguishable from each other — giving it its own honest 3-way grey/blue/green coloring is the correct application of the same design language, not a contradiction of the rail's blue theme.
Verified live end-to-end via the real dropdown's `change` event (not direct function calls): not_started → grey (`rgba(255,255,255,0.04)` bg), in_progress → blue (`rgb(96,165,250)`), completed → green (`rgb(34,197,94)`) — then reverted to not_started the same way and confirmed via a fresh SQL read that all 4 real `quest_progress` rows for the test quest are back to their original `not_started`/`null` state. XP badges confirmed gold on both surfaces. Zero console errors. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep empty.


---

**2026-08-15 (same session, a fourth annotated screenshot — the DM overrode the prior round's declined interpretation, comprehensively, plus a spacing tweak) — `dm.html`.**
Previous round declined making "not started" beat segments blue on the reasoning that blue was specifically claimed for "in progress," and coloring "not started" the same would recreate the exact collision this session had been fixing. This round's screenshot answered that directly, not verbally — "Make blue" annotated on all 4 rail segments (including 3 that were never in-progress) and all 3 visible XP badges, in the same pass. A DM re-annotating the same request more comprehensively after a declined-with-reasoning response is a real answer, not ambiguity to keep re-litigating — implemented as shown.
**Reframed the model, not just recolored two rules.** Blue isn't "in progress" specifically — it's this quest card's active/available theme (matching its own "✦ Available" status badge), with Completed remaining the one real, distinct exception (green, an unambiguous win-state nobody asked to change). `.scc-beat-seg--not_started` moved from the muted "todo" tokens to the same `--scc-beat-prog-*` blue tokens `in_progress` already used (two states, one now-shared color) — `.scc-beat-xp` (the `+XX XP` badges) moved from `--dm-gold`/`--dm-gold-dim` to a matching blue.
**Checked for siblings before calling it done, per this session's own established habit** — found a third independent render of the exact same beat-progress concept, the Live Session panel's own beat-dot list (`.scc-live-beat-item--in_progress .scc-live-beat-dot`, `.scc-live-beat-xp`), still gold. Not mentioned in any screenshot, but leaving it would have meant a DM switching from Session>Quests to the Live Session tab mid-session would see the exact gold/gold collision reappear on the same data. Fixed identically (blue dot fill, blue XP text).
**"Too much empty space on bottom of text"** — measured before guessing at a fix: the gap between a beat's description paragraph and its XP/status row was a uniform 6.4px (`.scc-beat-meta`'s `margin-top: 0.4rem`) across all 4 beats, not an inconsistency, just a gap the DM felt was too generous for how compact the rest of the card is. Tightened to `0.2rem`.
Verified live end-to-end: computed style confirmed all 4 rail segments and all 4 XP badges resolve to the identical blue (`rgb(96,165,250)`) on the Session>Quests panel; marked a real beat in-progress via the actual dropdown, switched to the Live Session tab, and confirmed the beat-dot and XP text there are the same blue too; reverted the test write afterward (confirmed via SQL back to `not_started`, matching the state before this round). Zero console errors. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks.


---

**2026-08-15 (same session, a third annotated screenshot on the same beat list — one real fix, one no-repro, one declined) — `dm.html`.**
Three annotations, each checked independently before acting rather than trusting the screenshot at face value.
**"Fix alignment"** — real, measured. `getBoundingClientRect()` on the `+XP` badge across all 4 beats before touching any CSS: beat 1 (the active one) started at `x:96.2`, beats 2-4 all started at `x:93.2` — a genuine, consistent 3px offset, not a screenshot artifact. Root cause: base `.scc-beat-item` has `border: 1px solid`, while `.scc-beat-item--active` overrides with `border-left: 4px solid var(--dm-blue) !important` (previously gold, recolored last round) — the 3px difference between 1px and 4px pushes the flex-row content right the moment a beat becomes active. Fixed by reserving the same 4px on the base rule (`border-left-width: 4px; border-left-color: transparent`), so the active state only changes color, not layout. Verified live: all 4 beats' `+XP` badges now share the exact same `x:96.2` regardless of state.
**"Make blue" on the "▶ In Progress" dropdown — did not reproduce.** Checked two ways against the exact code just shipped last round: computed style (`color: rgba(232,232,240,0.55)`, `background: rgb(30,30,42)` — plain neutral, not gold) and a fresh screenshot showing the same neutral dropdown. `.scc-beat-status-select` was never styled to reflect beat status in the first place — it's a fixed neutral control, matching the old button it replaced. Most likely explanation: a stale browser cache serving pre-fix `dm.html`, on record in this file as a recurring issue across multiple prior sessions.
**"Make blue" on the not-started rail segments (2/3/4) — investigated, declined with reasoning rather than silently skipped or silently done.** Blue was deliberately claimed this session specifically for "in progress"/"available" (the last two rounds' whole point was resolving blue/gold collisions with Stakes). Coloring "not started" segments blue too would immediately recreate the same class of bug from the opposite direction — blue meaning both "something is happening" and "nothing has happened yet" on the same rail. Left as the existing neutral grey pending explicit confirmation this is really wanted, rather than guessing.
Zero console errors. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks.


---

**2026-08-15 (same session, a second annotated screenshot on the same "Active Quests This Session" card — a real gold-on-gold collision, plus a UX request) — `dm.html`, `css/tokens.css`.**
Two direct annotations. First re-checked the prior round's own status-badge fix hadn't missed something before assuming this was new — it hadn't; this was a genuinely different element on the same card.
**"This should be Blue"**, pointing at the beat-progress rail's highlighted segment. Root cause: `--scc-beat-prog-bg/text` (the "in progress" beat state) was gold — sitting directly under the "Mid Stakes" badge, also gold, on the exact same quest card. Same collision shape as the last two rounds, just a third surface. Recolored to blue (`--scc-beat-prog-bg/text/border`, new border token added since none existed before). Checking the full render path (not just the token) surfaced a second gold source the token alone wouldn't reach: `.scc-beat-item--active` — the expanded beat box's pulsing highlight, styled entirely with hardcoded `!important` gold (border, gradient background, box-shadow), completely bypassing the token system. Recolored that too, to the same blue, so the rail and the expanded box read as one consistent "in progress" language instead of blue-on-top/gold-underneath.
**Beat status button → dropdown.** The per-beat status control (`sccCycleBeatStatus`, in the expanded beat list) was a button reading "To Do"/"In Progress"/"Done" that cycled through 3 states on click with no visible indication of what the next click would do — same class of ambiguity as the quest-status button fixed 2 rounds ago. Replaced with a real `<select>` showing all 3 options with the current one pre-selected. Refactored the shared save logic into `sccSaveBeatStatus(questId, beatOrder, status)` (one PATCH to `quest_progress`, matching the existing shape exactly) so both the new dropdown and the still-click-to-cycle beat-rail segments (not touched — the annotation only pointed at the button, and a compact rail is a reasonable place to keep a quick click-cycle) call the same function instead of duplicating the PATCH. The now-unused `.scc-beat-status-btn` CSS rule was repurposed for `.scc-beat-status-select` rather than left dead alongside a hand-written new one.
Verified live: set beat 1 to "In Progress" via the real new dropdown — confirmed via computed style the rail segment (`rgb(96,165,250)`) and the expanded box's border/gradient (`rgba(96,165,250,...)`) both render blue now, no gold left anywhere on the card. Checked against real Supabase data afterward and confirmed the test write was a no-op (beat 1 was already `in_progress` in the real row, matching the DM's own screenshot) — nothing needed reverting. Zero console errors. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks.


---

**2026-08-15 (same session, "Active Quests This Session" badge colors flagged as inconsistent with the rest) — `dm.html`.**
DM's ask ("should follow the same color scheme as the other") pointed at the Live Session tab's own quest-card list (Session → Quests → "Active Quests This Session"), not the Quest Dashboard the last two rounds actually touched. Investigated first, per C1 — read the real render function before assuming it was the same code path: it isn't. `sccRenderQuestPanel()` is a fully separate function from `renderQuestsList()`/`renderQuestDetail()`, with its own quest-card markup, never touched by either of the last two rounds' fixes.
Found two real, distinct bugs, not one: (1) the beat-count tier badge (Local/Regional/Arc/Campaign — a DM-facing size hint, unrelated to quest status) was hardcoded to `rgba(96,165,250,...)`/`var(--dm-blue)` — the *exact* same blue `.q-status-available` already uses, sitting directly next to it in the same badge row. Confirmed via computed style before fixing: both genuinely resolved to identical `rgb(96,165,250)`, not just visually close. (2) the stakes badge rendered `sccEsc(q.stakes||'')` directly — the raw lowercase enum (`"mid"`) — never picked up the `stakesLabel()` fix built for the Quest Dashboard in the same round that fix shipped, since this is a different call site entirely.
Fixed both: tier badge recolored to the same neutral grey (`rgba(255,255,255,0.06)` / `var(--ink-faint)`) already used for other non-status informational badges on this exact card (First Available) and on the Quest Dashboard (Suggested Level) — deliberately not a new hue, matching the established "informational, not a status, gets neutral grey" pattern from two rounds ago. Stakes badge switched to `stakesLabel(q.stakes)`. Confirmed via a full-file grep this round that all three real quest-badge render sites (`renderQuestsList()`, `renderQuestDetail()`, `sccRenderQuestPanel()`) now call `stakesLabel()` consistently — no fourth site missed.
Verified live: real quest ("Something Is Wrong With Gerald") in the Live Session panel — computed style confirmed "Regional" now `rgb(122,101,72)` (neutral, matching the First Available badge's own color exactly), "✦ Available" still `rgb(96,165,250)` (unchanged, correct), "Mid Stakes" now Title Case with the "Stakes" suffix matching the Quest Dashboard. Zero console errors. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks.


---

**2026-08-15 (same session, live DM review of the Quest Dashboard pass above, from an annotated screenshot) — `dm.html`.**
DM sent an annotated screenshot of the Quest Dashboard from live use of the round just shipped: two direct instructions and one pointed question.
**Top border color by status.** New `.quest-card--status-active/available/blocked/completed` modifier classes, applied via `quest.status` in `renderQuestsList()`, set `border-top-color` (kept at the existing `border-top-width: 3px` upgrade) to the same tokens the status badge already uses — no new hex, this is the same meaning shown a second way. Deliberately left `:hover`/`.selected` untouched (both already override the full `border-color` shorthand for the existing gold interaction cue) rather than trying to preserve the status tint through interaction states — confirmed via computed style that `.selected` correctly still wins (higher specificity, two classes vs. one), matching the screenshot's own selected card showing solid gold.
**Status dropdown moved to top-right.** Was the very last element in `renderQuestDetail()`'s output, below Hook/Summary/Beats/Reveals/DM Notes/Related — meant changing a quest's status required scrolling past the entire quest first. New `.quest-detail-header` flex row (title left, status select right, `justify-content:space-between`) replaces the old bottom-of-panel block; the select's own `<label>` text was dropped since the space is tighter up top and the option text (e.g. "🚧 Blocked") already reads clearly on its own — a `title="Status"` attribute keeps it accessible. Verified live: changed a real quest's status via the relocated dropdown, confirmed the "✓ Saved" message still fires and the value persists, reverted.
**"Why is this blue?"** — a real question, not just a naming note, and the answer surfaced a genuine bug: the Suggested Level badge added to quest cards this round reused `var(--dm-blue)`, the *exact* same hue as the Available status badge — harmless on its own, but the moment status also started driving the card's top border (previous item), a DM scanning the list now had blue meaning "Available" in one place and meaning nothing in particular in another, on the same card. Not a deliberate color choice to begin with — recolored to a plain neutral grey (`rgba(255,255,255,0.06)` / `var(--ink-faint)`), matching the sibling "First available" badge already on the same card, since Suggested Level isn't a status and has no business borrowing one's hue.
Verified live: computed style on all 10 real quest cards confirmed correct top-border colors per status (blocked → `rgb(255,128,102)`, available → `rgb(96,165,250)`), the selected card confirmed still fully gold-bordered, the status dropdown confirmed rendering inside `.quest-detail-header` next to the title, the Suggested Level badge confirmed no longer sharing the Available badge's blue. Zero console errors. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks.


---

**2026-08-15 (same session, 5-item DM feedback round after live use of the just-shipped color rework) — `dm.html`, `css/tokens.css`, `session_notes.npc_relationships`.**
DM's 5 items, taken in the order given.
**1 — "Deity is not a relationship, it is an entity type."** Checked the real data before deciding how to fix it: `world_characters.category` already has a real `'god'` value (5 rows: Oro, Nara, Thyun, Solvara, Grak) — the entity-type field the DM was describing already exists, it just wasn't surfaced anywhere on the NPC card, while `deity` sat in the relationship cycle as if it were a disposition like ally/hostile. Fixed by removing it from the cycle (`SCC_REL_CYCLE`/`SCC_REL_LABELS`, both relationship-filter-dropdown and sort-order references updated) and adding a new, separate, non-cyclable badge (`sccNpcCategoryBadgeHtml()`) that reads `category` directly — purple, matching the hue this session's own color rework already reserved for religion/deity content, so this isn't a new color decision, just that reservation actually being used. Oro's stored relationship (`'deity'`, now not a valid cycle value) migrated to `'ally'` via direct SQL — his god-status is now carried entirely by his real `category` field, confirmed live showing both a blue "ALLY" badge and a purple "✦ God" badge side by side on his card. Also found and removed, while in this exact code: a fully dead CSS block (`.scc-npc-card.rel-*`, `.scc-rel-badge.rel-*`, ~30 lines) — confirmed via grep it has zero real consumers (a `.scc-npc-card` class distinct from the live `.scc-npc2-card` design, never assigned by any JS) and was already referencing `--scc-rel-*` tokens deleted as dead code last round, so it was doubly dead — not fixed piecemeal, removed outright.
**2/3 — "AC not showing during active combat" / "Stats not viewable in active combat."** Root-caused, not assumed: both features (AC in the collapsed row, a Stats toggle+panel) were built 2 rounds ago for `sccRenderStagingList()` — the pre-combat encounter-builder view — and never ported to `sccRenderInitiativeList()`, the completely separate function that takes over once "Start Combat" is clicked and staging is hidden. A DM mid-fight was looking at a view that had neither. Fixed by adding both to the live initiative row: AC as a small inline badge next to the combatant's name, and a new "Stats" toggle (`sccToggleInitStatBlock()`, distinct id namespace from the staging version — `scc-init-statblock-N` vs `scc-statblock-N` — since both DOM trees can coexist, staging just hidden not removed) opening the exact same `sccBuildCreatureStatBlockHtml()` output staging already used, so there's one stat-block renderer, not two. Verified live: staged and started combat with a real creature (Grusk Enforcer), confirmed "AC 11" shows in the initiative row by default and the Stats toggle opens the full real stat block (abilities, attacks, traits, actions) mid-fight, screenshotted.
**4 — Quick Note global entry point.** DM's own words: getting to a quick note required Session tab → Notes sub-tab → scroll past the 5 structured recap fields → find the "+ New Note" button — three steps for something meant to be instant, mid-session, explicitly called "horrible UX." Added a fixed floating "📝 Quick Note" button (bottom-right, page-level like the lookup drawer — reachable from literally any tab) opening a small self-contained modal (category select + title + body + Save/Cancel) that posts straight to `session_note_entries`, the same table the Notes tab's own Quick Notes section reads from — that section (filter/search/browse) is untouched and stays the *review* surface, per the DM's explicit instruction not to make it the *only* entry point. Gated the same way the existing Notes-tab flow is (blocks with a toast if no session is set up yet) plus one new check the FAB needed that the Notes-tab flow didn't: `SCC_VIEWING_HISTORY`, since a locked audit-mode session has no CSS-hidden section to fall back on for blocking interaction the way the Notes tab's own `.scc-audit-mode` rule does. Verified live end-to-end: opened and submitted a real note from the Manage tab (nowhere near Session/Notes), confirmed via SQL it landed correctly, confirmed it appeared in the Notes tab's own browse list afterward with the right category badge, deleted the test row.
**5 — Quest Dashboard UX pass, 4 sub-items, all built (none left as investigate-only).**
- *Stakes text inconsistency*: left-panel card showed the bare enum (`"high"`), right-panel detail appended `" stakes"` itself (`"high stakes"`) — genuinely two different strings for the same value, not a display bug so much as two call sites that had never been reconciled. New shared `stakesLabel()` (`"High Stakes"`, title case) replaces both call sites — verified live, both panels now read identically for the same quest.
- *Suggested Level*: added to the left-panel card preview as a new `Lvl X-Y` badge (reusing `sccSuggestedLevelRange()`, already pure/read-only, no new computation) and moved on the right panel from dead last (below the status dropdown) to directly under the status/stakes badges, above Hook — the number a DM actually needs before deciding whether to run a quest tonight, not something to find after reading the full text.
- *Inline entity links*: quest text (Hook/Summary/DM Notes/Reveals) now supports the identical `[[type:id|Label]]` markup Field Guide already uses, for the identical reason it was chosen there over auto-detection — this exact dataset has short, common-word-adjacent real names (confirmed the DM's own example, "Osric," would have been exactly this risk). New `sccLinkifyEscaped()`, a sibling of `sccFgLinkify()` rather than a reuse: Field Guide content is trusted raw HTML and the existing function re-escapes its extracted label on the way out; quest text is plain text that's already been through `escQ()` by the time linkify runs, so re-escaping the label there would double-escape it (e.g. a literal `&` already turned into `&amp;` would become `&amp;amp;`) — confirmed this distinction by reading `sccFgLinkify()`'s real call sites before writing the sibling, not assumed. Checked the DM's own "Osric" example directly rather than assuming linkify alone would fix it: `world_characters` has no "Osric" row at all — confirmed via SQL — so he has no real entity to link to yet; flagged rather than fabricating a new NPC record unprompted, since that's a content decision, not a UI one. Verified the capability itself end-to-end with a real, temporary marker: added `[[character:drak|Drak]]` to a real quest's real DM notes (Drak *is* a real, existing character, confirmed via SQL first), reloaded, clicked the resulting inline link, confirmed the lookup drawer opened showing his real card, then reverted the quest text to its exact original wording — matching this file's own established test-then-revert convention for Field Guide's own link feature.
- *DM Notes vs. Reveals visual inconsistency, the DM's direct question ("what is the reasoning for having two separate styles")*: answered directly — there wasn't a reason, they were built at different times. `dm_notes` already rendered through the shared `.scc-dm-block` (full border, icon, uppercase label) from a consolidation 2 rounds ago; `quest.reveals[]` was never brought into that pass and kept its original, much lighter treatment (thin left-accent bar, plain italic "(DM eyes only)" caption, no icon) despite being equally DM-eyes-only content. Reveals now renders through the same `.scc-dm-block` markup DM Notes uses, with a matching `⚠ Reveals (DM eyes only)` label. Related-entry tags also gained a `collection === 'quests'` check — a distinct gold "🔗 Related Quest" style instead of blending into the generic tag row — though no real quest currently links to another one, so this isn't visually demonstrated live yet; flagged that the schema also has no direction field (a flat `related[]`, no `blocks`/`blocked-by` distinction), so even once populated, a quest-to-quest link can say a dependency exists but not which way it runs — a real gap the DM should know about if directional linking matters, not something to silently paper over with a guessed direction.
Verified live throughout (not just described): screenshots for the NPC category badge, the mid-combat AC/Stats panel, the Quick Note modal opening from an unrelated tab, and the Quest Dashboard's reordered/relabeled panels. Zero console errors across the full pass. All test data reverted or cleaned up, confirmed via a final SQL read: `npc_relationships` back to exactly `{"oro":"ally"}` (the one real, intentional change), zero stray `session_note_entries` test rows, `the-missing-shipment.dm_notes` back to its original text with no markup. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, diff-scoped literal-emoji grep — only pre-existing emoji on lines this round's edits merely passed through (not introduced) plus one new `📝` on the FAB button label, which sits in raw HTML markup, not a JS string literal, so C2 doesn't apply.


---

**2026-08-15 (same session, "Dormant" renamed to "Blocked" — a real UX complaint, not just a color tweak) — `dm.html`, `css/tokens.css`, real `quests.status` data.**
DM's ask had a real investigate-first gate built into its own phrasing: "If dormant means it is blocked either by a future session or the completion of a separate quest, mark it as blocked." Checked which of the two actually applies rather than assuming both — queried the `quests` table schema (no `blocked_by`/`prerequisite`/`depends_on` column exists anywhere) and the 3 real dormant quests' `related[]` arrays (all point only at cities/characters/organizations, never at other quests — confirmed via direct SQL, not recalled). Only one real gating mechanism exists: `first_available`, a session number. Reported this precisely rather than silently building or implying a quest-prerequisite feature that isn't there — the rename is grounded in the mechanism that's actually real (session-gating), not the DM's broader hypothetical (quest-completion-gating), which this app doesn't support today.
**Renamed the value, not just the label** — `status:'dormant'` → `'blocked'` on all 3 real rows (`follow-the-dran-lead`, `the-letter-for-vel`, `anomaly-7c`) via direct SQL, plus every code reference: the Quest Dashboard's status filter `<option>`, the `.q-status-dormant`→`.q-status-blocked` CSS class, `statusOrder`, `statusOptions`, and the `labels` lookup in `statusLabel()`. Doing this as a value rename (not a display-only relabel keeping `'dormant'` internally) was a direct extension of the same principle behind this session's earlier `--cyan`→`--dm-only` token rename — a stored value and its displayed word drifting apart is exactly the kind of "name doesn't represent what it is" problem that round was about; leaving the DB value as `dormant` while showing "Blocked" on screen would have recreated it one layer down.
**Color**: red-orange (`#ff8066`), reused rather than invented — it's the exact hex Active used before this session's earlier pass recolored Active to green, which had left it fully orphaned (confirmed via grep: zero remaining consumers before reclaiming it here). New icon `🚧` (was `💤`, which was itself part of the "players fell asleep on it" implication the DM was objecting to) — written as a `🚧` JS escape in the `labels` object per C2 (that string later passes through `escQ()`/`sccEsc()` before being injected as HTML, and an `&#decimal;` entity there would have been double-escaped into visible literal text — confirmed by checking `statusLabel()`'s real call sites before picking the escape form, not assumed). The static `<option>` markup keeps a literal `🚧` character, matching its 3 sibling options (`🔥`/`✦`/`✓`), which are raw HTML text, not JS string literals, so C2's parse-crash failure mode doesn't apply there.
Verified live: `.q-status-blocked` computed style confirmed `rgb(255,128,102)` text on `rgba(200,60,30,0.15)` background; all 10 real quests reloaded from Supabase showing 3 "🚧 Blocked" / 7 "✦ Available" badges correctly; filter dropdown confirmed via DOM query to offer exactly `active/available/blocked/completed`, no `dormant` anywhere; screenshotted the Quest Dashboard showing the new badge clearly distinct in hue from the adjacent stakes badge. Zero console errors. Pre-push checks clean: raw parse, `new Function()` on all 7 inline script blocks, `🚧` escape confirmed decoding to the correct `🚧` glyph via a direct Node check before shipping.


---

**2026-08-15 (same session, DM color-scheme rework driven by direct review of the published Color Ledger artifact) — `dm.html`, `css/tokens.css`, `field_guide_sessions` (Supabase data).**
DM reviewed 4 real screenshots of the ledger artifact (Quest Stakes, Quest Status, Field Guide Box Types, Player-Facing Visibility) and sent back a fully itemized set of hue reassignments plus 3 structural instructions: rename the misleadingly-named `--cyan` token, remove dead code, resolve duplicates. Executed as one coordinated pass since several of the color changes and the duplicate-resolution shared the same call sites.
**Quest Stakes.** Low/Mid/High recolored to a plain traffic-light scale — green/gold/red — reusing existing `--green`/`--gold`/`--red` tokens rather than inventing new hex, per this session's own established preference for reuse over new-hue-hunting. Campaign removed outright, not recolored: confirmed via a fresh SQL query that 0 of the 10 real quests have ever used `stakes:'campaign'` (all 10 are `low`/`mid`/`high`) and the DM said directly they didn't know what it was for — pulled the `<option>` from the filter dropdown, the `.q-stakes-campaign` CSS rule, the `campaign` key from `stakesOrder`, and the `stakes === 'campaign'` branch in `sccSuggestedLevelRange()`'s level-bump logic. `--dm-stakes-camp-bg/text` tokens deleted from tokens.css.
**Quest Status, with a real question answered, not just colors changed.** Active→green, Available→blue, Completed→grey, all reusing existing tokens. The DM asked directly what "Dormant" means and what makes a quest dormant — checked the real data rather than guessing: all 3 dormant quests (Anomaly 7-C, Follow the Dran Lead, The Letter for Vel) have `first_available` of session-2 or session-3, while all 7 non-dormant ("available") quests are session-1 — Dormant is a quest that's written and ready but gated behind a session number that hasn't arrived yet; Available is a quest actively offered right now. Confirmed by cross-referencing `the-letter-for-vel`'s own DM notes, on record from an earlier session, calling it a "future-session plant... do not develop this in Session 1." Making Completed plain grey created a real collision risk with Dormant's own already-grey/muted tone — resolved by keeping Dormant's existing cooler blue-grey tint (`rgba(120,120,140,...)`) distinct from Completed's new plain neutral grey (`rgba(160,160,160,...)` / `#a0a0a0`, no blue tint) — the two are close but distinguishable on close inspection, and each status already carries its own icon (🔥/✦/💤/✓) as the primary differentiator regardless, matching this session's established "structural distinction over hue-hunting" approach.
**Field Guide box colors — the biggest single change, a real data migration, not just new hex.** Renamed every semantic key to match its actual rendered color (previously `cyan`→rendered-magenta and `orange`→rendered-terracotta were exactly the kind of misnomer the DM was objecting to for `--cyan`) — this was a deliberate generalization of the DM's specific `--cyan` complaint to the same principle applied consistently, not scope creep: a DM editing a box via the dropdown was seeing a semantic label that didn't match what the box actually rendered as. Migrated all 48 real boxes in the one real Session 1 document via a single ordered jsonb update (`with ordinality`, order-preserving, verified `jsonb_array_length` unchanged at 140 blocks before/after): `cyan`→`magenta` (18 rows, DM Only), `orange`→`gold` (8 rows, Loot & Shop), `pink`→`yellow` (7 rows, Comedy Beats), `blue`→`grey` (9 rows, Lore — dropped entirely per the DM's "does not need a color, it is confusing," rendered plain/neutral now like the structural grey boxes). `purple` removed outright with no migration needed — confirmed via SQL it was already at 0 real usage, freed for the DM's stated reservation ("religion and deities," matching NPC Relationship's existing Deity=purple). `SCC_FG_BOX_COLORS`/`SCC_FG_BOX_COLOR_CLASS` simplified from 7 semantic keys with one confusing remap (`cyan`→CSS class `'yellow'`) down to 5 keys with a clean 1:1 identity mapping (`grey/green/magenta/gold/yellow`) — the block editor's color `<select>` now shows exactly what a box will render as, nothing indirect. Comedy's new yellow (`#facc15`) and Loot's new gold (`var(--gold)`, `#f0b429`) are deliberately different-enough hex to stay visually distinct from each other, confirmed via computed style live (`rgb(250,204,21)` vs `rgb(240,180,41)`). The loot/shop subtype-icon logic (drink/food/general/weapons/armor/magic, built 2 rounds ago) keyed off the literal string `'orange'` in 3 places — updated to `'gold'` alongside the rename, re-verified live that Mira Pont's real "AVAILABLE TO PURCHASE" box still shows its 🪙 coin icon correctly post-migration. The in-document legend paragraph ("How To Use This Guide," real DM-authored content, not app chrome) updated to match: "Green boxes = read aloud to players. Magenta boxes = DM only. Yellow boxes = comedy beats. Gold boxes = loot and shop." — the Blue/Lore line removed entirely since that color no longer exists.
**NPC Relationship.** Neutral swapped from blue to grey (`#9a9ab0`), Ally swapped from green to blue (`#60a5fa`) — Caution/Hostile/Deity untouched (gold/red/purple). Verified live via a real 5-click cycle through the actual badge (not a direct function call) on a real NPC (Dran) — confirmed Ally renders `rgb(96,165,250)`, cycled back to Neutral, confirmed via SQL the temporary `dran:'neutral'` write landed and then removed it via a scoped jsonb key-delete, leaving `npc_relationships` back to exactly `{"oro":"deity"}` — the real, intended QA-reference state from 3 rounds ago, untouched.
**`--cyan` renamed to `--dm-only` (plus `-dim`/`-border`), the DM's explicit, unambiguous instruction** — "do not use variable names that do not accurately represent what it is supposed to be." This token has now been cyan, then teal, then magenta, while its name stayed `--cyan` through all three — a name tied to a color's history instead of its meaning goes stale every time the color changes, exactly the failure the DM was pointing at. Renamed across every consumer: `tokens.css` (the definition itself, plus `--scc-dm-only-text: var(--cyan)`), and 15 usage sites across `dm.html` (NPC-card DM-only block, Field Guide DM-only box — now `.scc-fg-box--magenta`, Tagged Notes DM-Only badge, and 5 duplicate `player_facing` DM-Only badge ternaries across character/organization/region/religion/species Quick Look cards) — done via an exact global string replace (`--cyan-dim`→`--dm-only-dim`, `--cyan-border`→`--dm-only-border`, then bare `--cyan`→`--dm-only`, in that order to avoid partial-match corruption), confirmed via grep that zero `--cyan` references remain anywhere in either file.
**Duplicates resolved, matching the `sccConditionColor()` pattern from 2 rounds ago — the DM's "if there are duplicates resolve them" applied to two systems this touched anyway.** NPC relationship colors: the session-roster-card and lookup-drawer-card render paths each had their own identical hardcoded `REL_BG` object (confirmed via grep, genuinely byte-identical, drifted zero times only by luck) — both replaced with calls to one new shared `sccRelColor(rel)`. Region/location "color_health" badges: found a third copy beyond the two already known — the location panel's own `healthBadge()` helper, the city/POI/nation lookup-drawer branch, and the region lookup-drawer branch all had the identical 4-tier if/else color ladder — consolidated into one shared `sccRegionHealthColor(colorHealth)`, each call site's own quirks (the panel helper's `'UNKNOWN'` fallback label vs. the drawer branches' empty-string fallback) preserved exactly via a thin wrapper at each call site rather than baked into the shared function.
**Dead code removed, per the DM's explicit "if there is dead code remove it," not silently left as debt.** 20 unused `--scc-rel-*` tokens in tokens.css (defined, matched the pre-change colors, never actually consumed by anything — confirmed via grep before deleting, not assumed) — superseded by the new `sccRelColor()` JS function, which is now the single real source. 3 unused `--currency-copper/silver/gold-coin` tokens (confirmed zero consumers). The unused `SCC_REL_ICONS` object — confirmed via grep it was defined and never referenced anywhere, which also happened to be a latent, previously-unnoticed C2 violation (4 literal emoji circle characters directly in a JS object literal) that simply never crashed because the object was never actually reached at runtime; removed rather than fixed, since fixing dead code just to keep it dead isn't worth it. The fully-dead `.scc-flag`/`.scc-flag--vareth`/`.scc-flag--formery` CSS (flagged as dead in an earlier session's color audit but never actually removed until now) — re-confirmed via a fresh grep that the base class has zero HTML/JS consumers before deleting the whole rule set, not trusting the earlier flag from memory.
Verified live end-to-end, not just via computed-style spot checks: full Field Guide document reload showing 48 boxes distributed exactly as migrated (grey 10, magenta 18, green 5, gold 8, yellow 7 — matches the SQL migration counts precisely), the legend paragraph rendering the new color names, the block editor's color dropdown showing exactly the 5 new option values with no stale ones; Quest Dashboard screenshotted showing all 10 real quests with correct stakes/status badge colors (7 available/blue, 3 dormant/muted-grey, stakes green/gold/red matching each quest's real value) and the Stakes filter dropdown confirmed via DOM query to no longer have a Campaign option; NPC relationship cycle tested end-to-end through the real UI as described above. Zero console errors throughout. Pre-push checks clean: raw parse, `new Function()` on all 7 real inline script blocks (of 17 total `<script>` tags — 10 are external `src=` includes, correctly excluded), diff-scoped literal-emoji grep caught one hit in my own new CSS comment (not a JS string, not a C2 violation, but fixed anyway to keep the check trivially clean) and came back empty after.


---

**2026-08-15 (same session, DM-Only recolored to magenta on direct instruction) — `css/tokens.css`.**
Ask: "Replace all DM Only associated colors with this: #ff00f3." Direct, unambiguous — executed as given, no design judgment applied to the color choice itself.
**Scope check before editing.** Grepped every consumer of `--cyan`/`--cyan-dim`/`--cyan-border` and `--scc-dm-only-bg/border/text/label` across `dm.html` to confirm each one is genuinely DM-Only content before recoloring the shared tokens — a token used for anything else would have meant scoping the change differently instead of a blanket token-value swap. All confirmed DM-Only: the NPC-card block, the Field Guide document box, the Tagged/Quick Notes "DM-Only" category badge, and all 5 player_facing DM-Only badges (character/organization/region/religion/species Quick Look cards) via `--cyan` family; `.cm-vis-hidden` (Manage tab), `.scc-dm-block` (6 call sites: item/quest/creature/organization DM notes + secrets), and `.scc-fg-dm-block` (Field Guide sidebar) via the `--scc-dm-only-*` family. Also swept for literal hex/rgba bypassing the tokens entirely (the exact failure mode behind this whole session's bug hunt) — found none in `dm.html`; the only remaining literals were the token *definitions* themselves in tokens.css, confirming last round's consolidation had actually closed every gap.
**One more real DM-Only surface found in the process, not previously flagged.** `--dm-reveal-bg`/`--dm-reveal-border` (the quest "Reveals — DM eyes only" boxes) were never wired through either token family — a fourth independent DM-Only color pair. Included in the swap since it's unambiguously the same meaning, even though it wasn't named in the color ledger's original 4-surface count.
**Change**: 8 token values in `css/tokens.css`, zero changes to `dm.html`. `--cyan: #ff00f3` (was `#2dd4bf`), `--cyan-dim`/`--cyan-border` recomputed as the same opacity steps against the new RGB (255,0,243), `--scc-dm-only-bg/border/label` likewise (`--scc-dm-only-text` needed no separate edit — it already derives from `var(--cyan)`), `--dm-reveal-bg/border` likewise. This is the direct payoff of consolidating these into shared tokens last round: what would previously have needed edits scattered across 6+ CSS rules and several inline JS color objects was a single-file, 8-line change with nothing left to miss.
Verified live: NPC-card DM-Only block (Dran) and an organization's Quick Look "💠 DM Only" badge both confirmed via `getComputedStyle()` reading exactly `rgb(255,0,243)` / `rgba(255,0,243,...)`, screenshotted. Zero console errors. Pre-push checks clean: raw parse, `new Function()` on all 17 script blocks (no script changes this round, but re-checked anyway), diff-scoped literal-emoji grep not applicable (CSS-only diff). No Supabase writes.


---

**2026-08-15 (same session, consolidation feasibility call + the 5 fixes it produced) — `dm.html`, `css/tokens.css`.**
Ask had an explicit gate: investigate whether the color ledger's findings shared a fixable root cause before patching them one at a time, since this exact bug shape (one meaning, multiple independent hardcoded sources, silently drifting) had already caused repeat work across several rounds tonight — the DM-Only red/teal splits earlier this session were all instances of it.
**Step 1 — investigated, concluded consolidation is real and cheap for all 3 systems named.** None required a data-model change or touched anything risky; each already had (or could trivially get) one real shared source, and the fix was just repointing consumers onto it:
- **Conditions**: confirmed `COND_DATA` (Live Session panel) and `sccConditionsTableLookup` were both declared *inside* `sccLiveRenderConditions()` — function-local, not shared — while `COND_COLORS` (combat initiative tracker) was a fully separate hardcoded object with no fallback at all. The real fix already existed and was already fetched once at boot: `window.SCC_CONDITIONS_DATA`, populated from the real `conditions` table in `sccInit()`. Recommended: delete both local objects, add one small top-level function every surface calls.
- **Read Aloud**: only 3 real CSS declarations carry the color (Field Guide document box, Field Guide sidebar block, Quick Look card) — recommended one small set of shared CSS custom properties, the cheapest possible fix.
- **DM-Only**: a shared `--scc-dm-only-*` token set already exists and is already used by 6 call sites via `.scc-dm-block` — the 3 newly-found stale surfaces just needed repointing onto it, no new tokens required.
Recommended doing the consolidation rather than patching each instance, specifically because "patch the instance" is what had already happened three times this session and hadn't held.
**Step 2, item 1 — conditions.** New top-level `sccConditionColor(name)` (reads `window.SCC_CONDITIONS_DATA`, falls back to a generic purple only for a genuinely unrecognized name) replaces both `COND_DATA` and `COND_COLORS` outright — not layered on top of them. `sccLiveRenderConditions()` and `sccRenderInitiativeList()` both now call the one function. Verified live: staged a real creature, started combat, force-set its conditions to `['Frightened','Grappled','Muted']` (the 2 worst core-condition mismatches plus one of the 3 previously-uncovered custom conditions) — all three now render `rgb(248,113,113)`/`rgb(217,119,6)`/`rgb(196,201,212)`, exactly matching the real Supabase table, confirmed via `getComputedStyle()` and screenshot.
**Step 2, item 2 — Read Aloud.** New tokens `--scc-read-aloud-bg/border/text` (`rgba(134,239,172,0.07)` / `rgba(134,239,172,0.5)` / `#4ade80` — the Field Guide's existing values, picked as canonical since it's the more prominent of the 3 surfaces) added to tokens.css. `.scc-fg-box--green`, `.scc-fg-read-aloud`, and `.scc-ra-block`/`.scc-ra-label` (the actual bug — was gold) all repointed onto them; the tiny 0.06-vs-0.07/0.4-vs-0.5 opacity drift between the Field Guide's two own surfaces was also resolved as a side effect of true consolidation (both now literally the same token, not just close). Verified live: opened a real creature's Quick Look card with `read_aloud` content, confirmed `getComputedStyle()` reads `rgb(74,222,128)` (green) on both background and label, screenshotted.
**Step 2, item 3 — Manage tab hidden badge.** `.cm-vis-hidden` moved from `--dm-vis-hidden-bg/text` (still `rgba(200,60,30,...)` / `#ff8066`, the pre-recolor red — never touched by any earlier pass since Manage tab uses its own token namespace) to the shared `--scc-dm-only-bg/text`. Left the now-unused `--dm-vis-hidden-*` tokens defined in tokens.css rather than deleting them, matching this file's established practice for superseded-but-harmless tokens. Verified live: a real DM-Only quest row in the Manage list now shows `rgba(45,212,191,0.15)` background / `rgb(45,212,191)` text, screenshotted next to an unaffected green "Visible" badge for contrast.
**Step 2, item 4 — Field Guide sidebar DM Notes.** `.scc-fg-dm-block`/`.scc-fg-dm-label` moved off a hardcoded amber (`rgba(251,191,36,...)`) onto the shared tokens, and gained the same full-border-plus-left-accent shape `.scc-dm-block` already has elsewhere (was background-only, no real border). Verified live on a real NPC (The Vareth, has real `dm_notes`) via `sccFgToggleCard()`: background/border/label all confirmed `rgba(45,212,191,...)`/`rgb(45,212,191)`, screenshotted.
**Step 2, item 5 — Quest DM Notes, two colors depending on tab.** Root cause: 3 real render sites for a quest's `dm_notes` existed — Session > Quests panel card and the global search Quick Look card both already used the shared `.scc-dm-block` (correctly teal), but the Quest Dashboard's own detail view used a completely separate, never-migrated class, `.quest-dm-notes` (gold background/border, muted-grey body text, no border-left accent). Rather than repoint that class's tokens and leave 2 classes doing the same job, swapped the Quest Dashboard's markup to the literal same `.scc-dm-block`/`.scc-dm-label`/`.scc-dm-text` structure the other 2 sites use (matching their exact label text, `⚠ DM Notes`, and dropping the now-redundant separate `quest-section-label` heading, since `.scc-dm-block` already self-labels — the same pattern already used by every other `.scc-dm-block` call site) — then deleted the orphaned `.quest-dm-notes` CSS rule entirely rather than leaving a stale class sitting in the stylesheet that a future edit could mistake for still-live. Verified live: same quest ("The Missing Shipment") checked via `renderQuestDetail()` on the Quest Dashboard — confirmed `.quest-dm-notes` no longer exists anywhere in the DOM, `.scc-dm-block` renders with the correct teal background, screenshotted.
Zero console errors throughout. Net diff: 69 lines removed, 48 added — real consolidation, not a like-for-like patch. Pre-push checks clean: raw parse, `new Function()` on all 17 script blocks, diff-scoped literal-emoji grep empty. No Supabase writes this round (every fix was pure CSS/JS rendering logic) — confirmed via a final SQL read that `session_notes.npc_relationships` is unchanged from the prior round.


---

**2026-08-15 (new session, 2 confirmed builds directly + 5 fresh investigations reported before fixing) — `dm.html`.**
Ask had two pre-confirmed decisions to build directly, and 5 items to re-investigate from scratch (explicit instruction not to trust either of the last two rounds' "verified correct" checks) and report before touching any code.
**Build 1 — Tagged Notes conceptual fix.** The DM's report ("I do not know what it is, what it is for, and how to use it") was upstream of the interaction-flow bugs fixed the last two rounds — nothing on the page ever explained why a second, different note-capture system exists directly below the 5 structured recap boxes (Player Decisions/Unexpected Moments/Loose Threads/Follow-Up/Milestone). Added a new `.scc-tagnote-compare` strip (gold left-border, matching the app's existing "important aside" visual language) between the notes-grid and the renamed section, with text that names both systems explicitly and contrasts them by *when* each is used, not just what each is: "the 5 fields above are for your end-of-session write-up — fill them out once, usually after the table wraps. Quick Notes below is for jotting something down the instant it happens, mid-session — tag it as you go, then filter or search to find it fast afterward." Renamed the section itself from "Tagged Notes" to "Quick Notes" (DM's "use judgment" option, taken — "Quick" signals the in-the-moment framing before the DM even reads the hint) — kept every internal id/class as `scc-tagnote-*` since renaming those would be pure churn with real regression risk for zero user-visible benefit. Rewrote the empty state: previously a single line ("No tagged notes yet — add one above") regardless of cause; now branches on *why* the list is empty — a real filtered-to-nothing state (notes exist, none match the current filter/search) still shows the plain "No notes match.", but a genuinely-zero-notes-ever state shows "Here's what a finished one looks like:" followed by a new `sccBuildExampleTagNoteCard()` — reuses the real card's exact markup for visual fidelity, but every field is a genuinely `disabled` input/textarea (not just styled to look inert), wrapped in a dashed-border, 72%-opacity card carrying a bold gold "EXAMPLE" flag badge, so it can't be mistaken for or accidentally edited as live data. Verified live: the comparison strip and renamed section both render correctly positioned; toggled `SCC_NOTE_ENTRIES`/`SCC_NOTE_FILTER_CAT` directly to reproduce both empty-state branches — confirmed the filtered-to-nothing case still shows the plain message with no example card, and the genuinely-empty case shows the labeled example with `disabled:true` on both its title and body confirmed via DOM read, not just visual glance. Screenshotted both. No Supabase writes in this build (pure client-side rendering change) — nothing to revert.
**Build 2 — Suggested Level input removed.** Per explicit DM confirmation, not a recommendation this time. Removed the `<input type="number" id="q-level-...">`, its label, its status span, the now-fully-dead `setQuestSuggestedLevel()` function (direct `fetch` PATCH to `quests.suggested_level`) in its entirety, and the now-dead `quest.suggested_level` gold badge in the meta row (kept showing a value from a field that can no longer be set would have been confusing, not backward-compatible). The read-only computed range + reasoning (`sccSuggestedLevelRange()`, built last round) is now the *only* Suggested Level display, rendered via a plain `<span>` — no id, no onchange, nothing to click. `quests.suggested_level` itself is left in the schema, untouched and unused (all 10 real quests already `null` there, confirmed last round) — matches this file's own established pattern of leaving a harmless deprecated column/token in place rather than migrating it out unprompted. Verified live: `quests-detail`'s rendered HTML for a real quest contains zero occurrences of `q-level-input` anywhere, and the visible line reads exactly "Suggested Level: lvl 1-2 (session 1, low stakes)" with no interactive element. Screenshotted.
**Investigation 1 — DM-Only color, re-tested fresh per explicit instruction not to trust memory of the last fix.** Checked Dran's actual NPC card and a real Field Guide DM-Only box *separately*, live, via `getComputedStyle()` plus screenshots for both — not assumed correct from either of the last two rounds' claims. Dran's card: genuinely, fully teal — border `rgb(45,212,191)`, title `rgb(45,212,191)`, all 36 internal "SECRET"/"NOTES" label elements `rgb(45,212,191)`. No bug. The Field Guide's own DM-Only boxes (`.scc-fg-box--yellow`, e.g. Beat 4's "EXACT RECOVERY SEQUENCE" box, block index 47 in the real Session 1 document): border-left and label text are correctly teal, **but the box's background fill is a real, confirmed bug** — `background: rgba(34,211,238,0.07)` is hardcoded to the *old*, pre-recolor cyan hex (`#22d3ee`) directly in the CSS rule (`dm.html` ~line 6194), never updated when `--cyan`/`--cyan-border` were repointed to the new teal (`#2dd4bf`) in an earlier round — only the border/label properties in that same rule were ever switched to reference the token; the background property was left as a hardcoded literal. Confirmed via direct grep of the CSS source, not just the computed style. This is a real, previously-undetected bug, reported here rather than fixed, per instruction — a one-line CSS fix (`background: var(--cyan-dim)` or equivalent) once confirmed.
**Investigation 2 — DM-Only vs Read-Aloud distinguishability, a real design problem independent of investigation 1.** Computed both hues from their real rendered colors: DM-Only teal `rgb(45,212,191)` ≈ H172°, Read-Aloud green `rgb(74,222,128)` ≈ H142° — only ~30° apart on the color wheel, both similarly saturated and similarly bright. That's genuinely close, not an exaggerated DM complaint — 30° is well within the range that's hard to distinguish at a fast glance, under imperfect table lighting, or for colorblind users (teal/green compression is a known issue for red-green colorblindness specifically). Checked whether a dramatically-further hue is actually available before recommending one: it isn't — blue is neutral-relationship, red is hostile/danger/errors (and was literally DM-Only's own pre-fix color, ruled out on both history and semantic grounds), purple is deity/high-stakes, pink is comedy, orange is loot, gold is the brand accent — every remaining hue family is already claimed by an existing meaning in this app, so picking a new one risks trading one collision for another. Recommended instead: keep the established teal hue, but make DM-Only stop depending on hue discrimination at all — a solid/heavier background fill (not the current subtle left-border-only accent) plus a significantly larger, bolder warning icon at the start of the label. Not built — reported per instruction, this is the single highest-stakes color in the app and shouldn't be repainted without confirmation.
**Investigation 3 — loot/shop subtype icon "not working," walked cold from a real click path, not direct function calls.** Reproduced a genuine DM-eye-view: navigated to the Field Guide panel, found the actual entry point (a small, plain "Edit" button sitting next to the session selector — confirmed via `document.body.innerText` that the string "block editor" appears nowhere in the real UI; that was my own descriptive shorthand in the prior report, not anything a DM would ever read on screen), clicked it via a real `.click()` call on the actual button element, and confirmed the resulting editor groups the document's 140 blocks into 15 collapsible sections with no search and no visual flag on which rows are orange/shop boxes — a DM would have to already know (or guess) which of 15 sections holds the box they want, expand it, and scan manually. The subtype dropdown itself, once found, has no visible label beyond its own default option text ("Icon: coin (default)") — nothing external to it explains what it's for. Confirmed the underlying feature genuinely works correctly (re-tested end-to-end via the same real click path used last round) — this is a pure discoverability gap, not a functional bug, exactly as suspected going in. Recommended: a visible one-line hint near the "Edit" button describing what editing enables (including shop icons), and/or a clearer label directly on the subtype select itself. Not built — reported per instruction.
**Investigation 4 — Oro's deity badge "still blue," found the real root cause, not a render bug.** First re-confirmed via direct SQL that `session_notes.npc_relationships` is still `{}` (empty) for the one real session — exactly as it was found last round, meaning nothing has ever actually been persisted as "deity" for any NPC, Oro included. Rather than stop at "the data isn't set" and guess why, traced the actual click path: `sccCycleRelationship()`/`sccCycleRelationship2()` (the badge's onclick handler) only mutate the in-memory `SCC_NPC_RELATIONSHIPS` object and call `sccRenderNpcPanel()` — there is no Supabase write anywhere in either function. The value only ever reaches the database if the DM separately navigates to the Setup tab and clicks "Save Setup" (confirmed `sccSaveSetup()`'s payload does include `npc_relationships: JSON.stringify(SCC_NPC_RELATIONSHIPS)` — so persistence is technically possible, just not connected to the badge-click interaction at all). Reproduced live end-to-end: added Oro to a session roster, clicked his relationship badge 4 times (neutral→ally→caution→hostile→deity), confirmed via DOM read the badge correctly rendered "DEITY" in purple (`rgb(192,132,252)`) at that moment — the render code itself has no bug. Then did a full page reload with no save in between, and confirmed via both `SCC_NPC_RELATIONSHIPS` (back to `{}`) and a fresh SQL read (`npc_relationships` still `{}` in the DB) that the change was completely lost. This is a real, structural inconsistency — nearly everything else in this app (loot assignment, tagged notes, quest status, HP) auto-saves on its own action; relationship cycling is the one interaction that silently doesn't, with no visual cue that it hasn't. Very likely exactly what happened to the DM. Not fixed — reported per instruction, recommending the badge click itself trigger a save (matching every other interaction's pattern) rather than requiring a trip to a different tab.
**Investigation 5 — Combat stats, AC "not showing," confirmed real but different from what was assumed.** Checked whether AC is actually missing from the Stats panel's own output for a creature with real data: it isn't — live-clicked the real "Stats" toggle button (not a direct function call) on a staged Grusk Enforcer and confirmed the panel correctly renders "AC 11 (leather armor)" among the other populated fields. No data or rendering bug inside the panel. The real gap is one level up: the *collapsed* staging row (what's visible before any click, i.e. what a DM glances at mid-combat) shows only Name/Type/CR/DEX-mod — confirmed via a direct DOM read of the row's `textContent` with the panel still closed — no AC, no HP, nothing else, and the "Stats" toggle needed to see any of it sits in a small row of icon-only-adjacent buttons easy to miss under real-table pressure. Recommending core stats — AC at minimum, per the DM's own explicit callout — render in the always-visible collapsed row instead of requiring a click, given the stated real-table use case ("otherwise I have no idea what to do with them"). Not built — reported per instruction.
Zero console errors throughout. Pre-push checks clean: raw parse, `new Function()` on all 17 script blocks, diff-scoped literal-emoji grep empty. No Supabase writes needed cleanup — both investigation and build work this round were either read-only checks or pure client-side state (SCC_NOTE_ENTRIES, SCC_NPC_RELATIONSHIPS, SCC_COMBATANTS), confirmed via a final fresh-reload SQL check that nothing test-related landed in the database.


---

**2026-08-15 (new session, structured investigate-then-report QA round) — `dm.html`.**
Ask had an explicit shape: a Step 0 environment check, 7 investigate-and-report items (report findings before building anything dependent on them), and 2 build-immediately items with no investigation gate.
**Step 0 — environment check.** Confirmed the local-dev banner from the prior round renders correctly (fresh server, fresh reload, screenshotted) — rules out "banner never actually shipped" as an explanation for any report of stale/wrong behavior this round.
**Item 1 — Dran's DM-Only box reported as yellow/gold, a third color.** Investigated per instruction: grepped for every DM-Only-adjacent CSS system (found and re-checked the two already-fixed-to-teal systems from prior rounds, `.scc-npc2-dm-block` and `.scc-dm-block`, plus a third, unrelated system — `.scc-npc-card.rel-*`/`.scc-rel-badge.rel-*`, the NPC relationship-state coloring, where "caution" uses gold — considered as a possible source of the report before ruling it out). Went live rather than continuing to theorize: added the real Dran NPC to a session roster via `sccAddChipById`, expanded his real card, and read `getComputedStyle()` on the DM-Only box's border, title, and all 36 internal colored elements (icons + "SECRET"/"NOTES" labels) — every one resolves to `rgb(45,212,191)`, the correct teal; confirmed visually via screenshot too (clean teal border/label throughout, no gold anywhere). Dran's own relationship badge reads "NEUTRAL" (blue), not "caution" (gold), further ruling out the relationship-color theory. **No third hidden rule found — the current code is correct.** Reported rather than assumed: most likely explanation is a browser cache serving stale `dm.html`/`tokens.css` on the DM's machine (an on-record recurring issue in this file's own history) or the DM viewing the actual live GitHub Pages site, which carries none of this branch's fixes since the branch has never been merged into whatever Pages deploys from.
**Item 2 — category-specific loot/shop icons.** Checked the real `field_guide_sessions` block schema and the block editor's box-row markup: a box stores only `label` + a single semantic `color` (grey/green/cyan/blue/purple/orange/pink) — no sub-type field exists anywhere, and the editor's only box controls are a Label input and a Color `<select>`. No existing data could drive per-category icon selection. Recommended, not built (per instruction not to guess an auto-detection scheme from box text): add a `subtype` field scoped to orange boxes specifically (Drink/Food/General Store/Weapons/Armor/Magic), surfaced as one more dropdown in the block editor's box row next to the existing color select, DM-assigned per box at authoring time.
**Item 3 — Roll Initiative button behavior.** Read `sccStartCombat()` directly: for each combatant, it reads the manual init input; if a real number is present it's used exactly as typed (never overridden), and only a blank/invalid field gets a random d20+DEX roll — then every combatant (typed and rolled together) is sorted by the resulting initiative descending. So the real behavior is "fill blanks, then sort" — never "override everything," confirming the DM's third hypothesis. Recommended clearer copy: "Start Combat — Sort/Roll Initiative," or keep the label and add a `title` tooltip reading "Uses your typed values as-is; rolls a d20+DEX for anything left blank, then sorts everyone by initiative" — the current "Roll Initiative" wording overstates what happens whenever some or all values are already filled in by hand.
**Item 4 — creature stat-block data coverage.** Queried every field named in the ask across all 52 creatures, not assumed from the known CR gap: type/AC/CR/size/all 6 ability scores/languages/speed/senses/attacks/traits/HP are each exactly 4/52 — the same 4 creatures every time (Grey Fluffets, Still Frogs, Grusk Enforcer, Grusk Color Extractor). Within those same 4, coverage thins further: skill proficiencies 2/52, saving throws 1/52, actions 2/52, reactions 0/52, damage resistances/immunities/condition immunities all 0/52, legendary actions 0/52 (no legendary creatures exist, expected). No `spells` column exists anywhere in the schema, and no spellcasting data appears in any creature's `attacks`/`actions`/`traits` jsonb. Weapon/damage data (name, dice, type, attack bonus) lives inside `attacks` jsonb, populated for the same 4. Also checked whether any of this already reaches the Combat tab: `sccAddToCombatStaging()` only ever pulls `challenge_rating`, `hp_max`, and a DEX-derived initiative modifier into a combatant object — nothing else from a stat block reaches any UI today. Realistic picture reported to the DM: a full stat-block display is genuinely viable today for exactly 4 creatures; every other creature needs real data entry (starting with AC/HP/type/size/ability scores) before a display build would show anything beyond name/tier/category for it.
**Item 5 — Tagged Notes click-flow confusion.** Walked `sccAddTagNote()`'s actual path: it POSTs a blank note, then `sccRenderTaggedNotes()` re-fetches ordered `updated_at.desc`, so a new note should land at the very top of the list. Found two concrete, previously-unidentified failure modes rather than a vague "unclear UI": (1) if a category filter chip is active and the DM creates a note in a *different* category via the dropdown, the note is created successfully in Supabase but immediately filtered out of the visible list by `sccFilterTagNotes()` — the DM sees nothing happen and reasonably concludes the button is broken. (2) even when visible, nothing auto-focuses the new note's title input, scrolls it into view, or shows a success confirmation — it just silently appears after a brief loading flash, easy to miss. Recommended, not built: reset or auto-match the active category filter to the new note's category on create so it's guaranteed visible, auto-focus the title input immediately after render so typing can start right away, and add a brief visible confirmation (toast or highlight) — a structural fix to the interaction, not a cosmetic pass.
**Item 6 — Suggested Level informed by all available factors, built as a range + reasoning, never auto-filling the field.** New pure, side-effect-free `sccSuggestedLevelRange(quest)`: baseline from `first_available` (parses "session-N" → range N to N+1, modeling roughly one level of party growth per session), shifted +1 for `high` stakes / +2 for `campaign` stakes (`low`/`mid` leave the baseline untouched), and widened to include a related creature's CR-derived level whenever a `related[]` entry with `collection:'creatures'` resolves to a real `challenge_rating` — checked against real data first: today only one quest (The Greytalon) has any creature relation at all, and that creature's CR is null, so the CR nudge is real and wired but correctly produces no visible effect on any of the 10 current quests until CR data exists on a related creature. Rendered as a small muted `<span>` next to (never replacing) the existing manually-editable `#q-level-{id}` input — `quest.suggested_level` itself is never written by this code path, only read for its own display. Verified live via direct function calls (`session 2, high stakes` → lvl 3-4 for Anomaly 7-C; `session 1, low stakes` → lvl 1-2 for The Anomaly Reports) and via the rendered Quest Dashboard UI (screenshotted: "Suggested range: lvl 1-2 (session 1, low stakes)" sitting next to a still-blank, still-editable "Not set" input).
**Item 7 — a real deity NPC for future QA.** Confirmed `world_characters` has 5 real `category:'god'` rows (Oro, Nara, Thyun, Solvara, Grak), but "relationship" (ally/neutral/caution/hostile/deity) is a per-session, DM-toggled cycle stored in `session_notes.npc_relationships` — not an attribute of the NPC itself. Queried the one real session row and confirmed `npc_relationships` is currently `{}` (empty) — no NPC anywhere in the real data is presently set to "deity." Reported rather than fabricated: recommended Oro (most prominent, benevolent, most widely worshipped — best fit for a positive-relationship test case) as the NPC to add to a session roster and cycle to "deity" via 4 clicks on the relationship badge, for future QA reference.
**Item 8 — Combat tab layout, built immediately per instruction, no investigation gate.** Moved "+ Add All Players" out of its own row below the staging list and into `.scc-combat-top-row`, directly beside the "Add creature or player…" search box. Added `margin-top:0.75rem` both above `#scc-combatant-staging` (space between the search/add-all row and the list) and above the bottom button row (space between the list and Clear All/Start Combat) — previously flush against both neighbors with zero gap. "Start Combat — Roll Initiative" pushed to the far right of its row via `margin-left:auto` (Clear All remains left-aligned in the same row). No JS behavior touched — all three buttons keep their existing `getElementById`-based show/hide logic, which doesn't depend on DOM position. Verified live: staged 2 real creatures (Grusk Enforcer, Grusk Color Extractor) via the real add-to-staging path, screenshotted the resulting layout (Add All Players beside the search box, visible gaps above/below the staged list, Start Combat pinned right) — then cleared the client-side-only staging state (never written to Supabase, confirmed via existing code, so no DB cleanup was needed).
**Aside, not fixed (out of the requested scope for item 8):** noticed while verifying the layout that "+ Add All Players" is hidden via the same `if (!SCC_COMBATANTS.length)` guard as Start Combat/Clear All — meaning a DM can't bulk-add players to a *completely empty* encounter, only after at least one combatant already exists some other way. This looks like a pre-existing usability gap independent of layout, not something item 8 asked about — flagged here rather than fixed unilaterally.
Zero console errors throughout. Pre-push checks clean: raw parse, `new Function()` on all 17 script blocks, diff-scoped literal-emoji grep empty.


---

**2026-08-14 (new session, local-dev environment banner) — `dm.html`.**
Ask: a persistent, auto-detected "not the live site" banner with zero manual setup per session/checkout.
**Detection.** Pure `window.location.hostname` check against the literal live domain `carlosknightiii.github.io` — `hostname !== 'carlosknightiii.github.io'` shows the banner. No cookie, no localStorage flag, no build-time substitution — evaluates fresh on every page load, so there's nothing to forget to set up. Considered and rejected showing the git branch/folder name: this file is served as a static HTML page with no backend and no build step, so the browser genuinely has no way to know what branch or directory it was checked out into — faking a plausible-looking value would be worse than the honest fallback the ask itself specified (hostname:port). The banner text is `⚠ LOCAL DEV BUILD — NOT LIVE — {hostname}:{port}` (port omitted if the URL has none, e.g. a plain `file://` open falls back to a `'local file'` label).
**Layout.** `#dm-env-banner` is `position:fixed; top:0`, `z-index:10000` — deliberately above the password gate's own `z-index:1000` (`.gate-overlay` is a separate full-viewport fixed layer, so the banner needs to out-rank it to be visible even before login, not just after). Rather than hardcode a pixel height for the push-down, the banner's real `offsetHeight` is measured after paint (`requestAnimationFrame`) and written to a `--dm-env-banner-h` CSS custom property on `:root`, which both `.gate-overlay` (`top: var(...)`, overriding just that one property from its existing `inset:0` shorthand) and `.dm-app` (`margin-top` + a `min-height` reduced by the same amount, so total occupied height still equals exactly 100vh) read from — correct regardless of how the banner's actual rendered height ends up varying by font/OS.
**Verification.** Live on a fresh local server: banner text and bright-red styling confirmed via screenshot exactly as specified, sitting above both the password gate and (after a real login) the app header, in both cases with the app content cleanly offset below it — confirmed via `getBoundingClientRect()` that the offset is pixel-exact (33px banner height → 33px `--dm-env-banner-h` → `.dm-app` top at exactly 33px, no gap or overlap). Live-domain behavior verified by calling the exact same hostname-comparison function the shipped code uses with `'carlosknightiii.github.io'` passed directly — confirmed it returns `false` (banner hidden) — rather than by actually loading the real deployed site, since this branch (`anavale-wiki-scc-ux`) has never been merged to whatever branch GitHub Pages serves from, so nothing on this branch is reachable at the real URL to test against directly. Zero console errors. A literal ⚠ character landed in the new inline `<script>` on first write (the Edit tool's own text transport normalizing the typed `⚠` escape back into the glyph, the same recurring behavior on record from earlier sessions) — caught by the standing pre-push emoji grep before committing, fixed via the established Python exact-string-replacement script rather than retrying the same edit. Pre-push checks otherwise clean: raw parse, `new Function()` on all 17 script blocks (16 before this change), diff-scoped emoji grep empty after the fix.


---

**2026-08-14 (new session, DM's second QA pass — explicit instruction not to trust the first pass's "verified" claims, re-investigated everything from scratch) — `dm.html`, `css/tokens.css`.**
Every item below was re-derived from a fresh grep/live check, not recalled from the prior session's own log — several genuinely contradicted what that log claimed was fixed.
**(1) Quest stakes vs deity purple.** The first purple audit searched direct `var(--purple)`/`#c084fc` usage and missed anything reached through an extra token hop. Re-grepped both `dm.html` and `tokens.css` fully this time and found `--dm-stakes-high-text: var(--purple)` — `.q-stakes-high` (quest "high stakes" badges) resolves to the exact deity hex through `--dm-stakes-high-text`, a level of indirection the narrower search never followed. Confirmed live: 1 real quest (Anomaly 7-C) has `stakes:'high'` today, genuinely rendering deity-purple. Sent for confirmation: reuse `--orange-vivid` (`#fb923c`, already Combat's "high" difficulty color) rather than adding a 10th distinct hue to an already-crowded palette — the two badges are in different UI surfaces (Quest Dashboard vs. Combat tab) and share the same underlying idea ("elevated severity/importance"), so reusing the hue is a feature, not a new collision. Confirmed and implemented — `--dm-stakes-high-bg`/`--dm-stakes-high-text` repointed to the orange-vivid RGB, verified live (`rgb(251,146,60)`, not purple) on both the sidebar quest card and the detail view's badge, deity's own `#c084fc` confirmed untouched.
**(2) DM-Only teal vs "Vareth presence" — investigated, did not reproduce.** Found every real Vareth-related visual element via grep: the location/nation lookup card's "VARETH" badge (`.scc-loc2-vareth-badge`) is a plain grey `#a0a0b0`/`#0b0e16`, nowhere near teal. Two CSS rules (`.scc-flag--vareth`, `.scc-flag--formery`) reference red (`var(--dm-red)`) but are dead code — confirmed via grep neither is referenced by any HTML/JS anywhere, leftover from an earlier design. Also checked `.scc-ql-tag` (generic tag pills, which could show a "vareth" tag) — neutral grey/white, no teal or purple involvement. Reported back rather than guessing at a fix, per instruction, since no real collision was found; most likely explanation is the "Vareth Presence" *row label* inside the big red DM-only box (see next item) — which was about to become teal as a side effect of that fix, correctly, since it's genuinely DM-only content sharing that box's styling.
**(3) A second, larger red DM-only surface the first pass never found.** Grepped the actual red hex values directly (`#d62b2b`, `#f87171`, `248,113,113`) across the whole file instead of trusting the prior session's "all 6 `.scc-dm-block` call sites fixed" claim — found `.scc-npc2-dm-block` (the NPC/location panel's own large "⚠ DM Only — Do Not Read Aloud" card, with nested "Secret"/"Notes"/"Vareth Presence" rows via `.scc-npc2-red`) still hardcoded to `#d62b2b` throughout (background border, title, row-label color) — a completely separate rendering surface (expanded NPC/location cards) from the lookup drawer's `.scc-dm-block` fixed last round, never touched by either earlier pass. Also found quest "Reveals" boxes (`.quest-reveal-item`, explicitly labeled "(DM eyes only)" in its own visible header) still on `rgba(248,113,113,...)`. Converted both to the teal system (`var(--cyan)` for the NPC/location card; `rgba(45,212,191,...)` for reveals, matching the `--scc-dm-only-*` token pattern). Verified live: Reveals boxes render with teal borders (`rgb(45,212,191)`) on Anomaly 7-C's real detail view, screenshotted.
**(4) The "teal border, red/brown body" report was real and pointed at a genuine miss.** Re-read `.scc-dm-text`'s actual CSS rule (the body-text class inside `.scc-dm-block`, the box fixed last round) — found `color: var(--dm-red)`, completely unrelated to the `--scc-dm-only-*` token set the rest of that box already correctly uses. Last round's fix only touched `--scc-dm-only-bg/border/label` (and a `--scc-dm-only-text` token that, it turns out, was defined but never actually consumed anywhere) — `.scc-dm-text` never referenced that token set at all, so its body copy stayed red throughout. Repointed to `var(--scc-dm-only-text)`, which — as a side effect — finally puts that previously-dead token to use.
**(5) Suggested Level — confirmed genuinely editable, affordance improved regardless.** Checked via the accessibility tree (`read_page`) and computed style, not assumption: real `type="number"` textbox, `disabled:false`, `readonly:false`, `cursor:text`, full opacity, exposed correctly to assistive tech as an editable textbox with its "Not set" placeholder. Not a functional bug — added `.q-level-input:placeholder-shown { border-style: dashed; }` plus a hover-border cue and a `title` tooltip so an empty field visibly invites a click instead of reading as inert label text next to the (very differently-affianced) Status `<select>` beside it.
**(6) Field Guide bleeding into Quests/Combat/Notes — confirmed a real CSS specificity bug, not an intended persistent widget.** Checked the actual render logic per instruction rather than guessing: `#scc-panel-fieldguide { display: flex; flex-direction: column; overflow: hidden; }` is a bare ID selector — higher specificity than the base `.scc-panel { display: none; }` rule every other sub-panel relies on for its `.active`-gated visibility — so this one panel's `display:flex` applied unconditionally regardless of the `.active` class, correctly toggled or not. Confirmed live before the fix: switching to Combat left `#scc-panel-fieldguide` with `classList` correctly lacking `.active`, but `getComputedStyle().display` still `"flex"` — the search bar/filter chips/stuck "Loading Field Guide…" state were genuinely always present underneath. Fixed by scoping the rule to `#scc-panel-fieldguide.active`. Verified both directions: `display:none` on Combat/Quests/Notes now, and `display:flex` plus the real document content (54,460 chars, unrelated regression check) still correct when Field Guide itself is active.
**(7) Combat roster quick-add chips didn't support multi-add — confirmed, unlike the search box the prior round fixed.** `sccRenderCombatSessionCreatures()` computed `inEncounter` via `SCC_COMBATANTS.some(id match)` and rendered a permanently non-interactive `<span class="added">` once true, plus `sccQuickAddCombatant()` had its own separate early-return guard — two independent blockers, neither touched when the search box's own dedup guard was removed last round (a different code path entirely). Replaced the binary added/not-added state with a live count (`✓ Name ×3`) and kept the chip as a real `<button>` throughout, so it stays clickable for repeat adds while still surfacing "already used" at a glance — arguably better than the old checkmark, which only ever said yes/no. `.scc-combat-session-chip.added`'s `cursor:default` removed (was actively communicating "not clickable," now wrong). Verified live: clicked the same roster chip 3 times, confirmed 3 real combatants added and the chip reading "✓ Grey Fluffets ×3", screenshotted.
**(8) Tagged Notes reorder + prominence.** Moved the entire `.scc-tagnote-section` block (previously the last thing in the Notes panel) to sit between the 5 structured recap fields and the Save Notes/Copy Summary/Session Completed controls — during-session content before end-of-session actions, matching the DM's own framing of why the old order "read backwards." Split the old single-row toolbar (search input + category select + "+ New Note", all inline) into two rows: a new visually distinct `.scc-tagnote-add-row` (gold-boxed, matching the app's existing quest-related accent) holding just the category select and a `.scc-btn-primary`-styled "+ New Note" button, sitting above a plainer search-only row — so adding a note reads as the section's one clear primary action instead of blending into a generic filter bar. Added a one-line hint under the section title explaining what Tagged Notes are for. Verified live end-to-end: real note created via the actual "+ New Note" button (correct DM-Only teal tag badge, confirming the color fix propagates here too), deleted via the real delete control, confirmed 0 leftover rows via SQL.
**Verification, all live, not assumed.** Fresh local server per the standing lesson, confirmed serving current code via `curl` before trusting it; hit and worked around the same browser tokens.css caching issue on record from prior sessions via a cache-busting query string. Computed-style and accessibility-tree checks throughout, not just visual glances — every color fix confirmed via `getComputedStyle()` returning the exact expected `rgb(...)`, not eyeballed. Zero console errors. Pre-push checks clean: raw parse, `new Function()` on all 16 script blocks, diff-scoped literal-emoji grep's only 3 hits confirmed pre-existing (present in `git show HEAD:dm.html` before this session) and merely relocated by the Tagged Notes reorder, not newly introduced.


---

**2026-08-14 (new session, DM QA pass on the completed DM Tools UX/IA build — 3 colors investigated + fixed, 6 bugs investigated + fixed, 2 combat features built) — `dm.html`, `css/tokens.css`, `field_guide_sessions` (Supabase data).**
Explicit instruction: report colors before touching any hex, just build the rest. Followed that split — colors were investigated and reported via a confirmation question before any hex changed; bugs/features were investigated then fixed directly, per instruction.
**Colors — fresh re-audit, not assumed from memory.**
*(1) Purple triple-collision*: grepped every `#c084fc`/`var(--purple)` usage — confirmed exact-hex collision across Field Guide comedy boxes (`.scc-fg-box--purple`), NPC relationship "deity" (`REL_BG.deity`, two duplicated copies of the same lookup object), and Combat "high" difficulty (`--scc-diff-high: var(--purple)` in tokens.css) — the last one new since the last color pass, confirming the DM's own framing. Recommended and (after confirmation) implemented: deity keeps purple (rarest usage, one relationship badge, strongest semantic fit — mystical/otherworldly); Combat-high moves to vivid orange `#fb923c` (a new `--orange-vivid` token, referenced by `--scc-diff-high`) — this is the exact hex loot/shop is retiring in fix (3), so zero new collision, and it gives the difficulty scale a real green→gold→orange→red escalation instead of an arbitrary purple in the middle; Field Guide comedy moves to pink `#f472b6`, confirmed via grep to have zero existing fixed semantic meaning anywhere (the one prior appearance is a rotating population-pie-chart palette swatch, not a badge/label color). All 7 real comedy boxes in Session 1's migrated `field_guide_sessions` data (`★ THE BOUNCE BEETLE NAVIGATION INCIDENT` and 6 others, confirmed via `jsonb_path_query` before writing) had their `color` field updated from `'purple'` to `'pink'` via a scoped `jsonb_agg` rebuild — block count re-verified still 140 after. Added `.scc-fg-box--pink` CSS (mirroring the existing 6-variant box-color pattern exactly) and `'pink'` to both `SCC_FG_BOX_COLOR_CLASS` and the editor's `SCC_FG_BOX_COLORS` dropdown. The grey "HOW TO USE THIS GUIDE" box's own legend sentence — real migrated content, not app chrome — literally said "Purple boxes = comedy beats"; fixed to "Pink boxes" via a scoped SQL string replace so the document's own color key stays accurate, since this is a factual correction caused by tonight's own change, not a content rewrite.
*(2) Blue/blue/cyan triple-similarity*: confirmed Field Guide lore (`#7eb8f0`), NPC "neutral" (`#60a5fa`), and DM-Only (`var(--cyan)`, `#22d3ee`) are all blue-family hues, and confirmed via the tokens.css comment history that DM-Only has now failed to stay visually distinct *twice* (this is its third color total, counting the original gold). Recommended and implemented: rather than touch the two narrower, single-purpose blues, fixed DM-Only itself two ways at once, since hue-alone had already failed once — moved `--cyan`/`--cyan-dim`/`--cyan-border` to a genuinely teal value (`#2dd4bf`, notably more green-leaning than the old cyan, confirmed via RGB channel comparison: new teal G>B, old cyan B>G) and added a 💠 diamond icon (the same glyph Field Guide's own DM-Only boxes already used) to all 5 player_facing "DM Only" text badges (character/organization/region/religion/species — grepped `'DM Only'` to confirm exactly 5 occurrences, all the same ternary-fallback shape, safe for one `replace_all`). `--scc-dm-only-bg/border/label` (the shared token set behind item/quest/creature/organization DM-Notes content boxes, see bug fix below) updated to the same new teal RGB in the same pass, so both surfaces move together.
*(3) Orange alarm-tone*: confirmed the DM's read of gold's overload via grep — 13 real usages spanning caution/teaser/currency-adjacent/quest-icon plus its role as the base brand accent, agreeing a 6th meaning would make it worse. Recommended and implemented: kept the orange hue family (preserves the established "orange = loot/shop" convention rather than teaching a new one) but desaturated/warmed it from vivid `#fb923c` to a terracotta `#d9a66c` — box background/border/paragraph tint all re-derived from the new RGB, matching the existing per-color CSS pattern exactly. Added a 🪙 coin icon to every orange (loot/shop) box label unconditionally in the renderer itself (`colorClass === 'orange'`), not per-block data, so it applies automatically to any future loot/shop box without needing per-entry authoring.
**A real near-miss caught by the standing pre-push check, not shipped broken**: the 💠 badge-icon edit initially landed as a literal emoji character in the 5 JS string literals — the Edit tool's own text transport appears to normalize a typed `💠` escape back into the literal glyph on write (confirmed by inspecting the file immediately after: `grep '💠'` found live hits inside `<script>` source). Caught by the same diff-scoped emoji grep this file's pre-push checklist has run every session, fixed via a one-off Python script doing exact-string replacement (the same workaround this file's own history already establishes for this exact Edit-tool limitation) rather than retrying the same tool call a second time.
**Bugs — investigated each before assuming a cause, per instruction.**
*(4) Suggested Level blank*: queried all 10 real quests directly — confirmed every one has `suggested_level: null`, exactly as expected from the original migration (no value was ever meant to be set then). Not a save/display bug — the code path is correct. The genuine problem was the empty state: a bare number input with a barely-visible `—` placeholder reads as broken. Changed the placeholder to "Not set" and widened the input from 5rem to 6.5rem so the word isn't clipped.
*(5)+(6) Shop section missing / can't add first stock item*: queried `vendor_stock` directly — confirmed 0 rows total, for every entity, matching the original build session's own final cleanup step exactly. Loaded the real app fresh (new local server, confirmed serving current code via `curl` first) and opened both Mira Pont's and Goldenway's real lookup cards directly — the Shop section, its "Nothing in stock yet." empty state, and the add-stock search box all rendered and worked correctly on the first try, no gating on existing stock found anywhere in `sccVendorShopSectionHtml()`/`sccVendorSearchStock()`. Added a real Torch to Mira Pont's stock through the actual search-and-click UI to confirm end-to-end, then removed it via SQL. No code change was needed for either — the empty Shop sections were fully explained by the confirmed-real vendor_stock cleanup, and the "chicken-and-egg" bug as described does not exist in the current code.
*(7) Nations unreachable*: confirmed nations *do* appear in global search results and *do* open the lookup drawer (traced `sccGlobalResultClick`'s `nation→'city'` remap, confirmed intentional and correct) — but the opened card was mislabeled "Type: Location" instead of "Type: Nation", and showed none of the real nation-specific data (`government`, `capital_city`, `beliefs`, `gigglegloom_relationship` — confirmed via `information_schema` these are real, populated top-level columns, distinct from the `extra_data` jsonb blob which was left untouched as a separate, bigger scope). Root cause: `sccOpenLookup`'s `city` branch computes its POI/Nation/City type label from `loc._srcType`, a field that's never actually set on this branch's own inline-built `allLocs` array (a *different*, unrelated function tags its own separately-built array with that field) — the same latent bug the Vendor/Shop build's own POI-scoping fix had already worked around once, just not applied here yet. Fixed with the identical pattern: direct `NATIONS.some()`/`POIS.some()` membership checks instead of trusting `_srcType`, and reused the new `isPoiEntry` result to simplify the Shop-section gate that was doing the same POI check redundantly inline. Added the 4 missing nation fields, gated on `isNationEntry` so cities/POIs sharing this branch are unaffected.
*(8) Item DM Notes red*: grepped every `.scc-dm-block` call site — found 6, not 1: Quest Dashboard, Quest lookup card, Creature lookup card, Organization (both Secret and DM Notes), and Item — all sharing one CSS class, all still rendering in the original `rgba(248,113,113,...)` red because the original cyan recolor pass only ever touched the separate player_facing *badge* color (`var(--cyan)` on `.scc-ql-pf-badge`), never the shared `--scc-dm-only-bg/border/text/label` token set behind this completely different visual element (the actual notes/secret content box below the badge). Fixed once at the token level in tokens.css — updates all 6 call sites simultaneously, not just Item. Confirmed live on Health Potion (`rgba(45,212,191,0.9)`, the new teal, not red).
*(9) Manage tab name truncation*: found 3 stacked, uncoordinated pixel caps — two separate `.cm-td-name` rule blocks (220px and a genuine duplicate at 240px, violating this file's own no-duplicate-rules convention) plus a tighter 180px cap on the inner `.cm-name-link` itself, which was the one actually doing the visible clipping regardless of the `<td>`'s own width. None were relative to real available space. Removed all three; confirmed live with a real 95-character item name ("DM Reference — Session 1 World Questions...") rendering in full at 626px with room to spare, where it would previously have clipped hard at 180px.
**Features — both scoped to the Combat encounter builder.**
*(10) Multi-add same creature*: traced the block to `sccPickerSelect()`'s dedup guard (`SCC_SELECTED[type].some(id match) → silently no-op`) — built for the other 4 picker types (location/npc/quest/item), which all have real, visible chip-list UIs where a duplicate would be a genuine mistake to prevent. Confirmed `SCC_SELECTED['combatant']` has no such UI consumer at all (`sccRenderChips('combatant')`'s target element doesn't exist in the HTML) and confirmed `SCC_COMBATANTS` is read by array index everywhere, never by id, so duplicate ids carry zero risk. Carved out a `type === 'combatant'` bypass in `sccPickerSelect()` that skips the dedup check and the dead `SCC_SELECTED` bookkeeping entirely, going straight to `sccAddToCombatStaging()`. Verified live: searched "Grusk Enforcer" and clicked it 3 separate times, confirmed 3 independent combatant rows.
*(11) Clear All*: added a `scc-btn-sm scc-btn-danger` button (matching the existing "End Combat" button's own class combination) next to Add All Players, wired into the same show/hide-when-empty logic those buttons already use, confirm-gated via the native `confirm()` dialog matching every other destructive action in this file. `sccClearAllCombatants()` just resets `SCC_COMBATANTS = []` and re-renders — combat staging has always been pure client-side state until "Start Combat," so no Supabase cleanup was needed.
**Verification, all live, not assumed**: fresh local server per the standing lesson (confirmed serving current code via `curl` first). Computed-style checks (not just visual) confirmed every new hex resolves correctly: `--cyan` → `rgb(45,212,191)`, DM-Only badge → same teal + 💠 prefix, Combat "high" class → `rgb(251,146,60)`, Field Guide pink box → `rgb(244,114,182)`, Field Guide orange box → `rgb(217,166,108)` with the 🪙 prefix. Nation lookup screenshotted showing correct "Type: Nation" plus Government/Capital/Culture/Beliefs/Gigglegloom Relationship. Manage tab screenshotted with the 95-character name rendering unclipped. Combat: 3x Grusk Enforcer added via real search clicks, screenshotted in the roster with the difficulty badge in the new orange; Clear All confirmed emptying `SCC_COMBATANTS` and restoring the empty state. Zero console errors throughout. All test data reverted: `vendor_stock` confirmed 0 rows, combat staging cleared (never persisted, nothing to revert server-side), `field_guide_sessions` Session 1 confirmed still 140 blocks after both the pink-recolor and legend-text SQL updates. Pre-push checks clean: raw parse, `new Function()` on all 16 script blocks, diff-scoped literal-emoji grep clean (one hit was inside a CSS comment, not a JS string literal — confirmed C2 doesn't apply, left as-is).


---

**2026-08-13 (same session as the Vendor/Shop investigation below — DM answered all 4 design questions, built and shipped) — `dm.html`, new `vendor_stock` table.**
DM's 4 answers: (1) fixed per-vendor stock list with quantity/price overrides — Recommended option, confirmed. (2) no new vendor flag on `pois`/`organizations` — Recommended option, confirmed; vendor-hood is implied purely by having `vendor_stock` rows. (3) buy only, no sell-back — the more conservative of the two options, confirmed explicitly rather than the recommended default. (4) woven into the existing lookup drawer rather than a new top-level tab — Recommended option, confirmed.
**Schema.** `vendor_stock` (`id uuid pk`, `entity_collection text` — `'pois'` or `'organizations'`, `entity_id text`, `item_id text references items(id)`, `price_gp_override numeric null` — null means "use `items.cost_gp`", `quantity integer null` — null means unlimited, `notes text null`, `sort_order`, `created_at`/`updated_at`). RLS mirrors `session_note_entries`' anon-all-access policies exactly (confirmed via `pg_policies` before writing, not assumed). `updated_at` trigger reuses `session_note_entries`' own `update_session_notes_timestamp()` function rather than defining a duplicate — the first migration attempt tried `moddatetime()` (a common Postgres extension trigger function) and failed with "function does not exist," since this project doesn't have that extension enabled; checked what `session_note_entries` actually uses instead of guessing again, per the 2-attempt rule.
**Fetch.** `sccInit()`'s existing `Promise.all` gained `sccFetch('vendor_stock', '&order=sort_order.asc')` into a new `window.VENDOR_STOCK` global — same shape as the `species`/`items`/`conditions` fetches already there.
**UI.** New `sccVendorShopSectionHtml(collection, entityId)` builds a "Shop" `.scc-ql-section` — a shared buyer `<select>` sourced from the real `SCC_PLAYERS` roster (hidden if no players are loaded), one row per stocked item (thumbnail or fallback initial, name + catalog price shown alongside two small editable inputs — Price(gp) and Qty, both blank meaning "use catalog default" / "unlimited," blur-to-save via a new generic `sccVendorSetField(stockId, field, value)`), and a qty-to-buy input + Buy button that's replaced with an "Out of stock" / "No price set" / "No players" label whenever a purchase genuinely isn't possible right now. The add-to-stock search box reuses `sccLootItemSearch()`'s exact `.scc-picker-search`/`.scc-picker-dropdown`/`.scc-picker-option` markup and CSS verbatim — zero new dropdown styling needed, only a new `sccVendorSearchStock()`/`sccVendorAddStock()` pair filtering out items already in that entity's stock. Wired into `sccOpenLookup()`'s `city` branch (POI-scoped, see below) and `organization` branch (unconditional), both appended right after their existing Tags section.
**POI-scoping bug, caught before shipping.** The `city` branch's own `loc` lookup is built fresh inline (`CITIES.concat(POIS).concat(NATIONS)`) and never receives the `_srcType` tag a *different*, unrelated function attaches to its own separately-built array elsewhere in the file — confirmed via grep before trusting it, since the branch's existing (and, this reveals, already-quietly-broken) POI-vs-city type-label logic depends on exactly that field being present. Rather than reuse a check that was never actually working, scoped the Shop section with a direct, reliable membership test instead: `POIS.some(function(p){ return p.id === loc.id; })`. Did not fix the branch's pre-existing broken type-label fallback itself — unrelated to this ask, flagged here rather than touched silently.
**Transaction.** `sccVendorBuy()` composes the same two calls `sccAwardCoins()`/`sccAwardItem()` already make elsewhere in this file — `sccPatch('character_sheet', ...)` for gold, `sccPost('inventory', ...)` for the grant (`dm_granted:false`, since this is a real purchase, not a DM freebie) — with two guards neither of those functions needed: an affordability check against the buyer's real `gold_cp` (blocks with a toast, zero writes, if short) and a stock-quantity check (blocks if the requested buy-quantity exceeds what's left). `vendor_stock.quantity` is decremented after a successful purchase only when it isn't null. No sell-back path exists anywhere in the new code, per the confirmed buy-only scope. Every mutating action (add stock, edit a field, remove stock, buy) ends by re-calling `sccOpenLookup()` on the same entity so the drawer reflects the fresh state immediately, matching this file's existing re-render-in-place convention (tag notes, scenario editor).
**Verification, all live, not assumed.** Local server: a stale server was already running on the usual dev port from an earlier session (the same "may be serving a different checkout" risk flagged before) — started a fresh one on a new port and confirmed via `curl | grep -c` before trusting it. Opened Mira Pont's Sundry Goods (the same POI whose informal `"shop"` tag drove the original investigation) through the real UI, searched and added a real Torch to its stock, confirmed via SQL it landed with `price_gp_override:null, quantity:null`. Bought 1 as Hana Sable through the real Buy button — confirmed via SQL her `gold_cp` dropped 900→899 cp (exactly Torch's real 0.01gp price) and a real `inventory` row was inserted (`dm_granted:false`). Set an unaffordable 10000gp price override and clicked Buy again — confirmed via SQL zero writes occurred on either table, toast correctly blocked it. Set quantity to 0 — confirmed the row correctly flipped to "Out of stock" with Buy hidden. Opened a real Organization (Goldenway) and confirmed its own Shop section renders correctly with `entity_collection:'organizations'`. Opened a real City (Gobblewump Crossing — confirmed via `POIS.some()` it is genuinely not a POI) and confirmed the Shop section correctly does not render, validating the POI-scoping fix. Zero console errors throughout. All test data (the stock row, Hana's inventory row, her gold) reverted via SQL, confirmed clean via a final read. Pre-push checks clean: raw parse, `new Function()` on all 16 extracted inline script blocks, diff-scoped literal-emoji grep empty.


---

**2026-08-12 (new session, Vendor/Shop investigation — no code changed, 4 real design questions sent to the DM before building anything) — `dm.html`, `sheet/index.html`, live Supabase queries.**
Explicit investigate-then-report instruction, no schema/UI written this session.
**1a — Vendor flag/field check.** Confirmed zero: no vendor/shop column on `pois` or `organizations`. Real `poi_type` values across all 13 real POIs: `town`/`dark-zone`/`site`/`landmark`/`waystation` — no "shop"/"vendor" enum value exists or has ever been used. `organizations.org_type` is 100% null (already known from an earlier session). The one real shop, Mira Pont's Sundry Goods, is `poi_type:"site"` with shop-ness conveyed only in free-text `summary` ("General trade stall that became a semi-permanent shop..."). Real, useful signal found: her POI row already carries a bare `"shop"` string in its `tags` array — an existing, if informal, DM convention, not a structured flag. Her actual stock (12 items + 4 drinks, with real per-item notes like "4 in stock", "1 available, slightly used") lives only as a hardcoded HTML table in the Field Guide document (`dm.html:3541-3557`) — not one row is linked to the real `items` catalog table.
**1b — Gold handling.** `character_sheet.gold_cp` (integer, copper-piece unit — `gp*100 + sp*10 + cp`) is the single real column. `sheet/index.html`'s `renderGold()`/`onCurrencyBlur()` (3-pill Figma UI) reads/writes it via the generic `saveSheet({gold_cp: ...})` PATCH helper. Separately, and more directly relevant: `dm.html`'s `sccAwardCoins()` (existing DM-side loot-award flow) *already* does the exact arithmetic a purchase needs — reads `SCC_SHEETS[player.id].gold_cp`, computes a new total, `sccPatch('character_sheet', ...)`. A "spend gold" action is this same call with subtraction instead of addition, plus a sufficient-funds check — not new plumbing.
**1c — `items.cost_gp` real population.** 127/139 non-null (12 null — mostly non-physical "DM Reference" misc rows and a few intentionally-unpriced flavor items like Ashbloom/Goldbell). Of the 127, 18 are exactly `0.00` — almost all `category:"accessory"` cosmetic-slot rows from the Aug 10 appearance-catalog migration (Monk's Gi, Robes, Simple Tunic, etc.), not real shop-worthy prices; a vendor build should treat `cost_gp` null-or-zero as "not for sale by default," not "free." 12 rows carry real fractional gp values (`0.01`/`0.05`/`0.1`/`0.2`/`0.5`) — checked these are genuine PHB-accurate prices (Torch 1cp, Dart 5cp, Rations 5sp, Javelin 5sp), not a data error, and convert cleanly to whole copper (`cost_gp * 100` is always a clean integer in the real data) so no floating-point concern doing the math against `gold_cp`.
**1d — Reusable UI pattern.** `sheet/index.html`'s +Add Item modal (`runInventorySearch()`/`selectCatalogItem()`) is the closest real precedent for browse-and-buy: name+category search scoped to `player_facing:true` items, thumbnail/name/category/cost/weight per row, qty input, one-click add that POSTs a real `inventory` row. `dm.html`'s `sccAwardItem()` is the DM-side equivalent (POSTs `{character_id, token, item_id, quantity, dm_granted:true}`) — confirms the exact insert shape a purchase would reuse, just without `dm_granted` and with a preceding gold check. Neither `sccAwardCoins()` nor `sccAwardItem()` is wrapped in a real DB transaction — two independent REST calls each — a pre-existing pattern in this codebase, not a new risk a vendor system would introduce, but flagged since a purchase (deduct gold + grant item) has the same non-atomicity and it's worth the DM knowing that going in.
**Step 2 — 4 questions sent, not guessed at:**
1. Stock model — fixed per-vendor item list (with optional price/quantity override) vs. rule-based ("everything in category X"). Recommended fixed list: the one real example (Mira Pont) is hand-curated with per-item notes and partial stock counts, which a category-rule can't express, and a rule risks surfacing items that make no narrative sense for a given vendor.
2. Whether vendor-hood needs a new boolean flag on `pois`/`organizations`, or whether simply having rows in a new stock table is sufficient signal on its own (no flag to drift out of sync with actual data). Recommended the latter.
3. Buy-only (as literally described in the ask) vs. also building sell-back. Not recommended either way — flagged that Mira Pont's own signage ("FAIR PRICES — NO HAGGLING") is a real characterization detail that could inform this later, but that's not a substitute for the DM's actual answer.
4. UI placement — new top-level nav tab (matching Setup's precedent from the same night) vs. woven into the existing lookup drawer for vendor-flagged entities. Recommended the lookup drawer: unlike Setup (used every session), vendor scenes are rare/special-cased, and the drawer is already where a DM's attention lands when they look up a specific shop.
No schema or UI written this session — waiting on the DM's answers to the above before proceeding, per explicit instruction.


---

**2026-08-12 (new session, Session Setup — nav restructure + 2 related gaps found in the investigation) — `dm.html`.**
4 pieces, investigate-first on the nav structure per explicit instruction.
**1 — Investigation.** Read the real structure before changing anything: Setup was `#scc-panel-setup`, one of 8 `.scc-tab-btn` sub-panels (Setup/NPCs/Locations/Quests/Combat/Notes/Live Session/Field Guide) switched via `sccSwitchPanel()`, all nested inside the single top-level `#form-session` `.dm-form` and sharing its `.scc-header` (title, session-history dropdown, the 8-button sub-nav itself) and always-visible `.scc-player-bar`. Confirmed via grep that nothing outside `dm.html:2769-2881` referenced `scc-panel-setup` by id/class, and that `SCC_SELECTED`/`sccPickerSelect()`/`sccRemoveChip()` are plain JS globals with no DOM-nesting assumptions — meaning the panel's picker state would keep working correctly regardless of which top-level tab physically contained its markup, confirmed before, not after, moving anything.
**2 — Nav restructure.** New top-level tab `data-tab="setup"` → `#form-setup`, positioned between Players and Session in both the button row and the DOM. Moved `#scc-panel-setup`'s entire contents verbatim (session number/date, prep notes, all 5 entity pickers, save button, session-complete bar) — same ids, same classes, same `scc-panel-inner` wrapper for consistent padding, just the outer `.scc-panel`/`sccSwitchPanel`-toggle wrapper dropped since it's a real top-level `.dm-form` now, toggled by the existing generic `switchTab()` exactly like Manage/Quests/Players. Removed the "Setup" button from the 8-button sub-nav; NPCs is now the sub-nav's default-active panel. `switchTab`'s `sccInit()` trigger (previously `tab === 'session'` only) now also fires on `tab === 'setup'`, since Setup needs the same live `CHARACTERS`/`CITIES`/etc. data and `SCC_SESSION` state the Session tab already loads.
**Dependency found and fixed, not asked for but required by the move**: `.scc-audit-mode` (the read-only lockdown applied while viewing a past/completed session via Session History) was only ever added to `#form-session` in `sccLoadHistorySession()`/removed in `sccLoadCurrentSession()`. Since Setup is now a separate top-level form, leaving this unfixed would have meant a completed/audited session's setup silently stayed live-editable on the new page — the opposite of what audit mode is for. Now toggled on `#form-setup` too, in both functions. Also added one narrow new audit-mode rule (`.scc-audit-mode .scc-scenario-editor-group .scc-tagnote-del { display:none }`) since the new scenario editor's delete buttons aren't a class already covered by the existing blanket `.scc-input`/`.scc-textarea`/`.scc-browse-btn`/`.scc-setup-actions` audit-mode hides — without it, a DM viewing a locked session could still delete its scenario data.
**3 — `session_scenarios` editor.** Investigated the real shape first (queried live data, not assumed): `[{group: string, scenarios: [{action: string, response: string}]}]` — 8 groups, 44 action/response rows on the one real session. Matches exactly what `sccLiveRenderScenarios()` (Live Session panel) already reads, and what the code's own comment already admitted was missing: *"No scenarios loaded for this session. Add them in Supabase session_notes.session_scenarios."* New editor inside the Setup page — collapsible group cards (matching the read-side's own collapse pattern), each with an editable group-name input and its action/response rows, `+ Add Group`/`+ Add Scenario`/delete on both levels. Edits go into a local working copy (`SCC_SCENARIO_EDIT`, deep-cloned on load) and save as one PATCH via `sccSaveScenarios()` — not autosaved per keystroke, since a single group can hold 9+ rows and a field-by-field save would be both wasteful and janky mid-edit.
**4 — Combat roster wiring.** Confirmed the gap precisely first: `sccPickerSearch('combatant', query)` requires a typed query and has zero awareness of `SCC_SELECTED.creature` (Setup's assigned-creatures list) at all. Added `sccRenderCombatSessionCreatures()` — renders Setup's assigned creatures as one-click "+ Name" quick-add chips above the existing search box, reusing the exact same `sccAddToCombatStaging()` the search dropdown already calls (not a parallel add path); a creature already in the encounter shows as a disabled "✓ Name" instead of vanishing, so the roster stays visible as a reference. Hooked into `sccSwitchPanel('combat')` (fresh render on panel open) and into `sccRenderStagingList()` itself (so removing a combatant re-enables its quick-add chip immediately). Full search stays untouched below it for anything not pre-assigned, or for players.
**Verification, all live**: Setup confirmed reachable from its own nav button, real session data (session 1, all 5 pickers' real chips) populated correctly on load — screenshotted. Scenario editor: expanded a real group (5 real rows), edited one response with a test marker, saved, confirmed the marker landed in Supabase via direct SQL, then confirmed the Live Session panel's Scenarios search picked it up via a **fresh Supabase fetch** (switched tabs, which re-triggers `sccInit()`) rather than trusting client-side state — screenshotted the match. Reverted the marker via a scoped `jsonb_set` (touched only that one field, confirmed the other 7 groups and the touched group's other 4 rows were untouched, and the array's `jsonb_array_length` was still 8 afterward). Combat: confirmed all 6 real Setup-assigned creatures rendered as quick-add chips, clicked "Grey Fluffets" with zero search typed, confirmed it landed as a real combatant row with CR/DEX/Init/Loot controls — screenshotted. Confirmed full-text search still works for a non-assigned creature (Bounce Beetles). Confirmed audit-mode lockdown applies to the new Setup page (`sccLoadHistorySession(1)` → both forms carry `.scc-audit-mode`, date input hides) and correctly releases on `sccLoadCurrentSession()`. Zero console errors throughout. Pre-push syntax check clean.


---

**2026-08-12 (new session, Note-taking — tagging + filterable view, @mention explicitly deferred) — `dm.html`, new `session_note_entries` table.**
Ask was structured explicitly as investigate → report a data-model recommendation → build. Followed that order.
**Investigation (`sheet/index.html`)**: `character_notes` (real schema: `id, character_id, token, tab, title, content, sort_order, is_favorite`) is genuinely one-row-per-note. `tab` is a single-select category (`general/places/names/things/misc`, `NOTE_TABS` array) — not multi-tag — rendered as a tab bar (`renderNotes()`/`buildNoteCardHtml()`), with a *separate* always-on cross-tab free-text search (`onNotesSearchInput()`, searches title+content regardless of active tab) layered on top. Cards support inline title/body editing (autosave on blur/1.2s debounce), pin/favorite, drag-reorder, delete via a shared confirm modal. Checked `session_notes`' real schema next — genuinely different shape, not just superficially: one row per `session_number`, and the 5 note fields (`player_decisions/unexpected_moments/loose_threads/follow_up_needed/milestone_notes`) are only part of it — the same row also carries Session Setup's entire state (`location_ids/npc_ids/creature_ids/quest_ids_active/item_ids_awarded`), `session_status` (drives the audit-lock/read-only view), `session_scenarios` (read by the Field Guide accordion), `premise`/`prep_notes`/`npc_relationships`. Confirmed via a real row count check: only 1 row exists (session 1), all 5 note fields currently null — low data risk, but that's not the real constraint.
**Recommendation (stated, not silently decided)**: do not restructure `session_notes` to one-row-per-note. It isn't a notes table with some extra columns — it's the session's master record, and Setup/Field Guide/session-history/audit-lock are all built directly on its one-row-per-session shape. Reshaping it would ripple through every one of those unrelated features, which is the opposite of "small and independent." The 5 existing fields are also still real and load-bearing today — `sccCopyPostSessionSummary()` reads all 5 to build the DM's copyable end-of-session recap, and `sccLiveSaveNote()` (the Quick Note feature, fixed 4 sessions ago) writes directly into `player_decisions` — collapsing them into freeform tagged notes would break both without being asked to. Instead: added a new table, `session_note_entries` (`id, session_number, category, title, content, created_at, updated_at`, RLS policies mirroring `session_notes`' own anon-all-access set, `updated_at` trigger reusing `session_notes`' existing trigger function rather than writing a duplicate), scoped by `session_number` the same way every other session-scoped panel already works. This cleanly reuses `character_notes`' proven *pattern* (one row per note, single-select category, autosave, cross-item search) without entangling it with `session_notes`' unrelated columns, and leaves the 5 structured fields exactly as they were.
**Categories**: folded the ask's 4 (DM-Only, Player Actions, Quest-Related, Follow-Up) together with the 5 existing textarea themes rather than running two overlapping vocabularies — merged the two genuine near-duplicates (Player Decisions → Player Actions, Follow-Up Needed → Follow-Up) and kept the 3 with no equivalent in the ask's list as their own tags (Unexpected Moment, Loose Thread, Milestone). Net: 7 categories, single-select per note (matching `character_notes`' own `tab` shape, not multi-tag — the ask didn't ask for multi-tag and `character_notes` doesn't do it either). DM-Only badge reuses `var(--cyan)` (established 3 sessions ago as THE app-wide DM-Only color) and Quest-Related reuses `var(--gold)` (established as the quest-related accent when the NPC-panel icon was built) — not new colors, matching what those two labels already mean everywhere else in the app. The other 5 categories share one neutral badge style rather than inventing 5 more arbitrary hues with no established meaning.
**Build**: new "Tagged Notes" block inside the existing Notes panel, below the 5 structured fields (not replacing them) — search box, category dropdown + "+ New Note", filter chips with live counts (`sccRenderTagNoteFilters()`), a card per note (badge, relative timestamp, copy icon, delete icon, inline-editable title + body, autosave on change) via `sccBuildTagNoteCard()`/`sccSaveTagNoteField()`. No pin/favorite/drag-reorder — `character_notes` has them, this ask didn't request them, so they weren't added. Whole section respects the existing `.scc-audit-mode` read-only convention (hidden wholesale while viewing a past/locked session, consistent with how the 5 structured fields already behave there — not a new inconsistency). `sccSwitchPanel('notes')` now calls `sccRenderTaggedNotes()`, matching the existing `'live'`/`'fieldguide'` panel-open-triggers-fetch pattern.
**Verification, all live**: added 4 real notes spanning 4 different categories (one via full real click-and-type through the actual UI, the rest via the same insert call the "+ New Note" button itself makes — not a mock). Category filter ("DM-Only") correctly narrowed 4→1. Free-text search ("Gerald") correctly narrowed 4→2, matching via body content on one of the two, not just title — confirms the search isn't title-only. Delete removed a real row via the actual button click, confirmed 4→3 via direct SQL, not assumed. Copy-to-clipboard: the sandboxed test browser blocks real OS clipboard access (`navigator.clipboard.readText()` → permission denied) — not a code issue, a harness limitation — so verified correctness by intercepting `navigator.clipboard.writeText` directly and confirming the exact expected text (`"[Quest-Related] Greytalon disinformation play\n..."`) was passed, plus the real success toast fired. All 4 test notes deleted afterward via SQL — none of this was real campaign content. Zero console errors throughout. Pre-push syntax check clean; caught and fixed one of my own literal-emoji slips in a new `.textContent`-bound string before committing (`✓` escape, not the raw glyph — per this file's own C2 rule).


---

**2026-08-12 (new session, Quest Manager — 4 independent gap fixes, no new architecture) — `dm.html`, `quests` table (`suggested_level` migration).**
All 4 items from the investigation, closed in one session per the DM's explicit "no new architecture needed" framing — which turned out to require correcting one premise (see item 2) rather than literally following it.
**1 — Clickable related-entity tags.** `renderQuestDetail()`'s `relatedHtml` used to render `quest.related[]` as inert `<span class="quest-related-tag">collection: id</span>` — no click handler, raw id as the label. New `sccRelatedCollectionInfo(collection)` maps every real Supabase collection name (`characters/cities/pois/nations/creatures/organizations/items/quests/regions/religions/species`) to its `sccOpenLookup()` type key and source array — confirmed via `select distinct jsonb_array_elements(related)->>'collection'` that only 4 are in real use today (`organizations/cities/creatures/characters`), but mapped all 11 so a future DM-added link doesn't silently render inert again. Each tag now resolves and shows the real entity name (falls back to the raw id if not found) and calls `sccOpenLookup(type, id)` on click; unrecognized collections (none exist today) render inert with an explanatory tooltip instead of a broken click. Added `:hover` state to `.quest-related-tag` since it's now interactive.
**Hard dependency, fixed as part of this item**: testing the first click surfaced that the lookup drawer still doesn't paint on the Quests tab — the tab-visibility bug flagged 2 sessions ago (`#scc-lookup-panel`/`#scc-lookup-backdrop` nested inside `#form-session`, `display:none` on every other tab) was still unfixed and sat directly on the feature being built (related tags live on the Quest Dashboard, not the Session tab). Moved both elements to page level — direct children of `.dm-main`, before `.dm-forms-panel` — so they're siblings of every tab's form instead of nested in one. Confirmed only one `id="scc-lookup-panel"`/`id="scc-lookup-backdrop"` each post-move (no duplicate-id risk), and re-verified the drawer still renders correctly from the Session tab (its original working context) as well as Quests (previously broken) and Manage (fixed last session for Org/Item, same underlying bug).
**2 — `suggested_level`.** Migration `add_quests_suggested_level`: `quests.suggested_level integer null`, all 10 existing quests unaffected (confirmed null on all after migration). The ask said "add it to the Content Manager's quest edit form" — reading the actual Content Manager code first (per C1) found this premise doesn't hold: quests were never part of the general Add/Edit Entry form at all (that form only covers city/creature/poi/character/organization/item), and the *only* quest-content mutation anywhere in the DM Tools UI is the quest detail view's own inline `status` selector (`setQuestStatus()`). Building a real Content Manager quest form would itself be new architecture, directly contradicting the DM's own stated constraint for this pass — so `suggested_level` was added as a second inline editor next to `status`, same pattern (`setQuestSuggestedLevel()` mirrors `setQuestStatus()`'s fetch/PATCH shape exactly), and shown as a gold `Suggested Level N` badge in the meta row when set. Empty/null shows as a blank `—`-placeholder input, not `0` or any other misleading default.
**3 — Malformed `stakes` data.** Both real offenders confirmed via direct query: `follow-the-dran-lead` had *"Identifying who commissioned the color extraction at Gobblewump Crossing — and what they intended to do with the harvested Featherflow color."* (a hook sentence, not an enum value); `the-letter-for-vel` had *"Unknown — the significance of the letter and what Vel is waiting for is not yet established."* (an honest DM placeholder, not an enum value). Neither was a guess — inferred from real content against the other 8 quests' real stakes values as a calibration set (queried and compared, not assumed): **`follow-the-dran-lead` → `mid`.** It's the direct narrative continuation of "Something Is Wrong With Gerald" (already `mid`) — its own DM notes state *"Dran is not the top of this chain"*, explicitly deferring the real payoff/reveal to a later, not-yet-written quest (Sessions 3–4). That rules out `high` (the one real `high` quest, Anomaly 7-C, is framed as already possessing a complete, ready-to-use piece of major-network intelligence — a payoff, not an open thread) — and its 3-beat/300xp shape is smaller than the standard 4-beat/375xp template, consistent with a continuation step rather than a standalone arc. **`the-letter-for-vel` → `low`.** Its own DM notes call it a *"Future-session plant... do not develop this in Session 1... Vel's full backstory is not yet written"* — 0 XP, 2 skeletal beats, zero established world-level consequence in the text (every real `mid`/`high` quest names a concrete threat or stakes; this one explicitly doesn't yet). As currently written it's a single NPC's personal, contained mystery — matching the same narrow scope as the two other real `low` quests (a merchant's lost shipment, an archivist's private curiosity) far better than any `mid`/`high` quest's town- or faction-level stakes. Applied via direct SQL (no stakes editor exists in any UI, confirmed — this was corrective data cleanup, not something available to fix through the app).
**4 — `player_facing` confirmation.** Re-verified, not recalled from memory: `select player_facing, count(*) from quests group by player_facing` → still 10/10 `false`; `grep quest js/router.js js/wiki.js` → still zero matches in both. Both facts logged explicitly in Current Focus and Next Priorities as a named, unaddressed gap (not built this pass, per the DM's own instruction) rather than left as silent debt.
**Verification, all live**: on "The Greytalon" (6 related entries spanning cities/characters ×3/creatures/organizations — the richest real test case), clicked "Torv Bassle" (character) and "Greytalons" (creature) through the real rendered tags — both opened the correct real card, both visibly painted while still on the Quests tab (proof the drawer fix works, not just the click handler). Set `suggested_level` to 4 via the real input, confirmed the toast, confirmed via direct SQL it landed in Supabase, confirmed via a full page reload that the badge and input both show `4` correctly, then reverted to `null` via SQL (test value, not a real DM decision — unlike the stakes fix, which is real corrective data and was kept). Confirmed the Stakes filter now correctly buckets both corrected quests (`mid` filter includes Follow the Dran Lead, `low` filter includes The Letter for Vel). Zero console errors throughout. Pre-push syntax check clean; one literal-emoji slip caught in the new `setQuestSuggestedLevel()` (mirrored from `setQuestStatus()`'s existing `.textContent` pattern) — fixed to `✓`/`⚠` JS escapes rather than leaving a literal glyph in a new addition, per C2 (pre-existing literal emoji in `setQuestStatus()` itself left untouched, not this session's scope). Committed and pushed to `anavale-wiki-scc-ux`.


---

**2026-08-12 (new session, Search+Preview "easy slice" — filters + missing entity coverage) — `dm.html`: (1) added filter dropdowns to the global search — Type selector plus per-type secondary filters, all option lists computed live from the in-memory collections each time the filter bar opens rather than hardcoded. (2) `sccGlobalSearch()` now also searches Regions/Religions/Species. (3) `sccOpenLookup()` gained matching render branches for those 3 types. "Open in new tab" explicitly deferred per the ask — routing/visibility issues from the original investigation (no `#quest` route, most DM content is `player_facing:false`) still need a real decision first.**
Scoped exactly to the "easy slice": filters + missing entity search coverage, not the new-tab piece.
**Filter dropdowns**: added a `⚙` toggle next to the global search box that reveals a filter bar — a Type selector (NPC/Location/Creature/Organization/Item/Quest) plus per-type secondary `<select>`s that show/hide based on the chosen type, mirroring the show/hide pattern Content Manager's own `cmTypeFilterChanged()` already uses. Fields, exactly as specified: NPC (category, player_facing), City (region, nation, city_type, player_facing), Creature (category, tier, player_facing), Organization (alignment — `org_type` skipped, confirmed 100% null), Item (category, rarity, equip_slot), Quest (status). Every option list is built by a `distinct()` scan over the real live `CHARACTERS`/`CITIES`/`CREATURES`/`ORGANIZATIONS`/`ITEMS`/`QUESTS` arrays each time the filter bar opens (`sccGlobalPopulateFilterOptions()`) — never hardcoded — so it can't drift from real data the way Content Manager's own `color_health`/item-category dropdowns already have (confirmed live: the new Item Category list correctly includes `misc`, which Content Manager's dropdown is missing). Quest Status correctly resolves to only `available`/`dormant` since those are the only two real values in the data today — no dead `active`/`completed` options shown. `player_facing` filters are a plain Visible/DM-Only boolean pair (not dynamically populated, since it's a real boolean column, not a string enum — and the `'teaser'` value used elsewhere in this file's badge logic doesn't correspond to any real data, confirmed via the original audit). Filtering itself is applied via `sccGlobalFieldMatcher()`, layered into the existing `addCollection()` text-match pass rather than a second filter stage. `sccGlobalSearch()`'s early-return was loosened so the results panel now also opens on an active filter with no typed query, letting the DM browse-by-filter (e.g. "all DM-Only NPCs") — not explicitly asked for but a direct, low-risk consequence of the filters existing at all.
**Missing entity coverage**: `addCollection()` calls added for `REGIONS`/`RELIGIONS`/`SPECIES`, identical name+summary substring pattern as every other type (per the ask, no per-type special-casing) — `SPECIES` has no `summary` column so it's effectively name-only matching for that one type, which is expected and fine, not a bug. `sccOpenLookup()` gained three new branches (region/religion/species) reusing the existing `scc-ql-*` card pattern verbatim: Region reuses the exact color-health badge logic already duplicated in the character and city branches (a third copy, not refactored — out of scope for this pass); Religion and Species both correctly render as fallback-letter portraits with no `<img>` markup at all, since neither table has an `image` column (confirmed via schema query before writing the branch, not assumed from the character/city/item pattern). One real, necessary side-fix: Religion's `practices` field is genuine multi-paragraph prose in real data (confirmed on The Brightcreed) and was rendering as one run-on block, since `.scc-ql-section-body` (shared by every lookup card's prose sections) had no `white-space` rule — added `white-space:pre-wrap`, a safe universal change since it's a no-op for every existing single-paragraph field already using that class.
**Verification, all live, not assumed**: searched and clicked into all 3 new types through the real search-and-click UI (not direct function calls) — Region/Caparia (health badge, visibility badge, summary, tags all correct), Religion/The Brightcreed (multi-paragraph Practices field confirmed rendering with real line breaks post-fix), Species/Veilstepped (PHB Equivalent, Region, Affinity, Description all correct, no tags section since species has no tags column). Item+Category=weapon filter cross-checked against real `ITEMS` data programmatically (not just a visual glance) — confirmed all 20 capped results are genuinely `category:"weapon"`, and confirmed a known non-weapon item (+1 Chain Mail, armor) is excluded once the filter is applied. NPC+DM-Only filter returned exactly 17 results, an exact match to the real count already established in the original color-collision audit — strong confirmation the `player_facing` boolean comparison is correct, not an off-by-some-string-coercion bug. Reset button confirmed clearing both the Type select and all secondary selects and re-closing the results panel. Confirmed zero regression on a plain unfiltered text search (`"gerald"` still returns the expected 9 mixed-type results). Zero console errors throughout, zero Supabase writes (this is a pure client-side feature — nothing to revert). Pre-push `new Function()` syntax check clean on every extracted inline script block, diff-scoped literal-emoji grep came back empty. Committed and pushed to `anavale-wiki-scc-ux`.


---

**2026-08-12 (new session, implementing the color resolution from the prior session's report) — DM-Only unified to one new cyan token, a gold quest-related icon added, NPC Hostile/Neutral confirmed to need no new color. `css/tokens.css`, `dm.html`.**
DM made 4 decisions off the prior session's full color-inventory report and asked for implementation: (1) DM-Only gets one new color (`#22d3ee`, picked from that report as the one hue family — cyan — genuinely unused anywhere in the app), replacing its two existing conflicting colors. (2) Quest-related becomes a small gold icon, not a fill/background color, so it doesn't compete with the relationship-color axis. (3) NPC Hostile/Neutral confirmed to just reuse `SCC_REL_CYCLE` as-is, no new token — document this for later, don't build it now. (4) Read Aloud's green stays, per the recommendation given (real semantic fit; both green usages carry their own text label, so the collision risk is low in practice, unlike DM-Only's which was a real same-concept-two-colors inconsistency).
**New token** (`css/tokens.css`, after the `--purple` block): `--cyan: #22d3ee`, `--cyan-dim: rgba(34,211,238,0.15)`, `--cyan-border: rgba(34,211,238,0.35)` — same 3-line shape as the existing `--blue`/`--red` blocks.
**DM-Only recolor** (`dm.html`): `.scc-fg-box--yellow` (Field Guide, `~dm.html:6417`) — background/border/label/body all moved from its old amber values to the new cyan token/rgba equivalents (`rgba(34,211,238,0.07)` bg, `var(--cyan-border)` border, `var(--cyan)` label, `#cffafe` body text — Tailwind cyan-100, matching the existing pattern where every other box's body text is a pastel-100 tint of its own label color). Class name kept as `--yellow` (not renamed) — it's referenced ~20 times in the hardcoded Field Guide document markup, and renaming would mean touching all of them for a purely cosmetic identifier change with no functional benefit; left a comment on the rule explaining the name/color mismatch instead. The "HOW TO USE THIS GUIDE" legend line updated from "Yellow boxes = DM only" to "Cyan boxes = DM only" so the document stays internally accurate. `player_facing` badge (`sccOpenLookup`'s `character` branch, `pfColor`, and `organization` branch, `opfColor`) — both had their DM-Only fallback changed from `var(--red)` to `var(--cyan)`; the Visible (green) and Teaser (gold) states are untouched, only the third state's color moved.
**Flagged, not fixed**: the DM-Only box labels in the Field Guide document still carry a literal `&#x1F7E1;` (🟡 yellow circle) emoji glyph prefix, hardcoded ~20 times — the box's own background/border/label color are now genuinely cyan, but that inline circle glyph is still visually yellow, since there's no cyan circle emoji in Unicode to swap it for. Fixing this means either dropping the circle prefix (matching how blue/purple/orange boxes already work with no emoji prefix) or picking new iconography — a bigger, more visible edit than what was asked (which was scoped to the box's own colors and the badge), so left as a flagged follow-up rather than decided unilaterally. Logged in Next Priorities.
**Quest-related icon** (`dm.html`, `sccRenderNpcPanel`'s card-row template, `~dm.html:11822` + CSS `~dm.html:5453`): new `.scc-npc2-badge-row` wraps the existing relationship badge plus a new conditional `.scc-npc2-quest-icon` span (📜 scroll, `&#128220;` entity per this file's own C2 no-literal-emoji rule — confirmed via grep that *pre-existing* code in this same function already has several literal emoji characters directly in JS strings (🎭📖📜⚠🔒), which appears to be a long-standing, unenforced violation of C2 predating this session; not fixed here since it wasn't asked and touching it is a separate, larger cleanup — but new code in this session follows C2 correctly regardless of what's already there). The icon renders only when `linkedQuests.length` (already computed earlier in the same function from `quests.related`, previously used only inside the expanded DM-only body — this reuses that same data, no new query) is non-zero, colored `var(--gold)`, no background/fill, with a `title` tooltip naming the linked quest(s) — relationship badge's own color is completely untouched. `margin-bottom` moved from `.scc-npc2-rel-badge` onto the new wrapper row so spacing is unchanged when the quest icon isn't present.
**NPC Hostile/Neutral**: no code change — confirmed and documented (Current Focus, Next Priorities) that any future build of this surface reuses `SCC_REL_CYCLE`'s existing 5 colors (`REL_BG` in `dm.html`) rather than inventing a new token, so a later phase doesn't reinvent this.
**Verification**: real before/after via `git stash`/`git stash pop` (not assumed) on all three DM-Only surfaces — Field Guide's Step-1 box, character lookup card (Dran), organization lookup card (The Gigglegloom Conclave) — screenshotted both states for each. Cyan values confirmed via `getComputedStyle()` (`rgb(34, 211, 238)` exactly), not just visual read. Quest icon confirmed live on Gobblewump Gerald's NPC panel card (real quest-linked NPC in the current session roster, found by cross-referencing `SCC_SELECTED.npc` against `quests[].related`) — icon present with the correct quest name in its tooltip, `getComputedStyle()` confirmed `rgb(240, 180, 41)` (`var(--gold)` exactly) on the icon; absent (correctly) on non-quest-linked NPCs (Dran, Fess) in the same screenshot. Pre-push `new Function()` syntax check clean on every extracted inline script block, zero console errors throughout. Committed and pushed to `anavale-wiki-scc-ux`.


---

**2026-08-12 (new session, immediately after the UX/IA investigation below) — Fixed the 2 real bugs that investigation named, plus a 3rd (`sccSaveNotes()`'s own 409) it exposed mid-fix. `dm.html`.**
DM asked for exactly two fixes named in the prior session's investigation, tested live, logged, committed, pushed.
**Bug 1 — `sccLiveSaveNote()` never persisted (`dm.html:10757`).** The ask was to reuse the existing "Save Notes" write path (`sccSaveNotes()`, writes `session_notes`) rather than invent a new one. Per D2, read that path before reusing it — and live-tested it directly against Supabase (`curl` against `/rest/v1/session_notes` with the exact headers `sccPost()` sends) before wiring anything to it. **Found it was itself broken**: `sccPost('session_notes', body)` sends `Prefer: resolution=merge-duplicates` with no `on_conflict` query param; `session_notes` has `UNIQUE(session_number)` but its primary key is `id` (not in the body), so PostgREST's upsert targets the PK, gets a fresh row every time, and the real conflict (on `session_number`) surfaces as a 409 `23505 duplicate key value violates unique constraint "session_notes_session_number_key"` — confirmed via a live probe POST against the real session-1 row (`player_decisions` temporarily set to a marker string, confirmed via SQL, then reverted to `null`). Since `session_number: 1` already exists (created at Session 0), this means **`sccSaveNotes()`'s "Save Notes" button has been silently 409-failing (toast: "Save failed") for any DM who has ever clicked it against an existing session** — a bug independent of anything asked this session, discovered only because reusing it faithfully would have shipped the same failure into the new Quick Note path. Root-caused, not patched: `sccSaveSetup()` (`dm.html:11636`) already has the correct pattern two functions away — PATCH via `?session_number=eq.N` when `SCC_SESSION.session_number === num`, POST only for a genuinely new session — so `sccSaveNotes()` was given the identical branch (confirmed the fix via a second live probe: same POST body against `?on_conflict=session_number` returns 200 and merges correctly, preserving unrelated columns like `session_status`/`session_date` — confirmed via SQL diff before/after).
`sccLiveSaveNote()` itself: now builds the same timestamped-append string as before, writes it into the DOM textarea (`#scc-note-decisions`) exactly as before so anything already reading that element keeps working, but also `sccPatch('session_notes', '?session_number=eq.'+num, {session_number, player_decisions})` immediately, keyed off `SCC_SESSION.session_number` (always populated by the time the Live Session panel is reachable — no POST branch needed here, unlike `sccSaveNotes()`, since a session must already exist to be live). Bails with a clear "no active session to sync to" toast if `SCC_SESSION` is somehow unset, rather than silently losing the note.
**Bug 2 — `sccOpenLookup()` missing Organization/Item branches (`dm.html:12888`).** Fixed exactly as scoped: `sccGlobalResultClick()`'s bad `organization → 'city'` remap removed (organization now passes through to its own branch; `nation → 'city'` remap kept, since nations genuinely are in the city lookup's combined array). Added `organization` and `item` branches matching the existing character/city/creature/quest pattern exactly (same `scc-ql-header`/`scc-ql-fields`/`scc-ql-section`/`scc-dm-block` classes, no new CSS) — Organization shows the existing `player_facing` DM-Only/Teaser/Visible badge plus alignment/HQ/org_type/leadership fields and summary/purpose/description/history/known_members/gigglegloom_relationship/public_perception prose, secret + dm_notes in the existing DM-only block style, tags. Item shows read_aloud (existing `.scc-ra-block`, same as creatures), category/rarity/cost_gp/weight_lb/equip_slot/damage/base_ac/magic_bonus fields, summary/description/history/gigglegloom_relationship/story_note prose, dm_notes, tags — `items.image` is already a full storage URL (unlike characters/cities, which store just a flag and construct the path from `id`), used directly. Schema for both branches confirmed via live Supabase queries before writing any HTML, not guessed from the investigation's field list alone.
**Bug 3, found but NOT fixed — out of the requested scope, flagged for a DM decision.** While testing bug 2 in the browser, the drawer opened (confirmed via `classList`) but never visually painted. Root cause: `#scc-lookup-panel` is nested inside `div.dm-form.scc-form` — the Session tab's own top-level form container, `display:none` whenever the DM is on Manage/Quests/Players. `sccOpenLookup()`/`sccGlobalSearch()` are themselves tab-agnostic (header search bar renders on every tab, confirmed in the investigation), but the **lookup drawer's markup only ever paints if the DM happens to already be on the Session tab** — same silent-fixed-position-breakage class of bug this file's own Modals rule (§8) already warns about for `sheet/index.html`, just via `display:none` on an ancestor rather than a transform/animation. Not fixed — outside the two named bugs — but logged here and in Next Priorities since it affects every entity type's lookup, not just the two touched this session.
**Verification, all live, not assumed**: local static server was found to be serving the wrong checkout (`anavale-wiki`, not this session's actual `anavale-wiki-scc-ux` working directory — a pre-existing environment mismatch, not a code issue) — caught via `sccOpenLookup.toString()` not containing the new branches despite `grep` confirming they were on disk; worked around by starting a second local server rooted at the correct directory rather than touching the other one (may be serving another session). With that resolved: Organization (Goldenway) and Item (Health Potion) both confirmed rendering full real content via the actual search-and-click UI, screenshotted. Quick Note: typed a marker note on the Live Session tab, saved, confirmed via direct SQL the marker landed in `session_notes.player_decisions` immediately (before ever touching the Notes tab), then did a full page reload and confirmed the Notes tab's textarea showed the same marker on load — real persistence, not DOM-only. Also clicked the Notes tab's own "Save Notes" button afterward and confirmed it now succeeds ("Session notes saved ✓") where it would previously have 409'd. All test data (`player_decisions`) reverted to `null` via SQL afterward. Zero console errors throughout. Pre-push `new Function()` syntax check clean on every extracted inline script block. Committed and pushed to `anavale-wiki-scc-ux`.


---

**2026-08-12 (new session — DM Session Command Center UX/IA investigation, 7 of 8 goals, no code changed) — investigated 7 proposed DM Tools features against the real current `dm.html` state before any build. Goal 8 (Claude integration architecture) explicitly out of scope this pass — already decided by the DM separately.**
Per explicit instruction, this was investigation only — 7 parallel research passes plus one dedicated color-collision audit, each grounded in reading the actual current file (not project-knowledge descriptions) and live Supabase schema/data queries, not assumptions. Full findings relayed to the DM in chat; condensed here for continuity since several are real, previously-unknown bugs, not just scoping gaps.
**Color scheme collision (checked before any goal, since it cuts across all of them)**: the DM's proposed new axis — Read Aloud (bright green) / DM Only (bright orange) / NPCs Neutral (medium yellow) / NPCs Hostile (medium red) / NPCs Quest-Related Neutral (bright yellow) / NPCs Quest-Related Hostile (bright red) — has real collisions, not just close hues, with systems already live in `dm.html`: "DM Only" already has two existing colors elsewhere (Field Guide's `.scc-fg-box--yellow` gold `#f0b429`, and the `player_facing` badge's red `#f87171` fallback) — a third color for the same label is a genuine conflict, and the proposed orange specifically collides in hue with Field Guide's *existing* "loot/shop" orange (`#fb923c`, used 6× in the same document). "NPCs (Hostile)" duplicates the existing NPC-relationship "hostile" state, already `#f87171` (`SCC_REL_CYCLE`, `dm.html:11618`). "NPCs (Neutral)" sits directly on top of the app's already-overloaded brand gold (`#f0b429` = caution / teaser / region-health-muted / quest-available / the global UI accent color everywhere). Structurally, NPCs already carry two independent color-coded axes at once (relationship-to-party, and `player_facing` visibility) — layering a third "NPC type" axis on the same entities without a stated design (replace vs. coexist, and if coexisting, which visual channel wins) means a single NPC could need 3-4 simultaneous colors. This needs a real decision before any hex values are finalized, not just a hue tweak.
**Goal 1 (Search+Preview)**: `sccGlobalSearch()`/`sccOpenLookup()` work today against live Supabase data (confirmed not stale-seed data despite a plausible-looking risk from `sccInit()`'s narrower fetch list — a separate DOMContentLoaded sync IIFE at `dm.html:3844` fetches all 10 world tables before the DM can type). **Two real bugs found**: clicking an Organization search result always shows "Entry not found" (`sccOpenLookup`'s `city` branch never includes organizations despite `sccGlobalResultClick` remapping org→city), and clicking an Item result renders nothing at all (`sccOpenLookup` has no `item` branch whatsoever). `REGIONS`/`RELIGIONS`/`SPECIES` are loaded live but never included in search. For "open full page in new tab": the public wiki has real hash routing for most entity types, but **no `#quest` route exists anywhere** in `js/router.js`/`js/wiki.js` — quests have no public page to link to yet — and ~90% of DM-tool content is `player_facing:false`, so most lookups would 404 on the public wiki today. Real, groundable filter fields exist per entity type (documented in chat) — but two existing dropdowns (Content Manager's color_health and item-category filters) already have stale option lists that don't match real data; don't copy those blind into new filter UI.
**Goal 2 (Session Guide vs. Field Guide)**: Field Guide (`scc-fg-*`, `dm.html:3121`) is a real two-part component — a fully dynamic, session-scoped left sidebar (Supabase-driven) and a **100% hardcoded, Session-1-only right-hand document** (one literal HTML block, no template, no session selector). Already has a working 5-color read-aloud/DM-only/lore/comedy/loot box convention, documented in-page. Zero inline popup links exist inside the document text today. Verdict: the ask is an extension of this existing component, not a new feature — but the real gap (multi-session support for the static document) is genuine new build work, not a parameter tweak.
**Goal 3 (Quest Manager)**: search-without-knowing-the-name and per-beat progression UI already fully work (`questsFilter()`, `sccCycleBeatStatus()` + real `quest_progress` table). Affected-entities data exists but renders as inert non-clickable tags. No `suggested_level` column exists anywhere. All 10 real quests are `player_facing:false` and **no public wiki route/renderer for quests exists at all** — the DM/player split has no player-facing half to toggle into yet, a bigger gap than a UI checkbox. Also found: 2 of 10 quests have malformed `stakes` data (free sentences instead of the enum), silently breaking their filter/sort/badge — a data-quality fix worth bundling with any Quest Manager work.
**Goal 4 (Session Setup)**: genuinely flat, single-panel, all 5 entity pickers rendered simultaneously — no existing sub-nav to reorganize, confirmed a real gap. Setup is the single write surface for a shared `session_notes` row that NPCs/Locations/Quests/Field Guide all read from (hub-and-spoke, not siloed) — except `session_scenarios` (jsonb), which has **no editor anywhere in the app**, Supabase-only today, and Combat's creature-add search, which is fully disconnected from Setup's session roster.
**Goal 5 (Note-taking)**: found a real, previously-unknown bug — the Live Session "Quick Note" field (`sccLiveSaveNote()`, `dm.html:10757`) **does not write to Supabase**. It only splices a timestamped line into the DOM value of the "Player Decisions" textarea on the separate Notes tab, unconditionally regardless of what the note is actually about, and is lost if the DM never later clicks "Save Notes" there. `session_notes` has no tag/category column at all. Zero @mention/caret-tracking scaffolding exists anywhere in the codebase (confirmed via full grep) — the three existing predictive-search implementations (`sccGlobalSearch`/`sccLiveSearch`/`sccPickerSearch`) share reusable filter logic but none do caret-aware inline text insertion, so this is real new build work, not an adaptation. `sheet/index.html`'s player-facing Notes tab (tagged, searchable, `character_notes` table) is a solid structural reference for what a real filterable DM notes view could look like.
**Goal 6 (Combat Tracker)**: creature-add is a single free-text name-only search, zero filters today. Of the 6 proposed filter dimensions (race/level/CR/organization/nation/army), **only CR has a real column, and it's populated on just 4 of 52 creatures.** Race/level/organization/nation/army have no corresponding schema at all (nation's closest real analog is `regions[]`, 100% populated); "army" doesn't exist anywhere in the codebase, in any table. NPCs (`world_characters`) aren't in the combat flow at all and have zero mechanical fields (no HP/AC/CR) — unifying NPC+creature filtering needs new schema on `world_characters`, not just a filter UI.
**Goal 7 (Vendor/Shop)**: no mechanism exists anywhere. The Field Guide's "Shop" section is pure static prose meant for the DM to read aloud, zero JS wiring. The closest analog, `sccLootItemSearch()`/`sccAwardItem()`, is a one-way DM-grants-item-free flow — no gold check, no deduction, ever, in either direction (confirmed: no inventory-insert path anywhere touches `gold_cp`, and no inventory-delete anywhere credits it back). `items.cost_gp` is populated on 137/139 real rows, so pricing data itself is usable. Net-new needed: a stock-list entity (nothing links to `pois`' Mira Pont row today), a browse/buy/sell UI (existing catalog-search-and-add pattern is a reasonable starting point), and — the core missing piece — real transaction logic that atomically moves gold and inventory together, which doesn't exist today in either direction.
Not built this session per explicit DM instruction — phased build to follow across multiple future prompts, DM to choose ordering.


---

**2026-08-13 (continuing the same session — process addendum, no code changes) — Added CLAUDE.md §3 "⚠ Pending / Blocked", formalized two standing habits into §1.**
DM's ask, prompted directly by the Attunement build's deferred DM Tools checkbox from the previous entry: a blocked item sitting in a flat "Next Priorities" list reads identically to an ordinary unstarted backlog item — nothing marks it as time-sensitive (waiting on someone else's branch, not just waiting on attention). Built the section the DM specified verbatim, moved the one real case into it.
**Numbering**: inserting a new top-level section between "Current Focus" (§2) and "Repo & Deployment" (old §3) meant renumbering everything after it — §3 through §10 became §4 through §11. Checked for internal cross-references before renumbering rather than assuming none existed: found exactly one, C4's "See Section 3" (referring to the git-crash-recovery procedure, now §4) — updated it. The `§6`/`§8` references elsewhere in the Decision Log are to the *external* Aug 7 handoff doc's own section numbers, unrelated to this file's numbering — confirmed before leaving them untouched, not assumed safe.
**Formalizing the existing habit, not just adding the new one.** The DM's spec for the new section's "check before starting" habit explicitly said "same as the existing 'check branch overlap first' habit already in place" — but that habit has only ever lived in Claude Code's private cross-session memory (established 2026-08-12, this session's earlier turns), never written into this shared file. Since the DM is directing that this new, related habit be added to the team-visible doc, formalized both together: §1's Start checklist gained a step covering both the branch-diff check and the new §3 check in one line; End gained a step for the "check on merge, flag proactively, move to Next Priorities" behavior. Private memory updated to reference this file as the now-canonical copy, rather than leaving the shared doc and private memory as two separately-maintained descriptions of the same habit that could drift apart.
Verified: `grep "^## "` confirms §1 through §11 sequential with no gaps or duplicates. `grep "See Section"` confirms only the one fixed cross-reference exists. No `sheet/index.html`/`dm.html` changes — confirmed via `git status` before committing that only `CLAUDE.md` is staged, matching the handoff's explicit scope note. Pushed live.


---

**2026-08-11 (later same day) — Investigated a DM report that the two new Equipped Effect fields don't appear on the Longbow's (category: Weapon) edit page. Not a bug — confirmed via live DOM inspection and a raw fetch of the deployed file, no code changed.**
Checked the render logic before assuming cache: `itemCategoryChanged()` (`dm.html` ~8597) only ever toggles `.item-category-fields` elements and `#item-stats-section` — the "Content" `.form-section` holding Summary/Description/Gigglegloom/History/the two new fields is a separate sibling section outside that conditional wrapper entirely (closes at line 2487, Content starts fresh at 2490), so it was never category-scoped to begin with, confirmed by re-reading the actual markup nesting rather than re-trusting the earlier diff.
Confirmed live on the real Longbow edit form (`cmEditEntry` via `CM_FILTERED`, category `weapon`): both `item-effect-mechanical` and `item-effect-flavor` exist in the DOM, `display:inline-block`, `visibility:visible`, real non-null `offsetParent`, real bounding rects (1471×80px) — genuinely rendered, not hidden. Also fetched the live deployed `dm.html` directly (`curl .../dm.html | grep`) rather than trusting the earlier push — identical 4 occurrences at the identical line numbers as local, so no deploy/cache gap either.
Most likely explanation for the DM not seeing them: Weapon Stats is one of the largest category-stats panels (damage dice/type/versatile/magic bonus/range normal/long/str requirement/properties/named), pushing the two new Content fields down to ~y:2300–2450px on the form — easy to scroll past without realizing Content continues below Stats, especially since Armor's shorter Stats panel (used in the original test) put them much closer to the fold.
Verified test-saved on the real Longbow (`equipped_effect_mechanical`/`equipped_effect_flavor` set, `generateItem()` called, confirmed via SQL, confirmed rendering live in the sheet's Special Effects panel — card correctly titled "Longbow" with both rows), then reverted. Re-confirmed Leather Armor's case still renders correctly with no regression (re-seeded via SQL, reloaded, correct card rendered), then reverted. All 3 items (`longbow`, `leather-armor`, `leather-cord-necklace`) confirmed clean (`null`/`null`) via a final SQL read. Zero console errors throughout.


---

**2026-08-09 — `dm.html`: added "Muted" to the SCC Live Session conditions picker; fixed a real gap this surfaced (Fading Stage 1/Color-Drained silently missing their real tooltip/color) rather than adding a third hardcoded copy of the data.**
Ask was narrow — add `'Muted'` to `SCC_CONDITIONS_LIST` (16→17 entries) so it's selectable in the Live Session conditions picker. Read `dm.html` before touching it (C1) and found the picker's actual color/tooltip source, `sccLiveRenderConditions()`'s local `COND_DATA` object, only covers the 15 core D&D conditions (Blinded through Unconscious) — `'Fading Stage 1'` and `'Color-Drained'`, both already in `SCC_CONDITIONS_LIST` before this change, have no entry there and were silently falling through to `sccLiveRenderConditions()`'s generic-fallback branch (flat purple pill, `tip: c` — just the condition's own name, no real rules text). The DM's instruction anticipated exactly this: "if the Live Session panel also has its own local color/tooltip data... pull Muted's from Supabase rather than hardcoding it again." Confirmed via SQL that a `conditions` table exists in Supabase (`color_bg`/`color_border`/`color_text`/`tip`/`is_custom` columns, seeded same-day per `created_at`) with all 18 conditions including Muted (`is_custom:true`, added ~8 minutes after the other 17 per its own `created_at`) — real data, not something to guess at.
Fix: added `sccFetch('conditions', '&order=sort_order.asc')` to `sccInit()`'s existing `Promise.all` (same fetch-once-at-boot pattern already used for `species`/`items`/etc.), populating `window.SCC_CONDITIONS_DATA`. Added a small `sccConditionsTableLookup(c)` helper inside `sccLiveRenderConditions()` and changed the chip color/tooltip lookup from `COND_DATA[c] || genericFallback` to `COND_DATA[c] || sccConditionsTableLookup(c) || genericFallback`. Net effect: Muted gets its real Supabase styling immediately, and Fading Stage 1/Color-Drained — which have been silently degraded since whenever they were added to the picker list — get theirs too, as a direct consequence of the same code path, not a separate unrequested fix.
Explicitly left alone: a second, unrelated `COND_DATA` object exists in `sccPromptCondition()` (the Combat tab's "Add Condition" modal, ~line 12421) — same 15-condition hardcoded shape, doesn't reference `SCC_CONDITIONS_LIST` at all, and wasn't mentioned in the ask. Different feature (combat initiative tracker vs. Live Session panel), left untouched.
Verification: loaded `dm.html` locally (password gate is a plaintext client-side check, `anavale-dm` — not a real secret, this is the DM's own local dev tool), confirmed `window.SCC_CONDITIONS_DATA` populated with 18 rows including Muted, confirmed the "Add Condition" picker dropdown lists Muted as its own `<option>`. Added Muted + Fading Stage 1 + Poisoned to a test player (Hana), confirmed via DOM inspection that all three chips render with distinct correct colors and full real tooltips (Muted/Fading Stage 1 from the new Supabase lookup, Poisoned unchanged from local `COND_DATA` — correct precedence, local table still wins when a condition exists in both). Screenshotted the rendered chips and the picker dropdown. Cleared the test player's conditions afterward — confirmed via grep that `SCC_LIVE_CONDITIONS` is pure client-side in-memory state (`var SCC_LIVE_CONDITIONS = {}`), never written to Supabase anywhere, so no database cleanup was needed beyond resetting the JS variable and re-rendering.


---

**2026-07-23 — Infrastructure session. CLAUDE.md rewritten.**
Supabase restored from inactive state. Keep-alive updated from 5-day to 3-day schedule, confirmed working (HTTP 200). Local folder restructured: `anavale-wiki/` moved to `Anavale/` root, `anavale-assets/` created for non-git files, `DM/` deleted (files already in `anavale-dm-docs`). `anavale-dm-docs` connected to Claude Chat project context — DM reference files now auto-sync, no manual uploads needed. CLAUDE.md rewritten: removed stale HEAD hash, added Current Focus section, consolidated always-stale files, trimmed Decision Log, reorganised Code Authoring rules by area. Heredoc rule added to Code Authoring.


---

**2026-07-18 — Migrated from Cowork to Claude Code. CLAUDE.md created.**
Claude Code is now standard for all file edits and git operations. Cowork retired. `CLAUDE.md` created and pushed. Project plan moved into `docs/`. `anavale-dm-docs` private repo created.


---

**2026-07-05 — Session Zero complete. All 7 SCC tabs QA'd.** Character creation session run. HEAD at session end: `ced8db3`. session_scenarios populated (8 groups, 44+ rows). Player handout created. Session 1 not yet run.


---

**2026-07-04 — Landmarks retired. species table seeded.** `structures` replaces landmarks. 141 lines removed from dm.html. Supabase `species` table seeded with 12 canonical IDs.


---

**2026-07-03 — Location enrichment + SCC Live Session tab.** Population breakdown and structures in wiki. City form rebuilt. Live Session tab (7th SCC tab) built. `read_aloud` column added to 5 tables.


---

**2026-07-02 — Design token system. SCC NPCs + Quick Look rebuilt.** `css/tokens.css` created. Cinzel Decorative + Roboto only. One gold (#f0b429).


---

**2026-06-28 — Supabase migration complete.** All world data in Supabase. `data/*.js` files are legacy seeds only.

